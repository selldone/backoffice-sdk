/*
 * Copyright (c) 2023. Selldone® Business OS™
 *
 * Author: M.Pajuhaan
 * Web: https://selldone.com
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * All rights reserved. In the weave of time, where traditions and innovations intermingle, this content was crafted.
 * From the essence of thought, through the corridors of creativity, each word, and sentiment has been molded.
 * Not just to exist, but to inspire. Like an artist's stroke or a sculptor's chisel, every nuance is deliberate.
 * Our journey is not just about reaching a destination, but about creating a masterpiece.
 * Tread carefully, for you're treading on dreams.
 */

import {SetupService} from "@selldone/core-js/server/SetupService";
import {ShopMenuType} from "@selldone/core-js/enums/shop/ShopMenu";
import {OrderType} from "@selldone/core-js/enums/order/OrderType";
import {TimelineEmailType} from "@selldone/core-js/enums/timeline/TimelineEmailType";
import {OrderExportPdfType} from "@selldone/core-js/enums/order/OrderExportPdfType";
import {Currency} from "@selldone/core-js/enums/payment/Currency";
import {Application, Article, ArticleReport, Club} from "@selldone/core-js";
import {SocialNetwork} from "@selldone/core-js/enums/social/SocialNetwork";
import {OrderTypeCode} from "@selldone/core-js/enums/order/OrderTypeCode";
import {ShopChannelType} from "@selldone/core-js/enums/shop/notification-channels/ShopNotificationsChannels";
import {Language} from "@selldone/core-js/enums/language/Language";

export class API {
  selldone_api_url = "";
  selldone_dapi_url = "";

  constructor() {
    this.selldone_api_url = SetupService.GetMetaValue("selldone-api");
    this.selldone_dapi_url = SetupService.GetMetaValue("selldone-dapi");
  }

  //―――――――――――――――――――――― Users ――――――――――――――――――――
  /**
   * Suggestion users endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /users/suggestion/{search}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:50,2`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param search - Search text passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `UsersSearchController::api_getUsersSuggestion` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SUGGESTION_USERS("watch");
   * ```
   */
  GET_SUGGESTION_USERS(search: string) {
    return `${this.selldone_api_url}/users/suggestion/${search}`;
  }

  /**
   * Suggestion customers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /shops/{shop_id}/customers/suggestion/{search}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param search - Search text passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `UsersSearchController::api_getShopCustomersSuggestion` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SUGGESTION_CUSTOMERS(123, "watch");
   * ```
   */
  GET_SUGGESTION_CUSTOMERS(shop_id: string | number, search: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/suggestion/${search}`;
  }

  /**
   * Suggestion mention users endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /users/mention/{search}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:50,2`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param search - Search text passed as a route segment. The controller matches email, name, and user id.
   * @returns Absolute URL string using `selldone-api` setup metadata; call it with the SDK HTTP client using `GET`.
   *
   * Response shape:
   * - Returns an array of users with `id` and `name`; when `shop_id` is supplied as query input, `email` is also selected for owner/staff/vendor matches.
   * - If query `shop_id` is supplied, results are limited to the shop owner, staff permissions, or vendors associated with that shop.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing `backoffice:shop:read`.
   * - 404 not_found: `ErrorCodes::NOT_FOUND_SHOP_ERROR()` when query `shop_id` is supplied but not accessible.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SUGGESTION_MENTION_USERS("mehrdad");
   * ```
   */
  GET_SUGGESTION_MENTION_USERS(search: string) {
    return `${this.selldone_api_url}/users/mention/${search}`;
  }

  /**
   * Delete me endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/delete-me
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDeleteController::api_deleteMyAccount` expose observed keys including `agree`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DELETE_ME();
   * ```
   */
  POST_DELETE_ME() {
    return `${this.selldone_api_url}/user/delete-me`;
  }

  /**
   * Set subscribe endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/subscribed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserUnsubscribeController::api_setSubscribed` expose observed keys including `subscribed`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SUBSCRIBE();
   * ```
   */
  POST_SET_SUBSCRIBE() {
    return `${this.selldone_api_url}/user/subscribed`;
  }

  /**
   * Set primary email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/email
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserChangePrimaryEmailController::api_changePrimaryEmail` expose observed keys including `password`, `error`, `error_msg`, `user_id`, `secure_key`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_PRIMARY_EMAIL();
   * ```
   */
  POST_SET_PRIMARY_EMAIL() {
    return `${this.selldone_api_url}/user/email`;
  }

  //―――――――――――――――――――――― User ――――――――――――――――――――

  /**
   * Logout endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /logout-selldone
   * @auth Required by Laravel authentication middleware.
   * @middleware `web`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:webpages`, `App\Http\Middleware\ValidLocalDomainsMiddleware`, `App\Http\Middleware\CustomerActivityMiddleware`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LogoutController::logoutSelldonePost` expose observed keys including `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.LOGOUT();
   * ```
   */
  LOGOUT() {
    return `${this.selldone_api_url}/logout-selldone`;
  }

  /**
   * Login endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /login
   * @auth Public or controller-conditional; no Laravel authentication middleware is attached to the matched route.
   * @middleware `web`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:webpages`, `App\Http\Middleware\ValidLocalDomainsMiddleware`, `App\Http\Middleware\CustomerActivityMiddleware`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `GeneralPagesController::selldoneWebappPage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.LOGIN();
   * ```
   */
  LOGIN() {
    return `${this.selldone_api_url}/login`;
  }

  /**
   * Profile upload image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /profile/edit/upload/{type}
   * @endpoint POST /profile/edit/upload/{type}/{profile_id?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Spatie\LaravelImageOptimizer\Middlewares\OptimizeImages`
   * @scope SCOPE_USER_PROFILE_WRITE (user:profile:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param type - Endpoint type discriminator passed as a route segment.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProfileController::uploadProfileImage` expose observed keys including `photo`, `url`, `size`, `success`, `files`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PROFILE_UPLOAD_IMAGE("cover", 123);
   * ```
   */
  POST_PROFILE_UPLOAD_IMAGE(type: "avatar", profile_id: string | number) {
    if (!profile_id)
      return `${this.selldone_api_url}/profile/edit/upload/${type}`; // Main profile
    return `${this.selldone_api_url}/profile/edit/upload/${type}/${profile_id}`;
  }

  /**
   * Avatar 96 endpoint URL.
   *
   * Builds the profile image URL for the main profile id (`0`) and image type `avatar92`.
   * The optional `version` parameter is appended as a cache-busting query string and is not part of the Laravel route.
   *
   * @endpoint GET /profile/image/{profile_id}/{type}.jpg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param version - Optional cache-busting version appended as `?v=`.
   * @returns Absolute URL string for the JPEG avatar image resource.
   *
   * Response shape:
   * - Binary `image/jpeg` response for profile image type `avatar92`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 404 not_found: image/profile is not found by the backend image controller.
   *
   * @example
   * ```ts
   * const url = API.GET_AVATAR_96("v1");
   * ```
   */
  GET_AVATAR_96(version = null) {
    return `${this.selldone_api_url}/profile/image/0/avatar92.jpg${
      version ? "?v=" + version : ""
    }`;
  }

  /**
   * Avatar endpoint URL.
   *
   * Builds the profile image URL for the main profile id (`0`) and image type `avatar`.
   * The optional `version` parameter is appended as a cache-busting query string and is not part of the Laravel route.
   *
   * @endpoint GET /profile/image/{profile_id}/{type}.jpg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param version - Optional cache-busting version appended as `?v=`.
   * @returns Absolute URL string for the JPEG avatar image resource.
   *
   * Response shape:
   * - Binary `image/jpeg` response for profile image type `avatar`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 404 not_found: image/profile is not found by the backend image controller.
   *
   * @example
   * ```ts
   * const url = API.GET_AVATAR("v1");
   * ```
   */
  GET_AVATAR(version = null) {
    return `${this.selldone_api_url}/profile/image/0/avatar.jpg${
      version ? "?v=" + version : ""
    }`;
  }

  /**
   * 2fa check endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /security/2fa/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `Illuminate\Http\Middleware\HandleCors`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TwoStepVerificationController::api_get2StepModeOfUser` expose observed keys including `has_2fa`, `user`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_2FA_CHECK();
   * ```
   */
  GET_2FA_CHECK() {
    return `${this.selldone_api_url}/security/2fa/check`;
  }

  /**
   * Save preferences endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /preferences
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_SECURITY_WRITE (selldone:security:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserPreferencesController::api_setUserPreferences` expose observed keys including `level`, `rating`, `low_vision`, `lang`, `dark`, `template`, `samples`, `currency`, `calendar`, `preferences`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SAVE_PREFERENCES();
   * ```
   */
  PUT_SAVE_PREFERENCES() {
    return `${this.selldone_api_url}/preferences`;
  }

  /**
   * Upload KYC image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /personal-information/upload/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Spatie\LaravelImageOptimizer\Middlewares\OptimizeImages`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PersonalInformationController::uploadPersonalImage` expose observed keys including `photo`, `identify_card`, `status`, `passport`, `address_doc`, `address_approve_at`, `url`, `size`, `success`, `files`, `personal_information`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_KYC_IMAGE("cover");
   * ```
   */
  POST_UPLOAD_KYC_IMAGE(type: "identify-card" | "address-doc" | "passport") {
    return `${this.selldone_api_url}/personal-information/upload/${type}`;
  }

  /**
   * KYC image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /personal-information/image/{type}.jpg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @param type - Endpoint type discriminator passed as a route segment.
   * @param version - Optional cache-busting version appended as query string by the SDK.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `PersonalInformationController::getImagePersonalInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_KYC_IMAGE("cover", "v1");
   * ```
   */
  GET_KYC_IMAGE(
    type: "identify-card" | "address-doc" | "passport",
    version = null,
  ) {
    return `${this.selldone_api_url}/personal-information/image/${type}.jpg${
      version ? "?v=" + version : ""
    }`;
  }

  /**
   * Personal information endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /personal-information
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PersonalInformationController::api_getPersonalInformation` expose observed keys including `personal_information`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PERSONAL_INFORMATION();
   * ```
   */
  GET_PERSONAL_INFORMATION() {
    return `${this.selldone_api_url}/personal-information`;
  }

  /**
   * Edit personal information endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /personal-information/update
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PersonalInformationController::api_updatePersonalInformationValue` expose observed keys including `name`, `birthday`, `sex`, `error`, `personal_information`, `updates`, `need_review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_PERSONAL_INFORMATION();
   * ```
   */
  POST_EDIT_PERSONAL_INFORMATION() {
    return `${this.selldone_api_url}/personal-information/update`;
  }

  /**
   * Edit personal address endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /personal-address/update
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PersonalInformationController::api_updatePersonalAddress` expose observed keys including `address`, `postal_code`, `success`, `personal_information`, `address_approve_at`, `status_address`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_PERSONAL_ADDRESS();
   * ```
   */
  POST_EDIT_PERSONAL_ADDRESS() {
    return `${this.selldone_api_url}/personal-address/update`;
  }

  /**
   * Set profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /{any?}
   * @auth Public or controller-conditional; no Laravel authentication middleware is attached to the matched route.
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `Closure` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_PROFILE();
   * ```
   */
  POST_SET_PROFILE() {
    return `${this.selldone_api_url}/profiles`;
  }

  /**
   * My profiles endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /profiles/me
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_PROFILE_READ (profile)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProfileController::api_getMyProfiles` expose observed keys including `profiles`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PROFILES();
   * ```
   */
  GET_MY_PROFILES() {
    return `${this.selldone_api_url}/profiles/me`;
  }

  /**
   * Public profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /profiles/{user_id}/@{user_slug}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param user_id - User identifier used by the route.
   * @param user_slug - Slug value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PublicProfilePageController::api_getPublicProfile` expose observed keys including `profile`, `participants`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PUBLIC_PROFILE(102, "value");
   * ```
   */
  GET_PUBLIC_PROFILE(user_id: string | number, user_slug: string) {
    return `${this.selldone_api_url}/profiles/${user_id}/@${user_slug}`;
  }

  /**
   * Mobile verify endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/mobile/verify
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PhoneVerifyController::api_requestValidatePhone` expose observed keys including `dial_code`, `country_code`, `mobile`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::PHONE_NUMBER_VERIFIED_ALREADY_ERROR()`, `ErrorCodes::CANT_SEND_SMS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MOBILE_VERIFY();
   * ```
   */
  POST_MOBILE_VERIFY() {
    return `${this.selldone_api_url}/user/mobile/verify`;
  }

  /**
   * Mobile verify code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/mobile/verify-code
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_IDENTIFICATION (selldone:identification)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PhoneVerifyController::api_verifyPhone` expose observed keys including `mobile`, `code`, `phone`, `success`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MOBILE_VERIFY_CODE();
   * ```
   */
  POST_MOBILE_VERIFY_CODE() {
    return `${this.selldone_api_url}/user/mobile/verify-code`;
  }

  /**
   * Change password endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/security/password/change
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TwoStepVerificationController::api_changePassword` expose observed keys including `password`, `re_password`, `password_temp`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::PASSWORD_MISS_MATCH_ERROR()`, `ErrorCodes::PASSWORD_INCORRECT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CHANGE_PASSWORD();
   * ```
   */
  POST_CHANGE_PASSWORD() {
    return `${this.selldone_api_url}/user/security/password/change`;
  }

  /**
   * Change socials endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/security/socials-login
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TwoStepVerificationController::api_changeSocialsLogin` expose observed keys including `socials_login`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CHANGE_SOCIALS();
   * ```
   */
  POST_CHANGE_SOCIALS() {
    return `${this.selldone_api_url}/user/security/socials-login`;
  }

  /**
   * Security data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * The controller requires a web-authenticated user context through `Controller::isWebUser()`.
   *
   * @endpoint GET /user/security
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api` setup metadata; call it with the SDK HTTP client using `GET`.
   *
   * Response shape:
   * - `phone`: authenticated user's phone value.
   * - `password_temp`: password-security temporary-password flag when `passwordSecurity` exists.
   * - `two_fa_enable`: Google 2FA enabled flag when `passwordSecurity` exists.
   * - `password_strength`: password strength value when `passwordSecurity` exists.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the request is not a web user context.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SECURITY_DATA();
   * ```
   */
  GET_SECURITY_DATA() {
    return `${this.selldone_api_url}/user/security`;
  }

  /**
   * Generate 2fa security key endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /user/security/2fa/generate
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TwoStepVerificationController::api_generate2faSecret` expose observed keys including `user_id`, `google2fa_enable`, `google2fa_secret`, `secret`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_SET_PASSWORD_FOR_USER_ERROR()`, `ErrorCodes::NOT_VERIFIED_PHONE_NUMBER_FOR_USER_ERROR()`, `ErrorCodes::FIRST_DISABLE_TWO_FACTOR_AUTH_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GENERATE_2FA_SECURITY_KEY();
   * ```
   */
  POST_GENERATE_2FA_SECURITY_KEY() {
    return `${this.selldone_api_url}/user/security/2fa/generate`;
  }

  /**
   * Enable 2fa endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /user/security/2fa/enable
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TwoStepVerificationController::enable` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::PASSWORD_NOT_SET_ERROR()`, `ErrorCodes::TWO_FACTOR_AUTH_CODE_INVALID_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ENABLE_2FA();
   * ```
   */
  POST_ENABLE_2FA() {
    return `${this.selldone_api_url}/user/security/2fa/enable`;
  }

  /**
   * Disable 2fa endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/security/2fa/disable
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TwoStepVerificationController::disable` expose observed keys including `password`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::PASSWORD_INCORRECT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DISABLE_2FA();
   * ```
   */
  POST_DISABLE_2FA() {
    return `${this.selldone_api_url}/user/security/2fa/disable`;
  }

  /**
   * Login devices endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /user/security/devices
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_SECURITY_WRITE (selldone:security:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SessionController::api_getLoginDevices` expose observed keys including `sessions`, `current`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_LOGIN_DEVICES();
   * ```
   */
  GET_LOGIN_DEVICES() {
    return `${this.selldone_api_url}/user/security/devices`;
  }

  /**
   * Logout device endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/security/devices/logout
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_SECURITY_WRITE (selldone:security:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SessionController::logoutOtherDevices` expose observed keys including `user_agent`, `ip_address`, `success`, `session`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SESSION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_LOGOUT_DEVICE();
   * ```
   */
  POST_LOGOUT_DEVICE() {
    return `${this.selldone_api_url}/user/security/devices/logout`;
  }

  /**
   * User interest endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /user/interest
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserInterestController::api_setUserInterest` expose observed keys including `business`, `topic`, `interest`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_USER_INTEREST();
   * ```
   */
  PUT_USER_INTEREST() {
    return `${this.selldone_api_url}/user/interest`;
  }

  //―――――――――――――――――――――― FCM ――――――――――――――――――――
  /**
   * Set FCM endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /fcm/token
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FCMController::api_setFCMToken` expose observed keys including `token`, `os`, `success`, `exist`, `user_id`, `shop_id`, `ip`, `country`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_FCM();
   * ```
   */
  POST_SET_FCM() {
    return `${this.selldone_api_url}/fcm/token`;
  }

  //―――――――――――――――――――――― Affiliate (shops) ――――――――――――――――――――
  /**
   * My affiliates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /affiliates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePartnerController::api_getMyAffiliates` expose observed keys including `offset`, `limit`, `error`, `affiliates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATES();
   * ```
   */
  GET_MY_AFFILIATES() {
    return `${this.selldone_api_url}/affiliates`;
  }

  /**
   * My affiliate info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /affiliates/{affiliate_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePartnerController::api_getMyAffiliateInfo` expose observed keys including `success`, `affiliate`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATE_INFO(888);
   * ```
   */
  GET_MY_AFFILIATE_INFO(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}`;
  }

  /**
   * My affiliate payments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /affiliates/{affiliate_id}/payments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePartnerController::api_getMyAffiliatePayments` expose observed keys including `error`, `affiliate_payments`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATE_PAYMENTS(888);
   * ```
   */
  GET_MY_AFFILIATE_PAYMENTS(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/payments`;
  }

  /**
   * My affiliate orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /affiliates/{affiliate_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePartnerController::api_getMyAffiliateOrders` expose observed keys including `error`, `affiliate_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATE_ORDERS(888);
   * ```
   */
  GET_MY_AFFILIATE_ORDERS(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/orders`;
  }

  /**
   * My affiliate POS endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /affiliates/{affiliate_id}/pos
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSBasketController::api_getMyAffiliatePOS` expose observed keys including `shop`, `currency`, `error`, `error_msg`, `affiliate`, `transportations`, `affiliate_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATE_POS(888);
   * ```
   */
  GET_MY_AFFILIATE_POS(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos`;
  }

  /**
   * Affiliate POS basket add item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /affiliates/{affiliate_id}/pos/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSBasketController::api_addItemToBasket` expose observed keys including `product_id`, `error`, `error_msg`, `count`, `currency`, `preferences`, `available_count`, `success`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::ORDER_COUNT_MUST_BE_POSITIVE_ERROR()`, `ErrorCodes::NOT_FOUNT_PRODUCT_ERROR()`, `ErrorCodes::BASKET_ADD_ITEM_VARIANT_NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.AFFILIATE_POS_BASKET_ADD_ITEM(888);
   * ```
   */
  AFFILIATE_POS_BASKET_ADD_ITEM(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/add`;
  }

  /**
   * Affiliate POS basket remove item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   *
   * @endpoint DELETE /affiliates/{affiliate_id}/pos/{product_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSBasketController::api_deleteItemFromBasket` expose observed keys including `error`, `error_msg`, `success`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.AFFILIATE_POS_BASKET_REMOVE_ITEM(888, 123);
   * ```
   */
  AFFILIATE_POS_BASKET_REMOVE_ITEM(
    affiliate_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/${item_id}`;
  }

  /**
   * Affiliate POS action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /affiliates/{affiliate_id}/pos/actions/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSActionsController::api_sellerSetActions` expose observed keys including `error`, `error_msg`, `receiver_info`, `delivery_price`, `code`, `discount_id`, `coupon_id`, `campaign_id`, `source`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`, `ErrorCodes::DISCOUNT_CODE_EXPIRED_ERROR()`, `ErrorCodes::DISCOUNT_CODE_NOT_FREE_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_AFFILIATE_POS_ACTION(888, "value");
   * ```
   */
  PUT_AFFILIATE_POS_ACTION(
    affiliate_id: string | number,
    action: "delivery" | "discount-code" | "coupon" | "campaign",
  ) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/actions/${action}`;
  }

  /**
   * Affiliate POS action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   *
   * @endpoint DELETE /affiliates/{affiliate_id}/pos/actions/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSActionsController::api_sellerDeleteActions` expose observed keys including `error`, `error_msg`, `receiver_info`, `delivery_price`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_AFFILIATE_POS_ACTION(888, "value");
   * ```
   */
  DELETE_AFFILIATE_POS_ACTION(
    affiliate_id: string | number,
    action: "delivery" | "discount-code" | "coupon" | "campaign",
  ) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/actions/${action}`;
  }

  /**
   * Affiliate POS coupons list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   *
   * @endpoint GET /affiliates/{affiliate_id}/pos/coupons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSActionsController::api_getCouponsListForAffiliate` expose observed keys including `error`, `error_msg`, `coupons`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_POS_COUPONS_LIST(888);
   * ```
   */
  GET_AFFILIATE_POS_COUPONS_LIST(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/coupons`;
  }

  /**
   * Affiliate POS campaigns list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   *
   * @endpoint GET /affiliates/{affiliate_id}/pos/campaigns
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSActionsController::api_getCampaignsList` expose observed keys including `error`, `error_msg`, `campaigns`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_POS_CAMPAIGNS_LIST(888);
   * ```
   */
  GET_AFFILIATE_POS_CAMPAIGNS_LIST(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/campaigns`;
  }

  /**
   * Affiliate POS confirm payment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /affiliates/{affiliate_id}/pos/payment/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSBuyController::api_BuyAffiliatePosBasket` expose observed keys including `error`, `error_msg`, `currency`, `amount_check`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_POS_CONFIRM_PAYMENT(888, "stripe");
   * ```
   */
  GET_AFFILIATE_POS_CONFIRM_PAYMENT(
    affiliate_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/payment/${gateway_code}`;
  }

  /**
   * Affiliate POS orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /affiliates/{affiliate_id}/pos/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSOrdersController::api_admin_getBasketReceivedOrders` expose observed keys including `offset`, `limit`, `sortBy`, `statuses`, `items`, `buyer`, `payment`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_POS_ORDERS(888);
   * ```
   */
  GET_AFFILIATE_POS_ORDERS(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/orders`;
  }

  /**
   * Affiliate POS products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   *
   * @endpoint GET /affiliates/{affiliate_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSProductsController::api_getProductsListForAffiliatePOS` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_POS_PRODUCTS(888);
   * ```
   */
  GET_AFFILIATE_POS_PRODUCTS(affiliate_id: string | number) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/products`;
  }

  //―――――――――――――――――――――― Direct payment upload payment receipt ――――――――――――――――――――

  /**
   * Affiliate POS upload direct payment receipt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`, `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /affiliates/{affiliate_id}/pos/transactions/{transaction_id}/upload
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AFFILIATE_WRITE (backoffice:affiliate:write)
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param transaction_id - Transaction identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePOSBuyController::uploadImageForDIRPaymentReceipt` expose observed keys including `error`, `error_msg`, `photo`, `billing_details`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AFFILIATE_POS_UPLOAD_DIRECT_PAYMENT_RECEIPT(888, 777);
   * ```
   */
  POST_AFFILIATE_POS_UPLOAD_DIRECT_PAYMENT_RECEIPT(
    affiliate_id: string | number,
    transaction_id: string,
  ) {
    return `${this.selldone_api_url}/affiliates/${affiliate_id}/pos/transactions/${transaction_id}/upload`;
  }

  //―――――――――――――――――――――― Selldone referral programs ――――――――――――――――――――
  /**
   * My referral endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /referral
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_READ (selldone:monetization:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralController::api_getMyReferralDetails` expose observed keys including `offset`, `limit`, `referral`, `sessions`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_REFERRAL();
   * ```
   */
  GET_MY_REFERRAL() {
    return `${this.selldone_api_url}/referral`;
  }

  /**
   * My referral set bank endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /referral/bank
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_WRITE (selldone:monetization:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralController::api_saveMyReferralBank` expose observed keys including `bank`, `success`, `referral`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_REFERRAL_SET_BANK();
   * ```
   */
  PUT_MY_REFERRAL_SET_BANK() {
    return `${this.selldone_api_url}/referral/bank`;
  }

  /**
   * My referral transactions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /referral/transactions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_READ (selldone:monetization:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralController::api_getMyReferralTransactions` expose observed keys including `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_REFERRAL_TRANSACTIONS();
   * ```
   */
  GET_MY_REFERRAL_TRANSACTIONS() {
    return `${this.selldone_api_url}/referral/transactions`;
  }

  /**
   * My referral wallet transfer to account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /referral/transfer/{wallet_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_WRITE (selldone:monetization:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param wallet_id - wallet_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralWalletController::api_myReferralTransferToAccount` expose observed keys including `error`, `error_msg`, `credit`, `paid_at`, `total`, `success`, `referral`, `wallet`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::REFERRAL_PROGRAM_NOT_ENABLE_ERROR()`, `ErrorCodes::NOT_FOUND_REFERRAL_CREDIT_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_ACCOUNT_ERROR()`, `ErrorCodes::MINIMUM_TRANSACTION_AMOUNT_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_REFERRAL_WALLET_TRANSFER_TO_ACCOUNT(123);
   * ```
   */
  POST_MY_REFERRAL_WALLET_TRANSFER_TO_ACCOUNT(wallet_id: number) {
    return `${this.selldone_api_url}/referral/transfer/${wallet_id}`;
  }

  /**
   * My referral request transfer to bank endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /referral/withdraw/{wallet_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_WRITE (selldone:monetization:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param wallet_id - wallet_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralWalletController::api_myReferralTransferToBank` expose observed keys including `error`, `error_msg`, `payment_requested_at`, `success`, `referral`, `wallet`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::REFERRAL_PROGRAM_NOT_ENABLE_ERROR()`, `ErrorCodes::NOT_FOUND_REFERRAL_CREDIT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_REFERRAL_REQUEST_TRANSFER_TO_BANK(123);
   * ```
   */
  POST_MY_REFERRAL_REQUEST_TRANSFER_TO_BANK(wallet_id: number) {
    return `${this.selldone_api_url}/referral/withdraw/${wallet_id}`;
  }

  /**
   * Referral invites endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /invite/send
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ReferralInviteController::api_sendInvites` expose observed keys including `invites`, `shop_id`, `error`, `error_msg`, `success`, `count`, `emails`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_REFERRAL_INVITES();
   * ```
   */
  POST_REFERRAL_INVITES() {
    return `${this.selldone_api_url}/invite/send`;
  }

  /**
   * My referral fees endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /referral/fees
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_MONETIZATION_READ (selldone:monetization:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyReferralController::api_getMyReferralFees` expose observed keys including `fees`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_REFERRAL_FEES();
   * ```
   */
  GET_MY_REFERRAL_FEES() {
    return `${this.selldone_api_url}/referral/fees`;
  }

  /* GET_REFERRAL_PROGRAMS() {
     return `${this.selldone_api_url}/referral/programs`;
   }

   POST_APPLY_FOR_REFERRAL_PROGRAMS(program_id: string | number) {
     return `${this.selldone_api_url}/referral/programs/${program_id}/apply`;
   }

   POST_REFERRAL_PROGRAM_GET_CREDIT(
     program_id: string | number,
     link_id: string | number,
     currency: keyof typeof Currency,
   ) {
     return `${this.selldone_api_url}/referral/programs/${program_id}/links/${link_id}/get/${currency}`;
   }

   POST_REFERRAL_PROGRAM_REQUEST_CREDIT_TO_BANK(
     program_id: string | number,
     link_id: string | number,
     currency: keyof typeof Currency,
   ) {
     return `${this.selldone_api_url}/referral/programs/${program_id}/links/${link_id}/request/${currency}`;
   }

   GET_REFERRAL_LINK_INFO(referral_id: string | number) {
     return `${this.selldone_api_url}/referrals/${referral_id}`;
   }

   GET_REFERRAL_LINK_PAYMENTS(referral_id: string | number) {
     return `${this.selldone_api_url}/referrals/${referral_id}/payments`;
   }

   GET_REFERRAL_INVITE_CHECK_EMAIL() {
     return `${this.selldone_api_url}/invite/check`;
   }


   PUT_REFERRAL_PROGRAM_SAVE_BANK_DETAILS(
     program_id: string | number,
     link_id: string | number,
   ) {
     return `${this.selldone_api_url}/referral/programs/${program_id}/links/${link_id}/bank`;
   }
 */

  //―――――――――――――――――――――― Referral Linked Accounts ――――――――――――――――――――

  /**
   * Gift accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gift/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_getGiftAccounts` expose observed keys including `gift_accounts`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFT_ACCOUNTS();
   * ```
   */
  GET_GIFT_ACCOUNTS() {
    return `${this.selldone_api_url}/gift/accounts`;
  }

  /**
   * Add gift account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /gift/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_setGiftAccount` expose observed keys including `account_number`, `gift_account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_GIFT_ACCOUNT();
   * ```
   */
  POST_ADD_GIFT_ACCOUNT() {
    return `${this.selldone_api_url}/gift/accounts`;
  }

  /**
   * Delete gift account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /gift/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_setGiftAccount` expose observed keys including `account_number`, `gift_account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DELETE_GIFT_ACCOUNT();
   * ```
   */
  POST_DELETE_GIFT_ACCOUNT() {
    return `${this.selldone_api_url}/gift/accounts`;
  }

  //―――――――――――――――――――――― Shops ――――――――――――――――――――

  /**
   * My shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/me
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopController::getMyShops` expose observed keys including `offset`, `limit`, `search`, `sample`, `shopPermissions`, `success`, `shops`, `total`, `invites`, `samples`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_SHOPS();
   * ```
   */
  GET_MY_SHOPS() {
    return `${this.selldone_api_url}/shops/me`;
  }

  /**
   * My shop info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/me/{shop_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::G
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopController::getMyShopInfo` expose observed keys including `shopApps`, `shopGateways`, `domains`, `transportations`, `transportationServices`, `deliveryService`, `supports`, `agencyClient`, `penalties`, `currencies`, `topics`, `posts`, `comments`, `waiting`, `waiting_count`, `shop`, `data`, `orderQue`, `avocadoQue`, `access`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_SHOP_INFO(123);
   * ```
   */
  GET_MY_SHOP_INFO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/me/${shop_id}`;
  }

  /**
   * Shop image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/upload/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopImagesController::uploadShopIcon` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.UPLOAD_SHOP_IMAGE(123);
   * ```
   */
  UPLOAD_SHOP_IMAGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/upload/icon`;
  }

  /**
   * Shop fav icon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/upload/fav
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopImagesController::uploadShopFav` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.UPLOAD_SHOP_FAV_ICON(123);
   * ```
   */
  UPLOAD_SHOP_FAV_ICON(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/upload/fav`;
  }

  /**
   * Shop image logo endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/upload/logo
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopImagesController::uploadShopLogo` expose observed keys including `photo`, `theme`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.UPLOAD_SHOP_IMAGE_LOGO(123);
   * ```
   */
  UPLOAD_SHOP_IMAGE_LOGO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/upload/logo`;
  }

  /**
   * Check shop name endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shop/check-name
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAddController::api_checkShopNameAvailable` expose observed keys including `name`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::MIN_SHOP_NAME_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_SHOP_NAME();
   * ```
   */
  GET_CHECK_SHOP_NAME() {
    return `${this.selldone_api_url}/shop/check-name`;
  }

  /**
   * Add shop endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shop/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAddController::api_addShop` expose observed keys including `name`, `title`, `language`, `active`, `currency`, `info`, `error`, `error_msg`, `success`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP();
   * ```
   */
  POST_ADD_SHOP() {
    return `${this.selldone_api_url}/shop/add`;
  }

  /**
   * Edit shop endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/edit
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopEditController::api_editShop` expose observed keys including `name`, `title`, `language`, `info`, `error`, `error_msg`, `description`, `active`, `home`, `success`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::MIN_SHOP_NAME_ERROR()`, `ErrorCodes::SHOP_NAME_EXIST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP(123);
   * ```
   */
  PUT_EDIT_SHOP(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/edit`;
  }

  /**
   * Edit shop info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/edit/info
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopInfoController::api_editShopInfo` expose observed keys including `info`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_INFO(123);
   * ```
   */
  PUT_EDIT_SHOP_INFO(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/edit/info`;
  }

  /**
   * Edit shop support mode endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/edit/support-mode
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopInfoController::api_editShopSupportMode` expose observed keys including `support_mode`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_SUPPORT_MODE(123);
   * ```
   */
  PUT_EDIT_SHOP_SUPPORT_MODE(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/edit/support-mode`;
  }

  /**
   * Shop endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint DELETE /shops/{shop_id}/delete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_DELETE (backoffice:shop:delete)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopDeleteController::api_deleteShop` expose observed keys including `name`, `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP(123);
   * ```
   */
  DELETE_SHOP(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/delete`;
  }

  /**
   * Set shop currencies endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/currencies
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCurrencyController::api_setShopCurrencies` expose observed keys including `currencies`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NO_CURRENCY_ADDED_IN_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_CURRENCIES(123);
   * ```
   */
  POST_SET_SHOP_CURRENCIES(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/currencies`;
  }

  /**
   * Transfer ownership endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/ownership
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOwnershipController::api_transferShopOwnership` expose observed keys including `error`, `error_msg`, `shop_id`, `owner_id`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_BY_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSFER_OWNERSHIP(123);
   * ```
   */
  POST_TRANSFER_OWNERSHIP(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/ownership`;
  }

  /**
   * Check time can transfer ownership endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/ownership/check-time
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOwnershipController::api_checkCanTransferShopOwnershipDate` expose observed keys including `error`, `error_msg`, `can_transfer`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CHECK_TIME_CAN_TRANSFER_OWNERSHIP(123);
   * ```
   */
  POST_CHECK_TIME_CAN_TRANSFER_OWNERSHIP(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/ownership/check-time`;
  }

  /**
   * Check user can transfer ownership endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/ownership/check-user
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOwnershipController::api_checkCanTransferShopOwnershipToUser` expose observed keys including `error`, `error_msg`, `user_check`, `staff`, `registered`, `phone`, `kyc`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CHECK_USER_CAN_TRANSFER_OWNERSHIP(123);
   * ```
   */
  POST_CHECK_USER_CAN_TRANSFER_OWNERSHIP(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/ownership/check-user`;
  }

  /**
   * Shop create temporary access endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/temporary-access
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTemporaryAccessController::api_createTemporaryAccess` expose observed keys including `force`, `success`, `temp_access`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CREATE_TEMPORARY_ACCESS(123);
   * ```
   */
  POST_SHOP_CREATE_TEMPORARY_ACCESS(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/temporary-access`;
  }

  /**
   * Shop set restriction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/restriction
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRestrictionController::api_setRestriction` expose observed keys including `restriction`, `success`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_SET_RESTRICTION(123);
   * ```
   */
  PUT_SHOP_SET_RESTRICTION(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/restriction`;
  }

  //―――――――――――――――――――――― Realtime Data ――――――――――――――――――――

  /**
   * Shop realtime data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/real-time
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StatisticController::api_getRealTimeUserActivities` expose observed keys including `lat`, `lon`, `count`, `add_baskets`, `remove_baskets`, `products_views`, `buys`, `list`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_REALTIME_DATA(123);
   * ```
   */
  GET_SHOP_REALTIME_DATA(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/real-time`;
  }

  //―――――――――――――――――――――― Shop > Category ――――――――――――――――――――

  /**
   * My shop categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_READ (backoffice:category:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryController::api_getCategories` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_SHOP_CATEGORIES(123);
   * ```
   */
  GET_MY_SHOP_CATEGORIES(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories`;
  }

  /**
   * Set category parent endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/categories/{category_id}/edit/parent
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryEditController::api_editCategoryParent` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_CATEGORY_PARENT(123, 789);
   * ```
   */
  PUT_SET_CATEGORY_PARENT(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/edit/parent`;
  }

  /**
   * Edit category filter endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/categories/{category_id}/edit/filter
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryFilterController::api_editCategoryFilter` expose observed keys including `list`, `ignore`, `filters`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_CATEGORY_FILTER(123, 789);
   * ```
   */
  PUT_EDIT_CATEGORY_FILTER(
    shop_id: string | number,
    category_id: string | number | "root",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/edit/filter`;
  }

  /**
   * Category filter endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}/filter
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryFilterController::api_deleteCategoryFilter` expose observed keys including `filters`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CATEGORY_FILTER(123, 789);
   * ```
   */
  DELETE_CATEGORY_FILTER(
    shop_id: string | number,
    category_id: string | number | "root",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/filter`;
  }

  /**
   * Category filter endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/categories/{category_id}/edit/filter
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_READ (backoffice:category:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryFilterController::api_getCategoryFilter` expose observed keys including `filters`, `spec_keys`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CATEGORY_FILTER(123, 789);
   * ```
   */
  GET_CATEGORY_FILTER(
    shop_id: string | number,
    category_id: string | number | "root",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/edit/filter`;
  }

  /**
   * Upload category image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/categories/{category_id}/edit/upload
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryImageController::category_upload_image` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_CATEGORY_IMAGE(123, 789);
   * ```
   */
  POST_UPLOAD_CATEGORY_IMAGE(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/edit/upload`;
  }

  /**
   * Add category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/categories/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryAddController::api_addCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_CATEGORY(123);
   * ```
   */
  POST_ADD_CATEGORY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/add`;
  }

  /**
   * Edit category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/categories/{category_id}/edit
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryEditController::api_editCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_CATEGORY(123, 789);
   * ```
   */
  PUT_EDIT_CATEGORY(shop_id: string | number, category_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/edit`;
  }

  /**
   * Category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryDeleteController::api_deleteCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CATEGORY(123, 789);
   * ```
   */
  DELETE_CATEGORY(shop_id: string | number, category_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}`;
  }

  /**
   * My shop categories hierarchy endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/categories-hierarchy
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_READ (backoffice:category:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryController::api_getCategoriesHierarchy` expose observed keys including `categories`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_SHOP_CATEGORIES_HIERARCHY(123);
   * ```
   */
  GET_MY_SHOP_CATEGORIES_HIERARCHY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories-hierarchy`;
  }

  /**
   * Category add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/categories/{category_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryNoteController::api_addNote` expose observed keys including `body`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CATEGORY_ADD_NOTE(123, 789);
   * ```
   */
  POST_CATEGORY_ADD_NOTE(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/note`;
  }

  /**
   * Category note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryNoteController::api_deleteNote` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CATEGORY_NOTE(123, 789, "value");
   * ```
   */
  DELETE_CATEGORY_NOTE(
    shop_id: string | number,
    category_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/note/${note_index}`;
  }

  /**
   * Category set engine endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/categories/{category_id}/engine
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryEngineController::api_editCategoryEngine` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_CATEGORY_SET_ENGINE(123, 789);
   * ```
   */
  PUT_CATEGORY_SET_ENGINE(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/engine`;
  }

  /**
   * Category all sub categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/categories/{category_id}/sub-categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_READ (backoffice:category:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryController::api_getSubCategories` expose observed keys including `ids`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CATEGORY_ALL_SUB_CATEGORIES(123, 789);
   * ```
   */
  GET_CATEGORY_ALL_SUB_CATEGORIES(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/sub-categories`;
  }

  //―――――――――――――――――――――― Product ――――――――――――――――――――

  /**
   * Clone product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/products/{shop_id}/clone
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductCloneController::api_cloneProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CLONE_PRODUCT(123);
   * ```
   */
  POST_CLONE_PRODUCT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/products/${shop_id}/clone`;
  }

  /**
   * Set product category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/edit/category
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductEditController::api_editProductCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_CATEGORY(123, 456);
   * ```
   */
  PUT_SET_PRODUCT_CATEGORY(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/category`;
  }

  /**
   * All my products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/all-admin
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductController::api_getMyProductsAll` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ALL_MY_PRODUCTS(123);
   * ```
   */
  GET_ALL_MY_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/all-admin`;
  }

  /**
   * All my products list by params endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/list-custom-params
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductController::api_getProductsListByParams` expose observed keys including `params`, `list`, `products`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ALL_MY_PRODUCTS_LIST_BY_PARAMS(123);
   * ```
   */
  GET_ALL_MY_PRODUCTS_LIST_BY_PARAMS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/list-custom-params`;
  }

  /**
   * All my categories list by params endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/categories/list-custom-params
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_READ (backoffice:category:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryController::api_getCategoriesListByParams` expose observed keys including `params`, `list`, `categories`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ALL_MY_CATEGORIES_LIST_BY_PARAMS(123);
   * ```
   */
  GET_ALL_MY_CATEGORIES_LIST_BY_PARAMS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/list-custom-params`;
  }

  /**
   * Edit variant endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/variants/{variant_id}/edit
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVariantsController::api_editVariant` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_VARIANT(123, 456, 201);
   * ```
   */
  POST_EDIT_VARIANT(
    shop_id: string | number,
    product_id: string | number,
    variant_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variants/${variant_id}/edit`;
  }

  /**
   * Add variant endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/variant/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVariantsController::api_addVariant` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_VARIANT(123, 456);
   * ```
   */
  POST_ADD_VARIANT(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variant/add`;
  }

  /**
   * Add variant bulk endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/variant/add-bulk
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVariantsAddBulkController::api_addBulkVariants` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_VARIANT_BULK(123, 456);
   * ```
   */
  POST_ADD_VARIANT_BULK(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variant/add-bulk`;
  }

  /**
   * Variant endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/variants/{variant_id}/delete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVariantsController::api_deleteVariant` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_VARIANT(123, 456, 201);
   * ```
   */
  DELETE_VARIANT(
    shop_id: string | number,
    product_id: string | number,
    variant_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variants/${variant_id}/delete`;
  }

  /**
   * Restore deleted variant endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/variants/{variant_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVariantsController::api_restoreVariant` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_DELETED_VARIANT(123, 456, 201);
   * ```
   */
  POST_RESTORE_DELETED_VARIANT(
    shop_id: string | number,
    product_id: string | number,
    variant_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variants/${variant_id}/restore`;
  }

  /**
   * Product quantity endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/variants/{variant_id}/quantity
   * @endpoint POST /shops/{shop_id}/products/{product_id}/quantity
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductInventoryController::api_setQuantity` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_QUANTITY(123, 456, 201);
   * ```
   */
  POST_PRODUCT_QUANTITY(
    shop_id: string | number,
    product_id: string | number,
    variant_id = null,
  ) {
    if (variant_id !== null)
      return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/variants/${variant_id}/quantity`;

    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/quantity`;
  }

  /**
   * Add virtual item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/add-virtual
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddVirtualController::api_addVirtualProductItem` expose observed keys including `error`, `error_msg`, `quantity`, `success`, `virtual_item`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_VIRTUAL_ITEM(123, 456);
   * ```
   */
  POST_ADD_VIRTUAL_ITEM(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/add-virtual`;
  }

  /**
   * Edit virtual item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/edit-virtual/{virtualItem_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param virtualItem_id - virtualItem_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EditVirtualController::api_updateVirtualProductItem` expose observed keys including `error`, `error_msg`, `data`, `variant_id`, `success`, `virtual_item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`, `ErrorCodes::NOT_FOUND_VIRTUAL_ITEM_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.EDIT_VIRTUAL_ITEM(123, 456, 123);
   * ```
   */
  EDIT_VIRTUAL_ITEM(
    shop_id: string | number,
    product_id: string | number,
    virtualItem_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit-virtual/${virtualItem_id}`;
  }

  /**
   * Virtual item bulk endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/add-virtual-bulk
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddVirtualController::api_addVirtualProductItemBulk` expose observed keys including `dataset`, `error`, `error_msg`, `quantity`, `success`, `success_count`, `fail_count`, `errors`, `add`, `update`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_VIRTUAL_ITEM_BULK(123, 456);
   * ```
   */
  POST_VIRTUAL_ITEM_BULK(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/add-virtual-bulk`;
  }

  /**
   * Virtual item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/edit-virtual/{virtualItem_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param virtualItem_id - virtualItem_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EditVirtualController::api_deleteVirtualProductItem` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUND_VIRTUAL_ITEM_ERROR()`, `ErrorCodes::CANT_DELETE_PAYED_VIRTUAL_ITEM()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_VIRTUAL_ITEM(123, 456, 123);
   * ```
   */
  DELETE_VIRTUAL_ITEM(
    shop_id: string | number,
    product_id: string | number,
    virtualItem_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit-virtual/${virtualItem_id}`;
  }

  /**
   * All virtual items endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/virtual/items
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VirtualController::api_getVirtualItems` expose observed keys including `virtual_items`, `total`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ALL_VIRTUAL_ITEMS(123, 456);
   * ```
   */
  GET_ALL_VIRTUAL_ITEMS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/virtual/items`;
  }

  /**
   * Product info admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/admin
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductController::api_getMyProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_INFO_ADMIN(123, 456);
   * ```
   */
  GET_PRODUCT_INFO_ADMIN(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/admin`;
  }

  /**
   * Add product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/product/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductAddController::api_addProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT(123);
   * ```
   */
  POST_ADD_PRODUCT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/product/add`;
  }

  /**
   * Edit product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/edit
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductEditController::api_editProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_PRODUCT(123, 456);
   * ```
   */
  PUT_EDIT_PRODUCT(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit`;
  }

  /**
   * Edit product price endpoint URL.
   *
   * Builds the product price update URL. The backend route accepts an optional `variant_id`; this SDK method targets the product-level route without that segment.
   * This write endpoint modifies product pricing data.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/edit/price/{variant_id?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string for the product price update endpoint.
   *
   * Response shape:
   * - Backend returns the edited product/price response from `ProductEditController::api_editProductPrice`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing product write scope or product-region write permission.
   * - 404 not_found: shop or product is not found.
   * - 422 validation_error: price/currency fields fail Laravel validation.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_PRODUCT_PRICE(123, 456);
   * ```
   */
  PUT_EDIT_PRODUCT_PRICE(
    shop_id: string | number,
    product_id: string | number,
    variant_id: string | number,
  ) {
    // Fast edit price
    return `${
      this.selldone_api_url
    }/shops/${shop_id}/products/${product_id}/edit/price${
      variant_id ? "/" + variant_id : ""
    }`;
  }

  /**
   * Product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/delete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductDeleteController::api_deleteProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT(123, 456);
   * ```
   */
  DELETE_PRODUCT(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/delete`;
  }

  /**
   * Products bulk endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/bulk-actions/delete-products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductDeleteBulkController::api_deleteBulkProducts` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCTS_BULK(123);
   * ```
   */
  DELETE_PRODUCTS_BULK(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/bulk-actions/delete-products`;
  }

  /**
   * Shop auto remove duplicated products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/bulk-actions/remove-duplicates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductsBulkActionsRemoveDuplicatesController::api_removeDuplicatedProducts` expose observed keys including `mode`, `category_id`, `success`, `count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_AUTO_REMOVE_DUPLICATED_PRODUCTS(123);
   * ```
   */
  POST_SHOP_AUTO_REMOVE_DUPLICATED_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/bulk-actions/remove-duplicates`;
  }
  /**
   * Shop auto remove duplicated products preview endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/bulk-actions/remove-duplicates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductsBulkActionsRemoveDuplicatesController::api_getDuplicatedProducts` expose observed keys including `mode`, `category_id`, `offset`, `limit`, `total`, `products`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_AUTO_REMOVE_DUPLICATED_PRODUCTS_PREVIEW(123);
   * ```
   */
  GET_SHOP_AUTO_REMOVE_DUPLICATED_PRODUCTS_PREVIEW(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/bulk-actions/remove-duplicates`;
  }


  /**
   * Restore deleted product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductDeleteController::api_restoreProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_DELETED_PRODUCT(123, 456);
   * ```
   */
  POST_RESTORE_DELETED_PRODUCT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/restore`;
  }

  /**
   * Product extra endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/extra
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExtraController::api_setExtra` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_EXTRA(123, 456);
   * ```
   */
  PUT_PRODUCT_EXTRA(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/extra`;
  }

  /**
   * Add product rating endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/rating
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductRatingController::api_addRating` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT_RATING(123, 456);
   * ```
   */
  POST_ADD_PRODUCT_RATING(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/rating`;
  }

  /**
   * Product rating endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/rating
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductRatingController::api_deleteRating` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_RATING(123, 456);
   * ```
   */
  DELETE_PRODUCT_RATING(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/rating`;
  }

  /**
   * Add pros endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/pros
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_addPros` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PROS(123, 456);
   * ```
   */
  POST_ADD_PROS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/pros`;
  }

  /**
   * Pros endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/pros
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_deletePros` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PROS(123, 456);
   * ```
   */
  DELETE_PROS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/pros`;
  }

  /**
   * Add cons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/cons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_addCons` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_CONS(123, 456);
   * ```
   */
  POST_ADD_CONS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cons`;
  }

  /**
   * Cons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/cons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_deleteCons` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CONS(123, 456);
   * ```
   */
  DELETE_CONS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cons`;
  }

  /**
   * Update pros endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/pros
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_editPros` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_PROS(123, 456);
   * ```
   */
  PUT_UPDATE_PROS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/pros`;
  }

  /**
   * Update cons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/cons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductProsConsController::api_editCons` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_CONS(123, 456);
   * ```
   */
  PUT_UPDATE_CONS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cons`;
  }

  /**
   * Spec of product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/wizard/spec/{product_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductSpecController::adi_getProductSpec` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SPEC_OF_PRODUCT(123, "value");
   * ```
   */
  GET_SPEC_OF_PRODUCT(shop_id: string | number, product_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/wizard/spec/${product_code}`;
  }

  /**
   * Google product category lookup endpoint URL.
   *
   * Builds the optional-search Google category route used by the product wizard.
   *
   * @endpoint GET /shops/wizard/google-category/{search?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param gpc - Optional search text/category query appended as `{search}`.
   * @returns Absolute URL string for Google product category suggestions.
   *
   * Response shape:
   * - Backend response is defined by `ProductController::adi_getGoogleProductCategory` and contains Google product category matches.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 5xx server_error: backend/category provider failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GOOGLE_CATEGORY("watch");
   * ```
   */
  GET_GOOGLE_CATEGORY(gpc: string | null) {
    return `${this.selldone_api_url}/shops/wizard/google-category${
      gpc ? `/${gpc}` : ""
    }`;
  }

  /**
   * Save spec endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/spec
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductSpecController::api_saveSpec` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SAVE_SPEC(123, 456);
   * ```
   */
  POST_SAVE_SPEC(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/spec`;
  }

  /**
   * Upload product cover endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/cover/{variant_id?}
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/cover
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImagesController::upload_product_image_gallery` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PRODUCT_COVER(123, 456, 201);
   * ```
   */
  POST_UPLOAD_PRODUCT_COVER(
    shop_id: string | number,
    product_id: string | number,
    variant_id: string | number | null = null,
  ) {
    if (variant_id)
      return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/cover/${variant_id}`; // Maybe '/' cause problem!
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/cover`;
  }

  /**
   * Upload product main image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImagesController::upload_product_main_image` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PRODUCT_MAIN_IMAGE(123, 456);
   * ```
   */
  POST_UPLOAD_PRODUCT_MAIN_IMAGE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/icon`;
  }

  /**
   * Product images order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/images/order
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImagesController::adi_setProductImagesOrder` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_IMAGES_ORDER(123, 456);
   * ```
   */
  PUT_PRODUCT_IMAGES_ORDER(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/images/order`;
  }

  /**
   * Product image alt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/images/{image_id}/alt
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param image_id - Image identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImagesController::api_setProductImageAlt` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_IMAGE_ALT(123, 456, 202);
   * ```
   */
  PUT_PRODUCT_IMAGE_ALT(
    shop_id: string | number,
    product_id: string | number,
    image_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/images/${image_id}/alt`;
  }

  /**
   * Product image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/images/{image_id}/delete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param image_id - Image identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImagesController::delete_image_cover` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_IMAGE(123, 456, 202);
   * ```
   */
  DELETE_PRODUCT_IMAGE(
    shop_id: string | number,
    product_id: string | number,
    image_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/images/${image_id}/delete`;
  }

  /**
   * Product files endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/files
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileController::api_getFiles` expose observed keys including `content_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_FILES(123, 456);
   * ```
   */
  GET_PRODUCT_FILES(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files`;
  }

  /**
   * Product file upload URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/edit/upload/url
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileUploadController::api_getUploadFileUrl` expose observed keys including `filename`, `size`, `content_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_FILE_UPLOAD_URL(123, 456);
   * ```
   */
  GET_PRODUCT_FILE_UPLOAD_URL(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/url`;
  }

  /**
   * Upload product file endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/file
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileUploadController::upload_file` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::FILE_SIZE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PRODUCT_FILE(123, 456);
   * ```
   */
  POST_UPLOAD_PRODUCT_FILE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    // Auto get by upload url generation!
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/file`;
  }

  /**
   * Product file endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/files/{file_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_id - File identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `FileEditController::delete_file` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_FILE(123, 456, 203);
   * ```
   */
  DELETE_PRODUCT_FILE(
    shop_id: string | number,
    product_id: string | number,
    file_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files/${file_id}`;
  }

  /**
   * Download product file endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/files/{file_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_id - File identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `FileEditController::delete_file` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DOWNLOAD_PRODUCT_FILE(123, 456, 203);
   * ```
   */
  DOWNLOAD_PRODUCT_FILE(
    shop_id: string | number,
    product_id: string | number,
    file_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files/${file_id}`;
  }

  /**
   * Set product file name endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/files/{file_id}/name
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_id - File identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileEditController::setFileName` expose observed keys including `name`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_FILE_NAME(123, 456, 203);
   * ```
   */
  PUT_SET_PRODUCT_FILE_NAME(
    shop_id: string | number,
    product_id: string | number,
    file_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files/${file_id}/name`;
  }

  /**
   * Set product files sort endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/files/sort
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileEditController::setFilesSort` expose observed keys including `sort`, `content_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_FILES_SORT(123, 456);
   * ```
   */
  PUT_SET_PRODUCT_FILES_SORT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files/sort`;
  }

  /**
   * Set product file sample endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/files/{file_id}/sample
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_id - File identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileEditController::setSample` expose observed keys including `sample`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_FILE_SAMPLE(123, 456, 203);
   * ```
   */
  PUT_SET_PRODUCT_FILE_SAMPLE(
    shop_id: string | number,
    product_id: string | number,
    file_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/files/${file_id}/sample`;
  }

  /**
   * Product add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductNoteController::api_addNote` expose observed keys including `body`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_ADD_NOTE(123, 456);
   * ```
   */
  POST_PRODUCT_ADD_NOTE(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/note`;
  }

  /**
   * Product note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductNoteController::api_deleteNote` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_NOTE(123, 456, "value");
   * ```
   */
  DELETE_PRODUCT_NOTE(
    shop_id: string | number,
    product_id: string | number,
    note_index: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/note/${note_index}`;
  }

  /**
   * Product set tags endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductTagsController::api_setTags` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SET_TAGS(123, 456);
   * ```
   */
  POST_PRODUCT_SET_TAGS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/tags`;
  }

  /**
   * Product set page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/page
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductPageController::api_setPage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SET_PAGE(123, 456);
   * ```
   */
  POST_PRODUCT_SET_PAGE(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/page`;
  }

  /**
   * Product set outputs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/outputs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductOutputsController::api_setOutput` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SET_OUTPUTS(123, 456);
   * ```
   */
  POST_PRODUCT_SET_OUTPUTS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/outputs`;
  }

  /**
   * Set product locations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/locations
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductLocationsController::api_setLocations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_LOCATIONS(123, 456);
   * ```
   */
  PUT_SET_PRODUCT_LOCATIONS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/locations`;
  }


  /**
   * Product video upload endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/videos
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVideoController::api_uploadProductVideo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_VIDEO_UPLOAD(123, 456);
   * ```
   */
  POST_PRODUCT_VIDEO_UPLOAD(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/videos`;
  }
  /**
   * Product video endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/videos/{video_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param video_id - video_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVideoController::api_deleteProductVideo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_VIDEO(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_VIDEO(shop_id: string | number, product_id: string | number, video_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/videos/${video_id}`;
  }




  /**
   * Add product locations saves endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/product-locations
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductLocationSavedController::api_saveLocationSet` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT_LOCATIONS_SAVES(123);
   * ```
   */
  POST_ADD_PRODUCT_LOCATIONS_SAVES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/product-locations`;
  }

  /**
   * Product locations saves endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/product-locations
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductLocationSavedController::api_getLocationSet` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_LOCATIONS_SAVES(123);
   * ```
   */
  GET_PRODUCT_LOCATIONS_SAVES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/product-locations`;
  }

  /**
   * Search product repository sku endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /product-repository/search
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:100,30`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductRepositoryController::api_searchBySku` expose observed keys including `sku`, `result`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SEARCH_PRODUCT_REPOSITORY_SKU();
   * ```
   */
  GET_SEARCH_PRODUCT_REPOSITORY_SKU() {
    return `${this.selldone_api_url}/product-repository/search`;
  }

  /**
   * Add product by repository endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/product-repository/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:100,30`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductRepositoryController::api_addProductByRepository` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT_BY_REPOSITORY(123);
   * ```
   */
  POST_ADD_PRODUCT_BY_REPOSITORY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/product-repository/add`;
  }

  /**
   * Add custom badge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/badges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductBadgesController::adi_addBadge` expose observed keys including `title`, `pattern`, `link`, `error`, `error_msg`, `success`, `badge`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_CUSTOM_BADGE(123);
   * ```
   */
  POST_ADD_CUSTOM_BADGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/badges`;
  }

  /**
   * Edit custom badge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/badges/{badge_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param badge_id - badge_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductBadgesController::adi_editBadge` expose observed keys including `title`, `pattern`, `link`, `error`, `error_msg`, `success`, `badge`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_CUSTOM_BADGE(123, 123);
   * ```
   */
  POST_EDIT_CUSTOM_BADGE(shop_id: string | number, badge_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/badges/${badge_id}`;
  }

  /**
   * Custom badge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/badges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductBadgesController::adi_getBadge` expose observed keys including `success`, `badges`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CUSTOM_BADGE(123);
   * ```
   */
  GET_CUSTOM_BADGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/badges`;
  }

  /**
   * Set product badge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/badges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductBadgesController::adi_setProductBadges` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_PRODUCT_BADGE(123, 456);
   * ```
   */
  POST_SET_PRODUCT_BADGE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/badges`;
  }

  /**
   * Category move products order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryDeleteController::api_deleteCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CATEGORY_MOVE_PRODUCTS_ORDER(123, 789);
   * ```
   */
  POST_CATEGORY_MOVE_PRODUCTS_ORDER(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${
      category_id ? category_id : "root"
    }/products-order`;
  }

  /**
   * Category move categories order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryDeleteController::api_deleteCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CATEGORY_MOVE_CATEGORIES_ORDER(123, 789);
   * ```
   */
  POST_CATEGORY_MOVE_CATEGORIES_ORDER(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${
      category_id ? category_id : "root"
    }/categories-order`;
  }

  /**
   * Assign vendors to products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/marketplace/bulk-actions/assign-products-vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MarketplaceBulkActionsController::api_assignProductsVendors` expose observed keys including `product_ids`, `vendor_ids`, `pricing_id`, `quantity`, `clear`, `error`, `error_msg`, `shop_id`, `vendor_id`, `product_id`, `variant_id`, `enable`, `raw_price`, `price`, `currency`, `commission`, `discount`, `dis_start`, `dis_end`, `lead`, `success`, `count`, `deletes`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ASSIGN_VENDORS_TO_PRODUCTS(123);
   * ```
   */
  POST_ASSIGN_VENDORS_TO_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/marketplace/bulk-actions/assign-products-vendors`;
  }

  /**
   * Assign profile to products in category endpoint URL.
   *
   * Builds the category bulk profile assignment URL. Passing a falsy `category_id` makes the SDK send `root`, which the backend treats as the root category.
   * This write endpoint updates profile fields across products in a category.
   *
   * @endpoint POST /shops/{shop_id}/categories/{category_id}/bulk-actions/assign-products-profile
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier, or falsy value to target `root`.
   * @returns Absolute URL string for the bulk assignment endpoint.
   *
   * Response shape:
   * - `success`: true on completion.
   * - `count`: number of products updated.
   * - `message`: human-readable update summary.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing category write scope or product-region write permission.
   * - 404 not_found: shop, category, logistic profile, tax profile, or map tag is not found.
   * - 422 validation_error: `type` or `value` fails Laravel validation.
   *
   * @example
   * ```ts
   * const url = API.POST_ASSIGN_PROFILE_TO_PRODUCTS_IN_CATEGORY(123, 789);
   * ```
   */
  POST_ASSIGN_PROFILE_TO_PRODUCTS_IN_CATEGORY(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${
      category_id ? category_id : "root"
    }/bulk-actions/assign-products-profile`;
  }

  /**
   * Assign status to products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/marketplace/bulk-actions/assign-products-status
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductBulkActionsController::api_bulkSetProductsStatus` expose observed keys including `product_ids`, `status`, `error`, `error_msg`, `success`, `count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ASSIGN_STATUS_TO_PRODUCTS(123);
   * ```
   */
  POST_ASSIGN_STATUS_TO_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/marketplace/bulk-actions/assign-products-status`;
  }

  // ▀▀▀▀▀▀▀▀▀ 🌸 Add extra pricing 🌸 ▀▀▀▀▀▀▀▀▀

  /**
   * Product add extra pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/extra-pricings
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExtraPricingAddController::add` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_ADD_EXTRA_PRICINGS(123, 456);
   * ```
   */
  POST_PRODUCT_ADD_EXTRA_PRICINGS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/extra-pricings`;
  }

  /**
   * Product edit extra pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/extra-pricings/{extra_pricing_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param extra_pricing_id - extra_pricing_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExtraPricingEditController::edit` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_EDIT_EXTRA_PRICINGS(123, 456, 123);
   * ```
   */
  PUT_PRODUCT_EDIT_EXTRA_PRICINGS(
    shop_id: string | number,
    product_id: string | number,
    extra_pricing_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/extra-pricings/${extra_pricing_id}`;
  }

  /**
   * Product edit extra pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/extra-pricings/{extra_pricing_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param extra_pricing_id - extra_pricing_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExtraPricingDeleteController::delete` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_EDIT_EXTRA_PRICINGS(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_EDIT_EXTRA_PRICINGS(
    shop_id: string | number,
    product_id: string | number,
    extra_pricing_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/extra-pricings/${extra_pricing_id}`;
  }

  //―――――――――――――――――――――― Product > Thresholds ――――――――――――――――――――

  /**
   * Set product thresholds endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/thresholds
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductThresholdsController::api_setProductThreshold` expose observed keys including `custom`, `progress`, `carts`, `variables`, `thresholds`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_THRESHOLDS(123, 456);
   * ```
   */
  PUT_SET_PRODUCT_THRESHOLDS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/thresholds`;
  }

  //―――――――――――――――――――――― Product > 3D / AR ――――――――――――――――――――

  /**
   * Upload product 3d model endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/ar/{type}/{variant_id?}
   * @endpoint POST /shops/{shop_id}/products/{product_id}/edit/upload/ar/{file_type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_type - Route parameter passed through to the backend endpoint.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `Product3DARController::api_set3DModel` expose observed keys including `error`, `error_msg`, `ar`, `url`, `path`, `name`, `size`, `diff_size`, `success`, `files`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`, `ErrorCodes::BAD_FILE_FORMAT_ERROR()`, `ErrorCodes::BAD_3D_FILE_FORMAT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PRODUCT_3D_MODEL(123, 456, "value", 201);
   * ```
   */
  POST_UPLOAD_PRODUCT_3D_MODEL(
    shop_id: string | number,
    product_id: string | number,
    file_type: "src" | "ios",
    variant_id = null,
  ) {
    if (variant_id)
      return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/ar/${file_type}/${variant_id}`; // Maybe '/' cause problem!
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/edit/upload/ar/${file_type}`;
  }

  /**
   * Product 3d model endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/ar/{type}/{variant_id?}
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/ar/{file_type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param file_type - Route parameter passed through to the backend endpoint.
   * @param variant_id - Product variant identifier scoped to the product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `Product3DARController::api_delete3DModel` expose observed keys including `error`, `error_msg`, `ar`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_3D_MODEL(123, 456, "value", 201);
   * ```
   */
  DELETE_PRODUCT_3D_MODEL(
    shop_id: string | number,
    product_id: string | number,
    file_type: "src" | "ios",
    variant_id: string | number | null = null,
  ) {
    if (variant_id)
      return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ar/${file_type}/${variant_id}`; // Maybe '/' cause problem!
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ar/${file_type}`;
  }

  //―――――――――――――――――――――― Shop > Orders ――――――――――――――――――――

  /**
   * Orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets-{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsController::api_admin_getBasketReceivedOrders` expose observed keys including `offset`, `limit`, `sortBy`, `statuses`, `product_id`, `affiliate_id`, `customer_id`, `shop_connect_id`, `subscription_price_id`, `items`, `buyer`, `connect_orders`, `payment`, `with`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ORDERS(123, "cover");
   * ```
   */
  GET_ORDERS(shop_id: string | number, type: OrderType) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets-${type}`;
  }

  /**
   * Vendors orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/vendor-orders-{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VendorOrdersMarketplaceController::api_admin_getVendorsOrders` expose observed keys including `statuses`, `items`, `buyer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_VENDORS_ORDERS(123, "cover");
   * ```
   */
  GET_VENDORS_ORDERS(shop_id: string | number, type: OrderType) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/vendor-orders-${type}`;
  }

  /**
   * Order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{basket_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `BasketMerchantsController::api_admin_getBasketReceivedOrderInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ORDER_INFO(123, 333);
   * ```
   */
  GET_ORDER_INFO(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}`;
  }

  /**
   * Update order state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/state
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `BasketMerchantsActionsController::api_admin_postUpdateDeliveryState` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_ORDER_STATE(123, 333);
   * ```
   */
  POST_UPDATE_ORDER_STATE(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/state`;
  }

  /**
   * Reject order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsActionsController::api_admin_setRejectOrder` expose observed keys including `reject`, `express`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_REJECT_ORDER(123, 333);
   * ```
   */
  PUT_REJECT_ORDER(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/reject`;
  }

  /**
   * Reject order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/process-center/baskets/{basket_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsActionsController::api_admin_deleteRejectOrder` expose observed keys including `error`, `error_msg`, `reject`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_REJECT_ORDER(123, 333);
   * ```
   */
  DELETE_REJECT_ORDER(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/reject`;
  }

  /**
   * Update order delivery return endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/delivery-returned
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsActionsController::api_admin_postUpdateDeliveryStateReturnedDelivery` expose observed keys including `delivery_state`, `changed`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_ORDER_DELIVERY_RETURN(123, 333);
   * ```
   */
  POST_UPDATE_ORDER_DELIVERY_RETURN(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/delivery-returned`;
  }

  /**
   * Set tracking order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/tracking
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsActionsController::api_admin_setDeliveryTrackingOrder` expose observed keys including `tracking_code`, `tracking_url`, `delivery_info`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_TRACKING_ORDER(123, 333);
   * ```
   */
  PUT_SET_TRACKING_ORDER(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/tracking`;
  }

  /**
   * Check fulfillment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/fulfillment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingOrdersController::api_admin_setCheckOrderFulfillment` expose observed keys including `check`, `fulfillment_id`, `error`, `error_msg`, `check_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_CHECK_FULFILLMENT(123, 333);
   * ```
   */
  PUT_CHECK_FULFILLMENT(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/fulfillment`;
  }

  /**
   * Service item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/service/{item_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketServiceController::api_admin_setOrderServiceItem` expose observed keys including `price`, `tasks`, `reserved_at`, `subscription`, `charge`, `start_at`, `end_at`, `success`, `service_item`, `item_price`, `basket_price`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ITEM_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SERVICE_ITEM(123, 333, 123);
   * ```
   */
  PUT_SERVICE_ITEM(
    shop_id: string | number,
    basket_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/service/${item_id}`;
  }

  /**
   * Service item tasks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/service/{item_id}/tasks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketServiceController::api_updateTasks` expose observed keys including `tasks`, `success`, `service_item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ITEM_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SERVICE_ITEM_TASKS(123, 333, 123);
   * ```
   */
  PUT_SERVICE_ITEM_TASKS(
    shop_id: string | number,
    basket_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/service/${item_id}/tasks`;
  }

  /**
   * Basket item file download link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{basket_id}/files/{file_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param file_id - File identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketItemFilesController::api_getFileDownloadLink` expose observed keys including `error`, `error_msg`, `ResponseContentDisposition`, `url`, `expire`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_BASKET_ITEM_FILE_DOWNLOAD_LINK(123, 333, 203);
   * ```
   */
  GET_BASKET_ITEM_FILE_DOWNLOAD_LINK(
    shop_id: string | number,
    basket_id: string | number,
    file_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/files/${file_id}`;
  }

  /**
   * Update vendor order by marketplace owner endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/vendor-orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VendorOrdersMarketplaceActionsController::api_admin_updateVendorOrderDeliveryState` expose observed keys including `vendor_id`, `delivery_state`, `error`, `error_msg`, `success`, `vendor_order`, `basket`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_VENDOR_ORDER_BY_MARKETPLACE_OWNER(123, 333);
   * ```
   */
  PUT_UPDATE_VENDOR_ORDER_BY_MARKETPLACE_OWNER(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/vendor-orders`;
  }

  /**
   * Update order receiver info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/receiver-info
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantEditReceiverInfoController::xapi_editOrderReceiverInfoBySeller` expose observed keys including `receiver_info`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_ORDER_RECEIVER_INFO(123, 333);
   * ```
   */
  PUT_UPDATE_ORDER_RECEIVER_INFO(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/receiver-info`;
  }

  /**
   * Basket chat add message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/chat
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketChatAdminController::api_addChat` expose observed keys including `body`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BASKET_CHAT_ADD_MESSAGE(123, 333);
   * ```
   */
  POST_BASKET_CHAT_ADD_MESSAGE(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/chat`;
  }

  /**
   * Basket chat message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/process-center/baskets/{basket_id}/chat/{message_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param message_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `BasketChatAdminController::api_deleteChat` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_BASKET_CHAT_MESSAGE(123, 333, "value");
   * ```
   */
  DELETE_BASKET_CHAT_MESSAGE(
    shop_id: string | number,
    basket_id: string | number,
    message_index: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/chat/${message_index}`;
  }

  /**
   * Basket refund vendor order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/vendor-orders/{vendor_order_id}/refund
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_order_id - vendor_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VendorOrdersMarketplaceRefundController::api_admin_refundVendorOrder` expose observed keys including `currency`, `amount`, `note`, `error`, `error_msg`, `success`, `account`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_VENDOR_ORDER_ERROR()`, `ErrorCodes::NOT_VALID_CURRENCY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BASKET_REFUND_VENDOR_ORDER(123, 123);
   * ```
   */
  POST_BASKET_REFUND_VENDOR_ORDER(
    shop_id: string | number,
    vendor_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-orders/${vendor_order_id}/refund`;
  }

  /**
   * Basket create secure link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/secure-link
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketSecureLinkController::api_createSecureLinkForOrder` expose observed keys including `url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BASKET_CREATE_SECURE_LINK(123, 333);
   * ```
   */
  POST_BASKET_CREATE_SECURE_LINK(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/secure-link`;
  }

  /**
   * Basket send order update status email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/send-update-status
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketSendUpdateEmailController::api_sendUpdateStatusEmail` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BASKET_SEND_ORDER_UPDATE_STATUS_EMAIL(123, 333);
   * ```
   */
  POST_BASKET_SEND_ORDER_UPDATE_STATUS_EMAIL(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/send-update-status`;
  }

  //―――――――――――――――――――――― Shop > Message Template ――――――――――――――――――――
  /**
   * Shop template messages endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/template-messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTemplateMessageController::api_getTemplateMessagesList` expose observed keys including `offset`, `limit`, `search`, `templates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TEMPLATE_MESSAGES(123);
   * ```
   */
  GET_SHOP_TEMPLATE_MESSAGES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/template-messages`;
  }

  /**
   * Shop template message add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/template-messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTemplateMessageController::api_addTemplateMessagesList` expose observed keys including `title`, `body`, `error`, `error_msg`, `success`, `template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_TEMPLATE_MESSAGE_ADD(123);
   * ```
   */
  POST_SHOP_TEMPLATE_MESSAGE_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/template-messages`;
  }

  /**
   * Shop template message edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/template-messages/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTemplateMessageController::api_updateTemplateMessagesList` expose observed keys including `title`, `body`, `error`, `error_msg`, `success`, `template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_TEMPLATE_MESSAGE_EDIT(123, 123);
   * ```
   */
  PUT_SHOP_TEMPLATE_MESSAGE_EDIT(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/template-messages/${template_id}`;
  }

  //―――――――――――――――――――――― Shop > Fulfillments ――――――――――――――――――――
  /**
   * Update fulfillment order state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/fulfillments/{basket_id}/state
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `DropShippingMainSellerOrderProcessingController::api_admin_postUpdateDeliveryState` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_FULFILLMENT_ORDER_STATE(123, 333);
   * ```
   */
  POST_UPDATE_FULFILLMENT_ORDER_STATE(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/fulfillments/${basket_id}/state`;
  }

  /**
   * Update fulfillment order delivery return endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/fulfillments/{basket_id}/delivery-returned
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMainSellerOrderProcessingController::api_admin_postUpdateDeliveryStateReturnedDelivery` expose observed keys including `error`, `error_msg`, `delivery_state`, `changed`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_FULFILLMENT_ORDER_DELIVERY_RETURN(123, 333);
   * ```
   */
  POST_UPDATE_FULFILLMENT_ORDER_DELIVERY_RETURN(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/fulfillments/${basket_id}/delivery-returned`;
  }

  /**
   * Reject fulfillment order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/fulfillments/{basket_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMainSellerOrderProcessingController::api_admin_setRejectOrder` expose observed keys including `reject`, `express`, `error`, `error_msg`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_REJECT_FULFILLMENT_ORDER(123, 333);
   * ```
   */
  PUT_REJECT_FULFILLMENT_ORDER(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/fulfillments/${basket_id}/reject`;
  }

  /**
   * Reject fulfillment order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/process-center/fulfillments/{basket_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMainSellerOrderProcessingController::api_admin_deleteRejectOrder` expose observed keys including `error`, `error_msg`, `reject`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_REJECT_FULFILLMENT_ORDER(123, 333);
   * ```
   */
  DELETE_REJECT_FULFILLMENT_ORDER(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/fulfillments/${basket_id}/reject`;
  }

  /**
   * Set tracking fulfillment order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/fulfillments/{basket_id}/tracking
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMainSellerOrderProcessingController::api_admin_setDeliveryTrackingOrder` expose observed keys including `tracking_code`, `tracking_url`, `error`, `error_msg`, `delivery_info`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_TRACKING_FULFILLMENT_ORDER(123, 333);
   * ```
   */
  PUT_SET_TRACKING_FULFILLMENT_ORDER(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/fulfillments/${basket_id}/tracking`;
  }

  //―――――――――――――――――――――― Shop > Orders > Timeline ――――――――――――――――――――

  /**
   * Order timeline endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/timeline/{type}/{basket_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @param order_id - Order/basket identifier scoped to the route shop and order type.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TimelineController::api_getOrderTimelineByAdmin` expose observed keys including `timelines`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ORDER_TIMELINE(123, "cover", 333);
   * ```
   */
  GET_ORDER_TIMELINE(
    shop_id: string | number,
    type:
      | "physical"
      | "virtual"
      | "file"
      | "service"
      | "pos"
      | "avocado"
      | "hyper"
      | "dropshipping",
    order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/timeline/${type}/${order_id}`;
  }

  /**
   * Order email preview endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/timeline/orders/{basket_id}/emails/{email_type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param order_id - Order/basket identifier scoped to the route shop and order type.
   * @param email_type - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TimelineEmailsController::api_getOrderTimelineEmailView` expose observed keys including `timeline_id`, `error`, `error_msf`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ORDER_EMAIL_PREVIEW(123, 333, "value");
   * ```
   */
  GET_ORDER_EMAIL_PREVIEW(
    shop_id: string | number,
    order_id: string | number,
    email_type: TimelineEmailType,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/timeline/orders/${order_id}/emails/${email_type}`;
  }

  /**
   * Order resend email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/timelines/{timeline_id}/email
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param timeline_id - timeline_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TimelineEmailsController::api_postOrderTimelineEmailResend` expose observed keys including `error`, `error_msg`, `data`, `resend`, `success`, `timeline`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_VENDOR_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ORDER_RESEND_EMAIL(123, 123);
   * ```
   */
  POST_ORDER_RESEND_EMAIL(
    shop_id: string | number,
    timeline_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/timelines/${timeline_id}/email`;
  }

  //―――――――――――――――――――――― Shop > Pdf ――――――――――――――――――――
  /**
   * Order PDF/HTML export endpoint URL.
   *
   * `type` selects the concrete Laravel route (`receipt` or `label`); `render` selects `pdf`, `html`, or for receipts `standard`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{basket_id}/receipt/{render}
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{basket_id}/label/{render}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param type - Order export type; backend routes support `receipt` and `label`.
   * @param render - Render type; receipt supports `pdf`, `html`, `standard`, label supports `pdf`, `html`.
   * @param size - Optional `size=square` query parameter appended by the SDK.
   * @returns Absolute URL string for the rendered order document.
   *
   * Response shape:
   * - Binary PDF/download or HTML response depending on `render`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing order read scope or order-region read permission.
   * - 404 not_found: shop or basket is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_ORDERS_PDF(123, 333, "receipt", "pdf");
   * ```
   */
  GET_ORDERS_PDF(
    shop_id: string | number,
    basket_id: string | number,
    type: OrderExportPdfType,
    render: "html" | "pdf" | "standard" = "pdf",
    size: null | "square" = null,
  ) {
    // type: label receipt
    return (
      `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/${type}/${render}` +
      (size ? `?size=${size}` : "")
    );
  }

  /**
   * POS order PDF export endpoint URL.
   *
   * `type` selects the concrete POS route (`receipt` or `label`); this SDK method fixes `render` to `pdf`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/pos-baskets/{basket_id}/receipt/{render}
   * @endpoint GET /shops/{shop_id}/process-center/pos-baskets/{basket_id}/label/{render}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - POS basket identifier scoped to the route shop.
   * @param type - POS export type; backend routes support `receipt` and `label`.
   * @returns Absolute URL string for the rendered POS order document.
   *
   * Response shape:
   * - Binary PDF response for the selected POS document.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing order read scope or order-region read permission.
   * - 404 not_found: shop or POS basket is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_ORDERS_PDF(123, 333, "receipt");
   * ```
   */
  GET_POS_ORDERS_PDF(
    shop_id: string | number,
    basket_id: string | number,
    type: OrderExportPdfType,
  ) {
    // type: label receipt
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/pos-baskets/${basket_id}/${type}/pdf`;
  }

  //―――――――――――――――――――――― Shop > Permission ――――――――――――――――――――

  /**
   * Shop permissions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/permissions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::api_getShopPermissions` expose observed keys including `offset`, `limit`, `sortBy`, `sortDesc`, `pending`, `search`, `staff`, `total`, `staff_count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_PERMISSIONS(123);
   * ```
   */
  GET_SHOP_PERMISSIONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/permissions`;
  }

  /**
   * Add shop permission endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/permissions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   * @permission ShopPermissionRegion::A
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::apiAddShopPermission` expose observed keys including `level`, `override`, `error`, `error_msg`, `success`, `invite`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_PERMISSION(123);
   * ```
   */
  POST_ADD_SHOP_PERMISSION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/permissions`;
  }

  /**
   * Edit shop permission endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/permissions/{permission_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   * @permission ShopPermissionRegion::A
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param permission_id - permission_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::apiEditShopPermission` expose observed keys including `override`, `error`, `error_msg`, `success`, `permission`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_PERMISSION(123, 123);
   * ```
   */
  PUT_EDIT_SHOP_PERMISSION(
    shop_id: string | number,
    permission_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/permissions/${permission_id}`;
  }

  /**
   * Shop permission endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/permissions/{permission_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param permission_id - permission_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::apiDeleteShopPermission` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_PERMISSION(123, 123);
   * ```
   */
  DELETE_SHOP_PERMISSION(
    shop_id: string | number,
    permission_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/permissions/${permission_id}`;
  }

  /**
   * Shop invite endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/invites/{invite_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param invite_id - invite_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::apiDeleteShopInvite` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_INVITE(123, 123);
   * ```
   */
  DELETE_SHOP_INVITE(shop_id: string | number, invite_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/invites/${invite_id}`;
  }

  /**
   * Shop invite action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/invites/{invite_id}/action/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param invite_id - invite_id route parameter identifier.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::api_shopInviteAction` expose observed keys including `error`, `error_msg`, `status`, `shopPermissions`, `success`, `id`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_INVITE_ACTION(123, 123, "value");
   * ```
   */
  PUT_SHOP_INVITE_ACTION(
    shop_id: string | number,
    invite_id: string | number,
    action: "ACCEPT" | "REJECT",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/invites/${invite_id}/action/${action}`;
  }

  /**
   * Shop permission leave endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/permissions/{permission_id}/leave
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param permission_id - permission_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PermissionController::apiLeaveShopPermission` expose observed keys including `error`, `error_msg`, `success`, `id`, `leave_shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_PERMISSION_LEAVE(123, 123);
   * ```
   */
  POST_SHOP_PERMISSION_LEAVE(
    shop_id: string | number,
    permission_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/permissions/${permission_id}/leave`;
  }

  //―――――――――――――――――――――― Shop > Role ――――――――――――――――――――

  /**
   * Shop roles endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/roles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_getShopRoles` expose observed keys including `roles`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_ROLES(123);
   * ```
   */
  GET_SHOP_ROLES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/roles`;
  }

  /**
   * Add shop role endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/roles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::apiAddShopRole` expose observed keys including `level`, `error`, `error_msg`, `success`, `role`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_BY_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_ROLE(123);
   * ```
   */
  POST_ADD_SHOP_ROLE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/roles`;
  }

  /**
   * Shop roles endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/roles/{permission_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_STAFF_WRITE (backoffice:staff:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param role_id - role_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::apiDeleteShopPermission` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_ROLES(123, 123);
   * ```
   */
  DELETE_SHOP_ROLES(shop_id: string | number, role_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/roles/${role_id}`;
  }

  /**
   * My roles endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /roles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_getMyRoles` expose observed keys including `roles`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_ROLES();
   * ```
   */
  GET_MY_ROLES() {
    return `${this.selldone_api_url}/roles`;
  }

  /**
   * Role invite action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   *
   * @endpoint PUT /roles/{role_id}/action/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_setRoleInviteAction` expose observed keys including `accepted`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_ROLE_INVITE_ACTION(123, "value");
   * ```
   */
  PUT_ROLE_INVITE_ACTION(
    role_id: string | number,
    action: "ACCEPT" | "REJECT",
  ) {
    return `${this.selldone_api_url}/roles/${role_id}/action/${action}`;
  }

  /**
   * My role endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /roles/{role_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_deleteMyRole` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_ROLE(123);
   * ```
   */
  DELETE_MY_ROLE(role_id: string | number) {
    return `${this.selldone_api_url}/roles/${role_id}`;
  }

  /**
   * My role info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /roles/{role_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_getMyRoleInfo` expose observed keys including `shop`, `shopGateways`, `role`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_ROLE_INFO(123);
   * ```
   */
  GET_MY_ROLE_INFO(role_id: string | number) {
    return `${this.selldone_api_url}/roles/${role_id}`;
  }

  /**
   * My role tasks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /roles/{role_id}/tasks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_getMyRoleList` expose observed keys including `orders`, `total`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_ROLE_TASKS(123);
   * ```
   */
  GET_MY_ROLE_TASKS(role_id: string | number) {
    return `${this.selldone_api_url}/roles/${role_id}/tasks`;
  }

  /**
   * My role task endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /roles/{role_id}/tasks/{task_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @param task_id - task_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_getMyRoleTaskInfo` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_ROLE_TASK(123, 123);
   * ```
   */
  GET_MY_ROLE_TASK(role_id: string | number, task_id: string | number) {
    return `${this.selldone_api_url}/roles/${role_id}/tasks/${task_id}`;
  }

  /**
   * My role task action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /roles/{role_id}/tasks/{task_id}/actions/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param role_id - role_id route parameter identifier.
   * @param task_id - task_id route parameter identifier.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `RoleController::api_postRoleTaskAction` expose observed keys including `state`, `item_id`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_ROLE_TASK_ACTION(123, 123, "value");
   * ```
   */
  POST_MY_ROLE_TASK_ACTION(
    role_id: string | number,
    task_id: string | number,
    action: "update-status" | "return-request",
  ) {
    return `${this.selldone_api_url}/roles/${role_id}/tasks/${task_id}/actions/${action}`;
  }

  //―――――――――――――――――――――― Shop > Gateway ――――――――――――――――――――

  /**
   * Available gateways endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/available-gateways/{currency}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param currency - Currency code accepted by the backend route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_getAvailableGateways` expose observed keys including `gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AVAILABLE_GATEWAYS(123, "USD");
   * ```
   */
  GET_AVAILABLE_GATEWAYS(
    shop_id: string | number,
    currency: keyof typeof Currency,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/available-gateways/${currency}`;
  }

  /**
   * Set gateway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_SetGatewayToShop` expose observed keys including `enable`, `livemode`, `manual`, `private`, `public`, `limit`, `error`, `error_msg`, `shop_gateway`, `valid_config`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_GATEWAY(123, "stripe");
   * ```
   */
  POST_SET_GATEWAY(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}`;
  }

  /**
   * Gateway shop add blockchain wallet endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/blockchain/wallet
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BlockchainController::api_shop_addWallet` expose observed keys including `recovery`, `error`, `error_msg`, `wallet_id`, `user_id`, `private`, `success`, `wallet`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_AVAILABLE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GATEWAY_SHOP_ADD_BLOCKCHAIN_WALLET(123, "stripe");
   * ```
   */
  POST_GATEWAY_SHOP_ADD_BLOCKCHAIN_WALLET(
    shop_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/blockchain/wallet`;
  }

  /**
   * Gateway shop generate blockchain recovery phrases endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/blockchain/recovery
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BlockchainController::api_generateNewRecoveryPhrases` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GATEWAY_SHOP_GENERATE_BLOCKCHAIN_RECOVERY_PHRASES(123, "stripe");
   * ```
   */
  POST_GATEWAY_SHOP_GENERATE_BLOCKCHAIN_RECOVERY_PHRASES(
    shop_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/blockchain/recovery`;
  }

  /**
   * Gateway shop config endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/gateways/{gateway_code}/config
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_SetShopGatewayConfig` expose observed keys including `methods`, `connect`, `client_id`, `error`, `message`, `config`, `success`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_AVAILABLE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_GATEWAY_SHOP_CONFIG(123, "stripe");
   * ```
   */
  PUT_GATEWAY_SHOP_CONFIG(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/config`;
  }

  /**
   * Gateway shop theme endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/gateways/{gateway_code}/theme
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_SetShopGatewayTheme` expose observed keys including `theme`, `labels`, `layout`, `variables`, `rules`, `success`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_AVAILABLE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_GATEWAY_SHOP_THEME(123, "stripe");
   * ```
   */
  PUT_GATEWAY_SHOP_THEME(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/theme`;
  }

  /**
   * Gateway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/gateways/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_DeleteGatewayToShop` expose observed keys including `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_AVAILABLE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GATEWAY(123, "stripe");
   * ```
   */
  DELETE_GATEWAY(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}`;
  }

  /**
   * Shop gateways by owner endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gateways
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_getShopGateways` expose observed keys including `data`, `shop_gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_GATEWAYS_BY_OWNER(123);
   * ```
   */
  GET_SHOP_GATEWAYS_BY_OWNER(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways`;
  }

  /**
   * Shop gateway info data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gateways/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_getShopGatewayInfo` expose observed keys including `shop_gateway`, `data`, `config`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GATEWAY_NOT_CONFIGURE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_GATEWAY_INFO_DATA(123, "stripe");
   * ```
   */
  GET_SHOP_GATEWAY_INFO_DATA(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}`;
  }

  /**
   * Shop gateway transactions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gateways/{gateway_code}/transactions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_getShopGatewayTransactions` expose observed keys including `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GATEWAY_NOT_CONFIGURE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_GATEWAY_TRANSACTIONS(123, "stripe");
   * ```
   */
  GET_SHOP_GATEWAY_TRANSACTIONS(
    shop_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/transactions`;
  }

  /**
   * Check payment gateway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gateways/{gateway_code}/check-payment/{unique_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @param unique_id - unique_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::checkOnlinePaymentForOrder` expose observed keys including `error`, `error_msg`, `success`, `payment_at`, `status`, `transaction_status`, `message`, `receipt_url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_PAYMENT_GATEWAY(123, "stripe", 123);
   * ```
   */
  GET_CHECK_PAYMENT_GATEWAY(
    shop_id: string | number,
    gateway_code: string,
    unique_id: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/check-payment/${unique_id}`;
  }

  /**
   * Payment gateway capture endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/payments/{payment_id}/capture
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @param payment_id - payment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayCaptureController::api_capturePayment` expose observed keys including `amount`, `error`, `error_msg`, `price`, `tax`, `success`, `payment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAYMENT_GATEWAY_CAPTURE(123, "stripe", 123);
   * ```
   */
  POST_PAYMENT_GATEWAY_CAPTURE(
    shop_id: string | number,
    gateway_code: string,
    payment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/payments/${payment_id}/capture`;
  }

  /**
   * Confirm dir payment manually endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/dir/{gateway_code}/completed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketPaymentController::api_admin_confirmDIRPaymentCompleted` expose observed keys including `success`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_ENABLE_ERROR()`, `ErrorCodes::GATEWAY_NOT_DIR_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONFIRM_DIR_PAYMENT_MANUALLY(123, 333, "stripe");
   * ```
   */
  POST_CONFIRM_DIR_PAYMENT_MANUALLY(
    shop_id: string | number,
    basket_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/dir/${gateway_code}/completed`;
  }

  /**
   * Auto config gateway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/auto-config
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::api_RepairShopGateway` expose observed keys including `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_AVAILABLE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AUTO_CONFIG_GATEWAY(123, "stripe");
   * ```
   */
  POST_AUTO_CONFIG_GATEWAY(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/auto-config`;
  }

  /**
   * Add bill endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/bill
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillController::api_addNewBill` expose observed keys including `price`, `error`, `error_msg`, `success`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_BILL(123, 333);
   * ```
   */
  POST_ADD_BILL(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bill`;
  }

  /**
   * Update bill status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/bill/{bill_id}/status
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param bill_id - bill_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillController::api_updateStatus` expose observed keys including `status`, `error`, `error_msg`, `success`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BILL_ERROR()`, `ErrorCodes::CAN_NOT_EDIT_PAYED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_BILL_STATUS(123, 333, 123);
   * ```
   */
  PUT_UPDATE_BILL_STATUS(
    shop_id: string | number,
    basket_id: string | number,
    bill_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bill/${bill_id}/status`;
  }

  /**
   * Edit bill price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/bill/{bill_id}/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param bill_id - bill_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillController::api_updatePrice` expose observed keys including `price`, `error`, `error_msg`, `success`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BILL_ERROR()`, `ErrorCodes::CAN_NOT_EDIT_PAYED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_BILL_PRICE(123, 333, 123);
   * ```
   */
  PUT_EDIT_BILL_PRICE(
    shop_id: string | number,
    basket_id: string | number,
    bill_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bill/${bill_id}/price`;
  }

  /**
   * Bill endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/process-center/baskets/{basket_id}/bill/{bill_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param bill_id - bill_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillController::api_deleteBill` expose observed keys including `error`, `error_msg`, `success`, `bill_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BILL_ERROR()`, `ErrorCodes::CAN_NOT_EDIT_PAYED_ERROR()`, `ErrorCodes::CAN_NOT_DELETE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_BILL(123, 333, 123);
   * ```
   */
  DELETE_BILL(
    shop_id: string | number,
    basket_id: string | number,
    bill_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bill/${bill_id}`;
  }

  /**
   * Confirm cash payment for bill manually endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/bill/{bill_id}/payment/{gateway_code}/completed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param bill_id - bill_id route parameter identifier.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillPaymentController::api_confirmCashPaymentForBill` expose observed keys including `error`, `error_msg`, `success`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_BILL_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONFIRM_CASH_PAYMENT_FOR_BILL_MANUALLY(123, 333, 123, "stripe");
   * ```
   */
  POST_CONFIRM_CASH_PAYMENT_FOR_BILL_MANUALLY(
    shop_id: string | number,
    basket_id: string | number,
    bill_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bill/${bill_id}/payment/${gateway_code}/completed`;
  }

  /**
   * Basket bills endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{basket_id}/bills
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BillController::api_orderBillsList` expose observed keys including `offset`, `limit`, `bills`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_BASKET_BILLS(123, 333);
   * ```
   */
  GET_BASKET_BILLS(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/bills`;
  }

  /**
   * Payment gateway actions refund endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/payments/{payment_id}/refund
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @param payment_id - payment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayActionsRefundController::api_refundPayment` expose observed keys including `amount`, `error`, `error_msg`, `success`, `currency`, `message`, `payment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAYMENT_GATEWAY_ACTIONS_REFUND(123, "stripe", 123);
   * ```
   */
  POST_PAYMENT_GATEWAY_ACTIONS_REFUND(
    shop_id: string | number,
    gateway_code: string,
    payment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/payments/${payment_id}/refund`;
  }

  /**
   * Payment gateway actions delivery endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/gateways/{gateway_code}/payments/{payment_id}/delivery
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @param payment_id - payment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayActionsDeliveryController::api_deliveryPayment` expose observed keys including `error`, `error_msg`, `success`, `message`, `payment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAYMENT_GATEWAY_ACTIONS_DELIVERY(123, "stripe", 123);
   * ```
   */
  POST_PAYMENT_GATEWAY_ACTIONS_DELIVERY(
    shop_id: string | number,
    gateway_code: string,
    payment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gateways/${gateway_code}/payments/${payment_id}/delivery`;
  }

  //―――――――――――――――――――――― COD Payments' Confirmation ――――――――――――――――――――

  /**
   * Confirm COD payment manually for online basket endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/cod/{gateway_code}/completed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketPaymentController::api_admin_confirmCODPaymentCompleted` expose observed keys including `success`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_ENABLE_ERROR()`, `ErrorCodes::GATEWAY_NOT_COD_ERROR()`, `ErrorCodes::COD_PAYED_BEFORE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONFIRM_COD_PAYMENT_MANUALLY_FOR_ONLINE_BASKET(123, 333, "stripe");
   * ```
   */
  POST_CONFIRM_COD_PAYMENT_MANUALLY_FOR_ONLINE_BASKET(
    shop_id: string | number,
    basket_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/cod/${gateway_code}/completed`;
  }

  /**
   * Confirm COD payment manually for POS basket endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/pos-baskets/{pos_basket_id}/cod/{gateway_code}/completed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param pos_basket_id - pos_basket_id route parameter identifier.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketPaymentController::confirmCODPaymentCompletedForPOS` expose observed keys including `success`, `status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::GATEWAY_NOT_ENABLE_ERROR()`, `ErrorCodes::GATEWAY_NOT_COD_ERROR()`, `ErrorCodes::COD_PAYED_BEFORE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONFIRM_COD_PAYMENT_MANUALLY_FOR_POS_BASKET(123, 123, "stripe");
   * ```
   */
  POST_CONFIRM_COD_PAYMENT_MANUALLY_FOR_POS_BASKET(
    shop_id: string | number,
    pos_basket_id: string | number,
    gateway_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/pos-baskets/${pos_basket_id}/cod/${gateway_code}/completed`;
  }

  //―――――――――――――――――――――― Search ――――――――――――――――――――

  /**
   * Search query admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /{shop_id}/search/admin/suggestion/{text}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param query - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SearchController::api_searchSuggestionAdmin` expose observed keys including `items`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SEARCH_QUERY_ADMIN(123, "value");
   * ```
   */
  GET_SEARCH_QUERY_ADMIN(shop_id: string | number, query: string) {
    return `${this.selldone_api_url}/${shop_id}/search/admin/suggestion/${query}`;
  }

  //―――――――――――――――――――――― Shop > Blog ――――――――――――――――――――
  /**
   * Shop blogs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/blogs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogController::api_getShopBlogs` expose observed keys including `offset`, `limit`, `cluster_id`, `user`, `articles`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_BLOGS(123);
   * ```
   */
  GET_SHOP_BLOGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/blogs`;
  }

  /**
   * Shop blog categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/blog/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogCategoryController::api_getShopBlogCategories` expose observed keys including `categories`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_BLOG_CATEGORIES(123);
   * ```
   */
  GET_SHOP_BLOG_CATEGORIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/blog/categories`;
  }

  /**
   * Add shop blog category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/blog/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogCategoryController::api_admin_addShopBlogCategory` expose observed keys including `category`, `description`, `star`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_BLOG_CATEGORY(123);
   * ```
   */
  POST_ADD_SHOP_BLOG_CATEGORY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/blog/categories`;
  }

  /**
   * Edit shop blog category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/blog/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogCategoryController::api_admin_editShopBlogCategory` expose observed keys including `category`, `description`, `star`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_BLOG_CATEGORY(123, 789);
   * ```
   */
  PUT_EDIT_SHOP_BLOG_CATEGORY(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/blog/categories/${category_id}`;
  }

  /**
   * Shop blog category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/blog/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogCategoryController::api_admin_deleteShopBlogCategory` expose observed keys including `category_id`, `updated_at`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_BLOG_CATEGORY(123, 789);
   * ```
   */
  DELETE_SHOP_BLOG_CATEGORY(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/blog/categories/${category_id}`;
  }

  /**
   * Upload shop blog category icon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/blog/categories/{category_id}/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogCategoryController::upload_shopBlogCategoryIcon` expose observed keys including `photo`, `icon`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_SHOP_BLOG_CATEGORY_ICON(123, 789);
   * ```
   */
  POST_UPLOAD_SHOP_BLOG_CATEGORY_ICON(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/blog/categories/${category_id}/icon`;
  }

  //―――――――――――――――――――――― Company ――――――――――――――――――――

  /**
   * My companies endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /company
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyController::api_getMyCompanies` expose observed keys including `offset`, `limit`, `companies`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_COMPANIES();
   * ```
   */
  GET_MY_COMPANIES() {
    return `${this.selldone_api_url}/company`;
  }

  /**
   * Upload company logo endpoint URL.
   *
   * Builds the multipart upload URL for a company logo. The SDK `type` parameter is typed as `"logo"`, matching the concrete Laravel route.
   *
   * @endpoint POST /company/{company_id}/logo
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param type - Must be `logo`; included by the SDK as the final route segment.
   * @returns Absolute URL string for the company logo upload endpoint.
   *
   * Response shape:
   * - Backend returns upload result keys from `CompanyController::company_upload_image`, including updated logo/image data.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing company write scope.
   * - 404 not_found: company is not found or not owned by the user.
   * - 422 validation_error: uploaded file fails Laravel validation.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_COMPANY_IMAGE(999, "logo");
   * ```
   */
  POST_UPLOAD_COMPANY_IMAGE(company_id: string | number, type: "logo") {
    return `${this.selldone_api_url}/company/${company_id}/${type}`;
  }

  /**
   * Add company endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /company
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyController::api_addCompany` expose observed keys including `name`, `website`, `address`, `phone`, `mobile`, `tax`, `country`, `error`, `error_msg`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::HAVE_A_COMPANY_PENDING_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_COMPANY();
   * ```
   */
  POST_ADD_COMPANY() {
    return `${this.selldone_api_url}/company`;
  }

  /**
   * Edit company endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /company/{company_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyController::api_editCompany` expose observed keys including `name`, `website`, `address`, `phone`, `mobile`, `tax`, `country`, `error`, `error_msg`, `register`, `register_at`, `rejected`, `success`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_COMPANY(999);
   * ```
   */
  PUT_EDIT_COMPANY(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}`;
  }

  /**
   * My company info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /company/{company_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyController::api_getMyCompanyInfo` expose observed keys including `accounts`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_COMPANY_INFO(999);
   * ```
   */
  GET_MY_COMPANY_INFO(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}`;
  }

  /**
   * My company documents endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /company/{company_id}/documents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyDocumentController::list` expose observed keys including `offset`, `limit`, `documents`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_COMPANY_DOCUMENTS(999);
   * ```
   */
  GET_MY_COMPANY_DOCUMENTS(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/documents`;
  }

  /**
   * My company add document endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /company/{company_id}/documents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyDocumentController::add` expose observed keys including `type`, `url`, `error`, `error_msg`, `success`, `document`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_COMPANY_ADD_DOCUMENT(999);
   * ```
   */
  POST_MY_COMPANY_ADD_DOCUMENT(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/documents`;
  }

  /**
   * My company download document endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /company/{company_id}/documents/{document_id}/download
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param document_id - document_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyDocumentController::download` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_COMPANY_DOWNLOAD_DOCUMENT(999, 123);
   * ```
   */
  GET_MY_COMPANY_DOWNLOAD_DOCUMENT(
    company_id: string | number,
    document_id: string | number,
  ) {
    return `${this.selldone_api_url}/company/${company_id}/documents/${document_id}/download`;
  }

  /**
   * My company document endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /company/{company_id}/documents/{document_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param document_id - document_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CompanyDocumentController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_COMPANY_DOCUMENT(999, 123);
   * ```
   */
  DELETE_MY_COMPANY_DOCUMENT(
    company_id: string | number,
    document_id: string | number,
  ) {
    return `${this.selldone_api_url}/company/${company_id}/documents/${document_id}`;
  }

  //―――――――――――――――――――――― Company > App ――――――――――――――――――――

  /**
   * Company apps endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /company/{company_id}/apps
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_getApps` expose observed keys including `apps`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_COMPANY_APPS(999);
   * ```
   */
  GET_COMPANY_APPS(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps`;
  }

  /**
   * Upload app image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /company/{company_id}/apps/{app_id}/image/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `AppController::app_upload_image` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_APP_IMAGE(999, 101, "cover");
   * ```
   */
  POST_UPLOAD_APP_IMAGE(
    company_id: string | number,
    app_id: string | number,
    type: "icon" | "logo",
  ) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/image/${type}`;
  }

  /**
   * App images order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /company/{company_id}/apps/{app_id}/images/order
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_setAppImagesOrder` expose observed keys including `orders`, `order`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_APP_IMAGES_ORDER(999, 101);
   * ```
   */
  PUT_APP_IMAGES_ORDER(company_id: string | number, app_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/images/order`;
  }

  /**
   * Upload app image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /company/{company_id}/apps/{app_id}/images/{image_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @param image_id - Image identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_deleteAppImage` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_PRODUCT_IMAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_UPLOAD_APP_IMAGE(999, 101, 202);
   * ```
   */
  DELETE_UPLOAD_APP_IMAGE(
    company_id: string | number,
    app_id: string | number,
    image_id: string | number,
  ) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/images/${image_id}`;
  }

  /**
   * Add app endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /company/{company_id}/apps
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_addApp` expose observed keys including `category`, `enable`, `github`, `code`, `name`, `description`, `redirect`, `mode`, `note`, `permissions`, `privacy`, `web`, `error`, `error_msg`, `app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NO_ACCESS_TO_ADD_APPLICATION_ERROR()`, `ErrorCodes::APPLICATION_CODE_DUPLICATED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_APP(999);
   * ```
   */
  POST_ADD_APP(company_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps`;
  }

  /**
   * Revoke app API endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /company/{company_id}/apps/{app_id}/revoke-api
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_regenerateApiKeys` expose observed keys including `error`, `error_msg`, `secret`, `api`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_REVOKE_APP_API(999, 101);
   * ```
   */
  POST_REVOKE_APP_API(company_id: string | number, app_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/revoke-api`;
  }

  /**
   * Edit app endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /company/{company_id}/apps/{app_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_editApp` expose observed keys including `category`, `enable`, `github`, `code`, `name`, `description`, `redirect`, `note`, `permissions`, `privacy`, `web`, `user_id`, `secret`, `personal_access_client`, `password_client`, `revoked`, `shop_id`, `client_id`, `public`, `private`, `video`, `app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_APP(999, 101);
   * ```
   */
  PUT_EDIT_APP(company_id: string | number, app_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}`;
  }

  /**
   * Generate enterprise credential endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /company/{company_id}/apps/{app_id}/enterprise/credential
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_generateCredential` expose observed keys including `error`, `error_msg`, `success`, `credential`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GENERATE_ENTERPRISE_CREDENTIAL(999, 101);
   * ```
   */
  POST_GENERATE_ENTERPRISE_CREDENTIAL(
    company_id: string | number,
    app_id: string | number,
  ) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/enterprise/credential`;
  }

  /**
   * App edit publish endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /company/{company_id}/apps/{app_id}/publish/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_WRITE (backoffice:company:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @param upload_mode - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::app_edit_publish` expose observed keys including `pages`, `header`, `variables`, `success`, `app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_APP_EDIT_PUBLISH(999, 101, "value");
   * ```
   */
  POST_APP_EDIT_PUBLISH(
    company_id: string | number,
    app_id: string | number,
    upload_mode: Application.AppVersionType,
  ) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}/publish/${upload_mode}`;
  }

  //―――――――――――――――――――――― App Store ――――――――――――――――――――
  /**
   * Appstore list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/app-store/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_getAppStoreList` expose observed keys including `categories`, `error`, `apps`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_APPSTORE_LIST(123);
   * ```
   */
  GET_APPSTORE_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/app-store/list`;
  }

  /**
   * Add enterprise app endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/app-store/enterprise
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_addEnterpriseApp` expose observed keys including `credential`, `error`, `error_msg`, `shop_id`, `app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_ENTERPRISE_APP(123);
   * ```
   */
  POST_ADD_ENTERPRISE_APP(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/app-store/enterprise`;
  }

  /**
   * App data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /company/{company_id}/apps/{app_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param company_id - Company identifier owned by the authenticated user.
   * @param app_id - Application identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AppController::api_getMyAppInfo` expose observed keys including `client`, `app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_APP_DATA(999, 101);
   * ```
   */
  GET_APP_DATA(company_id: string | number, app_id: string | number) {
    return `${this.selldone_api_url}/company/${company_id}/apps/${app_id}`;
  }

  //―――――――――――――――――――――― Shop > Apps ――――――――――――――――――――
  /**
   * Shop apps admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/apps
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_getShopApps` expose observed keys including `categories`, `error`, `shop_apps`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_APPS_ADMIN(123);
   * ```
   */
  GET_SHOP_APPS_ADMIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps`;
  }

  /**
   * Set app info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/apps/{app_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param app_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_setAppInfo` expose observed keys including `enable`, `variables`, `error`, `shop_app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_APP_INFO(123, "value");
   * ```
   */
  POST_SET_APP_INFO(shop_id: string | number, app_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps/${app_code}`;
  }

  /**
   * Set review endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/apps/{app_code}/review
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param app_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_setAppReview` expose observed keys including `rate`, `message`, `error`, `app_review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_REVIEW(123, "value");
   * ```
   */
  PUT_SET_REVIEW(shop_id: string | number, app_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps/${app_code}/review`;
  }

  /**
   * App endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/apps/{app_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param app_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_deleteShopApp` expose observed keys including `error`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_APPLICATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_APP(123, "value");
   * ```
   */
  DELETE_APP(shop_id: string | number, app_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps/${app_code}`;
  }

  /**
   * Shop app info full endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /shops/{shop_id}/apps/{app_code}/full
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param app_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_getAppInfoForApplicationPage` expose observed keys including `error`, `app`, `shop_app`, `developer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_APPLICATION_ERROR()`, `ErrorCodes::APPLICATION_DISABLE_ERROR()`, `ErrorCodes::APPLICATION_NOT_PUBLISHED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_APP_INFO_FULL(123, "value");
   * ```
   */
  GET_SHOP_APP_INFO_FULL(shop_id: string | number, app_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps/${app_code}/full`;
  }

  /**
   * Shop app info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /shops/{shop_id}/apps/{app_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param app_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAppController::api_getAppInfoForForm` expose observed keys including `error`, `app`, `shop_app`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_APP_INFO(123, "value");
   * ```
   */
  GET_SHOP_APP_INFO(shop_id: string | number, app_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/apps/${app_code}`;
  }

  //―――――――――――――――――――――― Transportation ――――――――――――――――――――

  /**
   * Shop transportations admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_getShopTransportations` expose observed keys including `error`, `transportations`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TRANSPORTATIONS_ADMIN(123);
   * ```
   */
  GET_SHOP_TRANSPORTATIONS_ADMIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportation`;
  }

  /**
   * Set transportation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/transportation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_setShopTransportation` expose observed keys including `type`, `currency`, `days`, `time_spans`, `title`, `enable`, `distance`, `max_weight`, `max_w`, `max_l`, `max_h`, `holidays`, `const`, `distance_cof`, `weight_cof`, `price_cof`, `distance_weight_cof`, `free_shipping`, `free_shipping_limit`, `cod`, `sod`, `eta`, `etd`, `profiles`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_TRANSPORTATION(123);
   * ```
   */
  POST_SET_TRANSPORTATION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportation`;
  }

  /**
   * Upload transportation logo endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/logo
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_uploadShopTransportationPLogo` expose observed keys including `photo`, `logo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_TRANSPORTATION_LOGO(123, 123);
   * ```
   */
  POST_UPLOAD_TRANSPORTATION_LOGO(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/logo`;
  }

  /**
   * Transportation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/transportations/{transportation_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_deleteShopTransportation` expose observed keys including `enable`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_TRANSPORTATION(123, 123);
   * ```
   */
  DELETE_TRANSPORTATION(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}`;
  }

  /**
   * Shop transportation info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_getShopTransportation_info` expose observed keys including `error`, `transportation`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TRANSPORTATION_INFO(123, 123);
   * ```
   */
  GET_SHOP_TRANSPORTATION_INFO(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}`;
  }

  /**
   * Shop transportation orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportation-orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationOrderController::api_getShopTransportationOrders` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TRANSPORTATION_ORDERS(123);
   * ```
   */
  GET_SHOP_TRANSPORTATION_ORDERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportation-orders`;
  }

  /**
   * Transportation persons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/persons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationPersonController::api_getShopTransportationPersons` expose observed keys including `offset`, `limit`, `persons`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_TRANSPORTATION_PERSONS(123, 123);
   * ```
   */
  GET_TRANSPORTATION_PERSONS(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/persons`;
  }

  /**
   * Transportation add person endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/persons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationPersonController::api_addShopTransportationPersons` expose observed keys including `enable`, `error`, `person`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_USER_BY_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_ADD_PERSON(123, 123);
   * ```
   */
  POST_TRANSPORTATION_ADD_PERSON(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/persons`;
  }

  /**
   * Transportation person update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/transportations/{transportation_id}/persons/{person_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param person_id - person_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationPersonController::api_updateShopTransportationPerson` expose observed keys including `enable`, `success`, `person`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_PERSON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_TRANSPORTATION_PERSON_UPDATE(123, 123, 123);
   * ```
   */
  PUT_TRANSPORTATION_PERSON_UPDATE(
    shop_id: string | number,
    transportation_id: string | number,
    person_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/persons/${person_id}`;
  }

  /**
   * Transportation person endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/transportations/{transportation_id}/persons/{person_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param person_id - person_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationPersonController::api_deleteShopTransportationPerson` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_PERSON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_TRANSPORTATION_PERSON(123, 123, 123);
   * ```
   */
  DELETE_TRANSPORTATION_PERSON(
    shop_id: string | number,
    transportation_id: string | number,
    person_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/persons/${person_id}`;
  }

  /**
   * Transportation person reset endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/persons/{person_id}/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param person_id - person_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationPersonController::api_resetShopTransportationPerson` expose observed keys including `success`, `person`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_PERSON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_PERSON_RESET(123, 123, 123);
   * ```
   */
  POST_TRANSPORTATION_PERSON_RESET(
    shop_id: string | number,
    transportation_id: string | number,
    person_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/persons/${person_id}/reset`;
  }

  /**
   * Transportation services endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/services
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationServiceController::api_getShopTransportationServices` expose observed keys including `offset`, `limit`, `services`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_TRANSPORTATION_SERVICES(123, 123);
   * ```
   */
  GET_TRANSPORTATION_SERVICES(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services`;
  }

  /**
   * Transportation add service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/services
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_addShopTransportationServices` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_ADD_SERVICE(123, 123);
   * ```
   */
  POST_TRANSPORTATION_ADD_SERVICE(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services`;
  }

  /**
   * Transportation service update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_updateShopTransportationService` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_TRANSPORTATION_SERVICE_UPDATE(123, 123, 123);
   * ```
   */
  PUT_TRANSPORTATION_SERVICE_UPDATE(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}`;
  }

  /**
   * Transportation service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_deleteShopTransportationService` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_TRANSPORTATION_SERVICE(123, 123, 123);
   * ```
   */
  DELETE_TRANSPORTATION_SERVICE(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}`;
  }

  /**
   * Transportation service reset endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_resetShopTransportationService` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_SERVICE_RESET(123, 123, 123);
   * ```
   */
  POST_TRANSPORTATION_SERVICE_RESET(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}/reset`;
  }

  /**
   * Delivery services options endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/delivery-services
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationServiceController::api_getDeliveryServicesAvailableToAdd` expose observed keys including `delivery_services`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DELIVERY_SERVICES_OPTIONS(123, 123);
   * ```
   */
  GET_DELIVERY_SERVICES_OPTIONS(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/delivery-services`;
  }

  /**
   * Delivery service info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationServiceController::api_getShopTransportationServiceInfo` expose observed keys including `transportation`, `service`, `warehouse`, `config`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_SERVICE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DELIVERY_SERVICE_INFO(123, 123, 123);
   * ```
   */
  GET_DELIVERY_SERVICE_INFO(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}`;
  }

  /**
   * Delivery service price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_ShopTransportationService_CalculatePrice` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DELIVERY_SERVICE_PRICE(123, 123, 123);
   * ```
   */
  GET_DELIVERY_SERVICE_PRICE(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}/price`;
  }

  /**
   * Delivery service add order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}/order
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_ShopTransportationService_CreateOrder` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DELIVERY_SERVICE_ADD_ORDER(123, 123, 123);
   * ```
   */
  POST_DELIVERY_SERVICE_ADD_ORDER(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}/order`;
  }

  /**
   * Delivery service order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}/order/{uid}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @param uid - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationServiceController::api_ShopTransportationService_OrderDetail` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DELIVERY_SERVICE_ORDER_INFO(123, 123, 123, "value");
   * ```
   */
  GET_DELIVERY_SERVICE_ORDER_INFO(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
    uid: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}/order/${uid}`;
  }

  /**
   * Delivery service profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/services/{service_id}/profile
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param service_id - service_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationServiceController::api_getShopDeliveryServiceProfile` expose observed keys including `success`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_SERVICE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DELIVERY_SERVICE_PROFILE(123, 123, 123);
   * ```
   */
  GET_DELIVERY_SERVICE_PROFILE(
    shop_id: string | number,
    transportation_id: string | number,
    service_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/services/${service_id}/profile`;
  }

  // ―――――― + Delivery person access: (by secure) ――――――
  /**
   * Transportation order status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/orders/{transportation_order_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param transportation_order_id - transportation_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `TransportationOrderController::api_updateShopTransportationOrderStatus` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_ORDER_STATUS(123, 123, 123);
   * ```
   */
  POST_TRANSPORTATION_ORDER_STATUS(
    shop_id: string | number,
    transportation_id: string | number,
    transportation_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/orders/${transportation_order_id}`;
  }

  /**
   * Transportation order info for delivery person endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/transportations/{transportation_id}/orders/{transportation_order_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param transportation_order_id - transportation_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationOrderController::api_getShopTransportationOrderDeliveryInfo` expose observed keys including `receiver_info`, `basket_status`, `basket_id`, `basket_type`, `user_id`, `status`, `send_at`, `deliver_at`, `return_at`, `updated_at`, `payment_at`, `type`, `order_price`, `order_currency`, `service`, `person`, `pickup`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ORDER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_TRANSPORTATION_ORDER_INFO_FOR_DELIVERY_PERSON(123, 123, 123);
   * ```
   */
  GET_TRANSPORTATION_ORDER_INFO_FOR_DELIVERY_PERSON(
    shop_id: string | number,
    transportation_id: string | number,
    transportation_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/orders/${transportation_order_id}`;
  }

  /**
   * Transportation order COD payment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/orders/{transportation_order_id}/payment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @param transportation_order_id - transportation_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationOrderController::api_confirmShopTransportationOrderCODPayment` expose observed keys including `confirm`, `error`, `error_msg`, `person_id`, `receiver_info`, `basket_status`, `basket_id`, `basket_type`, `user_id`, `status`, `send_at`, `deliver_at`, `return_at`, `updated_at`, `payment_at`, `type`, `order_price`, `order_currency`, `service`, `person`, `pickup`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ORDER_ERROR()`, `ErrorCodes::COD_PAYED_BEFORE_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_ORDER_COD_PAYMENT(123, 123, 123);
   * ```
   */
  POST_TRANSPORTATION_ORDER_COD_PAYMENT(
    shop_id: string | number,
    transportation_id: string | number,
    transportation_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/orders/${transportation_order_id}/payment`;
  }

  /**
   * Transportation set pickup addresses endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/transportations/{transportation_id}/pickups
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param transportation_id - transportation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransportationController::api_setShopTransportationPickups` expose observed keys including `pickups`, `error`, `error_msg`, `success`, `transportation`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_TRANSPORTATION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSPORTATION_SET_PICKUP_ADDRESSES(123, 123);
   * ```
   */
  POST_TRANSPORTATION_SET_PICKUP_ADDRESSES(
    shop_id: string | number,
    transportation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/transportations/${transportation_id}/pickups`;
  }



// ―――――――――― Shop ▶ Logistic ▶ Transportations ▶ Boxes ――――――――――

  /**
   * Shop boxes endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/boxes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_READ (backoffice:logistic:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBoxesListController::api_getShopBoxes` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_BOXES(123);
   * ```
   */
  GET_SHOP_BOXES(
    shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/boxes`;
  }

  /**
   * Shop boxes add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/boxes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBoxesAddController::api_addShopBox` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_BOXES_ADD(123);
   * ```
   */
  POST_SHOP_BOXES_ADD(
    shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/boxes`;
  }

  /**
   * Shop boxes edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/boxes/{box_uid}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param box_uid - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBoxesEditController::api_editShopBox` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_BOXES_EDIT(123, "value");
   * ```
   */
  PUT_SHOP_BOXES_EDIT(
    shop_id: string | number,
    box_uid: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/boxes/${box_uid}`;
  }

  /**
   * Shop box endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/boxes/{box_uid}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param box_uid - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBoxesDeleteController::api_deleteShopBox` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_BOX(123, "value");
   * ```
   */
  DELETE_SHOP_BOX(
    shop_id: string | number,
    box_uid: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/boxes/${box_uid}`;
  }


  // ―――――――――――――――――――――――――――――――――――

  //―――――――――――――――――――――― Warehouse ――――――――――――――――――――

  /**
   * Shop warehouse admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/warehouse
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseController::api_getWarehouse` expose observed keys including `warehouse`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_WAREHOUSE_ADMIN(123);
   * ```
   */
  GET_SHOP_WAREHOUSE_ADMIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse`;
  }

  /**
   * Shop warehouse admin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/warehouse
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `WarehouseController::api_setWarehouse` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WAREHOUSE_ADMIN(123);
   * ```
   */
  POST_SHOP_WAREHOUSE_ADMIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse`;
  }

  /**
   * Shop warehouse bulk change price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/warehouse/bulk-actions/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkPriceController::api_bulkActionPrice` expose observed keys including `mode`, `percent`, `constant`, `currency`, `ending`, `strategy`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`, `ErrorCodes::NOT_FOUND_VENDOR_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE(123);
   * ```
   */
  POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/price`;
  }

  /**
   * Shop warehouse bulk change price report endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/warehouse/bulk-actions/price/report
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkPriceController::api_bulkActionPriceReport` expose observed keys including `mode`, `percent`, `constant`, `currency`, `ending`, `strategy`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`, `ErrorCodes::NOT_FOUND_VENDOR_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE_REPORT(123);
   * ```
   */
  POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE_REPORT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/price/report`;
  }

  /**
   * Shop warehouse bulk change price list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/warehouse/bulk-actions/price/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkPriceController::api_bulkActionPriceList` expose observed keys including `mode`, `offset`, `limit`, `currency`, `percent`, `constant`, `ending`, `strategy`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`, `ErrorCodes::NOT_FOUND_VENDOR_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE_LIST(123);
   * ```
   */
  POST_SHOP_WAREHOUSE_BULK_CHANGE_PRICE_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/price/list`;
  }

  /**
   * Shop warehouse bulk change discount endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/warehouse/bulk-actions/discount
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkDiscountController::api_bulkActionDiscount` expose observed keys including `percent`, `dis_start`, `dis_end`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT(123);
   * ```
   */
  POST_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/discount`;
  }

  /**
   * Shop warehouse bulk change discount report endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/warehouse/bulk-actions/discount/report
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkDiscountController::api_bulkActionDiscountReport` expose observed keys including `percent`, `dis_start`, `dis_end`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT_REPORT(123);
   * ```
   */
  GET_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT_REPORT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/discount/report`;
  }

  /**
   * Shop warehouse bulk change discount list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/warehouse/bulk-actions/discount/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOGISTIC_WRITE (backoffice:logistic:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `WarehouseBulkDiscountController::api_bulkActionDiscountList` expose observed keys including `percent`, `dis_start`, `dis_end`, `offset`, `limit`, `currency`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT_LIST(123);
   * ```
   */
  GET_SHOP_WAREHOUSE_BULK_CHANGE_DISCOUNT_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/warehouse/bulk-actions/discount/list`;
  }

  //―――――――――――――――――――――― Page Builder ――――――――――――――――――――
  /**
   * Upload page cover image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/upload/cover-image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderImageController::uploadImageOfPage` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PAGE_COVER_IMAGE(123, 111);
   * ```
   */
  POST_UPLOAD_PAGE_COVER_IMAGE(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/upload/cover-image`;
  }

  /**
   * Page history endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pages/{page_id}/history/{history_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @param history_id - history_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderHistoryController::api_getMyPageHistoryContent` expose observed keys including `content`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`, `ErrorCodes::PAGE_HISTORY_NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAGE_HISTORY(123, 111, 123);
   * ```
   */
  GET_PAGE_HISTORY(
    shop_id: string | number,
    page_id: string | number,
    history_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/history/${history_id}`;
  }

  /**
   * Set history persistent endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/pages/{page_id}/history/{history_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @param history_id - history_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderHistoryController::api_setMyPageHistoryPersistent` expose observed keys including `persistent`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`, `ErrorCodes::PAGE_HISTORY_NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_HISTORY_PERSISTENT(123, 111, 123);
   * ```
   */
  PUT_SET_HISTORY_PERSISTENT(
    shop_id: string | number,
    page_id: string | number,
    history_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/history/${history_id}`;
  }

  /**
   * Edit page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/pages/{page_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderEditController::api_updatePage` expose observed keys including `name`, `title`, `content`, `published`, `prompt`, `cluster_id`, `error`, `error_msg`, `direction`, `html`, `css`, `seo`, `color`, `note`, `description`, `background`, `embedded_page_ids`, `page`, `history`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_PAGE(123, 111);
   * ```
   */
  PUT_EDIT_PAGE(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}`;
  }

  /**
   * Page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/pages/{page_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderDeleteController::api_deletePage` expose observed keys including `page_id`, `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PAGE(123, 111);
   * ```
   */
  DELETE_PAGE(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}`;
  }

  /**
   * Add shop page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderAddController::api_addPage` expose observed keys including `name`, `title`, `content`, `published`, `prompt`, `cluster_id`, `description`, `direction`, `error`, `error_msg`, `page`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_PAGE(123);
   * ```
   */
  POST_ADD_SHOP_PAGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages`;
  }

  /**
   * Shop pages endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/pages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderController::api_getMyPagesList` expose observed keys including `offset`, `limit`, `search`, `cluster_id`, `error`, `error_msg`, `pages`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_PAGES(123);
   * ```
   */
  GET_SHOP_PAGES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages`;
  }

  /**
   * Page data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pages/{page_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderController::api_getMyPageInfo` expose observed keys including `page`, `histories`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAGE_DATA(123, 111);
   * ```
   */
  GET_PAGE_DATA(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}`;
  }

  /**
   * Augment data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/{asset_type}/augment/{asset_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param asset_type - Route parameter passed through to the backend endpoint.
   * @param asset_id - asset_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `PageBuilderAugmentController::api_getAssetAugment` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AUGMENT_DATA(123, "value", 123);
   * ```
   */
  GET_AUGMENT_DATA(
    shop_id: string | number,
    asset_type: "product" | "vendor" | "include",
    asset_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/${asset_type}/augment/${asset_id}`;
  }

  /**
   * Page data update preview endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/preview
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,1`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderRealtimeController::api_updatePagePreview` expose observed keys including `content`, `id`, `shop_id`, `name`, `title`, `css`, `background`, `color`, `published`, `cluster_id`, `direction`, `success`, `audiences`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAGE_DATA_UPDATE_PREVIEW(123, 111);
   * ```
   */
  POST_PAGE_DATA_UPDATE_PREVIEW(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/preview`;
  }

  /**
   * Page preview acknowledge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/acknowledge
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,1`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderRealtimeController::api_pageLivePreviewAcknowledge` expose observed keys including `presence`, `success`, `audiences`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAGE_PREVIEW_ACKNOWLEDGE(123, 111);
   * ```
   */
  POST_PAGE_PREVIEW_ACKNOWLEDGE(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/acknowledge`;
  }

  /**
   * Page embed code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pages/{page_id}/embed
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderController::api_getMyPageEmbedCode` expose observed keys including `shop`, `page_name`, `head`, `body`, `html`, `embed`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAGE_EMBED_CODE(123, 111);
   * ```
   */
  GET_PAGE_EMBED_CODE(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/embed`;
  }

  /**
   * Upload page image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/upload/content-image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderImageController::uploadImageOfContent` expose observed keys including `photo`, `lottie`, `success`, `files`, `url`, `path`, `name`, `size`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PAGE_IMAGE(123, 111);
   * ```
   */
  POST_UPLOAD_PAGE_IMAGE(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/upload/content-image`;
  }

  /**
   * Upload page video endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/upload/content-video
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderVideoController::uploadVideoOfContent` expose observed keys including `video`, `url`, `path`, `name`, `size`, `item`, `success`, `files`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_PAGE_VIDEO(123, 111);
   * ```
   */
  POST_UPLOAD_PAGE_VIDEO(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/upload/content-video`;
  }

  /**
   * Shop home page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/home/default
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_setDefaultHomePage` expose observed keys including `home`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_HOME_PAGE(123);
   * ```
   */
  PUT_SHOP_HOME_PAGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/home/default`;
  }

  /**
   * Shop domain home page endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/home/domains/{domain_address}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_setDomainHomePage` expose observed keys including `home`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_DOMAIN_HOME_PAGE(123, "value");
   * ```
   */
  PUT_SHOP_DOMAIN_HOME_PAGE(shop_id: string | number, domain: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/home/domains/${domain}`;
  }

  /**
   * Page builder template content endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /page-templates/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderTemplateController::api_getTemplateContent` expose observed keys including `error`, `error_msg`, `page`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAGE_BUILDER_TEMPLATE_CONTENT(123);
   * ```
   */
  GET_PAGE_BUILDER_TEMPLATE_CONTENT(template_id: string | number) {
    return `${this.selldone_api_url}/page-templates/${template_id}`;
  }

  /**
   * Page files endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pages/{page_id}/files
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderFilesController::api_getPageFiles` expose observed keys including `images`, `videos`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAGE_FILES(123, 111);
   * ```
   */
  GET_PAGE_FILES(shop_id: string | number, page_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/files`;
  }

  /**
   * Page element upload image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/elements/{element_id}/image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param element_id - element_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderEditElementController::api_uploadElementImage` expose observed keys including `photo`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAGE_ELEMENT_UPLOAD_IMAGE(123, 123);
   * ```
   */
  POST_PAGE_ELEMENT_UPLOAD_IMAGE(
    shop_id: string | number,
    element_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/elements/${element_id}/image`;
  }

  /**
   * Page elements add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/elements
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderAddElementController::api_addElement` expose observed keys including `title`, `section`, `tags`, `published`, `success`, `element`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PAGE_ELEMENTS_ADD(123);
   * ```
   */
  POST_PAGE_ELEMENTS_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/elements`;
  }

  /**
   * Page element edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/elements/{element_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param element_id - element_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderEditElementController::api_editElement` expose observed keys including `title`, `section`, `tags`, `published`, `error`, `error_msg`, `user_id`, `success`, `element`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PAGE_ELEMENT_EDIT(123, 123);
   * ```
   */
  PUT_PAGE_ELEMENT_EDIT(shop_id: string | number, element_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/elements/${element_id}`;
  }

  /**
   * Page element endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/elements/{element_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param element_id - element_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PageBuilderDeleteElementController::api_deleteElement` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PAGE_ELEMENT(123, 123);
   * ```
   */
  DELETE_PAGE_ELEMENT(shop_id: string | number, element_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/elements/${element_id}`;
  }

  //―――――――――――――――――――――― Popup ――――――――――――――――――――
  /**
   * Add shop popup endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/popups
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::api_addPopup` expose observed keys including `title`, `published`, `note`, `direction`, `transition`, `position`, `registered`, `purchased`, `sex`, `age`, `levels`, `countries`, `delay`, `repeat`, `interval`, `hide`, `style`, `cluster_id`, `error`, `error_msg`, `popup`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_POPUP(123);
   * ```
   */
  POST_ADD_SHOP_POPUP(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups`;
  }

  /**
   * Edit popup endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/popups/{page_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param popup_id - Popup identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::api_updatePopup` expose observed keys including `title`, `published`, `note`, `direction`, `transition`, `position`, `registered`, `purchased`, `sex`, `age`, `levels`, `countries`, `delay`, `repeat`, `interval`, `hide`, `style`, `cluster_id`, `error`, `error_msg`, `content`, `popup`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::POPUP_NOTFOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_POPUP(123, 222);
   * ```
   */
  PUT_EDIT_POPUP(shop_id: string | number, popup_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups/${popup_id}`;
  }

  /**
   * Upload popup cover image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/popups/{page_id}/upload/cover-image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param popup_id - Popup identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::uploadImageOfPopup` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_POPUP_COVER_IMAGE(123, 222);
   * ```
   */
  POST_UPLOAD_POPUP_COVER_IMAGE(
    shop_id: string | number,
    popup_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups/${popup_id}/upload/cover-image`;
  }

  /**
   * Upload popup image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/popups/{page_id}/upload/content-image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param popup_id - Popup identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::uploadImageOfContent` expose observed keys including `photo`, `lottie`, `success`, `files`, `url`, `path`, `name`, `size`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_POPUP_IMAGE(123, 222);
   * ```
   */
  POST_UPLOAD_POPUP_IMAGE(shop_id: string | number, popup_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups/${popup_id}/upload/content-image`;
  }

  /**
   * Popup endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/popups/{page_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param popup_id - Popup identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::api_deletePopup` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::POPUP_NOTFOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_POPUP(123, 222);
   * ```
   */
  DELETE_POPUP(shop_id: string | number, popup_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups/${popup_id}`;
  }

  /**
   * Shop popups endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/popups
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::api_getPopupsList` expose observed keys including `offset`, `limit`, `cluster_id`, `popups`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_POPUPS(123);
   * ```
   */
  GET_SHOP_POPUPS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups`;
  }

  /**
   * Popup data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/popups/{popup_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param popup_id - Popup identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PopupController::api_getPopupInfo` expose observed keys including `popup`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::POPUP_NOTFOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POPUP_DATA(123, 222);
   * ```
   */
  GET_POPUP_DATA(shop_id: string | number, popup_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/popups/${popup_id}`;
  }

  //―――――――――――――――――――――― Shop > Return Request ――――――――――――――――――――

  /**
   * Private image path endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/assets/images/{file_name}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param file_name - Route parameter passed through to the backend endpoint.
   * @param size - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `PrivateCDN::getPrivateImage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRIVATE_IMAGE_PATH(123, "value", "value");
   * ```
   */
  GET_PRIVATE_IMAGE_PATH(
    shop_id: string | number,
    file_name: string,
    size = null,
  ) {
    return (
      `${this.selldone_api_url}/shops/${shop_id}/assets/images/${file_name}` +
      (size !== null ? `?size=${size}` : "")
    );
  }

  /**
   * Return requests list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/return-request
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketItemReturnController::api_adminGetItemReturnRequests` expose observed keys including `return_requests`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_RETURN_REQUESTS_LIST(123);
   * ```
   */
  GET_RETURN_REQUESTS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/return-request`;
  }

  /**
   * Update return request state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/return/{item_id}/state
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketMerchantsActionsController::api_admin_postUpdateReturnRequestState` expose observed keys including `state`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_RETURN_REQUEST_STATE(123, 333, 123);
   * ```
   */
  POST_UPDATE_RETURN_REQUEST_STATE(
    shop_id: string | number,
    basket_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/return/${item_id}/state`;
  }

  //―――――――――――――――――――――― Bug Report ――――――――――――――――――――
  /**
   * Bug report endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /bug/report
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BugController::api_sendBugReport` expose observed keys including `page`, `reaction`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BUG_REPORT();
   * ```
   */
  POST_BUG_REPORT() {
    return `${this.selldone_api_url}/bug/report`;
  }

  //―――――――――――――――――――――― Passport ――――――――――――――――――――

  /**
   * Tokens endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /tokens
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_TOKEN_READ (selldone:token:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_getMyTokens` expose observed keys including `tokens`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_TOKENS();
   * ```
   */
  GET_TOKENS() {
    return `${this.selldone_api_url}/tokens`;
  }

  /**
   * Token endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /tokens/{token_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_TOKEN_WRITE (selldone:token:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param token_id - token_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_deleteMyToken` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_TOKEN(123);
   * ```
   */
  DELETE_TOKEN(token_id: string | number) {
    return `${this.selldone_api_url}/tokens/${token_id}`;
  }

  /**
   * Clients endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /clients
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_TOKEN_READ (selldone:token:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_getMyClients` expose observed keys including `clients`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CLIENTS();
   * ```
   */
  GET_CLIENTS() {
    return `${this.selldone_api_url}/clients`;
  }

  /**
   * Shop clients endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/clients
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_getShopClients` expose observed keys including `clients`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CLIENTS(123);
   * ```
   */
  GET_SHOP_CLIENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clients`;
  }

  /**
   * Create new shop client endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/clients
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_createShopClient` expose observed keys including `name`, `redirect`, `user_id`, `secret`, `redirect_uris`, `grant_types`, `personal_access_client`, `password_client`, `revoked`, `shop_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_NEW_SHOP_CLIENT(123);
   * ```
   */
  POST_CREATE_NEW_SHOP_CLIENT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clients`;
  }

  /**
   * Update shop client endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/clients/{client_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param client_id - client_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_updateShopClient` expose observed keys including `name`, `redirect`, `secret`, `redirect_uris`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_SHOP_CLIENT(123, 123);
   * ```
   */
  PUT_UPDATE_SHOP_CLIENT(shop_id: string | number, client_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clients/${client_id}`;
  }

  /**
   * Shop client endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/clients/{client_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param client_id - client_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_deleteShopClient` expose observed keys including `revoked`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CLIENT(123, 123);
   * ```
   */
  DELETE_SHOP_CLIENT(shop_id: string | number, client_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clients/${client_id}`;
  }

  /**
   * My personal access tokens endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /personal-access-tokens
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_TOKEN_READ (selldone:token:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_getMyPersonalAccessTokens` expose observed keys including `tokens`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PERSONAL_ACCESS_TOKENS();
   * ```
   */
  GET_MY_PERSONAL_ACCESS_TOKENS() {
    return `${this.selldone_api_url}/personal-access-tokens`;
  }

  /**
   * My personal access token endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /personal-access-tokens/{token_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_TOKEN_WRITE (selldone:token:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param token_id - token_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PassportController::api_deleteMyPersonalAccessToken` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_PERSONAL_ACCESS_TOKEN(123);
   * ```
   */
  DELETE_MY_PERSONAL_ACCESS_TOKEN(token_id: string | number) {
    return `${this.selldone_api_url}/personal-access-tokens/${token_id}`;
  }

  //―――――――――――――――――――――― SEO ――――――――――――――――――――

  /**
   * SEO preview product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/seo/preview
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopSeoPreviewController::api_getProductSeoPreview` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SEO_PREVIEW_PRODUCT(123, 456);
   * ```
   */
  GET_SEO_PREVIEW_PRODUCT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/seo/preview`;
  }

  /**
   * SEO preview shop endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/seo/preview
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopStructuredDataController::api_getShopSeoPreview` expose observed keys including `Organization`, `html`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SEO_PREVIEW_SHOP(123);
   * ```
   */
  GET_SEO_PREVIEW_SHOP(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/seo/preview`;
  }

  //―――――――――――――――――――――― Shop > Profiles ――――――――――――――――――――

  /**
   * Update shop profile image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/profiles/{type}/upload
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_BUSINESS_PROFILE_WRITE (backoffice:business-profile:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopProfileController::upload_shopProfile` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_SHOP_PROFILE_IMAGE(123, "cover");
   * ```
   */
  POST_UPDATE_SHOP_PROFILE_IMAGE(
    shop_id: string | number,
    type: "about-us" | "contact-us" | "privacy" | "terms",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/profiles/${type}/upload`;
  }

  /**
   * Save shop profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/profiles/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_BUSINESS_PROFILE_WRITE (backoffice:business-profile:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopProfileController::api_setShopProfile` expose observed keys including `body`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_PROFILE_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SAVE_SHOP_PROFILE(123, "cover");
   * ```
   */
  POST_SAVE_SHOP_PROFILE(
    shop_id: string | number,
    type: "about-us" | "contact-us" | "privacy" | "terms",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/profiles/${type}`;
  }

  /**
   * Shop profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/profiles/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_BUSINESS_PROFILE_READ (backoffice:business-profile:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopProfileController::api_getShopProfile` expose observed keys including `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_PROFILE_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_PROFILE(123, "cover");
   * ```
   */
  GET_SHOP_PROFILE(
    shop_id: string | number,
    type: "about-us" | "contact-us" | "privacy" | "terms",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/profiles/${type}`;
  }

  //―――――――――――――――――――――― Accounts ――――――――――――――――――――
  /**
   * Accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_getAllAccounts` expose observed keys including `offset`, `limit`, `with_trashed`, `updated_at`, `feeReceipt`, `error`, `accounts`, `more`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNTS();
   * ```
   */
  GET_ACCOUNTS() {
    return `${this.selldone_api_url}/accounts`;
  }

  /**
   * Add new account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /accounts/new
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_createNewAccount` expose observed keys including `account_name`, `type`, `currency`, `error`, `error_msg`, `success`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::CANT_OPEN_NEW_ACCOUNT_HAS_PENDING()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_NEW_ACCOUNT();
   * ```
   */
  POST_ADD_NEW_ACCOUNT() {
    return `${this.selldone_api_url}/accounts/new`;
  }

  /**
   * Account info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_getAccountInfo` expose observed keys including `feeReceipt`, `error`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNT_INFO("value");
   * ```
   */
  GET_ACCOUNT_INFO(account_number: string) {
    return `${this.selldone_api_url}/accounts/${account_number}`;
  }

  /**
   * Account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /accounts/{account_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_id - Wallet/account identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountDeleteController::api_deleteAccount` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_ACCOUNT(666);
   * ```
   */
  DELETE_ACCOUNT(account_id: string | number) {
    return `${this.selldone_api_url}/accounts/${account_id}`;
  }
  /**
   * Restore account endpoint URL.
   *
   * Builds the legacy SDK URL for account restore.
   *
   * @endpoint POST /accounts/{account_id}/restore
   * @auth TODO(api-docs): verify whether this SDK URL is stale before use.
   *
   * @param account_id - Account identifier intended for restore.
   * @returns Absolute URL string for the SDK account restore path.
   *
   * Response shape:
   * - TODO(api-docs): unresolved route mismatch; inspect `Backend/routes/api/wallet/gifts-referrals-accounts.php` and `AccountDeleteController::api_restoreAccount` before relying on this endpoint.
   *
   * Possible errors:
   * - TODO(api-docs): route mismatch may return 404 even with valid authentication.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_ACCOUNT(666);
   * ```
   */
  POST_RESTORE_ACCOUNT(account_id: string | number) {
    return `${this.selldone_api_url}/accounts/${account_id}/restore`;
  }

  /**
   * Check account number endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_getAccountInfo` expose observed keys including `feeReceipt`, `error`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_ACCOUNT_NUMBER("value");
   * ```
   */
  GET_CHECK_ACCOUNT_NUMBER(account_number: string) {
    return `${
      this.selldone_api_url
    }/accounts/check-number/${account_number.replace(/-/g, "")}`;
  }

  /**
   * Edit account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /accounts/{account_number}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_updateAccountInfo` expose observed keys including `account_name`, `company_id`, `success`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_ACCOUNT("value");
   * ```
   */
  PUT_EDIT_ACCOUNT(account_number: string) {
    return `${this.selldone_api_url}/accounts/${account_number}`;
  }

  /**
   * Active account list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/list-active/{currency}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param currency - Currency code accepted by the backend route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_getAllAccountsListSimpleActive` expose observed keys including `error`, `accounts`, `offset`, `more`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACTIVE_ACCOUNT_LIST("USD");
   * ```
   */
  GET_ACTIVE_ACCOUNT_LIST(currency: keyof typeof Currency) {
    return `${this.selldone_api_url}/accounts/list-active/${currency}`;
  }

  /**
   * Account charges history endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}/charges/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ChargeController::api_getChargeTransactions` expose observed keys including `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNT_CHARGES_HISTORY("value", "stripe");
   * ```
   */
  GET_ACCOUNT_CHARGES_HISTORY(account_number: string, gateway_code: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/charges/${gateway_code}`;
  }

  /**
   * Check charge payment gateway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}/charges/{gateway_code}/check-payment/{unique_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param gateway_code - Payment gateway code in the route path.
   * @param unique_id - unique_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::checkOnlinePaymentForCharge` expose observed keys including `error`, `error_msg`, `success`, `payment_at`, `status`, `transaction_status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_CHARGE_PAYMENT_GATEWAY("value", "stripe", 123);
   * ```
   */
  GET_CHECK_CHARGE_PAYMENT_GATEWAY(
    account_number: string,
    gateway_code: string,
    unique_id: string,
  ) {
    return `${this.selldone_api_url}/accounts/${account_number}/charges/${gateway_code}/check-payment/${unique_id}`;
  }

  /**
   * Account download monthly receipt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}/receipt/{date?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param date - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ReceiptController::downloadAccountMonthlyChargeReceipt` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNT_DOWNLOAD_MONTHLY_RECEIPT("value", "value");
   * ```
   */
  GET_ACCOUNT_DOWNLOAD_MONTHLY_RECEIPT(account_number: string, date: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/receipt/${date}`;
  }

  /**
   * Account download single charge receipt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/{account_number}/charges/{charge_id}/receipt
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param charge_id - charge_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ReceiptController::downloadAccountSingleChargeReceipt` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNT_DOWNLOAD_SINGLE_CHARGE_RECEIPT("value", 123);
   * ```
   */
  GET_ACCOUNT_DOWNLOAD_SINGLE_CHARGE_RECEIPT(
    account_number: string,
    charge_id: string | number,
  ) {
    return `${this.selldone_api_url}/accounts/${account_number}/charges/${charge_id}/receipt`;
  }



  /**
   * Account exchange with another wallet check endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /accounts/{account_number}/exchange/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountExchangeController::api_postTopupWalletWithAnotherWalletCheck` expose observed keys including `account_from`, `from`, `to`, `exchange_rate`, `free_balance`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ACCOUNT_EXCHANGE_WITH_ANOTHER_WALLET_CHECK("value");
   * ```
   */
  POST_ACCOUNT_EXCHANGE_WITH_ANOTHER_WALLET_CHECK(account_number: string | number) {
    return `${this.selldone_api_url}/accounts/${account_number}/exchange/check`;
  }
  /**
   * Account exchange with another wallet endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /accounts/{account_number}/exchange
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountExchangeController::api_postTopupWalletWithAnotherWallet` expose observed keys including `amount`, `account_from`, `success`, `exchange_rate`, `withdraw`, `deposit`, `from`, `to`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_SUFFICIENT_BALANCE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ACCOUNT_EXCHANGE_WITH_ANOTHER_WALLET("value");
   * ```
   */
  POST_ACCOUNT_EXCHANGE_WITH_ANOTHER_WALLET(account_number: string | number) {
    return `${this.selldone_api_url}/accounts/${account_number}/exchange`;
  }

  //―――――――――――――――――――――― Claim Free Initial Charge ――――――――――――――――――――

  /**
   * Claim free charge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /accounts/{account_number}/claim-free-charge
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftChargeController::api_ClaimInitialFreeCharge` expose observed keys including `success`, `charge`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_BEFORE_RECEIVED_CHARGE_ERROR()`, `ErrorCodes::NOT_GIFT_NOT_EXIST_FOR_CURRENCY_ERROR()`, `ErrorCodes::NOT_FREE_CHARGE_NOT_AVAILABLE_ERROR()`, `ErrorCodes::NOT_TOTAL_CHARGE_NOT_MEET_CONDITION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CLAIM_FREE_CHARGE("value");
   * ```
   */
  POST_CLAIM_FREE_CHARGE(account_number: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/claim-free-charge`;
  }

  //―――――――――――――――――――――― Shop > Accounts ――――――――――――――――――――

  /**
   * Add shop accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FINANCE_WRITE (backoffice:finance:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAccountController::api_setShopAccount` expose observed keys including `account_number`, `success`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_ACCOUNTS(123);
   * ```
   */
  POST_ADD_SHOP_ACCOUNTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/accounts`;
  }

  /**
   * Shop accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint DELETE /shops/{shop_id}/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FINANCE_WRITE (backoffice:finance:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAccountController::api_deleteShopAccount` expose observed keys including `account_number`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_ACCOUNTS(123);
   * ```
   */
  DELETE_SHOP_ACCOUNTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/accounts`;
  }

  //―――――――――――――――――――――― Shop > Subscription ――――――――――――――――――――
  /**
   * Shop subscriptions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/subscriptions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SubscriptionController::api_getShopSubscriptions` expose observed keys including `subscriptions`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SUBSCRIPTIONS(123);
   * ```
   */
  GET_SHOP_SUBSCRIPTIONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscriptions`;
  }

  /**
   * Add shop subscription endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/subscriptions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SubscriptionController::api_addSubscription` expose observed keys including `renewal`, `code`, `account_number`, `voucher_code`, `error`, `error_msg`, `type`, `used`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_SUBSCRIPTION(123);
   * ```
   */
  POST_ADD_SHOP_SUBSCRIPTION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscriptions`;
  }

  /**
   * Shop subscription endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/subscriptions/{subscription_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param subscription_id - subscription_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SubscriptionController::api_cancelSubscription` expose observed keys including `error`, `error_msg`, `success`, `id`, `shop_license`, `used`, `balance`, `payed`, `used_at`, `vouchers`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_SUBSCRIPTION(123, 123);
   * ```
   */
  DELETE_SHOP_SUBSCRIPTION(
    shop_id: string | number,
    subscription_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscriptions/${subscription_id}`;
  }

  /**
   * Shop subscription price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/subscription/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SubscriptionController::api_getShopSubscriptionPrice` expose observed keys including `type`, `code`, `currency`, `price`, `start_at`, `end_at`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SUBSCRIPTION_PRICE(123);
   * ```
   */
  GET_SHOP_SUBSCRIPTION_PRICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscription/price`;
  }

  /**
   * Shop subscription endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/subscriptions/{subscription_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param subscription_id - subscription_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SubscriptionController::api_updateSubscription` expose observed keys including `renewal`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_SUBSCRIPTION(123, 123);
   * ```
   */
  PUT_SHOP_SUBSCRIPTION(
    shop_id: string | number,
    subscription_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscriptions/${subscription_id}`;
  }

  //―――――――――――――――――――――― Accounts > Receipt ――――――――――――――――――――
  /**
   * Receipt info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /accounts/receipt/check/{receipt_number}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param receipt_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountController::api_getReceiptInfoPublic` expose observed keys including `error`, `receipt`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_RECEIPT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_RECEIPT_INFO("value");
   * ```
   */
  GET_RECEIPT_INFO(receipt_number: string) {
    return `${this.selldone_api_url}/accounts/receipt/check/${receipt_number}`;
  }

  /**
   * Check receipt number endpoint URL.
   *
   * Builds the public receipt lookup URL after removing hyphens from the provided receipt number.
   *
   * @endpoint GET /accounts/receipt/check/{receipt_number}
   * @domain api.localhost
   * @auth Public or controller-conditional; no route-level authentication middleware is attached to the matched route.
   * @middleware `api-selldone`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param receipt_number - Receipt number; SDK strips `-` before appending it to the route.
   * @returns Absolute URL string for public receipt lookup.
   *
   * Response shape:
   * - Backend returns public receipt/account information from `AccountController::api_getReceiptInfoPublic`.
   *
   * Possible errors:
   * - 404 not_found: receipt/account cannot be found.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_RECEIPT_NUMBER("ABC-123");
   * ```
   */
  GET_CHECK_RECEIPT_NUMBER(receipt_number: string) {
    return `${
      this.selldone_api_url
    }/accounts/receipt/check/${receipt_number.replace(/-/g, "")}`;
  }

  //―――――――――――――――――――――― Accounts > Charge ――――――――――――――――――――
  /**
   * Charge gateways endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gateways/{currency}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param currency - Currency code accepted by the backend route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ChargeController::api_getChargesGateways` expose observed keys including `gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHARGE_GATEWAYS("USD");
   * ```
   */
  GET_CHARGE_GATEWAYS(currency: keyof typeof Currency) {
    return `${this.selldone_api_url}/gateways/${currency}`;
  }

  /**
   * Payment status interval endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gateways/{gateway_code}/transactions/{transaction_id}/{unique_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param gateway - Route parameter passed through to the backend endpoint.
   * @param transaction_id - Transaction identifier used by the route.
   * @param unique_id - unique_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GatewayController::checkIntervalPaymentForCharge` expose observed keys including `error`, `error_msg`, `success`, `payment_at`, `target_type`, `target_id`, `status`, `card`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PAYMENT_STATUS_INTERVAL("value", 777, 123);
   * ```
   */
  GET_PAYMENT_STATUS_INTERVAL(
    gateway: string,
    transaction_id: string | number,
    unique_id: string,
  ) {
    return `${this.selldone_api_url}/gateways/${gateway}/transactions/${transaction_id}/${unique_id}`;
  }

  /**
   * Request charge account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /accounts/{account_number}/charge/{gateway}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,5`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param gateway - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ChargeController::api_createNewCharge` expose observed keys including `amount`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::CHARGE_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_REQUEST_CHARGE_ACCOUNT("value", "value");
   * ```
   */
  POST_REQUEST_CHARGE_ACCOUNT(account_number: string, gateway: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/charge/${gateway}`;
  }

  //―――――――――――――――――――――― 🎗️ Subscription ――――――――――――――――――――
  /**
   * Selldone subscribe now endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /accounts/{account_number}/subscribe/{gateway}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,5`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @param gateway - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddCardController::api_addCardToAccount` expose observed keys including `amount`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_MATCH_ACCOUNT_CURRENCY_ERROR()`, `ErrorCodes::CHARGE_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SELLDONE_SUBSCRIBE_NOW("value", "value");
   * ```
   */
  POST_SELLDONE_SUBSCRIBE_NOW(account_number: string, gateway: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/subscribe/${gateway}`;
  }

  /**
   * Selldone subscribe cancel endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint DELETE /accounts/{account_number}/subscribe
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,5`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddCardController::api_removeCardFromAccount` expose observed keys including `invoice_now`, `feedback`, `error`, `error_msg`, `success`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SELLDONE_SUBSCRIBE_CANCEL("value");
   * ```
   */
  DELETE_SELLDONE_SUBSCRIBE_CANCEL(account_number: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/subscribe`;
  }

  /**
   * Account subscription manually check auto charges endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /accounts/{account_number}/check-subscribe
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,5`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddCardController::api_checkAccountPayments` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ACCOUNT_SUBSCRIPTION_MANUALLY_CHECK_AUTO_CHARGES("value");
   * ```
   */
  POST_ACCOUNT_SUBSCRIPTION_MANUALLY_CHECK_AUTO_CHARGES(
    account_number: string,
  ) {
    return `${this.selldone_api_url}/accounts/${account_number}/check-subscribe`;
  }

  //―――――――――――――――――――――― Accounts > Transaction ――――――――――――――――――――

  /**
   * Transaction info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /transactions/{account_number}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransactionController::api_getAllTransactions` expose observed keys including `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_TRANSACTION_INFO("value");
   * ```
   */
  GET_TRANSACTION_INFO(account_number: string) {
    return `${this.selldone_api_url}/transactions/${account_number}`;
  }

  /**
   * Create new transaction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /transactions/send/new
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransactionController::api_transactionPreConfirmation` expose observed keys including `error`, `from`, `to`, `amount`, `receipt`, `description`, `currency`, `data`, `account_from_name`, `to_user_name`, `to_user_id`, `fee`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::SOURCE_AND_DESTINATION_ACCOUNT_ARE_SAME_ERROR()`, `ErrorCodes::NOT_VALID_ACCOUNT_TO_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_MATCH_CURRENCY_FROM_TO_ERROR()`, `ErrorCodes::SOURCE_ACCOUNT_NOT_ACTIVE_ERROR()`, `ErrorCodes::DESTINATION_ACCOUNT_NOT_ACTIVE_ERROR()`, `ErrorCodes::NOT_SUFFICIENT_BALANCE_ERROR()`, `ErrorCodes::RECEIPT_NUMBER_ERROR()`, `ErrorCodes::AMOUNT_NOT_MATCH_RECEIPT_ERROR()`, `ErrorCodes::RECEIPT_PAYED_BEFORE_ERROR()`, `ErrorCodes::RECEIPT_CANCELED_BEFORE_ERROR()`, `ErrorCodes::RECEIPT_NOT_MATCH_DESTINATION_ACCOUNT_ERROR()`, `ErrorCodes::RECEIPT_NOT_VALID_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_NEW_TRANSACTION();
   * ```
   */
  POST_CREATE_NEW_TRANSACTION() {
    return `${this.selldone_api_url}/transactions/send/new`;
  }

  /**
   * Confirm transaction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /transactions/send/confirm
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `TransactionController::api_confirmTransaction` expose observed keys including `data`, `from`, `to`, `amount`, `error`, `error_msg`, `receipt`, `transaction`, `currency`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::TRANSACTION_CODE_NOT_VALID_ERROR()`, `ErrorCodes::NOT_VALID_ACCOUNT_FROM_ERROR()`, `ErrorCodes::NOT_VALID_ACCOUNT_TO_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::SOURCE_ACCOUNT_NOT_ACTIVE_ERROR()`, `ErrorCodes::DESTINATION_ACCOUNT_NOT_ACTIVE_ERROR()`, `ErrorCodes::NOT_MATCH_ACCOUNT_CURRENCY_ERROR()`, `ErrorCodes::NOT_SUFFICIENT_BALANCE_ERROR()`, `ErrorCodes::RECEIPT_NUMBER_ERROR()`, `ErrorCodes::AMOUNT_NOT_MATCH_RECEIPT_ERROR()`, `ErrorCodes::RECEIPT_PAYED_BEFORE_ERROR()`, `ErrorCodes::RECEIPT_CANCELED_BEFORE_ERROR()`, `ErrorCodes::RECEIPT_NOT_MATCH_DESTINATION_ACCOUNT_ERROR()`, `ErrorCodes::RECEIPT_NOT_VALID_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONFIRM_TRANSACTION();
   * ```
   */
  POST_CONFIRM_TRANSACTION() {
    return `${this.selldone_api_url}/transactions/send/confirm`;
  }

  /**
   * Account connected shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /accounts/{account_number}/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_READ (backoffice:wallet:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param account_number - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AccountConnectedShopsController::api_getAccountConnectedShopsList` expose observed keys including `offset`, `limit`, `sortBy`, `sortDesc`, `shop_accounts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ACCOUNT_CONNECTED_SHOPS("value");
   * ```
   */
  GET_ACCOUNT_CONNECTED_SHOPS(account_number: string) {
    return `${this.selldone_api_url}/accounts/${account_number}/shops`;
  }


  //―――――――――――――――――――――― Application Builder > Android ――――――――――――――――――――

  /**
   * Application android endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/native/android
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `NativeAppController::api_getBuildAndroidApp` expose observed keys including `application`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_NATIVE_APP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_APPLICATION_ANDROID(123);
   * ```
   */
  GET_APPLICATION_ANDROID(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/native/android`;
  }

  /**
   * Build application android endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/native/android
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `NativeAppController::api_requestBuildAndroidApp` expose observed keys including `package`, `version_code`, `version_name`, `app_name`, `app_title`, `splash_title`, `splash_message`, `splash_color`, `splash_dark`, `sign_mode`, `app_icon`, `splash_image`, `keystore`, `keystore_password`, `keystore_alias`, `alias_password`, `base_uri`, `timeout`, `headers`, `Authorization`, `name`, `contents`, `filename`, `query`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::APPLICATION_BUILD_INVALID_MIN_VERSION()`, `ErrorCodes::BAD_FILE_FORMAT_ERROR()`, `ErrorCodes::NOT_FOUND_NATIVE_APP_ERROR()`, `ErrorCodes::INTERNAL_SERVERS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BUILD_APPLICATION_ANDROID(123);
   * ```
   */
  POST_BUILD_APPLICATION_ANDROID(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/native/android`;
  }

  /**
   * Application android state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/native/android/state
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `NativeAppController::api_getAndroidBuiltInfo` expose observed keys including `pending`, `app`, `builder_response`, `exception`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_APPLICATION_ANDROID_STATE(123);
   * ```
   */
  GET_APPLICATION_ANDROID_STATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/native/android/state`;
  }

  //―――――――――――――――――――――― Application Builder > iOS ――――――――――――――――――――
  /**
   * Application IOS image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/native/ios/splash/{size}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param size - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `NativeAppController::api_uploadIosSplash` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_APPLICATION_IOS_IMAGE(123, "value");
   * ```
   */
  GET_APPLICATION_IOS_IMAGE(
    shop_id: string | number,
    size:
      | "1170x2532"
      | "1080x2340"
      | "1242x2688"
      | "828x1792"
      | "1125x2436"
      | "750x1334"
      | "1242x2208"
      | "640x1136"
      | "1536x2048"
      | "1668x2224"
      | "2048x2732",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/native/ios/splash/${size}`;
  }

  //―――――――――――――――――――――― Shop > Plugins > Google Tag Manager ――――――――――――――――――――

  /**
   * Shop plugin set config endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/plugins/{plugin_name}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param plugin_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPluginController::api_setShopPluginSetting` expose observed keys including `enable`, `setting`, `error`, `plugin`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_PLUGIN_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_PLUGIN_SET_CONFIG(123, "value");
   * ```
   */
  POST_SHOP_PLUGIN_SET_CONFIG(
    shop_id: string | number,
    plugin_code: "TagManager" | "VerificationTag",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/plugins/${plugin_code}`;
  }

  //―――――――――――――――――――――― Export ――――――――――――――――――――
  /**
   * Export products endpoint URL.
   *
   * Builds one of the concrete product export routes selected by `file`.
   *
   * @endpoint GET /shops/{shop_id}/products/excel
   * @endpoint GET /shops/{shop_id}/products/csv
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param file - Export format route segment; `excel` or `csv`.
   * @returns Absolute URL string for the product export download.
   *
   * Response shape:
   * - File download response in Excel or CSV format.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing product read scope or product-region read permission.
   * - 404 not_found: shop is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_PRODUCTS(123, "csv");
   * ```
   */
  GET_EXPORT_PRODUCTS(shop_id: string | number, file: "excel" | "csv") {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${file}`;
  }

  /**
   * Export categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param file - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryDeleteController::api_deleteCategory` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_CATEGORIES(123, "value");
   * ```
   */
  GET_EXPORT_CATEGORIES(shop_id: string | number, file: "excel" | "csv") {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${file}`;
  }

  /**
   * Export gift cards endpoint URL.
   *
   * Builds one of the concrete gift-card export routes selected by `file`.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/export/excel
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/export/csv
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_READ (backoffice:giftcard:read)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param giftcard_type_id - Gift-card type identifier scoped to the shop.
   * @param file - Export format route segment; `excel` or `csv`.
   * @returns Absolute URL string for the gift-card export download.
   *
   * Response shape:
   * - File download response in Excel or CSV format.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing gift-card read scope or incentives-region permission.
   * - 404 not_found: shop or gift-card type is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_GIFT_CARDS(123, 456, "excel");
   * ```
   */
  GET_EXPORT_GIFT_CARDS(
    shop_id: string | number,
    giftcard_type_id: string | number,
    file: "excel" | "csv",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${giftcard_type_id}/export/${file}`;
  }

  /**
   * Export gift-card data endpoint URL.
   *
   * Builds one of the concrete gift-card data export routes selected by `file`.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/data/export/excel
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/data/export/csv
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_READ (backoffice:giftcard:read)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param giftcard_type_id - Gift-card type identifier scoped to the shop.
   * @param file - Export format route segment; `excel` or `csv`.
   * @returns Absolute URL string for the gift-card data export download.
   *
   * Response shape:
   * - File download response in Excel or CSV format.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing gift-card read scope or incentives-region permission.
   * - 404 not_found: shop or gift-card type is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_GIFT_CARDS_DATA(123, 456, "csv");
   * ```
   */
  GET_EXPORT_GIFT_CARDS_DATA(
    shop_id: string | number,
    giftcard_type_id: string | number,
    file: "excel" | "csv",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${giftcard_type_id}/data/export/${file}`;
  }

  /**
   * Export inventory endpoint URL.
   *
   * Builds one of the concrete inventory export routes selected by `file`.
   *
   * @endpoint GET /shops/{shop_id}/inventory/excel
   * @endpoint GET /shops/{shop_id}/inventory/csv
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param file - Export format route segment; `excel` or `csv`.
   * @returns Absolute URL string for the inventory export download.
   *
   * Response shape:
   * - File download response in Excel or CSV format.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing product read scope or product-region permission.
   * - 404 not_found: shop is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_INVENTORY(123, "excel");
   * ```
   */
  GET_EXPORT_INVENTORY(shop_id: string | number, file: "excel" | "csv") {
    return `${this.selldone_api_url}/shops/${shop_id}/inventory/${file}`;
  }

  /**
   * Export for auction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/for-auction/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExportController::api_exportProductForAuctionToExcel` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_FOR_AUCTION(123, 456);
   * ```
   */
  GET_EXPORT_FOR_AUCTION(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/for-auction/excel`;
  }

  /**
   * Export for available endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/for-available/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductExportController::api_exportProductForAvailableToExcel` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_FOR_AVAILABLE(123, 456);
   * ```
   */
  GET_EXPORT_FOR_AVAILABLE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/for-available/excel`;
  }

  /**
   * Export basket tasks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{type}/export/tasks/{render}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @param render - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OrdersExportController::exportOrders` expose observed keys including `start`, `end`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_BASKET_TASKS(123, "cover", "value");
   * ```
   */
  GET_EXPORT_BASKET_TASKS(
    shop_id: string | number,
    type: Lowercase<OrderTypeCode>,
    render: "excel" | "csv" | "pdf",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${type}/export/tasks/${render}`;
  }

  /**
   * Export basket labels endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/{type}/export/labels
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OrdersExportController::exportLabels` expose observed keys including `page`, `detail`, `start`, `end`, `delivery_states`, `title`, `format`, `margin_left`, `margin_right`, `margin_top`, `margin_bottom`, `margin_footer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_BASKET_LABELS(123, "cover");
   * ```
   */
  GET_EXPORT_BASKET_LABELS(
    shop_id: string | number,
    type: Lowercase<OrderTypeCode>,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${type}/export/labels`;
  }

  //―――――――――――――――――――――― File type converter ――――――――――――――――――――

  /**
   * Converter excel csv endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/converter/excel/csv
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FileTypesConverterController::api_convertFilesToCSV` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CONVERTER_EXCEL_CSV(123);
   * ```
   */
  POST_CONVERTER_EXCEL_CSV(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/converter/excel/csv`;
  }

  //―――――――――――――――――――――― Shop > Custom Domain ――――――――――――――――――――

  //todo: add get domains list!

  /**
   * Create client for domain endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/domain/{domain_id}/add-client
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain_id - Custom domain identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_addClientForDomain` expose observed keys including `success`, `client`, `message`, `name`, `redirect`, `redirect_uris`, `grant_types`, `public`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_CLIENT_FOR_DOMAIN(123, 555);
   * ```
   */
  POST_CREATE_CLIENT_FOR_DOMAIN(
    shop_id: string | number,
    domain_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/domain/${domain_id}/add-client`;
  }

  /**
   * Add domain endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/domain
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_setShopDomain` expose observed keys including `ssl_proxy`, `domain`, `error`, `error_msg`, `approved`, `valid_txt`, `dns`, `challenge_domain`, `dns_challenge`, `sec`, `code`, `detail`, `success`, `cdn`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::DOMAIN_ALREADY_USED_BY_YOU_ERROR()`, `ErrorCodes::DOMAIN_ALREADY_USED_BY_ANOTHER_ERROR()`, `ErrorCodes::GET_DNS_INFO_ERROR()`, `ErrorCodes::DOMAIN_DNS_NOT_SET_CORRECTLY_ERROR()`, `ErrorCodes::DATABASE_TRANSACTION_FAILED()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_DOMAIN(123);
   * ```
   */
  POST_ADD_DOMAIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/domain`;
  }

  /**
   * Domain info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/domains/{domain}/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_getDomainNameServer` expose observed keys including `error`, `error_msg`, `name`, `redirect`, `redirect_uris`, `grant_types`, `public`, `sec`, `dns`, `domain`, `client`, `approved`, `cdn`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`, `ErrorCodes::GET_DNS_INFO_ERROR()`, `ErrorCodes::DOMAIN_DNS_NOT_SET_CORRECTLY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DOMAIN_INFO(123, "value");
   * ```
   */
  GET_DOMAIN_INFO(shop_id: string | number, domain: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/domains/${domain}/check`;
  }

  /**
   * Edit domain endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/domains/{domain}/{param}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain - Route parameter passed through to the backend endpoint.
   * @param param - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_setDomainParam` expose observed keys including `value`, `error`, `error_msg`, `local`, `sub`, `primary`, `enable`, `domain`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_DOMAIN(123, "value", "value");
   * ```
   */
  PUT_EDIT_DOMAIN(
    shop_id: string | number,
    domain: string,
    param: "primary" | "enable",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/domains/${domain}/${param}`;
  }

  /**
   * Edit domain endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/domains/{domain}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_deleteShopDomain` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EDIT_DOMAIN(123, "value");
   * ```
   */
  DELETE_EDIT_DOMAIN(shop_id: string | number, domain: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/domains/${domain}`;
  }

  /**
   * Set domain setting endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/domain/{domain_id}/setting
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain_id - Custom domain identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainSettingController::api_setShopDomainSetting` expose observed keys including `https`, `currencies`, `languages`, `success`, `domain`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_DOMAIN_SETTING(123, 555);
   * ```
   */
  POST_SET_DOMAIN_SETTING(
    shop_id: string | number,
    domain_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/domain/${domain_id}/setting`;
  }

  /**
   * Domain check SSL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/domain/{domain_id}/check-ssl
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param domain_id - Custom domain identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `DomainSettingController::api_checkSSL` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DOMAIN_CHECK_SSL(123, 555);
   * ```
   */
  POST_DOMAIN_CHECK_SSL(shop_id: string | number, domain_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/domain/${domain_id}/check-ssl`;
  }

  /**
   * Shop domains endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/domains
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainController::api_getShopDomainsList` expose observed keys including `offset`, `limit`, `domains`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_DOMAINS(123);
   * ```
   */
  GET_SHOP_DOMAINS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/domains`;
  }

  //―――――――――――――――――――――― Domain verifications ――――――――――――――――――――

  /**
   * Shop domain verifications endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/verifications
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainVerificationsController::api_GetShopDomainVerifications` expose observed keys including `offset`, `limit`, `sortBy`, `sortDesc`, `search`, `verifications`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_DOMAIN_VERIFICATIONS(123);
   * ```
   */
  GET_SHOP_DOMAIN_VERIFICATIONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/verifications`;
  }

  /**
   * Shop domain verification endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/verifications/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainVerificationsController::api_SetShopDomainVerification` expose observed keys including `domain`, `error`, `error_msg`, `message`, `success`, `verification`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_DOMAIN_VERIFICATION(123, "code");
   * ```
   */
  POST_SHOP_DOMAIN_VERIFICATION(shop_id: string | number, code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/verifications/${code}`;
  }

  /**
   * Shop domain verification endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint DELETE /shops/{shop_id}/verifications/{verification_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param verification_id - verification_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DomainVerificationsController::api_DeleteShopDomainVerification` expose observed keys including `domain`, `error`, `message`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_DOMAIN_VERIFICATION(123, 123);
   * ```
   */
  DELETE_SHOP_DOMAIN_VERIFICATION(
    shop_id: string | number,
    verification_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/verifications/${verification_id}`;
  }

  //―――――――――――――――――――――― Shop > Footer ――――――――――――――――――――
  /**
   * Edit shop footer sections endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/footer/sections/{section}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param sec_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFooterController::api_setFooterSection` expose observed keys including `url`, `html`, `sec1`, `sec2`, `sec3`, `footer`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_SHOP_FOOTER_SECTIONS(123, "value");
   * ```
   */
  POST_EDIT_SHOP_FOOTER_SECTIONS(
    shop_id: string | number,
    sec_index: 1 | 2 | 3 | 4,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/footer/sections/${sec_index}`;
  }

  //―――――――――――――――――――――― Shop > Theme ――――――――――――――――――――
  /**
   * Edit shop theme endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/theme
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopThemeController::api_setShopTheme` expose observed keys including `color_light`, `color_dark`, `color_deep_dark`, `color_info`, `color_buy`, `color_bill`, `light_checkout`, `light_header`, `light_footer`, `light_filter`, `mobile`, `tablet`, `pc`, `mobile_f`, `tablet_f`, `pc_f`, `static`, `hover_actions`, `only_available`, `hide_empty_folders`, `wrap_title`, `smart_price`, `order`, `variants_mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_SHOP_THEME(123);
   * ```
   */
  POST_EDIT_SHOP_THEME(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/theme`;
  }

  //―――――――――――――――――――――― Shop > Menu ――――――――――――――――――――

  /**
   * Shop menu endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/menus/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_getMenu` expose observed keys including `menu`, `enable`, `translations`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MENU(123, "cover");
   * ```
   */
  GET_SHOP_MENU(shop_id: string | number, type: ShopMenuType) {
    return `${this.selldone_api_url}/shops/${shop_id}/menus/${type}`;
  }

  /**
   * Edit shop menu endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/menus/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_setMenu` expose observed keys including `menu`, `enable`, `translations`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EDIT_SHOP_MENU(123, "cover");
   * ```
   */
  POST_EDIT_SHOP_MENU(shop_id: string | number, type: ShopMenuType) {
    return `${this.selldone_api_url}/shops/${shop_id}/menus/${type}`;
  }

  /**
   * Shop menu generate categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/menus/generate/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_generateMenuCategories` expose observed keys including `language`, `success`, `categories`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MENU_GENERATE_CATEGORIES(123);
   * ```
   */
  GET_SHOP_MENU_GENERATE_CATEGORIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/menus/generate/categories`;
  }

  /**
   * Translate shop menu endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/menus/{type}/translate
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMenuController::api_translateMenu` expose observed keys including `menu`, `language`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSLATE_SHOP_MENU(123, "cover");
   * ```
   */
  POST_TRANSLATE_SHOP_MENU(shop_id: string | number, type: ShopMenuType) {
    return `${this.selldone_api_url}/shops/${shop_id}/menus/${type}/translate`;
  }

  //―――――――――――――――――――――― Shop > Social ――――――――――――――――――――
  /**
   * Set shop social endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/edit/social/{network}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param network_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSocialsController::api_setShopSocial` expose observed keys including `url`, `active`, `success`, `social`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SOCIAL_NETWORK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_SHOP_SOCIAL(123, "value");
   * ```
   */
  PUT_SET_SHOP_SOCIAL(
    shop_id: string | number,
    network_code: keyof typeof SocialNetwork,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/edit/social/${network_code}`;
  }

  /**
   * Shop social endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/edit/social/{network}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param network_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSocialsController::api_removeShopSocial` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SOCIAL_NETWORK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_SOCIAL(123, "value");
   * ```
   */
  DELETE_SHOP_SOCIAL(
    shop_id: string | number,
    network_code: keyof typeof SocialNetwork,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/edit/social/${network_code}`;
  }


/*  POST_COMMANDER_AI(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/ai-commander`;
  }*/
  //―――――――――――――――――――――― Shop > Discount Codes ――――――――――――――――――――

  /**
   * Discount codes endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/discount-codes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeController::api_getDiscountCodes` expose observed keys including `offset`, `limit`, `cluster_id`, `discount_codes`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DISCOUNT_CODES(123);
   * ```
   */
  GET_DISCOUNT_CODES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes`;
  }

  /**
   * Create discount code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/discount-codes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeEditController::api_createShopDiscountCode` expose observed keys including `code`, `percent`, `limit`, `currency`, `cluster_id`, `error`, `error_msg`, `success`, `discount_code`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::DISCOUNT_CODE_DUPLICATED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_DISCOUNT_CODE(123);
   * ```
   */
  POST_CREATE_DISCOUNT_CODE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes`;
  }

  /**
   * Create discount code AI endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/discount-codes/ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeAddAiController::api_AiCreateShopDiscountCode` expose observed keys including `prompt`, `voice`, `cluster_id`, `currency`, `error`, `error_msg`, `success`, `discount_code`, `debug`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_DISCOUNT_CODE_AI(123);
   * ```
   */
  POST_CREATE_DISCOUNT_CODE_AI(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/ai`;
  }

  /**
   * Edit discount code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/discount-codes/{discount_code_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeEditController::api_editDiscountCode` expose observed keys including `code`, `percent`, `limit`, `cluster_id`, `currency`, `error`, `error_msg`, `dis_start`, `dis_end`, `title`, `description`, `success`, `discount_code`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::DISCOUNT_CODE_DUPLICATED_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_DISCOUNT_CODE(123, 123);
   * ```
   */
  PUT_EDIT_DISCOUNT_CODE(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}`;
  }

  /**
   * Discount code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/discount-codes/{discount_code_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeEditController::api_deleteDiscountCode` expose observed keys including `code`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_DISCOUNT_CODE(123, 123);
   * ```
   */
  DELETE_DISCOUNT_CODE(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}`;
  }

  /**
   * Restore discount code endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/discount-codes/{discount_code_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeEditController::api_RestoreDiscountCode` expose observed keys including `error`, `error_msg`, `success`, `discount_code`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_DISCOUNT_CODE(123, 123);
   * ```
   */
  POST_RESTORE_DISCOUNT_CODE(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}/restore`;
  }

  /**
   * Discount code data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/discount-codes/{discount_code_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeController::api_getAdminDiscountCodeInfo` expose observed keys including `success`, `discount_code`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DISCOUNT_CODE_DATA(123, 123);
   * ```
   */
  GET_DISCOUNT_CODE_DATA(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}`;
  }

  /**
   * Discount code orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/discount-codes/{discount_code_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeController::api_getShopDiscountCodeOrders` expose observed keys including `error`, `discount_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DISCOUNT_CODE_ORDERS(123, 123);
   * ```
   */
  GET_DISCOUNT_CODE_ORDERS(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}/orders`;
  }

  /**
   * Discount code add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/discount-codes/{discount_code_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeNoteController::api_addNote` expose observed keys including `body`, `user_id`, `user_name`, `date`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DISCOUNT_CODE_ADD_NOTE(123, 123);
   * ```
   */
  POST_DISCOUNT_CODE_ADD_NOTE(
    shop_id: string | number,
    discount_code_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}/note`;
  }

  /**
   * Discount code note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/discount-codes/{discount_code_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DiscountCodeNoteController::api_deleteNote` expose observed keys including `error`, `error_msg`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_DISCOUNT_CODE_NOTE(123, 123, "value");
   * ```
   */
  DELETE_DISCOUNT_CODE_NOTE(
    shop_id: string | number,
    discount_code_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}/note/${note_index}`;
  }

  //―――――――――――――――――――――― Shop > Coupon ――――――――――――――――――――

  /**
   * Coupons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/coupons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponController::api_getCoupons` expose observed keys including `offset`, `limit`, `cluster_id`, `coupons`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_COUPONS(123);
   * ```
   */
  GET_COUPONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons`;
  }

  /**
   * Create coupon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/coupons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponEditController::api_createShopCoupon` expose observed keys including `enable`, `percent`, `limit`, `charge`, `currency`, `title`, `only_first_buy`, `min_purchase`, `qualify`, `qualify_single`, `qualify_daily`, `qualify_monthly`, `qualify_yearly`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `products`, `start`, `end`, `cluster_id`, `error`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_COUPON(123);
   * ```
   */
  POST_CREATE_COUPON(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons`;
  }

  /**
   * Create coupon AI endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/coupons/ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponAddAiController::api_AiCreateShopCoupon` expose observed keys including `prompt`, `voice`, `cluster_id`, `currency`, `error`, `error_msg`, `success`, `coupon`, `debug`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_COUPON_AI(123);
   * ```
   */
  POST_CREATE_COUPON_AI(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/ai`;
  }

  /**
   * Edit coupon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/coupons/{coupon_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponEditController::api_editCoupon` expose observed keys including `enable`, `percent`, `limit`, `charge`, `title`, `only_first_buy`, `min_purchase`, `qualify`, `qualify_single`, `qualify_daily`, `qualify_monthly`, `qualify_yearly`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `products`, `start`, `end`, `cluster_id`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_COUPON(123, 123);
   * ```
   */
  PUT_EDIT_COUPON(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}`;
  }

  /**
   * Coupon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/coupons/{coupon_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponEditController::api_deleteCoupon` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_COUPON(123, 123);
   * ```
   */
  DELETE_COUPON(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}`;
  }

  /**
   * Restore coupon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/coupons/{coupon_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponEditController::api_restoreCoupon` expose observed keys including `error`, `error_msg`, `success`, `coupon`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_COUPON(123, 123);
   * ```
   */
  POST_RESTORE_COUPON(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}/restore`;
  }

  /**
   * Coupon data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/coupons/{coupon_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponController::api_getAdminCouponInfo` expose observed keys including `success`, `coupon`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_COUPON_DATA(123, 123);
   * ```
   */
  GET_COUPON_DATA(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}`;
  }

  /**
   * Coupon orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/coupons/{coupon_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponController::api_getShopCouponOrders` expose observed keys including `error`, `coupon_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_COUPON_ORDERS(123, 123);
   * ```
   */
  GET_COUPON_ORDERS(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}/orders`;
  }

  /**
   * Coupon add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/coupons/{coupon_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponNoteController::api_addNote` expose observed keys including `body`, `user_id`, `user_name`, `date`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_COUPON_ADD_NOTE(123, 123);
   * ```
   */
  POST_COUPON_ADD_NOTE(shop_id: string | number, coupon_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}/note`;
  }

  /**
   * Coupon note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/coupons/{coupon_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CouponNoteController::api_deleteNote` expose observed keys including `error`, `error_msg`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_COUPON_NOTE(123, 123, "value");
   * ```
   */
  DELETE_COUPON_NOTE(
    shop_id: string | number,
    coupon_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}/note/${note_index}`;
  }

  //―――――――――――――――――――――― Shop > Offer ――――――――――――――――――――

  /**
   * Offers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/offers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferController::api_getOffers` expose observed keys including `offset`, `limit`, `cluster_id`, `offers`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_OFFERS(123);
   * ```
   */
  GET_OFFERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers`;
  }

  /**
   * Create offer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/offers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferEditController::api_createShopOffer` expose observed keys including `enable`, `per_order`, `currency`, `start`, `end`, `title`, `min_quantity`, `min_purchase`, `buy_products`, `get_products`, `percent`, `cluster_id`, `error`, `error_msg`, `success`, `offer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_OFFER(123);
   * ```
   */
  POST_CREATE_OFFER(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers`;
  }

  /**
   * Edit offer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/offers/{offer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferEditController::api_editOffer` expose observed keys including `enable`, `per_order`, `start`, `end`, `title`, `min_quantity`, `min_purchase`, `buy_products`, `get_products`, `percent`, `cluster_id`, `error`, `error_msg`, `description`, `success`, `offer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_OFFER(123, 123);
   * ```
   */
  PUT_EDIT_OFFER(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}`;
  }

  /**
   * Offer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/offers/{offer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferEditController::api_deleteOffer` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_OFFER(123, 123);
   * ```
   */
  DELETE_OFFER(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}`;
  }

  /**
   * Restore offer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/offers/{offer_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferEditController::api_restoreOffer` expose observed keys including `error`, `error_msg`, `success`, `offer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_OFFER(123, 123);
   * ```
   */
  POST_RESTORE_OFFER(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}/restore`;
  }

  /**
   * Offer data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/offers/{offer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferController::api_getAdminOfferInfo` expose observed keys including `success`, `offer`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_OFFER_DATA(123, 123);
   * ```
   */
  GET_OFFER_DATA(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}`;
  }

  /**
   * Offer orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/offers/{offer_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferController::api_getShopOfferOrders` expose observed keys including `error`, `offer_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_OFFER_ORDERS(123, 123);
   * ```
   */
  GET_OFFER_ORDERS(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}/orders`;
  }

  /**
   * Offer add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/offers/{offer_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferNoteController::api_addNote` expose observed keys including `body`, `user_id`, `user_name`, `date`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_OFFER_ADD_NOTE(123, 123);
   * ```
   */
  POST_OFFER_ADD_NOTE(shop_id: string | number, offer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}/note`;
  }

  /**
   * Offer note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/offers/{offer_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `OfferNoteController::api_deleteNote` expose observed keys including `error`, `error_msg`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_OFFER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_OFFER_NOTE(123, 123, "value");
   * ```
   */
  DELETE_OFFER_NOTE(
    shop_id: string | number,
    offer_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}/note/${note_index}`;
  }

  //―――――――――――――――――――――― Shop > Lottery ――――――――――――――――――――
  /**
   * Shop lottery config endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/lottery
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_setShopLotteryConfig` expose observed keys including `enable`, `amount`, `currency`, `error`, `error_msg`, `lottery`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_LOTTERY_CONFIG(123);
   * ```
   */
  PUT_SHOP_LOTTERY_CONFIG(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lottery`;
  }

  /**
   * Shop lottery config endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/lottery
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_clearShopLotteryConfig` expose observed keys including `lottery`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LOTTERY_CONFIG(123);
   * ```
   */
  DELETE_SHOP_LOTTERY_CONFIG(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lottery`;
  }

  /**
   * Lottery lotteries endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/lotteries
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOTTERY_WRITE (backoffice:lottery:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_getLotteries` expose observed keys including `offset`, `limit`, `lotteries`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_LOTTERY_LOTTERIES(123);
   * ```
   */
  GET_LOTTERY_LOTTERIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries`;
  }

  /**
   * Create lottery endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/lotteries
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOTTERY_WRITE (backoffice:lottery:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_createShopLotteryTickets` expose observed keys including `enable`, `quantity`, `title`, `description`, `color`, `chance`, `free_for_first`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `amount`, `currency`, `discount`, `discount_limit`, `product_id`, `variant_id`, `card_type_id`, `success`, `lottery`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_CARD_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_LOTTERY(123);
   * ```
   */
  POST_CREATE_LOTTERY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries`;
  }

  /**
   * Edit lottery endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/lotteries/{lottery_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOTTERY_WRITE (backoffice:lottery:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param lottery_id - lottery_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_editLottery` expose observed keys including `enable`, `quantity`, `title`, `description`, `color`, `chance`, `free_for_first`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `amount`, `currency`, `discount`, `discount_limit`, `product_id`, `variant_id`, `card_type_id`, `code`, `success`, `lottery`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_LOTTERY_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_CARD_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_LOTTERY(123, 123);
   * ```
   */
  PUT_EDIT_LOTTERY(shop_id: string | number, lottery_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries/${lottery_id}`;
  }

  /**
   * Upload lottery image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/lotteries/{lottery_id}/image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOTTERY_WRITE (backoffice:lottery:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param lottery_id - lottery_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::lottery_upload_image` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_LOTTERY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_LOTTERY_IMAGE(123, 123);
   * ```
   */
  POST_UPLOAD_LOTTERY_IMAGE(
    shop_id: string | number,
    lottery_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries/${lottery_id}/image`;
  }

  /**
   * Lottery endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/lotteries/{lottery_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_LOTTERY_WRITE (backoffice:lottery:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param lottery_id - lottery_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `LotteryController::api_deleteLottery` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_LOTTERY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_LOTTERY(123, 123);
   * ```
   */
  DELETE_LOTTERY(shop_id: string | number, lottery_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries/${lottery_id}`;
  }

  //―――――――――――――――――――――― Shop > Exchange Rates ――――――――――――――――――――
  /**
   * Exchange rates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /shops/{shop_id}/exchange/rates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopExchangeController::api_admin_getShopCurrenciesRate` expose observed keys including `rates`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXCHANGE_RATES(123);
   * ```
   */
  GET_EXCHANGE_RATES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/exchange/rates`;
  }

  /**
   * Set exchange rates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/exchange/rates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopExchangeController::api_setShopCurrenciesRate` expose observed keys including `rate`, `from`, `to`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_MATCH_CURRENCY_FROM_TO_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_EXCHANGE_RATES(123);
   * ```
   */
  POST_SET_EXCHANGE_RATES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/exchange/rates`;
  }

  /**
   * Exchange rates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/exchange/rates/{rate_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param rate_id - rate_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopExchangeController::api_deleteShopCurrenciesRate` expose observed keys including `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_EXCHANGE_RATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EXCHANGE_RATES(123, 123);
   * ```
   */
  DELETE_EXCHANGE_RATES(shop_id: string | number, rate_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/exchange/rates/${rate_id}`;
  }

  /**
   * Exchange rate auto endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/exchange/rates/{rate_id}/auto
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param rate_id - rate_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopExchangeController::api_setShopCurrenciesRateAuto` expose observed keys including `auto`, `error`, `error_msg`, `success`, `rate`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_EXCHANGE_RATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXCHANGE_RATE_AUTO(123, 123);
   * ```
   */
  PUT_EXCHANGE_RATE_AUTO(shop_id: string | number, rate_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/exchange/rates/${rate_id}/auto`;
  }

  /**
   * Selldone exchange rates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /exchange/rates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SaminExchangeController::api_getGlobalCurrenciesRate` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SELLDONE_EXCHANGE_RATES();
   * ```
   */
  GET_SELLDONE_EXCHANGE_RATES() {
    return `${this.selldone_api_url}/exchange/rates`;
  }

  //―――――――――――――――――――――― Shop > Gift Card ――――――――――――――――――――

  /**
   * Gift card types endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::api_getGiftCardTypes` expose observed keys including `offset`, `limit`, `currency`, `cluster_id`, `gift_card_types`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFT_CARD_TYPES(123);
   * ```
   */
  GET_GIFT_CARD_TYPES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types`;
  }

  /**
   * Create gift card type endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::api_createShopGiftCardType` expose observed keys including `title`, `color`, `amount`, `life`, `currency`, `cluster_id`, `error`, `error_msg`, `success`, `gift_card_type`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::CHARGE_AMOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_GIFT_CARD_TYPE(123);
   * ```
   */
  POST_CREATE_GIFT_CARD_TYPE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types`;
  }

  /**
   * Gift card type endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/gift-card-types/{gift_card_type_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::api_deleteGiftCardType` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GIFT_CARD_TYPE(123, 123);
   * ```
   */
  DELETE_GIFT_CARD_TYPE(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}`;
  }

  /**
   * Edit gift card type endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/gift-card-types/{gift_card_type_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::api_editGiftCardType` expose observed keys including `title`, `color`, `amount`, `life`, `currency`, `cluster_id`, `error`, `error_msg`, `success`, `gift_card_type`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_GIFT_CARD_TYPE(123, 123);
   * ```
   */
  PUT_EDIT_GIFT_CARD_TYPE(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}`;
  }

  /**
   * Gift card type data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::api_getGiftCardTypeInfo` expose observed keys including `success`, `gift_card_type`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFT_CARD_TYPE_DATA(123, 123);
   * ```
   */
  GET_GIFT_CARD_TYPE_DATA(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}`;
  }

  /**
   * Gift card type cards endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/cards
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_READ (backoffice:giftcard:read)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardController::api_getGiftCards` expose observed keys including `offset`, `limit`, `giftcards`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFT_CARD_TYPE_CARDS(123, 123);
   * ```
   */
  GET_GIFT_CARD_TYPE_CARDS(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/cards`;
  }

  /**
   * Gift card type cards add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/cards
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardController::api_createShopGiftCard` expose observed keys including `count`, `enable`, `error`, `error_msg`, `success`, `created_vitems_count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GIFT_CARD_TYPE_CARDS_ADD(123, 123);
   * ```
   */
  POST_GIFT_CARD_TYPE_CARDS_ADD(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/cards`;
  }

  /**
   * Update gift card type bg endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/bg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypesController::uploadBgImageOfCardType` expose observed keys including `bg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_GIFT_CARD_TYPE_BG(123, 123);
   * ```
   */
  POST_UPDATE_GIFT_CARD_TYPE_BG(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/bg`;
  }

  /**
   * Gift card type card endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/gift-card-types/{gift_card_type_id}/cards/{card_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @param card_id - card_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardController::api_deleteGiftCard` expose observed keys including `quantity`, `success`, `id`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_CARD_ERROR()`, `ErrorCodes::CANT_REMOVE_GIFT_CARD_BECAUSE_HAS_OWNER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GIFT_CARD_TYPE_CARD(123, 123, 123);
   * ```
   */
  DELETE_GIFT_CARD_TYPE_CARD(
    shop_id: string | number,
    gift_card_type_id: string | number,
    card_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/cards/${card_id}`;
  }

  /**
   * Edit gift card type card endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/gift-card-types/{gift_card_type_id}/cards/{card_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @param card_id - card_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardController::api_editGiftCard` expose observed keys including `enable`, `status`, `quantity`, `success`, `giftcard`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_CARD_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_GIFT_CARD_TYPE_CARD(123, 123, 123);
   * ```
   */
  PUT_EDIT_GIFT_CARD_TYPE_CARD(
    shop_id: string | number,
    gift_card_type_id: string | number,
    card_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/cards/${card_id}`;
  }

  /**
   * Gift card type orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/gift-card-types/{gift_card_type_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardOrdersController::api_getShopGiftCardTypeOrders` expose observed keys including `error`, `giftcard_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFT_CARD_TYPE_ORDERS(123, 123);
   * ```
   */
  GET_GIFT_CARD_TYPE_ORDERS(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/orders`;
  }

  /**
   * Issue gift card type card endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/cards/{card_id}/issue
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @param card_id - card_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardIssueController::api_issueGiftCard` expose observed keys including `customer_id`, `name`, `phone`, `error`, `error_msg`, `status`, `success`, `giftcard`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_CARD_ERROR()`, `ErrorCodes::GIFT_CARD_USED_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ISSUE_GIFT_CARD_TYPE_CARD(123, 123, 123);
   * ```
   */
  POST_ISSUE_GIFT_CARD_TYPE_CARD(
    shop_id: string | number,
    gift_card_type_id: string | number,
    card_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/cards/${card_id}/issue`;
  }

  /**
   * Issue gift card type card POS endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/issue
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardIssueController::api_issueGiftCardForCustomer` expose observed keys including `customer_id`, `error`, `error_msg`, `success`, `giftcard`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ISSUE_GIFT_CARD_TYPE_CARD_POS(123, 123);
   * ```
   */
  POST_ISSUE_GIFT_CARD_TYPE_CARD_POS(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    // Auto assign giftcard!
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/issue`;
  }

  /**
   * Gift card type add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardNoteController::api_addNote` expose observed keys including `body`, `user_id`, `user_name`, `date`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GIFT_CARD_TYPE_ADD_NOTE(123, 123);
   * ```
   */
  POST_GIFT_CARD_TYPE_ADD_NOTE(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/note`;
  }

  /**
   * Gift card type note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/gift-card-types/{gift_card_type_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardNoteController::api_deleteNote` expose observed keys including `error`, `error_msg`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GIFT_CARD_TYPE_NOTE(123, 123, "value");
   * ```
   */
  DELETE_GIFT_CARD_TYPE_NOTE(
    shop_id: string | number,
    gift_card_type_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/note/${note_index}`;
  }

  /**
   * Gift card auto create product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/gift-card-types/{gift_card_type_id}/auto-create-product
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_type_id - gift_card_type_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftCardTypeLinkProductController::api_autoCreateProduct` expose observed keys including `name`, `title`, `type`, `error`, `error_msg`, `success`, `gift_card_type`, `created_vitems_count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::CARD_TYPE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GIFT_CARD_AUTO_CREATE_PRODUCT(123, 123);
   * ```
   */
  POST_GIFT_CARD_AUTO_CREATE_PRODUCT(
    shop_id: string | number,
    gift_card_type_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-card-types/${gift_card_type_id}/auto-create-product`;
  }

  //―――――――――――――――――――――― Shop > Campaigns ――――――――――――――――――――
  /**
   * Campaigns endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/campaigns
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_getShopCampaignsList` expose observed keys including `offset`, `limit`, `cluster_id`, `campaigns`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CAMPAIGNS(123);
   * ```
   */
  GET_CAMPAIGNS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns`;
  }

  /**
   * Add campaign endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/campaigns
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_addShopCampaign` expose observed keys including `banner`, `notification`, `name`, `enable`, `auto`, `cluster_id`, `error`, `error_msg`, `page_id`, `discount_id`, `start_at`, `end_at`, `success`, `campaign`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_CAMPAIGN(123);
   * ```
   */
  POST_ADD_CAMPAIGN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns`;
  }

  /**
   * Campaign endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/campaigns/{campaign_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_deleteShopCampaign` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CAMPAIGN(123, 123);
   * ```
   */
  DELETE_CAMPAIGN(shop_id: string | number, campaign_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}`;
  }

  /**
   * Campaign info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/campaigns/{campaign_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_getShopCampaign` expose observed keys including `campaign`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CAMPAIGN_INFO(123, 123);
   * ```
   */
  GET_CAMPAIGN_INFO(shop_id: string | number, campaign_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}`;
  }

  /**
   * Edit campaign info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/campaigns/{campaign_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_addShopCampaign` expose observed keys including `banner`, `notification`, `name`, `enable`, `auto`, `cluster_id`, `error`, `error_msg`, `page_id`, `discount_id`, `start_at`, `end_at`, `success`, `campaign`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_CAMPAIGN_INFO(123, 123);
   * ```
   */
  PUT_EDIT_CAMPAIGN_INFO(
    shop_id: string | number,
    campaign_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}`;
  }

  /**
   * Set campaign landing endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/campaigns/{campaign_id}/page
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_setShopCampaignPage` expose observed keys including `page_id`, `success`, `campaign`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_CAMPAIGN_LANDING(123, 123);
   * ```
   */
  PUT_SET_CAMPAIGN_LANDING(
    shop_id: string | number,
    campaign_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}/page`;
  }

  /**
   * Add campaign link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/campaigns/{campaign_id}/links
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_addCampaignLink` expose observed keys including `source`, `medium`, `term`, `content`, `link`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`, `ErrorCodes::MAX_ADD_LIMIT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_CAMPAIGN_LINK(123, 123);
   * ```
   */
  POST_ADD_CAMPAIGN_LINK(
    shop_id: string | number,
    campaign_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}/links`;
  }

  /**
   * Campaign link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/campaigns/{campaign_id}/links/{link_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @param link_id - link_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::api_deleteCampaignLink` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`, `ErrorCodes::NOT_FOUND_CAMPAIGN_LINK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_CAMPAIGN_LINK(123, 123, 123);
   * ```
   */
  DELETE_CAMPAIGN_LINK(
    shop_id: string | number,
    campaign_id: string | number,
    link_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}/links/${link_id}`;
  }

  //―――――――――――――――――――――― Shop > Affiliate ――――――――――――――――――――
  /**
   * Affiliates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/affiliates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateController::api_getShopAffiliatesList` expose observed keys including `offset`, `limit`, `cluster_id`, `data`, `affiliates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATES(123);
   * ```
   */
  GET_AFFILIATES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates`;
  }

  /**
   * Create affiliate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/affiliates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateController::api_addShopAffiliate` expose observed keys including `enable`, `pos`, `products`, `fix`, `percent`, `currency`, `name`, `domain_id`, `commission`, `cluster_id`, `error`, `error_msg`, `affiliate_id`, `success`, `affiliate`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_AFFILIATE(123);
   * ```
   */
  POST_CREATE_AFFILIATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates`;
  }

  /**
   * Update affiliate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/affiliates/{affiliate_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateController::api_editShopAffiliate` expose observed keys including `enable`, `pos`, `products`, `fix`, `percent`, `name`, `domain_id`, `commission`, `cluster_id`, `error`, `error_msg`, `user_id`, `address`, `web`, `tel`, `bank`, `affiliate_id`, `success`, `affiliate`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_DOMAIN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_AFFILIATE(123, 888);
   * ```
   */
  PUT_UPDATE_AFFILIATE(
    shop_id: string | number,
    affiliate_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}`;
  }

  /**
   * Affiliate info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/affiliates/{affiliate_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateController::api_getShopAffiliate` expose observed keys including `success`, `affiliate`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_INFO(123, 888);
   * ```
   */
  GET_AFFILIATE_INFO(shop_id: string | number, affiliate_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}`;
  }

  /**
   * Affiliate orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/affiliates/{affiliate_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateOrdersController::api_getShopAffiliateOrders` expose observed keys including `error`, `affiliate_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_ORDERS(123, 888);
   * ```
   */
  GET_AFFILIATE_ORDERS(
    shop_id: string | number,
    affiliate_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}/orders`;
  }

  /**
   * Affiliate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/affiliates/{affiliate_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateController::api_deleteShopAffiliate` expose observed keys including `affiliate_id`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_AFFILIATE(123, 888);
   * ```
   */
  DELETE_AFFILIATE(shop_id: string | number, affiliate_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}`;
  }

  /**
   * Update affiliate status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/affiliates/{affiliate_id}/orders/{affiliate_order_id}/status
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param affiliate_order_id - affiliate_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateOrdersController::api_setAffiliateAccept` expose observed keys including `status`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_AFFILIATE_STATUS(123, 888, 123);
   * ```
   */
  POST_UPDATE_AFFILIATE_STATUS(
    shop_id: string | number,
    affiliate_id: string | number,
    affiliate_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}/orders/${affiliate_order_id}/status`;
  }

  /**
   * Affiliate payment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/affiliates/{affiliate_id}/payment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePaymentsController::api_setShopAffiliatePaymentAction` expose observed keys including `amount`, `note`, `error`, `error_msg`, `payment`, `balance`, `payment_at`, `success`, `affiliate`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AFFILIATE_PAYMENT(123, 888);
   * ```
   */
  POST_AFFILIATE_PAYMENT(
    shop_id: string | number,
    affiliate_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}/payment`;
  }

  /**
   * Affiliate payments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/affiliates/{affiliate_id}/payments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliatePaymentsController::api_getShopAffiliatePayments` expose observed keys including `error`, `affiliate_payments`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AFFILIATE_PAYMENTS(123, 888);
   * ```
   */
  GET_AFFILIATE_PAYMENTS(
    shop_id: string | number,
    affiliate_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}/payments`;
  }

  /**
   * Export affiliate orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/affiliates/{affiliate_id}/export/orders/{render}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param affiliate_id - Affiliate identifier used by the route.
   * @param render - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AffiliateExportController::exportAffiliateOrders` expose observed keys including `start`, `end`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_AFFILIATE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_AFFILIATE_ORDERS(123, 888, "value");
   * ```
   */
  GET_EXPORT_AFFILIATE_ORDERS(
    shop_id: string | number,
    affiliate_id: string | number,
    render: "excel" | "csv" | "pdf",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/affiliates/${affiliate_id}/export/orders/${render}`;
  }

  //―――――――――――――――――――――― Shop > email ――――――――――――――――――――
  /**
   * Emails endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/emails
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_getShopEmailsList` expose observed keys including `emails`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EMAILS(123);
   * ```
   */
  GET_EMAILS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails`;
  }

  /**
   * Create email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/emails
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_addShopEmail` expose observed keys including `name`, `currency`, `schedule_at`, `structure`, `filter`, `subject`, `from`, `cluster_id`, `error`, `error_msg`, `html`, `text`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_EMAIL(123);
   * ```
   */
  POST_CREATE_EMAIL(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails`;
  }

  /**
   * Update email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/emails/{email_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_editShopEmail` expose observed keys including `name`, `schedule_at`, `structure`, `filter`, `subject`, `from`, `cluster_id`, `error`, `error_msg`, `html`, `text`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_EMAIL(123, 123);
   * ```
   */
  PUT_UPDATE_EMAIL(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}`;
  }

  /**
   * Upload image email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/emails/{email_id}/image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `EmailMarketingController::api_uploadEmailImage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_IMAGE_EMAIL(123, 123);
   * ```
   */
  POST_UPLOAD_IMAGE_EMAIL(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/image`;
  }

  /**
   * Email render endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/emails/{email_id}/render
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `EmailMarketingController::renderEmail` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EMAIL_RENDER(123, 123);
   * ```
   */
  GET_EMAIL_RENDER(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/render`;
  }

  /**
   * Email info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/emails/{email_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_getShopEmail` expose observed keys including `cost`, `enable`, `success`, `data`, `service`, `mail_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EMAIL_INFO(123, 123);
   * ```
   */
  GET_EMAIL_INFO(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}`;
  }

  /**
   * Email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/emails/{email_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_deleteShopEmail` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EMAIL(123, 123);
   * ```
   */
  DELETE_EMAIL(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}`;
  }

  /**
   * Send email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/emails/{email_id}/send
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_sendEmailShop` expose observed keys including `in_que`, `approve`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_EMAIL(123, 123);
   * ```
   */
  POST_SEND_EMAIL(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/send`;
  }

  /**
   * Cancel email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/emails/{email_id}/cancel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_cancelSendEmailShop` expose observed keys including `error`, `error_msg`, `in_que`, `approve`, `user_id`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CANCEL_EMAIL(123, 123);
   * ```
   */
  POST_CANCEL_EMAIL(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/cancel`;
  }

  /**
   * Email review action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/emails/{email_id}/approve
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - email_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmailMarketingController::api_reviewAction` expose observed keys including `action`, `approve`, `user_id`, `success`, `user`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EMAIL_REVIEW_ACTION(123, 123);
   * ```
   */
  POST_EMAIL_REVIEW_ACTION(
    shop_id: string | number,
    email_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/approve`;
  }

  /**
   * Email statistic endpoint URL.
   *
   * Builds the legacy SDK URL for shop email statistics.
   *
   * @endpoint GET /shops/{shop_id}/emails/{email_id}/statistic
   * @auth TODO(api-docs): verify whether this SDK URL is stale before use.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param email_id - Shop marketing email identifier.
   * @returns Absolute URL string for the SDK email statistic path.
   *
   * Response shape:
   * - TODO(api-docs): unresolved route; no controller response can be documented as fact.
   *
   * Possible errors:
   * - TODO(api-docs): route may return 404 because no active Laravel route was found.
   *
   * @example
   * ```ts
   * const url = API.GET_EMAIL_STATISTIC(123, 456);
   * ```
   */
  GET_EMAIL_STATISTIC(shop_id: string | number, email_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/emails/${email_id}/statistic`;
  }

  //―――――――――――――――――――――― Shop > Campaigns > Notification ――――――――――――――――――――
  /**
   * Upload campaign notification image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/campaigns/{campaign_id}/notification/upload/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param campaign_id - campaign_id route parameter identifier.
   * @param type - Endpoint type discriminator passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CampaignController::upload_image_notification` expose observed keys including `photo`, `notification`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_CAMPAIGN_NOTIFICATION_IMAGE(123, 123, "cover");
   * ```
   */
  POST_UPLOAD_CAMPAIGN_NOTIFICATION_IMAGE(
    shop_id: string | number,
    campaign_id: string | number,
    type: "image" | "bg",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/campaigns/${campaign_id}/notification/upload/${type}`;
  }

  //―――――――――――――――――――――― Shop > Clubs ――――――――――――――――――――
  /**
   * Set shop club endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/clubs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ClubController::api_setShopClub` expose observed keys including `monthly`, `annually`, `percent`, `limit`, `free_shipping`, `currency`, `level`, `club`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_CLUB(123);
   * ```
   */
  POST_SET_SHOP_CLUB(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clubs`;
  }

  /**
   * Shop club endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/clubs/{level}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param level - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ClubController::api_deleteShopClub` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CLUB(123, "value");
   * ```
   */
  DELETE_SHOP_CLUB(shop_id: string | number, level: keyof typeof Club.Levels) {
    return `${this.selldone_api_url}/shops/${shop_id}/clubs/${level}`;
  }

  //―――――――――――――――――――――― Shop > Statistic ――――――――――――――――――――
  /**
   * Shop statistic sessions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/statistic/sessions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StatisticController::api_getShopSessions` expose observed keys including `sessions`, `with_details`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STATISTIC_SESSIONS(123);
   * ```
   */
  GET_SHOP_STATISTIC_SESSIONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/statistic/sessions`;
  }

  /**
   * Shop statistic timeline endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/statistic/timeline
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StatisticController::api_getUserActivitiesByTime` expose observed keys including `hour`, `day`, `add_baskets`, `remove_baskets`, `products_views`, `buys`, `timeline`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STATISTIC_TIMELINE(123);
   * ```
   */
  GET_SHOP_STATISTIC_TIMELINE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/statistic/timeline`;
  }

  /**
   * Shop statistic country endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/statistic/country
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StatisticController::api_getUserActivitiesByCountry` expose observed keys including `country_code`, `add_baskets`, `remove_baskets`, `products_views`, `buys`, `countries`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STATISTIC_COUNTRY(123);
   * ```
   */
  GET_SHOP_STATISTIC_COUNTRY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/statistic/country`;
  }

  //―――――――――――――――――――――― Shop > Contact Us ――――――――――――――――――――
  /**
   * Shop support setting endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/support-setting
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_setShopSupportSetting` expose observed keys including `popup`, `support`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SUPPORT_SETTING(123);
   * ```
   */
  POST_SHOP_SUPPORT_SETTING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/support-setting`;
  }

  /**
   * Shop contact us list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/contacts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SUPPORT_TICKETS (backoffice:support-tickets)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_getAdminShopContacts` expose observed keys including `offset`, `limit`, `categories`, `contacts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONTACT_US_LIST(123);
   * ```
   */
  GET_SHOP_CONTACT_US_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/contacts`;
  }

  /**
   * Shop contact us update message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/contacts/{contact_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SUPPORT_TICKETS (backoffice:support-tickets)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param contact_id - contact_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_setAdminUpdateContact` expose observed keys including `message`, `link`, `mail`, `product`, `error`, `error_msg`, `user`, `user_id`, `officer`, `date`, `messages`, `officer_id`, `waiting`, `notify_at`, `contact`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUND_CONTACT_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CONTACT_US_UPDATE_MESSAGE(123, 123);
   * ```
   */
  PUT_SHOP_CONTACT_US_UPDATE_MESSAGE(
    shop_id: string | number,
    contact_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/contacts/${contact_id}`;
  }

  /**
   * Shop contact us message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/contacts/{contact_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SUPPORT_TICKETS (backoffice:support-tickets)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param contact_id - contact_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_adminShopDeleteContact` expose observed keys including `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CONTACT_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CONTACT_US_MESSAGE(123, 123);
   * ```
   */
  DELETE_SHOP_CONTACT_US_MESSAGE(
    shop_id: string | number,
    contact_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/contacts/${contact_id}`;
  }

  /**
   * Shop contact us message response endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/contacts/{support_id}/{index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SUPPORT_TICKETS (backoffice:support-tickets)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param contact_id - contact_id route parameter identifier.
   * @param index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_removeContactResponse` expose observed keys including `error`, `error_msg`, `messages`, `contact`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CONTACT_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CONTACT_US_MESSAGE_RESPONSE(123, 123, "value");
   * ```
   */
  DELETE_SHOP_CONTACT_US_MESSAGE_RESPONSE(
    shop_id: string | number,
    contact_id: string | number,
    index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/contacts/${contact_id}/${index}`;
  }

  /**
   * Shop contact us close ticket endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/contacts/{contact_id}/close
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SUPPORT_TICKETS (backoffice:support-tickets)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param contact_id - contact_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContactController::api_closeShopContactTicket` expose observed keys including `close`, `error`, `error_msg`, `closed`, `waiting`, `closed_at`, `contact`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONTACT_US_CLOSE_TICKET(123, 123);
   * ```
   */
  POST_SHOP_CONTACT_US_CLOSE_TICKET(
    shop_id: string | number,
    contact_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/contacts/${contact_id}/close`;
  }

  //―――――――――――――――――――――― Shop > FAQ ――――――――――――――――――――
  /**
   * Shop faqs list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/faqs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FAQ_READ (backoffice:faq:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFAQController::api_getAdminShopFAQs` expose observed keys including `faqs`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_FAQS_LIST(123);
   * ```
   */
  GET_SHOP_FAQS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/faqs`;
  }

  /**
   * Shop faqs tags endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/faqs/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FAQ_READ (backoffice:faq:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFAQController::api_getAdminShopFAQTags` expose observed keys including `tags`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_FAQS_TAGS(123);
   * ```
   */
  GET_SHOP_FAQS_TAGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/faqs/tags`;
  }

  /**
   * Update shop FAQ endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/faqs/{faq_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FAQ_WRITE (backoffice:faq:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param faq_id - faq_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFAQController::api_setAdminUpdateFAQ` expose observed keys including `question`, `answer`, `priority`, `officer_id`, `link`, `tag`, `faq`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_FAQ_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_SHOP_FAQ(123, 123);
   * ```
   */
  PUT_UPDATE_SHOP_FAQ(shop_id: string | number, faq_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/faqs/${faq_id}`;
  }

  /**
   * Add shop FAQ endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/faqs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FAQ_WRITE (backoffice:faq:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFAQController::api_addAdminUpdateFAQ` expose observed keys including `question`, `answer`, `priority`, `shop_id`, `user_id`, `officer_id`, `link`, `tag`, `success`, `faq`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_FAQ(123);
   * ```
   */
  POST_ADD_SHOP_FAQ(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/faqs`;
  }

  /**
   * Shop FAQ endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/faqs/{faq_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_FAQ_WRITE (backoffice:faq:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param faq_id - faq_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFAQController::api_adminShopDeleteFAQ` expose observed keys including `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_FAQ_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_FAQ(123, 123);
   * ```
   */
  DELETE_SHOP_FAQ(shop_id: string | number, faq_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/faqs/${faq_id}`;
  }

  //―――――――――――――――――――――― Selldone > FAQ ――――――――――――――――――――

  /**
   * Samin new FAQ endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /faqs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FAQController::api_addQuestion` expose observed keys including `question`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SAMIN_NEW_FAQ();
   * ```
   */
  POST_SAMIN_NEW_FAQ() {
    return `${this.selldone_api_url}/faqs`;
  }

  //―――――――――――――――――――――― Gifts ――――――――――――――――――――

  /**
   * Gifts received list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gift/received-list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_getReceivedGifts` expose observed keys including `gifts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFTS_RECEIVED_LIST();
   * ```
   */
  GET_GIFTS_RECEIVED_LIST() {
    return `${this.selldone_api_url}/gift/received-list`;
  }

  /**
   * Gifts sent list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /gift/sent-list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_getSentGifts` expose observed keys including `offset`, `limit`, `gifts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFTS_SENT_LIST();
   * ```
   */
  GET_GIFTS_SENT_LIST() {
    return `${this.selldone_api_url}/gift/sent-list`;
  }

  /**
   * Apply gift endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /gift/apply
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_applyGifts` expose observed keys including `account_number`, `gift_id`, `error`, `error_msg`, `status`, `pay_at`, `gift`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_ERROR()`, `ErrorCodes::CANT_GET_GIFT_ERROR()`, `ErrorCodes::NOT_VALID_ACCOUNT_TO_ERROR()`, `ErrorCodes::NOT_VALID_ACCOUNT_FROM_ERROR()`, `ErrorCodes::NOT_MATCH_CURRENCY_FROM_TO_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_APPLY_GIFT();
   * ```
   */
  POST_APPLY_GIFT() {
    return `${this.selldone_api_url}/gift/apply`;
  }

  /**
   * Gifts pending list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gift/pending-list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_getReceivedPendingGifts` expose observed keys including `gift`, `count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GIFTS_PENDING_LIST();
   * ```
   */
  GET_GIFTS_PENDING_LIST() {
    return `${this.selldone_api_url}/gift/pending-list`;
  }

  /**
   * Send gift endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /gift/send
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `App\Http\Middleware\CheckNotBanned:wallet`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GiftController::api_sendGift` expose observed keys including `receiver`, `currency`, `amount`, `account_number`, `success`, `gift`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_GIFT_PROGRAM_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_VALID_CURRENCY_TO_TRANSACTION_ERROR()`, `ErrorCodes::NOT_MATCH_ACCOUNT_CURRENCY_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_SUFFICIENT_BALANCE_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_GIFT();
   * ```
   */
  POST_SEND_GIFT() {
    return `${this.selldone_api_url}/gift/send`;
  }

  //―――――――――――――――――――――― Stripe ――――――――――――――――――――
  /**
   * Check samin stripe payment intent endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /gateways/stripe/{payment_id}/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param paymentIntent_id - paymentIntent_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StripeController::checkStripePaymentForSamin` expose observed keys including `error`, `error_msg`, `success`, `payment_at`, `paymentIntent`, `target_type`, `target_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHECK_SAMIN_STRIPE_PAYMENT_INTENT(123);
   * ```
   */
  GET_CHECK_SAMIN_STRIPE_PAYMENT_INTENT(
    paymentIntent_id: string /*Stripe payment intent ID*/,
  ) {
    return `${this.selldone_api_url}/gateways/stripe/${paymentIntent_id}/check`;
  }

  //―――――――――――――――――――――― PayPal ――――――――――――――――――――
  /**
   * Selldone paypal client token endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint POST /gateways/paypal/client-token/{currency}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param currency - Currency code accepted by the backend route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PaypalController::getClientTokenForSelldone` expose observed keys including `error`, `error_msg`, `success`, `token`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SELLDONE_PAYPAL_CLIENT_TOKEN("USD");
   * ```
   */
  GET_SELLDONE_PAYPAL_CLIENT_TOKEN(currency: keyof typeof Currency) {
    return `${this.selldone_api_url}/gateways/paypal/client-token/${currency}`;
  }

  //―――――――――――――――――――――― Orders ――――――――――――――――――――

  //―――――――――――――――――――――― Shop > Events ――――――――――――――――――――
  /**
   * Shop events endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/events
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopEventsController::api_getEvents` expose observed keys including `start`, `end`, `extra`, `product_dis_events`, `discount_code_events`, `campaign_events`, `blogs_events`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_EVENTS(123);
   * ```
   */
  GET_SHOP_EVENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/events`;
  }

  //―――――――――――――――――――――― Shop > Bots ――――――――――――――――――――
  /**
   * Set shop bot endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/bots
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBotsController::api_setBot` expose observed keys including `driver`, `enable`, `form_params`, `url`, `bot`, `error_config`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_SHOP_BOT(123);
   * ```
   */
  SET_SHOP_BOT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/bots`;
  }

  //―――――――――――――――――――――― Shop > Notifications ――――――――――――――――――――

  /**
   * Set shop notification channels endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/notification-channels/{channel}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param channel - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNotificationsController::api_connectNotificationChannel` expose observed keys including `channel`, `enable`, `notification`, `url`, `webhook`, `shop_id`, `error`, `error_msg`, `secret`, `info`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_SHOP_NOTIFICATION_CHANNELS(123, "value");
   * ```
   */
  SET_SHOP_NOTIFICATION_CHANNELS(
    shop_id: string | number,
    channel: ShopChannelType,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/notification-channels/${channel}`;
  }

  /**
   * Shop notification channel config endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/notification-channels/{channel}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param channel - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNotificationsController::api_setNotificationChannelConfig` expose observed keys including `error`, `error_msg`, `webhook`, `info`, `id`, `enable`, `success`, `notification`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_NOTIFICATION_CHANNEL_CONFIG(123, "value");
   * ```
   */
  PUT_SHOP_NOTIFICATION_CHANNEL_CONFIG(
    shop_id: string | number,
    channel: ShopChannelType,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/notification-channels/${channel}`;
  }

  /**
   * Shop notification channels endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/notification-channels
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNotificationsController::api_getShopNotification` expose observed keys including `notifications`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_NOTIFICATION_CHANNELS(123);
   * ```
   */
  GET_SHOP_NOTIFICATION_CHANNELS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/notification-channels`;
  }

  /**
   * Set test shop notification channel endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/notification-channels/{channel}/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param channel - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNotificationsController::api_testNotificationChannel` expose observed keys including `error`, `error_msg`, `parse_mode`, `rez`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_TEST_SHOP_NOTIFICATION_CHANNEL(123, "value");
   * ```
   */
  SET_TEST_SHOP_NOTIFICATION_CHANNEL(
    shop_id: string | number,
    channel: ShopChannelType,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/notification-channels/${channel}/test`;
  }

  //―――――――――――――――――――――― Shop > Sms ――――――――――――――――――――
  /**
   * Shop SMS list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/sms/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsController::api_getSmsList` expose observed keys including `list`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SMS_LIST(123);
   * ```
   */
  GET_SHOP_SMS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/list`;
  }

  /**
   * Shop SMS service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/sms/service
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsServiceController::api_getShopSmsService` expose observed keys including `error`, `error_msg`, `sms_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SMS_SERVICE(123);
   * ```
   */
  GET_SHOP_SMS_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/service`;
  }

  /**
   * Set shop SMS service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/sms/service
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsServiceController::api_setShopSmsService` expose observed keys including `enable`, `driver`, `option`, `token_1`, `token_2`, `token_3`, `token_4`, `token_5`, `test`, `network`, `error`, `error_msg`, `success`, `sms_service`, `send_test`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_SMS_SERVICE(123);
   * ```
   */
  POST_SET_SHOP_SMS_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/service`;
  }

  /**
   * Reset shop SMS service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/sms/service/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsServiceController::api_resetShopSmsService` expose observed keys including `error`, `error_msg`, `errors`, `success`, `sms_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESET_SHOP_SMS_SERVICE(123);
   * ```
   */
  POST_RESET_SHOP_SMS_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/service/reset`;
  }

  /**
   * Shop SMS service action sync endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/sms/service/actions/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsServiceActionsController::api_syncContacts` expose observed keys including `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SMS_SERVICE_ACTION_SYNC(123);
   * ```
   */
  POST_SHOP_SMS_SERVICE_ACTION_SYNC(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/service/actions/sync`;
  }

  /**
   * Shop SMS templates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/sms/templates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::list` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `templates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SMS_TEMPLATES(123);
   * ```
   */
  GET_SHOP_SMS_TEMPLATES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates`;
  }

  /**
   * Shop SMS add template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/sms/templates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::add` expose observed keys including `error`, `error_msg`, `code`, `enable`, `language`, `mode`, `text`, `data`, `success`, `template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SMS_ADD_TEMPLATE(123);
   * ```
   */
  POST_SHOP_SMS_ADD_TEMPLATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates`;
  }

  /**
   * Shop SMS edit template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/sms/templates/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::edit` expose observed keys including `error`, `error_msg`, `code`, `enable`, `language`, `mode`, `text`, `data`, `user_id`, `errors`, `success`, `template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_SMS_EDIT_TEMPLATE(123, 123);
   * ```
   */
  PUT_SHOP_SMS_EDIT_TEMPLATE(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates/${template_id}`;
  }

  /**
   * Shop SMS template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/sms/templates/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_SMS_TEMPLATE(123, 123);
   * ```
   */
  DELETE_SHOP_SMS_TEMPLATE(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates/${template_id}`;
  }

  /**
   * Shop SMS template reset errors endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/sms/templates/{template_id}/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::resetErrors` expose observed keys including `error`, `error_msg`, `errors`, `success`, `template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SMS_TEMPLATE_RESET_ERRORS(123, 123);
   * ```
   */
  POST_SHOP_SMS_TEMPLATE_RESET_ERRORS(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates/${template_id}/reset`;
  }

  /**
   * Shop SMS template send test endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/sms/templates/{template_id}/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::test` expose observed keys including `error`, `error_msg`, `response`, `request`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SMS_TEMPLATE_SEND_TEST(123, 123);
   * ```
   */
  POST_SHOP_SMS_TEMPLATE_SEND_TEST(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/templates/${template_id}/test`;
  }

  /**
   * Shop SMS template default body endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/sms/defaults/{code}/{language}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @param language - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSmsTemplatesController::getDefaultBody` expose observed keys including `mode`, `error`, `error_msg`, `language`, `code`, `body`, `tokens`, `driver`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SMS_TEMPLATE_DEFAULT_BODY(123, "code", "value");
   * ```
   */
  GET_SHOP_SMS_TEMPLATE_DEFAULT_BODY(
    shop_id: string | number,
    code:
      | "shop-login"
      | "shop-order-payment"
      | "shop-order-payment-admin"
      | "avocado-order-submit"
      | "avocado-order-submit-seller"
      | "avocado-ready-to-pay"
      | string,
    language: keyof typeof Language,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/sms/defaults/${code}/${language}`;
  }

  //―――――――――――――――――――――― Shop > Emails ――――――――――――――――――――
  /**
   * Shop emails list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/email/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopEmailsController::api_getEmailsList` expose observed keys including `list`, `groups`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_EMAILS_LIST(123);
   * ```
   */
  GET_SHOP_EMAILS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/list`;
  }

  /**
   * Shop email render URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/email/render/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopEmailsController::api_getEmailRender` expose observed keys including `order_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_EMAIL_RENDER_URL(123, "code");
   * ```
   */
  GET_SHOP_EMAIL_RENDER_URL(
    shop_id: string | number,
    code:
      | "shop-customer-join"
      | "shop-giftcard"
      | "shop-login"
      | "login-code"
      | "order-checkout"
      | "order-payment"
      | "order-update"
      | "order-payment-admin"
      | "pos-checkout"
      | "pos-payment"
      | "pos-payment-admin"
      | "shop-contact"
      | "shop-vendor-join"
      | "shop-vendor-invite"
      | "shop-vendor-order"
      | "shop-vendor-products"
      | "bulk-orders"       // Send daily orders
      | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/render/${code}`;
  }

  /**
   * Shop email send me endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/email/send/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopEmailsTestController::api_sendEmailTest` expose observed keys including `order_id`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_EMAIL_SEND_ME(123, "code");
   * ```
   */
  POST_SHOP_EMAIL_SEND_ME(
    shop_id: string | number,
    code:
      | "shop-customer-join"
      | "shop-giftcard"
      | "shop-login"
      | "login-code"
      | "order-checkout"
      | "order-payment"
      | "order-update"
      | "order-payment-admin"
      | "pos-checkout"
      | "pos-payment"
      | "pos-payment-admin"
      | "shop-contact"
      | "shop-vendor-join"
      | "shop-vendor-invite"
      | "shop-vendor-order"
      | "shop-vendor-products"
      | "bulk-orders"
      | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/send/${code}`;
  }

  /**
   * Shop mail service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/email/service
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailServiceController::api_getShopMailService` expose observed keys including `error`, `error_msg`, `mail_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MAIL_SERVICE(123);
   * ```
   */
  GET_SHOP_MAIL_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/service`;
  }

  /**
   * Set shop mail service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/email/service
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailServiceController::api_setShopMailService` expose observed keys including `enable`, `driver`, `host`, `port`, `from`, `from_name`, `api`, `username`, `password`, `endpoint`, `encryption`, `error`, `error_msg`, `success`, `mail_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_MAIL_SERVICE(123);
   * ```
   */
  POST_SET_SHOP_MAIL_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/service`;
  }

  /**
   * Reset shop mail service endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/email/service/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailServiceController::api_resetShopMailService` expose observed keys including `error`, `error_msg`, `errors`, `success`, `mail_service`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESET_SHOP_MAIL_SERVICE(123);
   * ```
   */
  POST_RESET_SHOP_MAIL_SERVICE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/service/reset`;
  }

  /**
   * Shop mail service test endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/email/service/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,10`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailServiceController::api_testShopMailService` expose observed keys including `error`, `error_msg`, `success`, `to`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_MAIL_SERVICE_TEST(123);
   * ```
   */
  POST_SHOP_MAIL_SERVICE_TEST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/service/test`;
  }

  /**
   * Shop mail template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/email/templates/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailTemplateController::api_getShopMailTemplates` expose observed keys including `error`, `error_msg`, `code`, `mail_template`, `default`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MAIL_TEMPLATE(123, "code");
   * ```
   */
  GET_SHOP_MAIL_TEMPLATE(
    shop_id: string | number,
    code:
      | "shop-customer-join"
      | "shop-giftcard"
      | "shop-login"
      | "login-code"
      | "order-checkout"
      | "order-payment"
      | "order-payment-admin"
      | "pos-checkout"
      | "pos-payment"
      | "pos-payment-admin"
      | "shop-contact"
      | "shop-vendor-join"
      | "shop-vendor-invite"
      | "shop-vendor-order"
      | "shop-vendor-products"
      | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/templates/${code}`;
  }

  /**
   * Set shop mail template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/email/templates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMailTemplateController::api_setShopMailTemplate` expose observed keys including `code`, `enable`, `subject`, `title`, `category`, `message`, `footer`, `action`, `header_logo`, `header_image`, `footer_image`, `header_image_file`, `footer_image_file`, `error`, `error_msg`, `success`, `mail_template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_MAIL_TEMPLATE(123);
   * ```
   */
  POST_SET_SHOP_MAIL_TEMPLATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/email/templates`;
  }

  //―――――――――――――――――――――― Shop > Preferences ――――――――――――――――――――
  /**
   * Set shop preferences endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/preferences
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPreferencesController::api_setShopPreferences` expose observed keys including `key`, `enable`, `error`, `error_msg`, `success`, `preferences`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_BY_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_SHOP_PREFERENCES(123);
   * ```
   */
  SET_SHOP_PREFERENCES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/preferences`;
  }

  //―――――――――――――――――――――― Address Book (Shared!) ――――――――――――――――――――

  /**
   * My addresses endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /address/all
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_ADDRESS (address)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_addressesGetAll` expose observed keys including `success`, `addresses`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_ADDRESSES();
   * ```
   */
  GET_MY_ADDRESSES() {
    return `${this.selldone_api_url}/address/all`;
  }

  /**
   * My addresses endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /address/{address_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_ADDRESS (address)
   * @permission None found in matched controller method by static inspection.
   *
   * @param address_id - address_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_deleteAddress` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ADDRESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_ADDRESSES(123);
   * ```
   */
  DELETE_MY_ADDRESSES(address_id: string | number) {
    return `${this.selldone_api_url}/address/${address_id}`;
  }

  /**
   * Address endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /address/{address_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_ADDRESS (address)
   * @permission None found in matched controller method by static inspection.
   *
   * @param address_id - address_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_updateAddress` expose observed keys including `title`, `address`, `country`, `state`, `city`, `location`, `no`, `unit`, `name`, `phone`, `message`, `postal`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ADDRESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_ADDRESS(123);
   * ```
   */
  PUT_ADDRESS(address_id: string | number) {
    return `${this.selldone_api_url}/address/${address_id}`;
  }

  /**
   * Address endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /address
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_ADDRESS (address)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_addAddress` expose observed keys including `location`, `title`, `address`, `city`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADDRESS();
   * ```
   */
  POST_ADDRESS() {
    return `${this.selldone_api_url}/address`;
  }

  /**
   * Geo to address endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /address/gto-to-address
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_geoToAddress` expose observed keys including `lat`, `lon`, `local`, `http_errors`, `address`, `no`, `postal`, `country`, `state`, `city`, `error`, `error_msg`, `full_address`, `state_code`, `context`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_GEO_TO_ADDRESS();
   * ```
   */
  GET_GEO_TO_ADDRESS() {
    return `${this.selldone_api_url}/address/gto-to-address`;
  }

  /**
   * Autocomplete endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /address/autocomplete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AddressController::api_autocomplete` expose observed keys including `text`, `lat`, `lon`, `local`, `countries`, `http_errors`, `address`, `title`, `geometry`, `country`, `state`, `city`, `postal`, `raw`, `list`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AUTOCOMPLETE();
   * ```
   */
  GET_AUTOCOMPLETE() {
    return `${this.selldone_api_url}/address/autocomplete`;
  }

  //―――――――――――――――――――――― Help ――――――――――――――――――――

  /**
   * Save help UID endpoint URL.
   *
   * Builds the staff/admin help UID update URL.
   *
   * @endpoint PUT /api/helps/{help_id}/uid
   * @auth Admin/staff web session required.
   * @middleware `web`, `Authenticate:web`, `EnsureEmailIsVerified`, `CheckSelldoneStaffAccess:HELP`
   * @scope None found in matched route/controller method by static inspection.
   * @permission Selldone staff HELP access.
   *
   * @param help_id - Help article identifier.
   * @returns Absolute URL string for the help UID update endpoint.
   *
   * Response shape:
   * - Backend response body is defined by `HelpController::api_updateHelpUID`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing staff/admin web session.
   * - 403 forbidden: missing HELP staff access.
   * - 404 not_found: help article is not found.
   *
   * @example
   * ```ts
   * const url = API.SAVE_HELP_UID(123);
   * ```
   */
  SAVE_HELP_UID(help_id: string | number) {
    return `${this.selldone_api_url}/helps/${help_id}/uid`;
  }

  //―――――――――――――――――――――― Shop > Countries service ――――――――――――――――――――
  /**
   * Set shop countries endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/countries
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCountriesController::api_setShopCountries` expose observed keys including `countries`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_SHOP_COUNTRIES(123);
   * ```
   */
  SET_SHOP_COUNTRIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/countries`;
  }

  //―――――――――――――――――――――― Shop > Language pack ――――――――――――――――――――
  /**
   * Install shop language pack endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/languages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_installLanguage` expose observed keys including `language`, `error`, `error_msg`, `options`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_INSTALL_SHOP_LANGUAGE_PACK(123);
   * ```
   */
  POST_INSTALL_SHOP_LANGUAGE_PACK(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/languages`;
  }

  /**
   * Uninstall shop language pack endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/languages/{language_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param language_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_uninstallLanguage` expose observed keys including `error`, `error_msg`, `options`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_UNINSTALL_SHOP_LANGUAGE_PACK(123, "value");
   * ```
   */
  DELETE_UNINSTALL_SHOP_LANGUAGE_PACK(
    shop_id: string | number,
    language_code: keyof typeof Language,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/languages/${language_code}`;
  }

  /**
   * Shop save override language pack endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/languages/{language_code}/packs/{pack}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param language_code - Code value passed as a route segment.
   * @param pack - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLanguageController::api_setOverridePack` expose observed keys including `pack`, `success`, `language_pack`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_SAVE_OVERRIDE_LANGUAGE_PACK(123, "value", "value");
   * ```
   */
  POST_SHOP_SAVE_OVERRIDE_LANGUAGE_PACK(
    shop_id: string | number,
    language_code: keyof typeof Language,
    pack: "global" | "shop",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/languages/${language_code}/packs/${pack}`;
  }

  /**
   * Shop override language pack endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/languages/{language_code}/packs/{pack}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param language_code - Code value passed as a route segment.
   * @param pack - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLanguageController::api_getOverridePack` expose observed keys including `pack`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_OVERRIDE_LANGUAGE_PACK(123, "value", "value");
   * ```
   */
  GET_SHOP_OVERRIDE_LANGUAGE_PACK(
    shop_id: string | number,
    language_code: keyof typeof Language,
    pack: "global" | "shop",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/languages/${language_code}/packs/${pack}`;
  }

  /**
   * Shop download override language pack endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/languages/{language_code}/export
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param language_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLanguageController::api_downloadOverridePack` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_DOWNLOAD_OVERRIDE_LANGUAGE_PACK(123, "value");
   * ```
   */
  GET_SHOP_DOWNLOAD_OVERRIDE_LANGUAGE_PACK(
    shop_id: string | number,
    language_code: keyof typeof Language,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/languages/${language_code}/export`;
  }

  //―――――――――――――――――――――― Files list ――――――――――――――――――――

  /**
   * Deprecated public images list endpoint URL.
   *
   * This SDK method throws `Deprecated! It's very slow!` before returning a URL, so callers cannot reach the backend through this method.
   *
   * @endpoint GET /shops/{shop_id}/public/images/{folder?}
   * @auth TODO(api-docs): not callable because the SDK method throws before returning.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param folder - Optional folder segment that would be appended if the method did not throw.
   * @returns Never returns normally; throws the deprecation string before the URL expression.
   *
   * Response shape:
   * - TODO(api-docs): inactive/deprecated route; no active backend response can be documented as fact.
   *
   * Possible errors:
   * - Throws `Deprecated! It's very slow!` synchronously from the SDK method.
   *
   * @example
   * ```ts
   * // Deprecated: this throws before returning a URL.
   * API.GET_PUBLIC_FILES_IMAGES_LIST(123);
   * ```
   */
  GET_PUBLIC_FILES_IMAGES_LIST(shop_id: string | number, folder = null) {
    throw "Deprecated! It's very slow!";
    return (
      `${this.selldone_api_url}/shops/${shop_id}/public/images` +
      (folder ? `/${folder}` : "")
    );
  }

  /**
   * Shop static files list endpoint URL.
   *
   * Builds the URL for listing static shop files. `folder` is an optional catch-all path segment.
   *
   * @endpoint GET /shops/{shop_id}/static/{folder?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param folder - Optional folder path appended after `/static`.
   * @returns Absolute URL string for the static file listing endpoint.
   *
   * Response shape:
   * - Backend returns static file/folder listing data from `ShopStaticPagesController::api_shop_owner_getStaticPagesList`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing shop read scope or settings-region permission.
   * - 404 not_found: shop or folder is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STATIC_FILES_LIST(123, "images");
   * ```
   */
  GET_SHOP_STATIC_FILES_LIST(shop_id: string | number, folder = null) {
    return (
      `${this.selldone_api_url}/shops/${shop_id}/static` +
      (folder ? `/${folder}` : "")
    );
  }

  /**
   * Shop static folder/file upload endpoint URL.
   *
   * Builds the URL for the static file/folder upload route. `folder` is an optional catch-all path segment.
   *
   * @endpoint POST /shops/{shop_id}/static/{folder?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param folder - Optional folder path appended after `/static`.
   * @returns Absolute URL string for the static upload/create-folder endpoint.
   *
   * Response shape:
   * - Backend returns upload/folder creation result from `ShopStaticPagesController::api_shop_owner_uploadFileFolders`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing shop write scope or settings-region permission.
   * - 422 validation_error: uploaded file/folder payload fails backend validation.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_STATIC_CREATE_FOLDER(123, "docs");
   * ```
   */
  POST_SHOP_STATIC_CREATE_FOLDER(shop_id: string | number, folder = null) {
    return (
      `${this.selldone_api_url}/shops/${shop_id}/static` +
      (folder ? `/${folder}` : "")
    );
  }

  /**
   * Shop static asset endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/static/{asset_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param asset_id - asset_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopStaticPagesController::api_shop_owner_removeFile` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_STATIC_ASSET(123, 123);
   * ```
   */
  DELETE_SHOP_STATIC_ASSET(
    shop_id: string | number,
    asset_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/static/${asset_id}`;
  }

  /**
   * Shop static files disk info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/static-disk
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopStaticPagesController::api_shop_owner_getDiskInfo` expose observed keys including `disk`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STATIC_FILES_DISK_INFO(123);
   * ```
   */
  GET_SHOP_STATIC_FILES_DISK_INFO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/static-disk`;
  }

  //―――――――――――――――――――――― Shop > Users ――――――――――――――――――――
  /**
   * Funnel users count endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/funnel/users/count
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_getFunnelUsersCount` expose observed keys including `filters`, `count`, `label`, `success`, `funnel_date`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_FUNNEL_USERS_COUNT(123);
   * ```
   */
  POST_FUNNEL_USERS_COUNT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/funnel/users/count`;
  }

  /**
   * Funnel users data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/funnel/users/data
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_getFunnelUsersData` expose observed keys including `filters`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_FUNNEL_USERS_DATA(123);
   * ```
   */
  POST_FUNNEL_USERS_DATA(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/funnel/users/data`;
  }

  /**
   * Download all shop customers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/funnel/users/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopCustomersController::api_downloadAllShopCustomers` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DOWNLOAD_ALL_SHOP_CUSTOMERS(123);
   * ```
   */
  GET_DOWNLOAD_ALL_SHOP_CUSTOMERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/funnel/users/excel`;
  }

  //―――――――――――――――――――――― support ――――――――――――――――――――
  /**
   * Support endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/support
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SupportController::api_sendSupportRequest` expose observed keys including `message`, `url`, `category`, `error`, `error_msg`, `support`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SUPPORT(123);
   * ```
   */
  POST_SUPPORT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/support`;
  }

  /**
   * Support response endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/support/{support_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param support_id - support_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SupportController::api_sendSupportMessage` expose observed keys including `message`, `error`, `error_msg`, `user`, `user_id`, `date`, `messages`, `waiting`, `support`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SUPPORT_RESPONSE(123, 123);
   * ```
   */
  PUT_SUPPORT_RESPONSE(shop_id: string | number, support_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/support/${support_id}`;
  }

  /**
   * Support close endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/support/{support_id}/close
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param support_id - support_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SupportController::api_closeSupportTicket` expose observed keys including `error`, `error_msg`, `closed`, `waiting`, `closed_at`, `support`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SUPPORT_CLOSE(123, 123);
   * ```
   */
  PUT_SUPPORT_CLOSE(shop_id: string | number, support_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/support/${support_id}/close`;
  }

  /**
   * Support rate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/support/{support_id}/rate
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param support_id - support_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SupportController::api_sendSupportRating` expose observed keys including `rate`, `error`, `error_msg`, `support`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SUPPORT_RATE(123, 123);
   * ```
   */
  PUT_SUPPORT_RATE(shop_id: string | number, support_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/support/${support_id}/rate`;
  }

  //―――――――――――――――――――――― AI ――――――――――――――――――――
  /**
   * AI remove background product main image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ai/remove-bg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIProductMainImageRemoveBackgroundController::api_removeProductMainImageBackground` expose observed keys including `error`, `error_msg`, `icon`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_REMOVE_BACKGROUND_PRODUCT_MAIN_IMAGE(123, 456);
   * ```
   */
  POST_AI_REMOVE_BACKGROUND_PRODUCT_MAIN_IMAGE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ai/remove-bg`;
  }

  /**
   * AI upscale background product main image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ai/upscale
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIProductMainImageUpscaleController::api_upscaleProductImage` expose observed keys including `error`, `error_msg`, `icon`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_UPSCALE_BACKGROUND_PRODUCT_MAIN_IMAGE(123, 456);
   * ```
   */
  POST_AI_UPSCALE_BACKGROUND_PRODUCT_MAIN_IMAGE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ai/upscale`;
  }

  /**
   * AI remove background product images endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/images/{image_id}/ai/remove-bg
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param image_id - Image identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIProductImagesRemoveBackgroundController::api_removeProductImageBackground` expose observed keys including `error`, `error_msg`, `path`, `success`, `product`, `variant`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_IMAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_REMOVE_BACKGROUND_PRODUCT_IMAGES(123, 456, 202);
   * ```
   */
  POST_AI_REMOVE_BACKGROUND_PRODUCT_IMAGES(
    shop_id: string | number,
    product_id: string | number,
    image_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/images/${image_id}/ai/remove-bg`;
  }

  /**
   * AI add product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/product/add-ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductAddAIController::api_addAIProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_ADD_PRODUCT(123);
   * ```
   */
  POST_AI_ADD_PRODUCT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/product/add-ai`;
  }

  /**
   * AI edit product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/update-ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductUpdateAIController::api_updateAIProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_EDIT_PRODUCT(123, 456);
   * ```
   */
  POST_AI_EDIT_PRODUCT(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/update-ai`;
  }

  /**
   * AI create new product image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ai/reimagine
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIProductMainImageReimagineController::api_reimagineProductImage` expose observed keys including `prompt`, `variant_id`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_CREATE_NEW_PRODUCT_IMAGE(123, 456);
   * ```
   */
  POST_AI_CREATE_NEW_PRODUCT_IMAGE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ai/reimagine`;
  }

  //―――――――――――――――――――――― TAX ――――――――――――――――――――
  /**
   * Set shop tax setting endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/tax
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxController::api_setShopTaxSetting` expose observed keys including `profile_id`, `enable`, `tax_number`, `included`, `shipping`, `fix`, `fix_vat`, `fix_shipping`, `pos_vat`, `pos_tax_included`, `stripe_tax_code`, `icon`, `meta`, `name`, `label`, `error`, `error_msg`, `success`, `tax_profile`, `tax`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_SHOP_TAX_SETTING(123);
   * ```
   */
  PUT_SET_SHOP_TAX_SETTING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/tax`;
  }

  /**
   * Shop tax endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/tax
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxController::api_getShopTax` expose observed keys including `countries`, `taxes`, `overrides`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TAX(123);
   * ```
   */
  GET_SHOP_TAX(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/tax`;
  }

  /**
   * Shop tax endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/tax/{vat_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vat_id - vat_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxController::api_deleteOverrideShopTax` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_TAX(123, 123);
   * ```
   */
  DELETE_SHOP_TAX(shop_id: string | number, vat_id: string | number) {
    // By vat id! Not shop_vat_id!
    return `${this.selldone_api_url}/shops/${shop_id}/tax/${vat_id}`;
  }

  /**
   * Set override shop tax endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/tax/{vat_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vat_id - vat_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxController::api_setOverrideShopTax` expose observed keys including `profile_id`, `shipping`, `tax`, `cvat`, `bvat`, `error`, `error_msg`, `override`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_OVERRIDE_SHOP_TAX(123, 123);
   * ```
   */
  POST_SET_OVERRIDE_SHOP_TAX(
    shop_id: string | number,
    vat_id: string | number,
  ) {
    // By vat id! Not shop_vat_id!
    return `${this.selldone_api_url}/shops/${shop_id}/tax/${vat_id}`;
  }

  /**
   * Add shop tax profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/tax-profiles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxProfileController::add` expose observed keys including `name`, `label`, `icon`, `enable`, `included`, `shipping`, `fix`, `fix_vat`, `fix_shipping`, `pos_tax_included`, `pos_vat`, `success`, `tax_profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_TAX_PROFILE(123);
   * ```
   */
  POST_ADD_SHOP_TAX_PROFILE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/tax-profiles`;
  }

  /**
   * Shop tax profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/tax-profiles/{profile_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxProfileController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_TAX_PROFILE(123, 123);
   * ```
   */
  DELETE_SHOP_TAX_PROFILE(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/tax-profiles/${profile_id}`;
  }

  /**
   * Shop tax profile statistic endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/tax-profiles/{profile_id}/statistic
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopTaxProfileReportController::statistic` expose observed keys including `error`, `error_msg`, `products`, `statistic`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_TAX_PROFILE_STATISTIC(123, 123);
   * ```
   */
  GET_SHOP_TAX_PROFILE_STATISTIC(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/tax-profiles/${profile_id}/statistic`;
  }

  //―――――――――――――――――――――― POS Cash Register ――――――――――――――――――――

  /**
   * Cash register secure URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pos/secure/{type}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSDeviceController::api_generateSecureUrl` expose observed keys including `shop_id`, `type`, `user_id`, `success`, `url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CASH_REGISTER_SECURE_URL(123, "code");
   * ```
   */
  GET_CASH_REGISTER_SECURE_URL(shop_id: string | number, code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/secure/${code}`;
  }

  /**
   * Cash register status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/pos
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSController::api_getPOSData` expose observed keys including `currency`, `basket`, `devices`, `bill`, `gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CASH_REGISTER_STATUS(123);
   * ```
   */
  GET_CASH_REGISTER_STATUS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos`;
  }

  /**
   * Offline basket add item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pos/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_addItemToOfflineBasket` expose observed keys including `product_id`, `variant_id`, `count`, `currency`, `error`, `error_msg`, `available_count`, `success`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_PRODUCT_ERROR()`, `ErrorCodes::BASKET_ADD_ITEM_VARIANT_NOT_FOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_OFFLINE_BASKET_ADD_ITEM(123);
   * ```
   */
  POST_OFFLINE_BASKET_ADD_ITEM(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/add`;
  }

  /**
   * Offline basket remove item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/pos/{item_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_deleteItemToOfflineBasket` expose observed keys including `success`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_OFFLINE_BASKET_REMOVE_ITEM(123, 123);
   * ```
   */
  DELETE_OFFLINE_BASKET_REMOVE_ITEM(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/${item_id}`;
  }

  /**
   * POS device endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/pos/devices/{device_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param device_id - device_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSDeviceController::api_deletePosDevice` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_POS_DEVICE(123, 123);
   * ```
   */
  DELETE_POS_DEVICE(shop_id: string | number, device_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/devices/${device_id}`;
  }

  /**
   * POS basket action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/pos/actions/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_sellerSetActions` expose observed keys including `receiver_info`, `delivery_price`, `delivery`, `customer_id`, `giftcards`, `buyer_id`, `code`, `discount_id`, `coupon_id`, `campaign_id`, `error`, `error_msg`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`, `ErrorCodes::NOT_FOUND_DISCOUNT_CODE_ERROR()`, `ErrorCodes::DISCOUNT_CODE_EXPIRED_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CAMPAIGN_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_POS_BASKET_ACTION(123, "value");
   * ```
   */
  PUT_POS_BASKET_ACTION(
    shop_id: string | number,
    action:
      | "giftcards"
      | "customer"
      | "delivery"
      | "discount-code"
      | "coupon"
      | "campaign",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/actions/${action}`;
  }

  /**
   * POS basket action endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/pos/actions/{action}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param action - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_sellerDeleteActions` expose observed keys including `delivery`, `receiver_info`, `delivery_price`, `giftcards`, `error`, `error_msg`, `basket`, `bill`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_POS_BASKET_ACTION(123, "value");
   * ```
   */
  DELETE_POS_BASKET_ACTION(
    shop_id: string | number,
    action:
      | "giftcards"
      | "customer"
      | "delivery"
      | "discount-code"
      | "coupon"
      | "campaign",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/actions/${action}`;
  }

  /**
   * POS gift cards list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pos/gift-cards
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_getGiftCardsListOfBuyer` expose observed keys including `error`, `error_msg`, `giftcards`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_GIFT_CARDS_LIST(123);
   * ```
   */
  GET_POS_GIFT_CARDS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/gift-cards`;
  }

  /**
   * POS coupons list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pos/coupons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_getCouponsListForBuyer` expose observed keys including `error`, `error_msg`, `coupons`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_COUPONS_LIST(123);
   * ```
   */
  GET_POS_COUPONS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/coupons`;
  }

  /**
   * POS campaigns list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pos/campaigns
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_getCampaignsList` expose observed keys including `campaigns`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_CAMPAIGNS_LIST(123);
   * ```
   */
  GET_POS_CAMPAIGNS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/campaigns`;
  }

  /**
   * POS confirm payment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pos/payment/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketController::api_BuyPosBasket` expose observed keys including `currency`, `amount_check`, `error`, `error_msg`, `price`, `dis`, `discount_id`, `giftcards`, `status`, `reserved_at`, `delivery_state`, `session`, `user_id`, `amount_discount`, `amount_buy`, `created_at`, `updated_at`, `amount`, `wage`, `payed_by_gift_card`, `free_order`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::CHECKOUT_BASKET_EMPTY_ERROR()`, `ErrorCodes::GATEWAY_NOT_ENABLE_ERROR()`, `ErrorCodes::GATEWAY_CURRENCY_NOT_MATCH_ERROR()`, `ErrorCodes::SHOP_EXCHANGE_RATE_NOT_VALID_ERROR()`, `ErrorCodes::CANT_EXCHANGE_PRICE_TO_SELECTED_CURRENCY_ERROR()`, `ErrorCodes::BASKET_CHANGED_ERROR()`, `ErrorCodes::INSUFFICIENT_PRODUCT_QUANTITY_ERROR()`, `ErrorCodes::DISCOUNT_CODE_CURRENCY_NOT_MATCHED_ERROR()`, `ErrorCodes::DISCOUNT_CODE_NOT_FREE_ERROR()`, `ErrorCodes::DISCOUNT_CODE_EXPIRED_ERROR()`, `ErrorCodes::NOT_FOUND_COUPON_ERROR()`, `ErrorCodes::GIFTCARD_CURRENCY_NOT_MATCH_ERROR()`, `ErrorCodes::CLIENT_BASKET_PAYMENT_AMOUNT_NOT_MATCH_ERROR()`, `ErrorCodes::GIFT_CARD_NOT_ENABLE_ERROR()`, `ErrorCodes::GIFT_CARD_PAYMENT_AMOUNT_ERROR()`, `ErrorCodes::STRUCTURAL_SELLDONE_ERROR()`, `ErrorCodes::GATEWAY_AMOUNT_LESS_THAN_MINIMUM_ERROR()`, `ErrorCodes::GATEWAY_AMOUNT_GREATER_THAN_MAXIMUM_ERROR()`, `ErrorCodes::CHECKOUT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_CONFIRM_PAYMENT(123, "stripe");
   * ```
   */
  GET_POS_CONFIRM_PAYMENT(shop_id: string | number, gateway_code: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/pos/payment/${gateway_code}`;
  }

  /**
   * POS order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/pos-baskets/{basket_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `POSBasketController::api_admin_getPOSBasketReceivedOrderInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_POS_ORDER_INFO(123, 333);
   * ```
   */
  GET_POS_ORDER_INFO(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/pos-baskets/${basket_id}`;
  }

  /**
   * POS order reject endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/pos-baskets/{basket_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `POSBasketRejectController::api_rejectPOSOrder` expose observed keys including `error`, `error_msg`, `success`, `basket`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_POS_ORDER_REJECT(123, 333);
   * ```
   */
  POST_POS_ORDER_REJECT(shop_id: string | number, basket_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/pos-baskets/${basket_id}/reject`;
  }

  /**
   * Fetch POS customer-view data endpoint URL.
   *
   * Builds the DAPI URL used by the POS customer display device. This method uses `selldone-dapi`, not `selldone-api`.
   *
   * @endpoint GET /dapi/shops/{shop_id}/pos/{user_id}/customer-view/{token}
   * @auth Device/customer-view token in the route; no bearer-token requirement found on the DAPI route.
   * @middleware Device route group middleware from `Backend/routes/device.php`.
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param user_id - POS user/device user identifier from the route path.
   * @param token - Customer-view device token.
   * @returns Absolute DAPI URL string for customer-view data fetch.
   *
   * Response shape:
   * - Backend response body is defined by `POSDeviceController::dapi_getCustomerViewData`.
   *
   * Possible errors:
   * - 403 forbidden: invalid customer-view token.
   * - 404 not_found: shop/user/device context is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_FETCH_CUSTOMER_VIEW_DATA(123, 102, "device-token");
   * ```
   */
  GET_FETCH_CUSTOMER_VIEW_DATA(
    shop_id: string | number,
    user_id: string | number,
    token: string,
  ) {
    return `${this.selldone_dapi_url}/shops/${shop_id}/pos/${user_id}/customer-view/${token}`;
  }

  /**
   * POS basket create secure link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/pos-baskets/{basket_id}/secure-link
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::POS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PosBasketSecureLinkController::api_createSecureLinkForPosOrder` expose observed keys including `url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_POS_BASKET_CREATE_SECURE_LINK(123, 333);
   * ```
   */
  POST_POS_BASKET_CREATE_SECURE_LINK(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/pos-baskets/${basket_id}/secure-link`;
  }

  //―――――――――――――――――――――― POS HUB ――――――――――――――――――――
  /**
   * My hub generate pin endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/hub/pin
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopHubAuthController::api_generateHubPin` expose observed keys including `success`, `pin`, `valid_until`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_HUB_GENERATE_PIN(123);
   * ```
   */
  POST_MY_HUB_GENERATE_PIN(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/hub/pin`;
  }

  /**
   * My hub device test endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/hub/devices/{device_id}/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param device_id - device_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopHubTestController::api_testPrint` expose observed keys including `error`, `error_msg`, `success`, `device`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_HUB_DEVICE_TEST(123, 123);
   * ```
   */
  POST_MY_HUB_DEVICE_TEST(
    shop_id: string | number,
    device_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/hub/devices/${device_id}/test`;
  }

  /**
   * My hub devices endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/hub/devices
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopHubDevicesController::list` expose observed keys including `error`, `error_msg`, `success`, `devices`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_HUB_DEVICES(123);
   * ```
   */
  GET_MY_HUB_DEVICES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/hub/devices`;
  }

  //―――――――――――――――――――――― Customers ――――――――――――――――――――

  /**
   * Shop customers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/customers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_getShopCustomers` expose observed keys including `pending`, `offset`, `limit`, `search`, `segments`, `customers`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMERS(123);
   * ```
   */
  GET_SHOP_CUSTOMERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers`;
  }

  /**
   * Shop customer info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/customers/{customer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_getShopCustomerInfo` expose observed keys including `baskets`, `posBaskets`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMER_INFO(123, 444);
   * ```
   */
  GET_SHOP_CUSTOMER_INFO(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}`;
  }

  /**
   * Shop customer chips endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/chips
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_setShopCustomerChips` expose observed keys including `chips`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_CHIPS(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_CHIPS(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/chips`;
  }

  /**
   * Shop customer club endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/club
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersController::api_setShopCustomerClub` expose observed keys including `level`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_CLUB(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_CLUB(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/club`;
  }

  /**
   * Shop customer activity endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/customers/{customer_id}/activity
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersActivityController::api_listActivities` expose observed keys including `months`, `customer`, `activities`, `first`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMER_ACTIVITY(123, 444);
   * ```
   */
  GET_SHOP_CUSTOMER_ACTIVITY(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/activity`;
  }

  /**
   * Add new shop customer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/customers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAddCustomerController::api_addCustomerManually` expose observed keys including `phone`, `name`, `segments`, `currency`, `sex`, `birthday`, `level`, `address`, `billing`, `error`, `error_msg`, `success`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_NEW_SHOP_CUSTOMER(123);
   * ```
   */
  POST_ADD_NEW_SHOP_CUSTOMER(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers`;
  }

  /**
   * Shop customer update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAddCustomerController::api_editCustomerManually` expose observed keys including `phone`, `name`, `segments`, `currency`, `sex`, `birthday`, `level`, `address`, `error`, `error_msg`, `success`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_UPDATE(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_UPDATE(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}`;
  }

  /**
   * Shop customer segments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/segments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersSegmentsController::api_setCustomerSegments` expose observed keys including `segments`, `success`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_SEGMENTS(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_SEGMENTS(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/segments`;
  }

  /**
   * Shop customer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/customers/{customer_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAddCustomerController::api_removeCustomerManually` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CUSTOMER(123, 444);
   * ```
   */
  DELETE_SHOP_CUSTOMER(shop_id: string | number, customer_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}`;
  }

  /**
   * Shop customers segments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/customer/segments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersSegmentsController::api_getSegments` expose observed keys including `segments`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMERS_SEGMENTS(123);
   * ```
   */
  GET_SHOP_CUSTOMERS_SEGMENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/customer/segments`;
  }

  /**
   * Shop customer set access endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/access
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersAccessController::api_setAccess` expose observed keys including `access`, `success`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_SET_ACCESS(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_SET_ACCESS(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/access`;
  }

  /**
   * Shop customer set banned endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/banned
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersBannedController::api_setBanned` expose observed keys including `banned`, `success`, `customer`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_SET_BANNED(123, 444);
   * ```
   */
  PUT_SHOP_CUSTOMER_SET_BANNED(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/banned`;
  }

  /**
   * Shop customer create subscription portal URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/customers/{customer_id}/meta/{key}/portal
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomersMetaController::api_createCustomerPortalLink` expose observed keys including `error`, `error_msg`, `shop_id`, `any`, `url`, `return_url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CUSTOMER_CREATE_SUBSCRIPTION_PORTAL_URL(123, 444, "value");
   * ```
   */
  POST_SHOP_CUSTOMER_CREATE_SUBSCRIPTION_PORTAL_URL(
    shop_id: string | number,
    customer_id: string | number,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/meta/${key}/portal`;
  }

  //―――――――――――――――――――――― Reviews (From external source like facebook, google,...) ――――――――――――――――――――

  /**
   * Shop reviews sources endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/reviews/sources
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewSourceController::api_getReviewSources` expose observed keys including `offset`, `limit`, `search`, `sources`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_REVIEWS_SOURCES(123);
   * ```
   */
  GET_SHOP_REVIEWS_SOURCES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews/sources`;
  }

  /**
   * Shop review source info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/reviews/sources/{source_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param source_id - source_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewSourceController::api_getShopReviewSourceInfo` expose observed keys including `error`, `error_msg`, `source`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_REVIEW_SOURCE_INFO(123, 123);
   * ```
   */
  GET_SHOP_REVIEW_SOURCE_INFO(shop_id: string | number,source_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews/sources/${source_id}`;
  }

  /**
   * Shop reviews endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/reviews
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewsController::api_getReviewsList` expose observed keys including `offset`, `limit`, `search`, `source`, `reviews`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_REVIEWS(123);
   * ```
   */
  GET_SHOP_REVIEWS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews`;
  }
  /**
   * Shop review update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/reviews/{review_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewsController::api_updateReview` expose observed keys including `reviewer_name`, `customer_id`, `reviewer_avatar`, `error`, `message`, `success`, `review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_REVIEW_UPDATE(123, 123);
   * ```
   */
  POST_SHOP_REVIEW_UPDATE(shop_id: string | number,review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews/${review_id}`;
  }

  /**
   * Shop review source debug endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/reviews/sources/{source_id}/debug
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param source_id - source_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewsSyncController::api_getReviewsDebug` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_REVIEW_SOURCE_DEBUG(123, 123);
   * ```
   */
  GET_SHOP_REVIEW_SOURCE_DEBUG(shop_id: string | number,source_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews/sources/${source_id}/debug`;
  }
  /**
   * Shop review source sync endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/reviews/sources/{source_id}/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REVIEWS_READ (backoffice:reviews:read)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param source_id - source_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopReviewsSyncController::api_requestManuallySync` expose observed keys including `error`, `error_msg`, `success`, `source`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_REVIEW_SOURCE_SYNC(123, 123);
   * ```
   */
  POST_SHOP_REVIEW_SOURCE_SYNC(shop_id: string | number,source_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reviews/sources/${source_id}/sync`;
  }

  //―――――――――――――――――――――― Customer > Wallets ――――――――――――――――――――

  /**
   * Shop customer wallets endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/customers/{customer_id}/wallets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletsController::api_getCustomerWallets` expose observed keys including `wallets`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMER_WALLETS(123, 444);
   * ```
   */
  GET_SHOP_CUSTOMER_WALLETS(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/wallets`;
  }

  /**
   * Shop customer wallets add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/customers/{customer_id}/wallets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletsController::api_addCustomerWallet` expose observed keys including `currency`, `balance`, `error`, `error_msg`, `success`, `wallet`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CUSTOMER_WALLETS_ADD(123, 444);
   * ```
   */
  POST_SHOP_CUSTOMER_WALLETS_ADD(
    shop_id: string | number,
    customer_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/wallets`;
  }

  /**
   * Shop customer wallet add fund endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/customers/{customer_id}/wallets/{wallet_id}/fund
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @param wallet_id - wallet_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletTransactionsController::api_addFundCustomerWallet` expose observed keys including `amount`, `type`, `description`, `expiration_at`, `error`, `error_msg`, `success`, `wallet`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_CUSTOMER_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CUSTOMER_WALLET_ADD_FUND(123, 444, 123);
   * ```
   */
  POST_SHOP_CUSTOMER_WALLET_ADD_FUND(
    shop_id: string | number,
    customer_id: string | number,
    wallet_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/wallets/${wallet_id}/fund`;
  }

  /**
   * Shop customer wallet transactions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/customers/{customer_id}/wallets/{wallet_id}/transactions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @param wallet_id - wallet_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletTransactionsController::api_getCustomerWalletTransactions` expose observed keys including `offset`, `limit`, `search`, `sortBy`, `sortDesc`, `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CUSTOMER_WALLET_TRANSACTIONS(123, 444, 123);
   * ```
   */
  GET_SHOP_CUSTOMER_WALLET_TRANSACTIONS(
    shop_id: string | number,
    customer_id: string | number,
    wallet_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/wallets/${wallet_id}/transactions`;
  }

  /**
   * Shop customer wallet expire now endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/customers/{customer_id}/wallets/{wallet_id}/transactions/{transaction_id}/expire
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param customer_id - Shop customer identifier scoped to the route shop.
   * @param wallet_id - wallet_id route parameter identifier.
   * @param transaction_id - Transaction identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletTransactionsController::api_putCustomerWalletTransactionExpireNow` expose observed keys including `expire`, `error`, `error_msg`, `success`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CUSTOMER_WALLET_EXPIRE_NOW(123, 444, 123, 777);
   * ```
   */
  PUT_SHOP_CUSTOMER_WALLET_EXPIRE_NOW(
    shop_id: string | number,
    customer_id: string | number,
    wallet_id: string | number,
    transaction_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/customers/${customer_id}/wallets/${wallet_id}/transactions/${transaction_id}/expire`;
  }

  //―――――――――――――――――――――― Import Products ――――――――――――――――――――

  /**
   * Import products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/import/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImportController::api_importProduct` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_IMPORT_PRODUCTS(123);
   * ```
   */
  POST_IMPORT_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/products`;
  }

  /**
   * Import products list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/import/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImportController::api_getImportProductsList` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_IMPORT_PRODUCTS_LIST(123);
   * ```
   */
  GET_IMPORT_PRODUCTS_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/products`;
  }

  /**
   * Import product item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/import/products/{product_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductImportController::api_deleteImportProduct` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_IMPORT_PRODUCT_ITEM(123, 456);
   * ```
   */
  DELETE_IMPORT_PRODUCT_ITEM(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/products/${product_id}`;
  }

  /**
   * Import images list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/import/images
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImportController::api_getImportImagesList` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_IMPORT_IMAGES_LIST(123);
   * ```
   */
  GET_IMPORT_IMAGES_LIST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/images`;
  }

  /**
   * Import image item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/import/images/{image_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductImportController::api_deleteImportImage` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_IMAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_IMPORT_IMAGE_ITEM(123, 456);
   * ```
   */
  DELETE_IMPORT_IMAGE_ITEM(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/images/${product_id}`;
  }

  /**
   * Import image item try endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/import/images/{image_id}/try
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImportController::api_tryImportImage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_IMPORT_IMAGE_ITEM_TRY(123, 456);
   * ```
   */
  PUT_IMPORT_IMAGE_ITEM_TRY(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/images/${product_id}/try`;
  }

  /**
   * Shop importing info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/importing
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductImportInfoController::api_getShopImportQUeInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_IMPORTING_INFO(123);
   * ```
   */
  GET_SHOP_IMPORTING_INFO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/importing`;
  }

  //―――――――――――――――――――――― Import Categories ――――――――――――――――――――
  /**
   * Import categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/import/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CategoryImporterController::api_importCategory` expose observed keys including `dataset`, `error`, `error_msg`, `name`, `title`, `description`, `parent_id`, `success`, `success_count`, `fail_count`, `errors`, `add`, `update`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_IMPORT_CATEGORIES(123);
   * ```
   */
  POST_IMPORT_CATEGORIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/categories`;
  }

  //―――――――――――――――――――――― Import Inventory ――――――――――――――――――――
  /**
   * Import inventory endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/import/inventory
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InventoryImportController::api_importInventory` expose observed keys including `dataset`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_IMPORT_INVENTORY(123);
   * ```
   */
  POST_IMPORT_INVENTORY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/inventory`;
  }

  //―――――――――――――――――――――― Import Customers ――――――――――――――――――――

  /**
   * Import customers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/import/customers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CustomersImportController::api_importCustomers` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_IMPORT_CUSTOMERS(123);
   * ```
   */
  POST_IMPORT_CUSTOMERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/customers`;
  }

  //―――――――――――――――――――――― Finance ――――――――――――――――――――

  /**
   * Download finance data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/finance/data
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopFinanceController::api_downloadFinanceData` expose observed keys including `start`, `end`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DOWNLOAD_FINANCE_DATA(123);
   * ```
   */
  POST_DOWNLOAD_FINANCE_DATA(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/data`;
  }

  //―――――――――――――――――――――― Dropshipping > Main seller ――――――――――――――――――――

  /**
   * Shop dropshipping info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/dropshipping
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_getShopDropshippingInfo` expose observed keys including `error`, `error_msg`, `requests`, `fulfillments`, `orders`, `success`, `info`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_DROPSHIPPING_INFO(123);
   * ```
   */
  GET_SHOP_DROPSHIPPING_INFO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping`;
  }

  /**
   * Set product drop shipping settings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/dropshipping
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_setProductDropShippingSettings` expose observed keys including `reselling`, `shipping`, `error`, `error_msg`, `drop_shipping_products`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_PRODUCT_DROP_SHIPPING_SETTINGS(123, 456);
   * ```
   */
  PUT_SET_PRODUCT_DROP_SHIPPING_SETTINGS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/dropshipping`;
  }

  /**
   * Drop shipping requests endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/dropshipping/requests
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_getShopDropshippingRequests` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `statuses`, `dropship_requests`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_REQUESTS(123);
   * ```
   */
  GET_DROP_SHIPPING_REQUESTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/requests`;
  }

  /**
   * Set drop shipping request status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/dropshipping/requests/{request_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param request_id - request_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_setShopDropshippingSetRequestStatus` expose observed keys including `error`, `error_msg`, `status`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_DROP_SHIPPING_REQUEST_STATUS(123, 123);
   * ```
   */
  PUT_SET_DROP_SHIPPING_REQUEST_STATUS(
    shop_id: string | number,
    request_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/requests/${request_id}`;
  }

  /**
   * Drop shipping resellers shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/dropshipping/resellers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_getShopDropshippingResellersShops` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `resellerAccounts`, `shops`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLERS_SHOPS(123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLERS_SHOPS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/resellers`;
  }

  /**
   * Drop shipping reseller shop info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/dropshipping/resellers/{reseller_shop_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param reseller_id - reseller_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_getShopDropshippingResellerShopInfo` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `resellerAccounts`, `reseller`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_SHOP_INFO(123, 123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_SHOP_INFO(
    shop_id: string | number,
    reseller_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/resellers/${reseller_id}`;
  }

  /**
   * Drop shipping reseller create account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/dropshipping/resellers/{reseller_shop_id}/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param reseller_id - reseller_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_getShopDropshippingResellerShopCreateAccount` expose observed keys including `error`, `error_msg`, `currency`, `charge`, `success`, `reseller_account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DROP_SHIPPING_RESELLER_CREATE_ACCOUNT(123, 123);
   * ```
   */
  POST_DROP_SHIPPING_RESELLER_CREATE_ACCOUNT(
    shop_id: string | number,
    reseller_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/resellers/${reseller_id}/accounts`;
  }

  /**
   * Drop shipping reseller edit account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/dropshipping/resellers/{reseller_shop_id}/accounts/{account_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::WHOLESALER
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param reseller_id - reseller_id route parameter identifier.
   * @param account_id - Wallet/account identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingController::api_setShopDropshippingResellerShopEditAccount` expose observed keys including `error`, `error_msg`, `charge`, `success`, `reseller_account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_DROP_SHIPPING_RESELLER_EDIT_ACCOUNT(123, 123, 666);
   * ```
   */
  PUT_DROP_SHIPPING_RESELLER_EDIT_ACCOUNT(
    shop_id: string | number,
    reseller_id: string | number,
    account_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/dropshipping/resellers/${reseller_id}/accounts/${account_id}`;
  }

  // Process Center
  /**
   * Drop shipping order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/dropshipping-baskets/{fulfillment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param fulfillment_id - fulfillment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `DropShippingOrdersController::api_admin_getDropShippingReceivedOrderInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_ORDER_INFO(123, 123);
   * ```
   */
  GET_DROP_SHIPPING_ORDER_INFO(
    shop_id: string | number,
    fulfillment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/dropshipping-baskets/${fulfillment_id}`;
  }

  //―――――――――――――――――――――― Dropshipping > Middle seller ――――――――――――――――――――

  /**
   * Request dropship access endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/reseller/access
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_requestDropShipAccess` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `request`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_REQUEST_DROPSHIP_ACCESS(123);
   * ```
   */
  POST_REQUEST_DROPSHIP_ACCESS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reseller/access`;
  }

  /**
   * Drop shipping shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/reseller/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_getDropShippingShops` expose observed keys including `offset`, `limit`, `dropshipAccess`, `shops`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_SHOPS(123);
   * ```
   */
  GET_DROP_SHIPPING_SHOPS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reseller/shops`;
  }

  /**
   * Drop shipping shop products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/reseller/shops/{drop_shop_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param drop_shop_id - drop_shop_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_getDropShippingShopsAvailableProducts` expose observed keys including `offset`, `limit`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_SHOP_PRODUCTS(123, 123);
   * ```
   */
  GET_DROP_SHIPPING_SHOP_PRODUCTS(
    shop_id: string | number,
    drop_shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/reseller/shops/${drop_shop_id}/products`;
  }

  /**
   * Drop shipping shop categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/reseller/shops/{drop_shop_id}/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param drop_shop_id - drop_shop_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_getDropShippingShopsAvailableCategories` expose observed keys including `offset`, `limit`, `categories`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_SHOP_CATEGORIES(123, 123);
   * ```
   */
  GET_DROP_SHIPPING_SHOP_CATEGORIES(
    shop_id: string | number,
    drop_shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/reseller/shops/${drop_shop_id}/categories`;
  }

  /**
   * Drop shipping reseller receipts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/finance/reseller/receipts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_admin_getDropShippingReceipts` expose observed keys including `receipts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_RECEIPTS(123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_RECEIPTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/receipts`;
  }

  /**
   * Drop shipping reseller account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/finance/reseller/account
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_admin_getResellerAccountDigestInfo` expose observed keys including `shop_id`, `currency`, `error`, `error_msg`, `reseller_account`, `gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_ACCOUNT(123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_ACCOUNT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/account`;
  }

  /**
   * Drop shipping reseller accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/finance/reseller/accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_admin_getResellerAccounts` expose observed keys including `reseller_accounts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_ACCOUNTS(123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_ACCOUNTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/accounts`;
  }

  /**
   * Drop shipping reseller account charges endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/finance/reseller/accounts/{account_id}/charges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param account_id - Wallet/account identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_getChargeTransactions` expose observed keys including `error`, `error_msg`, `charges`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGES(123, 666);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGES(
    shop_id: string | number,
    account_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/accounts/${account_id}/charges`;
  }

  /**
   * Drop shipping reseller account charge refresh state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/finance/reseller/accounts/{account_id}/charges/{charge_id}/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param account_id - Wallet/account identifier used by the route.
   * @param charge_id - charge_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::checkOnlinePaymentForDropShipCharge` expose observed keys including `error`, `error_msg`, `success`, `payment_at`, `charge`, `status`, `transaction_status`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGE_REFRESH_STATE(123, 666, 123);
   * ```
   */
  POST_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGE_REFRESH_STATE(
    shop_id: string | number,
    account_id: string | number,
    charge_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/accounts/${account_id}/charges/${charge_id}/check`;
  }

  /**
   * Drop shipping reseller pay for fulfillments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/finance/reseller/receipts/pay
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_admin_getDropShippingReceiptPay` expose observed keys including `account_id`, `shop_id`, `currency`, `total_price`, `total_discount`, `count`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GIFTCARD_CURRENCY_NOT_MATCH_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DROP_SHIPPING_RESELLER_PAY_FOR_FULFILLMENTS(123);
   * ```
   */
  GET_DROP_SHIPPING_RESELLER_PAY_FOR_FULFILLMENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/receipts/pay`;
  }

  //  Product

  /**
   * Add product by drop shipping endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/reseller/products/add
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_addProductFromDropShipping` expose observed keys including `product_id`, `category_id`, `commissions`, `commission`, `error`, `error_msg`, `products`, `reselling_shops`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_ACTIVE_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CATEGORY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT_BY_DROP_SHIPPING(123);
   * ```
   */
  POST_ADD_PRODUCT_BY_DROP_SHIPPING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/reseller/products/add`;
  }

  /**
   * Add drop shipping variant endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/reseller/add-variant
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_addDropShippingMissedVariant` expose observed keys including `variant_id`, `error`, `error_msg`, `success`, `product_variant`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_DROP_SHIPPING_VARIANT(123, 456);
   * ```
   */
  POST_ADD_DROP_SHIPPING_VARIANT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/reseller/add-variant`;
  }

  /**
   * Drop shipping product re enable endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/reseller/re-enable
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_acceptProductChangesAndEnable` expose observed keys including `error`, `error_msg`, `condition`, `status`, `brand`, `warranty`, `spec`, `spec_order`, `pros`, `cons`, `message`, `inputs`, `outputs`, `lead`, `extra`, `return_warranty`, `original`, `quantity`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DROP_SHIPPING_PRODUCT_RE_ENABLE(123, 456);
   * ```
   */
  POST_DROP_SHIPPING_PRODUCT_RE_ENABLE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/reseller/re-enable`;
  }

  /**
   * Drop shipping product apply changes endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/reseller/apply-changes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingMiddleSellerController::api_acceptProductChangesAndEnable` expose observed keys including `error`, `error_msg`, `condition`, `status`, `brand`, `warranty`, `spec`, `spec_order`, `pros`, `cons`, `message`, `inputs`, `outputs`, `lead`, `extra`, `return_warranty`, `original`, `quantity`, `success`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DROP_SHIPPING_PRODUCT_APPLY_CHANGES(123, 456);
   * ```
   */
  POST_DROP_SHIPPING_PRODUCT_APPLY_CHANGES(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/reseller/apply-changes`;
  }

  /**
   * Drop shipping reseller account charge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/finance/reseller/accounts/{account_id}/charge/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param account_id - Wallet/account identifier used by the route.
   * @param gateway - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DropShippingFinanceController::api_createNewDropshipCharge` expose observed keys including `currency`, `price`, `return`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GIFTCARD_CURRENCY_NOT_MATCH_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::DROPSHIP_CHARGE_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGE(123, 666, "value");
   * ```
   */
  POST_DROP_SHIPPING_RESELLER_ACCOUNT_CHARGE(
    shop_id: string | number,
    account_id: string | number,
    gateway: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/finance/reseller/accounts/${account_id}/charge/${gateway}`;
  }

  //―――――――――――――――――――――― Instagram ――――――――――――――――――――

  /**
   * Instagram data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/channels/instagram
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InstagramController::api_fetchDataFromInstagram` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `instagram`, `medias`, `highlights`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_INSTAGRAM_DATA(123);
   * ```
   */
  GET_INSTAGRAM_DATA(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/instagram`;
  }

  /**
   * Instagram set setting endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/channels/instagram
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InstagramSettingController::api_setInstagramSetting` expose observed keys including `enable`, `error`, `error_mg`, `success`, `instagram`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_INSTAGRAM_SET_SETTING(123);
   * ```
   */
  POST_INSTAGRAM_SET_SETTING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/instagram`;
  }

  /**
   * Instagram media set products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/channels/instagram/medias/{media_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param media_id - media_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InstagramController::api_setMediaProduct` expose observed keys including `products`, `error`, `error_msg`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_INSTAGRAM_MEDIA_SET_PRODUCTS(123, 123);
   * ```
   */
  PUT_INSTAGRAM_MEDIA_SET_PRODUCTS(
    shop_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/instagram/medias/${media_id}/products`;
  }

  /**
   * Instagram media set caption endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/channels/instagram/medias/{media_id}/caption
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param media_id - media_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InstagramController::api_setMediaCaption` expose observed keys including `caption`, `error`, `error_msg`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_INSTAGRAM_MEDIA_SET_CAPTION(123, 123);
   * ```
   */
  PUT_INSTAGRAM_MEDIA_SET_CAPTION(
    shop_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/instagram/medias/${media_id}/caption`;
  }

  /**
   * Instagram story set products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/channels/instagram/stories/{story_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param story_id - story_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `InstagramController::api_setStoryProduct` expose observed keys including `products`, `error`, `error_msg`, `success`, `story`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_INSTAGRAM_STORY_SET_PRODUCTS(123, 123);
   * ```
   */
  PUT_INSTAGRAM_STORY_SET_PRODUCTS(
    shop_id: string | number,
    story_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/instagram/stories/${story_id}/products`;
  }

  //―――――――――――――――――――――― OpenAI ――――――――――――――――――――
  /**
   * Channels openai download knowledge base endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/channels/openai/knowledge-base/{resource}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param resource - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOpenaiGptPluginController::api_downloadKnowledgeBase` expose observed keys including `error`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CHANNELS_OPENAI_DOWNLOAD_KNOWLEDGE_BASE(123, "value");
   * ```
   */
  GET_CHANNELS_OPENAI_DOWNLOAD_KNOWLEDGE_BASE(
      shop_id: string | number,
      resource: 'products'|'instructions' ,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/channels/openai/knowledge-base/${resource}`;
  }

  //―――――――――――――――――――――― Experts > Expert Zone ――――――――――――――――――――

  /**
   * Set my expert profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserController::api_setMyExpertProfile` expose observed keys including `name`, `country`, `address`, `lng`, `lat`, `description`, `languages`, `industries`, `linkedin`, `instagram`, `facebook`, `pinterest`, `error`, `error_msg`, `location`, `socials`, `success`, `expert`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_MY_EXPERT_PROFILE();
   * ```
   */
  POST_SET_MY_EXPERT_PROFILE() {
    return `${this.selldone_api_url}/expert`;
  }

  /**
   * My expert upload image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserController::uploadMyExpertProfileImage` expose observed keys including `photo`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_EXPERT_UPLOAD_IMAGE();
   * ```
   */
  POST_MY_EXPERT_UPLOAD_IMAGE() {
    return `${this.selldone_api_url}/expert/image`;
  }

  /**
   * Expert jobs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/jobs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::api_user_getJobs` expose observed keys including `error`, `error_msg`, `unreadMessages`, `jobs`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOBS();
   * ```
   */
  GET_EXPERT_JOBS() {
    return `${this.selldone_api_url}/expert/jobs`;
  }

  /**
   * Add expert job endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/jobs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::api_user_addJob` expose observed keys including `topic`, `tags`, `available`, `description`, `price`, `currency`, `tasks`, `included`, `additional`, `works`, `permissions`, `error`, `error_msg`, `success`, `job`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_EXPERT_JOB();
   * ```
   */
  POST_ADD_EXPERT_JOB() {
    return `${this.selldone_api_url}/expert/jobs`;
  }

  /**
   * Edit expert job endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/jobs/{job_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::api_user_editJob` expose observed keys including `tags`, `available`, `description`, `price`, `currency`, `tasks`, `included`, `additional`, `works`, `permissions`, `error`, `error_msg`, `success`, `job`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_EXPERT_JOB(123);
   * ```
   */
  PUT_EDIT_EXPERT_JOB(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}`;
  }

  /**
   * Expert job upload images endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/jobs/{job_id}/images
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::uploadJobImageGallery` expose observed keys including `photo`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_UPLOAD_IMAGES(123);
   * ```
   */
  POST_EXPERT_JOB_UPLOAD_IMAGES(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/images`;
  }

  /**
   * Expert job image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /expert/jobs/{job_id}/images/{image_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param image_id - Image identifier scoped to the resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::deleteJobImage` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EXPERT_JOB_IMAGE(123, 202);
   * ```
   */
  DELETE_EXPERT_JOB_IMAGE(job_id: string | number, image_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/images/${image_id}`;
  }

  /**
   * Expert job images order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/jobs/{job_id}/images
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::api_setJobImagesOrder` expose observed keys including `orders`, `error`, `error_msg`, `order`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_JOB_IMAGES_ORDER(123);
   * ```
   */
  PUT_EXPERT_JOB_IMAGES_ORDER(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/images`;
  }

  /**
   * Expert job info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/jobs/{job_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserJobsController::api_user_getJobInfo` expose observed keys including `error`, `error_msg`, `expert`, `job`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_INFO(123);
   * ```
   */
  GET_EXPERT_JOB_INFO(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}`;
  }

  /**
   * Expert job contracts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert/jobs/{job_id}/contracts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_getContracts` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `contracts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CONTRACTS(123);
   * ```
   */
  GET_EXPERT_JOB_CONTRACTS(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/contracts`;
  }

  /**
   * Expert job contracts for user endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert/jobs/{job_id}/contracts/users/{user_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_getContractsForUser` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `contracts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CONTRACTS_FOR_USER(123, 102);
   * ```
   */
  GET_EXPERT_JOB_CONTRACTS_FOR_USER(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/contracts/users/${user_id}`;
  }

  /**
   * Expert job requests endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert/jobs/{job_id}/requests
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserRequestsController::api_user_getEligibleRequestsForJob` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `requests`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_REQUESTS(123);
   * ```
   */
  GET_EXPERT_JOB_REQUESTS(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/requests`;
  }

  /**
   * Expert job request start negotiation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /expert/jobs/{job_id}/requests/{request_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param request_id - request_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_interestedExpertForRequest` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_REQUEST_START_NEGOTIATION(123, 123);
   * ```
   */
  POST_EXPERT_JOB_REQUEST_START_NEGOTIATION(
    job_id: string | number,
    request_id: string | number,
  ) {
    // Signal only!
    return `${this.selldone_api_url}/expert/jobs/${job_id}/requests/${request_id}`;
  }

  /**
   * Expert job chats endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert/jobs/{job_id}/chats
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_getChats` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `folders`, `chats`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CHATS(123);
   * ```
   */
  GET_EXPERT_JOB_CHATS(job_id: string | number) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats`;
  }

  /**
   * Expert job chat messages endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert/jobs/{job_id}/chats/{user_id}/messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_getChatMessages` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `messages`, `total`, `seen_at`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CHAT_MESSAGES(123, 102);
   * ```
   */
  GET_EXPERT_JOB_CHAT_MESSAGES(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats/${user_id}/messages`;
  }

  /**
   * Expert job chat message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/jobs/{job_id}/chats/{user_id}/messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_sendChatMessage` expose observed keys including `message`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_JOB_CHAT_MESSAGE(123, 102);
   * ```
   */
  PUT_EXPERT_JOB_CHAT_MESSAGE(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats/${user_id}/messages`;
  }

  /**
   * Expert job chat message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /expert/jobs/{job_id}/chats/{user_id}/messages/{message_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_deleteChatMessage` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EXPERT_JOB_CHAT_MESSAGE(123, 102, 123);
   * ```
   */
  DELETE_EXPERT_JOB_CHAT_MESSAGE(
    job_id: string | number,
    user_id: string | number,
    message_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats/${user_id}/messages/${message_id}`;
  }

  /**
   * Expert job chat attachment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/jobs/{job_id}/chats/{user_id}/attachments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_sendChatAttachment` expose observed keys including `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_CHAT_ATTACHMENT(123, 102);
   * ```
   */
  POST_EXPERT_JOB_CHAT_ATTACHMENT(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats/${user_id}/attachments`;
  }

  /**
   * Expert job chat attachment URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/jobs/{job_id}/chats/{user_id}/attachments/{message_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserChatsController::api_getChatMessageAttachmentUrl` expose observed keys including `error`, `error_msg`, `ResponseContentDisposition`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CHAT_ATTACHMENT_URL(123, 102, 123);
   * ```
   */
  GET_EXPERT_JOB_CHAT_ATTACHMENT_URL(
    job_id: string | number,
    user_id: string | number,
    message_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/chats/${user_id}/attachments/${message_id}`;
  }

  /**
   * Expert job contract create endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/jobs/{job_id}/contracts/users/{user_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_createContractForUser` expose observed keys including `shop_id`, `title`, `description`, `tasks`, `cost`, `currency`, `days`, `permissions`, `error`, `error_msg`, `progress`, `code`, `grant_at`, `success`, `contract`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_VERIFIED_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_CONTRACT_CREATE(123, 102);
   * ```
   */
  POST_EXPERT_JOB_CONTRACT_CREATE(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/contracts/users/${user_id}`;
  }

  /**
   * Expert job contract required data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/jobs/{job_id}/contracts/users/{user_id}/required-data
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_getRequiredDataToCreateContractForUser` expose observed keys including `error`, `error_msg`, `job`, `shops`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_VERIFIED_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CONTRACT_REQUIRED_DATA(123, 102);
   * ```
   */
  GET_EXPERT_JOB_CONTRACT_REQUIRED_DATA(
    job_id: string | number,
    user_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert/jobs/${job_id}/contracts/users/${user_id}/required-data`;
  }

  /**
   * Expert job contract info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/contracts/{contract_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_getContract` expose observed keys including `error`, `error_msg`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_JOB_CONTRACT_INFO(123);
   * ```
   */
  GET_EXPERT_JOB_CONTRACT_INFO(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}`;
  }

  /**
   * Expert job contract set tasks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/contracts/{contract_id}/tasks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_setContractTask` expose observed keys including `tasks`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_JOB_CONTRACT_SET_TASKS(123);
   * ```
   */
  PUT_EXPERT_JOB_CONTRACT_SET_TASKS(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}/tasks`;
  }

  /**
   * Expert job contract edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/contracts/{contract_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_editContract` expose observed keys including `title`, `description`, `tasks`, `cost`, `days`, `permissions`, `error`, `error_msg`, `progress`, `code`, `grant_at`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_JOB_CONTRACT_EDIT(123);
   * ```
   */
  PUT_EXPERT_JOB_CONTRACT_EDIT(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}`;
  }

  /**
   * Expert job contract end endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /expert/contracts/{contract_id}/end
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_endContract` expose observed keys including `error`, `error_msg`, `end_at`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_CONTRACT_END(123);
   * ```
   */
  POST_EXPERT_JOB_CONTRACT_END(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}/end`;
  }

  /**
   * Expert job contract cancel endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /expert/contracts/{contract_id}/cancel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_cancelContract` expose observed keys including `error`, `error_msg`, `cancel_at`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_CONTRACT_CANCEL(123);
   * ```
   */
  POST_EXPERT_JOB_CONTRACT_CANCEL(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}/cancel`;
  }

  /**
   * Expert job contract response endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/contracts/{contract_id}/response
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsUserContractsController::api_user_setContractResponse` expose observed keys including `response`, `error`, `error_msg`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_JOB_CONTRACT_RESPONSE(123);
   * ```
   */
  POST_EXPERT_JOB_CONTRACT_RESPONSE(contract_id: string | number) {
    return `${this.selldone_api_url}/expert/contracts/${contract_id}/response`;
  }

  //―――――――――――――――――――――― Experts > Customer Zone ――――――――――――――――――――

  /**
   * Expert customer chats endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert-customer/chats
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerChatsController::api_getChats` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `folders`, `chats`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_CUSTOMER_CHATS();
   * ```
   */
  GET_EXPERT_CUSTOMER_CHATS() {
    return `${this.selldone_api_url}/expert-customer/chats`;
  }

  /**
   * Expert customer chat messages endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert-customer/chats/{job_id}/messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerChatsController::api_getChatMessages` expose observed keys including `offset`, `limit`, `messages`, `total`, `seen_at`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_CUSTOMER_CHAT_MESSAGES(123);
   * ```
   */
  GET_EXPERT_CUSTOMER_CHAT_MESSAGES(job_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/chats/${job_id}/messages`;
  }

  /**
   * Expert customer chat message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert-customer/chats/{job_id}/messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerChatsController::api_sendChatMessage` expose observed keys including `message`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_CUSTOMER_CHAT_MESSAGE(123);
   * ```
   */
  PUT_EXPERT_CUSTOMER_CHAT_MESSAGE(job_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/chats/${job_id}/messages`;
  }

  /**
   * Expert customer chat message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /expert-customer/chats/{job_id}/messages/{message_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerChatsController::api_deleteChatMessage` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EXPERT_CUSTOMER_CHAT_MESSAGE(123, 123);
   * ```
   */
  DELETE_EXPERT_CUSTOMER_CHAT_MESSAGE(
    job_id: string | number,
    message_id: string | number,
  ) {
    return `${this.selldone_api_url}/expert-customer/chats/${job_id}/messages/${message_id}`;
  }

  /**
   * Expert customer contracts for job endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /expert-customer/jobs/{job_id}/contracts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_getContracts` expose observed keys including `offset`, `limit`, `contracts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_CUSTOMER_CONTRACTS_FOR_JOB(123);
   * ```
   */
  GET_EXPERT_CUSTOMER_CONTRACTS_FOR_JOB(job_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/jobs/${job_id}/contracts`;
  }

  /**
   * Expert customer contract info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert-customer/contracts/{contract_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_getContract` expose observed keys including `error`, `error_msg`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_CUSTOMER_CONTRACT_INFO(123);
   * ```
   */
  GET_EXPERT_CUSTOMER_CONTRACT_INFO(contract_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/contracts/${contract_id}`;
  }

  /**
   * Expert customer contract pay now endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert-customer/contracts/{contract_id}/pay
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_payContract` expose observed keys including `account_number`, `error`, `error_msg`, `code`, `grant_at`, `payment_at`, `start_at`, `permissions`, `contract`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`, `ErrorCodes::NOT_MATCH_ACCOUNT_CURRENCY_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_SUFFICIENT_BALANCE_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CUSTOMER_CONTRACT_PAY_NOW(123);
   * ```
   */
  POST_EXPERT_CUSTOMER_CONTRACT_PAY_NOW(contract_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/contracts/${contract_id}/pay`;
  }

  /**
   * Expert customer contract complete endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert-customer/contracts/{contract_id}/complete
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_completeContract` expose observed keys including `accept`, `error`, `error_msg`, `code`, `grant_at`, `complete_at`, `permissions`, `end_at`, `contract`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CUSTOMER_CONTRACT_COMPLETE(123);
   * ```
   */
  POST_EXPERT_CUSTOMER_CONTRACT_COMPLETE(contract_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/contracts/${contract_id}/complete`;
  }

  /**
   * Expert customer contract permission endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert-customer/contracts/{contract_id}/permissions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ACCESS
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_setContractPermissions` expose observed keys including `grant`, `permission`, `error`, `error_msg`, `permissions`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CUSTOMER_CONTRACT_PERMISSION(123);
   * ```
   */
  POST_EXPERT_CUSTOMER_CONTRACT_PERMISSION(contract_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/contracts/${contract_id}/permissions`;
  }

  /**
   * Expert customer contract comment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert-customer/contracts/{contract_id}/comment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param contract_id - contract_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_commentContract` expose observed keys including `rate`, `comment`, `error`, `error_msg`, `rate_count`, `contract`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CUSTOMER_CONTRACT_COMMENT(123);
   * ```
   */
  POST_EXPERT_CUSTOMER_CONTRACT_COMMENT(contract_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/contracts/${contract_id}/comment`;
  }

  /**
   * Expert customer jobs history endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint GET /expert-customer/jobs/{job_id}/history
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerContractsController::api_getContractsHistory` expose observed keys including `contracts`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CUSTOMER_JOBS_HISTORY(123);
   * ```
   */
  POST_EXPERT_CUSTOMER_JOBS_HISTORY(job_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/jobs/${job_id}/history`;
  }

  /**
   * Expert send request for job endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert-customer/jobs/{job_id}/request
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param job_id - job_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsCustomerChatsController::api_setNewExpertRequestForJob` expose observed keys including `shop_id`, `store_name`, `name`, `detail`, `stage`, `budget`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_SEND_REQUEST_FOR_JOB(123);
   * ```
   */
  POST_EXPERT_SEND_REQUEST_FOR_JOB(job_id: string | number) {
    return `${this.selldone_api_url}/expert-customer/jobs/${job_id}/request`;
  }

  /**
   * Expert chat folders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /expert/chat/folders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsChatFoldersController::api_getFolders` expose observed keys including `folders`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPERT_CHAT_FOLDERS();
   * ```
   */
  GET_EXPERT_CHAT_FOLDERS() {
    return `${this.selldone_api_url}/expert/chat/folders`;
  }

  /**
   * Expert chat folder new endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /expert/chat/folders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsChatFoldersController::api_createNewFolder` expose observed keys including `name`, `order`, `error`, `error_msg`, `folder`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_EXPERT_CHAT_FOLDER_NEW();
   * ```
   */
  POST_EXPERT_CHAT_FOLDER_NEW() {
    return `${this.selldone_api_url}/expert/chat/folders`;
  }

  /**
   * Expert chat folder edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/chat/folders/{folder_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param folder_id - folder_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsChatFoldersController::api_editFolder` expose observed keys including `name`, `order`, `error`, `error_msg`, `folder`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_CHAT_FOLDER_EDIT(123);
   * ```
   */
  PUT_EXPERT_CHAT_FOLDER_EDIT(folder_id: string | number) {
    return `${this.selldone_api_url}/expert/chat/folders/${folder_id}`;
  }

  /**
   * Expert chat folder endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /expert/chat/folders/{folder_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param folder_id - folder_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsChatFoldersController::api_deleteFolder` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EXPERT_CHAT_FOLDER(123);
   * ```
   */
  DELETE_EXPERT_CHAT_FOLDER(folder_id: string | number) {
    return `${this.selldone_api_url}/expert/chat/folders/${folder_id}`;
  }

  /**
   * Expert chat folder add chat endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /expert/chat/folder
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ExpertsChatFoldersController::api_addChat` expose observed keys including `chat_id`, `folder_id`, `error`, `error_msg`, `folder`, `items`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EXPERT_CHAT_FOLDER_ADD_CHAT();
   * ```
   */
  PUT_EXPERT_CHAT_FOLDER_ADD_CHAT() {
    return `${this.selldone_api_url}/expert/chat/folder`;
  }

  //―――――――――――――――――――――― Feedback ――――――――――――――――――――

  /**
   * Send feedback nps endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /feedback/nps
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FeedbackController::api_feedback_send_nps` expose observed keys including `nps`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_FEEDBACK_NPS();
   * ```
   */
  POST_SEND_FEEDBACK_NPS() {
    return `${this.selldone_api_url}/feedback/nps`;
  }

  /**
   * Send feedback message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /feedback/message
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FeedbackController::api_feedback_send_message` expose observed keys including `message`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_FEEDBACK_MESSAGE();
   * ```
   */
  POST_SEND_FEEDBACK_MESSAGE() {
    return `${this.selldone_api_url}/feedback/message`;
  }

  //―――――――――――――――――――――― Avocado ――――――――――――――――――――
  /**
   * Set shop avocado endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/avocado
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_setShopAvocado` expose observed keys including `enable`, `delivery`, `title`, `message`, `avocado`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_SHOP_AVOCADO(123);
   * ```
   */
  PUT_SET_SHOP_AVOCADO(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocado`;
  }

  /**
   * Shop info for avocado endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/avocado-info
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_getShopInfoNeededForAvocado` expose observed keys including `shopGateways`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_INFO_FOR_AVOCADO(123);
   * ```
   */
  GET_SHOP_INFO_FOR_AVOCADO(shop_id: string | number) {
    // Abstract shop info for create avocado form (For shop admin)
    return `${this.selldone_api_url}/shops/${shop_id}/avocado-info`;
  }

  /**
   * Avocado create endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/avocados
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_createNewAvocado` expose observed keys including `currency`, `price`, `delivery`, `delivery_price`, `tax`, `tax_included`, `title`, `message`, `error`, `error_msg`, `success`, `avocado`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GATEWAY_NOT_CONFIGURE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AVOCADO_CREATE(123);
   * ```
   */
  POST_AVOCADO_CREATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocados`;
  }

  /**
   * Avocado edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/avocados/{avocado_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_editAvocado` expose observed keys including `currency`, `price`, `delivery`, `delivery_price`, `tax`, `tax_included`, `title`, `message`, `error`, `error_msg`, `success`, `avocado`, `status`, `delivery_state`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::GATEWAY_NOT_CONFIGURE_FOR_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_AVOCADO_EDIT(123, 123);
   * ```
   */
  PUT_AVOCADO_EDIT(shop_id: string | number, avocado_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocados/${avocado_id}`;
  }

  /**
   * Avocado progress endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/avocados/{avocado_id}/progress
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_setAvocadoProgress` expose observed keys including `progress`, `error`, `error_msg`, `success`, `avocado`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_AVOCADO_PROGRESS(123, 123);
   * ```
   */
  PUT_AVOCADO_PROGRESS(shop_id: string | number, avocado_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocados/${avocado_id}/progress`;
  }

  /**
   * Avocado item price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/avocados/{avocado_id}/items/{item_id}/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_setAvocadoItemPrice` expose observed keys including `price`, `currency`, `error`, `error_msg`, `status`, `success`, `item`, `avocado`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_VALID_CURRENCY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_AVOCADO_ITEM_PRICE(123, 123, 123);
   * ```
   */
  PUT_AVOCADO_ITEM_PRICE(
    shop_id: string | number,
    avocado_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocados/${avocado_id}/items/${item_id}/price`;
  }

  /**
   * Avocado item status endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/avocados/{avocado_id}/items/{item_id}/status
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoController::api_setAvocadoItemStatus` expose observed keys including `status`, `error`, `error_msg`, `price`, `success`, `item`, `avocado`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_AVOCADO_ITEM_STATUS(123, 123, 123);
   * ```
   */
  PUT_AVOCADO_ITEM_STATUS(
    shop_id: string | number,
    avocado_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/avocados/${avocado_id}/items/${item_id}/status`;
  }

  /**
   * Avocado order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/avocados/{avocado_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `AvocadoOrderProcessingController::api_admin_getAvocadoReceivedOrderInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AVOCADO_ORDER_INFO(123, 123);
   * ```
   */
  GET_AVOCADO_ORDER_INFO(
    shop_id: string | number,
    avocado_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/avocados/${avocado_id}`;
  }

  /**
   * Update avocado order state endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/avocados/{avocado_id}/state
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `AvocadoOrderProcessingController::api_admin_postUpdateDeliveryState` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_AVOCADO_ORDER_STATE(123, 123);
   * ```
   */
  POST_UPDATE_AVOCADO_ORDER_STATE(
    shop_id: string | number,
    avocado_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/avocados/${avocado_id}/state`;
  }

  /**
   * Update avocado order delivery return endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/avocados/{avocado_id}/delivery-returned
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoOrderProcessingController::api_admin_postUpdateDeliveryStateReturnedDelivery` expose observed keys including `error`, `error_msg`, `delivery_state`, `changed`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPDATE_AVOCADO_ORDER_DELIVERY_RETURN(123, 123);
   * ```
   */
  POST_UPDATE_AVOCADO_ORDER_DELIVERY_RETURN(
    shop_id: string | number,
    avocado_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/avocados/${avocado_id}/delivery-returned`;
  }

  /**
   * Reject avocado order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/avocados/{avocado_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoOrderProcessingController::api_admin_setRejectOrder` expose observed keys including `reject`, `express`, `error`, `error_msg`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_REJECT_AVOCADO_ORDER(123, 123);
   * ```
   */
  PUT_REJECT_AVOCADO_ORDER(
    shop_id: string | number,
    avocado_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/avocados/${avocado_id}/reject`;
  }

  /**
   * Reject avocado order endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/process-center/avocados/{avocado_id}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param avocado_id - avocado_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AvocadoOrderProcessingController::api_admin_deleteRejectOrder` expose observed keys including `error`, `error_msg`, `reject`, `reject_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_REJECT_AVOCADO_ORDER(123, 123);
   * ```
   */
  DELETE_REJECT_AVOCADO_ORDER(
    shop_id: string | number,
    avocado_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/avocados/${avocado_id}/reject`;
  }

  //―――――――――――――――――――――― Hyper ――――――――――――――――――――
  /**
   * Set shop hyper endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/hyper
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `HyperController::api_setShopHyper` expose observed keys including `enable`, `hyper`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_SHOP_HYPER(123);
   * ```
   */
  PUT_SET_SHOP_HYPER(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/hyper`;
  }

  /**
   * Hyper order info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/process-center/hypers/{hyper_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param hyper_id - hyper_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `HyperOrderProcessingController::api_admin_getHyperReceivedOrderInfo` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_HYPER_ORDER_INFO(123, 123);
   * ```
   */
  GET_HYPER_ORDER_INFO(shop_id: string | number, hyper_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/hypers/${hyper_id}`;
  }

  //―――――――――――――――――――――― Guild ――――――――――――――――――――
  /**
   * My guild endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /guild
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildController::api_getMyGuild` expose observed keys including `error`, `error_msg`, `guild`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_GUILD();
   * ```
   */
  GET_MY_GUILD() {
    return `${this.selldone_api_url}/guild`;
  }

  /**
   * Setup guild endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /guild
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildController::api_createGuild` expose observed keys including `name`, `title`, `desc`, `enable`, `flex`, `bordered`, `error`, `error_msg`, `guild`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SETUP_GUILD();
   * ```
   */
  POST_SETUP_GUILD() {
    return `${this.selldone_api_url}/guild`;
  }

  /**
   * Guild add member endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /guild/members
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildMemberController::api_createGuildMember` expose observed keys including `type`, `tags`, `url`, `order`, `title`, `fav`, `html`, `provider_name`, `provider_url`, `error`, `error_msg`, `like`, `save`, `report`, `member`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GUILD_ADD_MEMBER();
   * ```
   */
  POST_GUILD_ADD_MEMBER() {
    return `${this.selldone_api_url}/guild/members`;
  }

  /**
   * Guild edit member endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /guild/members/{member_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param member_id - member_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildMemberController::api_editGuildMember` expose observed keys including `type`, `tags`, `url`, `order`, `title`, `fav`, `html`, `provider_name`, `provider_url`, `error`, `error_msg`, `action`, `success`, `member`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GUILD_EDIT_MEMBER(123);
   * ```
   */
  POST_GUILD_EDIT_MEMBER(member_id: string | number) {
    return `${this.selldone_api_url}/guild/members/${member_id}`;
  }

  /**
   * Guild member endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /guild/members/{member_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param member_id - member_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildMemberController::api_deleteGuildMember` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GUILD_MEMBER(123);
   * ```
   */
  DELETE_GUILD_MEMBER(member_id: string | number) {
    return `${this.selldone_api_url}/guild/members/${member_id}`;
  }

  /**
   * Guild member like endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /guilds/{guild_id}/members/{member_id}/like
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param guild_id - guild_id route parameter identifier.
   * @param member_id - member_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildReactionController::api_likeMember` expose observed keys including `like`, `error`, `error_msg`, `success`, `action`, `likes`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GUILD_MEMBER_LIKE(123, 123);
   * ```
   */
  POST_GUILD_MEMBER_LIKE(
    guild_id: string | number,
    member_id: string | number,
  ) {
    return `${this.selldone_api_url}/guilds/${guild_id}/members/${member_id}/like`;
  }

  /**
   * Guild member recommendation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /guilds/{guild_id}/members/{member_id}/recommendation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param guild_id - guild_id route parameter identifier.
   * @param member_id - member_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GuildReactionController::api_recommendationMember` expose observed keys including `recommendation`, `error`, `error_msg`, `success`, `action`, `recoms`, `recommenders`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GUILD_MEMBER_RECOMMENDATION(123, 123);
   * ```
   */
  POST_GUILD_MEMBER_RECOMMENDATION(
    guild_id: string | number,
    member_id: string | number,
  ) {
    return `${this.selldone_api_url}/guilds/${guild_id}/members/${member_id}/recommendation`;
  }

  //―――――――――――――――――――――― Direct Feedback ――――――――――――――――――――
  /**
   * Send direct feedback endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/feedback/direct
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FeedbackDirectController::api_feedback_direct` expose observed keys including `message`, `to`, `success`, `feedback`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_DIRECT_FEEDBACK(123);
   * ```
   */
  POST_SEND_DIRECT_FEEDBACK(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/feedback/direct`;
  }

  //―――――――――――――――――――――― Shop Options ――――――――――――――――――――
  /**
   * Set shop options GDPR endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_GDPR(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_GDPR(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/gdpr`;
  }

  /**
   * Set shop options AMP endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_AMP(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_AMP(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/amp`;
  }

  /**
   * Set shop options size unit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_SIZE_UNIT(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_SIZE_UNIT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/size_unit`;
  }

  /**
   * Set shop options mass unit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_MASS_UNIT(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_MASS_UNIT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/mass_unit`;
  }

  /**
   * Set shop options distance unit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_DISTANCE_UNIT(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_DISTANCE_UNIT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/distance_unit`;
  }

  /**
   * Set shop options boost endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_BOOST(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_BOOST(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/boost`;
  }

  /**
   * Set shop options login methods endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_LOGIN_METHODS(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_LOGIN_METHODS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/login`;
  }

  /**
   * Shop option login method details endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/options/login/{login_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param login_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLoginOAuthController::api_getShopLoginMethodDetails` expose observed keys including `oauth`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_OPTION_LOGIN_METHOD_DETAILS(123, "value");
   * ```
   */
  GET_SHOP_OPTION_LOGIN_METHOD_DETAILS(
    shop_id: string | number,
    login_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/login/${login_code}`;
  }

  /**
   * Shop option login method client endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/login/{login_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param login_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLoginOAuthController::api_setShopLoginMethodDetails` expose observed keys including `client_id`, `client_secret`, `enable`, `error`, `error_msg`, `success`, `oauth`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_OPTION_LOGIN_METHOD_CLIENT(123, "value");
   * ```
   */
  POST_SHOP_OPTION_LOGIN_METHOD_CLIENT(
    shop_id: string | number,
    login_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/login/${login_code}`;
  }

  /**
   * Set shop options checkout endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_CHECKOUT(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_CHECKOUT(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/checkout`;
  }

  /**
   * Set shop options types endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_TYPES(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_TYPES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/types`;
  }

  /**
   * Set shop options shop map endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_SHOP_MAP(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_SHOP_MAP(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/shop_map`;
  }

  /**
   * Set shop options variants endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_VARIANTS(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_VARIANTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/variants`;
  }

  /**
   * Set shop options shipping restriction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_SHIPPING_RESTRICTION(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_SHIPPING_RESTRICTION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/shipping_restriction`;
  }

  /**
   * Set shop options labeling endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/options/{code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopOptionsController::api_setShopOption` expose observed keys including `enable`, `value`, `boost`, `product`, `category`, `code`, `error`, `error_msg`, `types`, `driver`, `color`, `style`, `volume`, `weight`, `pack`, `type`, `color_icon`, `style_icon`, `volume_icon`, `weight_icon`, `pack_icon`, `type_icon`, `shipping_restriction`, `mode`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_OPTIONS_LABELING(123);
   * ```
   */
  POST_SET_SHOP_OPTIONS_LABELING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/options/labeling`;
  }

  //―――――――――――――――――――――― Custom Order ID ――――――――――――――――――――

  /**
   * Shop order last ID endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/order-last-id
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomOrderIDController::get` expose observed keys including `order_last_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_ORDER_LAST_ID(123);
   * ```
   */
  GET_SHOP_ORDER_LAST_ID(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/order-last-id`;
  }

  /**
   * Set shop order last ID endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/order-last-id
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomOrderIDController::set` expose observed keys including `period`, `calendar`, `reset`, `last_id`, `reset_at`, `order_last_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_ORDER_LAST_ID(123);
   * ```
   */
  POST_SET_SHOP_ORDER_LAST_ID(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/order-last-id`;
  }

  //―――――――――――――――――――――― Search Console ――――――――――――――――――――
  /**
   * Search console connection endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/services/google/search
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GoogleSearchConsoleShopController::api_deleteShopSearchConsole` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SEARCH_CONSOLE_CONNECTION(123);
   * ```
   */
  DELETE_SEARCH_CONSOLE_CONNECTION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/services/google/search`;
  }

  /**
   * Shop services google search console data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/services/google/search
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::DASHBOARD
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GoogleSearchConsoleShopController::api_GetShopData` expose observed keys including `error`, `error_msg`, `code`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SERVICES_GOOGLE_SEARCH_CONSOLE_DATA(123);
   * ```
   */
  GET_SHOP_SERVICES_GOOGLE_SEARCH_CONSOLE_DATA(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/services/google/search`;
  }

  //―――――――――――――――――――――― Google Sheet ――――――――――――――――――――
  /**
   * Google sheet connection endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/services/google/sheet
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GoogleSheetController::api_deleteShopGoogleSheet` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_GOOGLE_SHEET_CONNECTION(123);
   * ```
   */
  DELETE_GOOGLE_SHEET_CONNECTION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/services/google/sheet`;
  }

  /**
   * Google sheet force sync endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/services/google/sheet/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `GoogleSheetController::api_forceUpdateShopGoogleSheet` expose observed keys including `error`, `error_msg`, `success`, `rez`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_GOOGLE_SHEET_FORCE_SYNC(123);
   * ```
   */
  POST_GOOGLE_SHEET_FORCE_SYNC(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/services/google/sheet/sync`;
  }

  //―――――――――――――――――――――― Partners Login ――――――――――――――――――――
  /**
   * Partner login apply dealt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /partners/apply-deal/{shop_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAgencyController::api_applyAgencyDealToShop` expose observed keys including `id`, `code`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PARTNER_LOGIN_APPLY_DEALT(123);
   * ```
   */
  POST_PARTNER_LOGIN_APPLY_DEALT(shop_id: string | number) {
    return `${this.selldone_api_url}/partners/apply-deal/${shop_id}`;
  }

  /**
   * Partner fix shop license endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /partners/fix-license/{shop_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAgencyController::api_fixMyShopAgencyLicense` expose observed keys including `error`, `error_msg`, `license`, `date`, `expire_at`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PARTNER_FIX_SHOP_LICENSE(123);
   * ```
   */
  POST_PARTNER_FIX_SHOP_LICENSE(shop_id: string | number) {
    return `${this.selldone_api_url}/partners/fix-license/${shop_id}`;
  }

  /**
   * Partner upgrade URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /partners/upgrade-url/{shop_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAgencyController::api_getUpgradePartnerLicenseUrl` expose observed keys including `error`, `error_msg`, `success`, `url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PARTNER_UPGRADE_URL(123);
   * ```
   */
  GET_PARTNER_UPGRADE_URL(shop_id: string | number) {
    return `${this.selldone_api_url}/partners/upgrade-url/${shop_id}`;
  }

  /**
   * My shop agency meta set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/agency-meta
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAgencyMetaController::api_setShopAgencyMeta` expose observed keys including `read_access`, `error`, `error_msg`, `agency_meta`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_SHOP_AGENCY_META_SET(123);
   * ```
   */
  PUT_MY_SHOP_AGENCY_META_SET(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/agency-meta`;
  }

  //―――――――――――――――――――――― Deals ――――――――――――――――――――
  /**
   * My partner purchase deals endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /partners/deals
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealsController::list` expose observed keys including `offset`, `limit`, `deals`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PARTNER_PURCHASE_DEALS();
   * ```
   */
  GET_MY_PARTNER_PURCHASE_DEALS() {
    return `${this.selldone_api_url}/partners/deals`;
  }

  /**
   * My partner purchase deal shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /partners/deals/{agency_purchase_id}/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_affiliate_purchase_id - agency_affiliate_purchase_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealShopsListController::list` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `shops`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PARTNER_PURCHASE_DEAL_SHOPS(123);
   * ```
   */
  GET_MY_PARTNER_PURCHASE_DEAL_SHOPS(
    agency_affiliate_purchase_id: string | number,
  ) {
    return `${this.selldone_api_url}/partners/deals/${agency_affiliate_purchase_id}/shops`;
  }

  /**
   * My partner pending purchase deals endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /partners/deals/pending
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealsController::listOfPendingPayments` expose observed keys including `pending_purchases`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PARTNER_PENDING_PURCHASE_DEALS();
   * ```
   */
  GET_MY_PARTNER_PENDING_PURCHASE_DEALS() {
    return `${this.selldone_api_url}/partners/deals/pending`;
  }

  /**
   * Partner purchase a deal endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /partners/deals/{ref_code}/plans/{plan_code}/purchase/{gateway}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Routing\Middleware\ThrottleRequestsWithRedis:20,5`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param ref_code - Code value passed as a route segment.
   * @param plan_code - Code value passed as a route segment.
   * @param gateway - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AgencyPurchaseDealController::api_purchaseDeal` expose observed keys including `amount`, `company_id`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`, `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`, `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::CHARGE_REQUEST_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PARTNER_PURCHASE_A_DEAL("value", "value", "value");
   * ```
   */
  POST_PARTNER_PURCHASE_A_DEAL(
    ref_code: string,
    plan_code: string,
    gateway: string,
  ) {
    return `${this.selldone_api_url}/partners/deals/${ref_code}/plans/${plan_code}/purchase/${gateway}`;
  }

  /**
   * My partner pending purchase check transaction endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /partners/deals/pending/{agency_affiliate_purchase_id}/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_affiliate_purchase_id - agency_affiliate_purchase_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealsController::checkPendingPayments` expose observed keys including `error`, `error_msg`, `success`, `status`, `payment_at`, `transaction_status`, `gateway_name`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PARTNER_PENDING_PURCHASE_CHECK_TRANSACTION(123);
   * ```
   */
  POST_MY_PARTNER_PENDING_PURCHASE_CHECK_TRANSACTION(
    agency_affiliate_purchase_id: string | number,
  ) {
    return `${this.selldone_api_url}/partners/deals/pending/${agency_affiliate_purchase_id}/check`;
  }

  /**
   * My partner purchase refund endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /partners/deals/{agency_affiliate_purchase_id}/refund/{uuid}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_affiliate_purchase_id - agency_affiliate_purchase_id route parameter identifier.
   * @param uuid - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealsController::refundPurchase` expose observed keys including `accept`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PARTNER_PURCHASE_REFUND(123, "value");
   * ```
   */
  POST_MY_PARTNER_PURCHASE_REFUND(
    agency_affiliate_purchase_id: string | number,
    uuid: string,
  ) {
    return `${this.selldone_api_url}/partners/deals/${agency_affiliate_purchase_id}/refund/${uuid}`;
  }

  /**
   * My partner purchase download receipt endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /partners/deals/{agency_affiliate_purchase_id}/receipt
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_affiliate_purchase_id - agency_affiliate_purchase_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ReceiptController::downloadDealPurchaseReceipt` expose observed keys including `error`, `error_msg`, `name`, `address`, `phone`, `custom_fields`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PARTNER_PURCHASE_DOWNLOAD_RECEIPT(123);
   * ```
   */
  GET_MY_PARTNER_PURCHASE_DOWNLOAD_RECEIPT(
    agency_affiliate_purchase_id: string | number,
  ) {
    return `${this.selldone_api_url}/partners/deals/${agency_affiliate_purchase_id}/receipt`;
  }

  /**
   * My partner purchase transfer ownership endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /partners/deals/{agency_affiliate_purchase_id}/transfer-ownership/{uuid}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_affiliate_purchase_id - agency_affiliate_purchase_id route parameter identifier.
   * @param uuid - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserDealsController::transferOwnership` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PARTNER_PURCHASE_TRANSFER_OWNERSHIP(123, "value");
   * ```
   */
  POST_MY_PARTNER_PURCHASE_TRANSFER_OWNERSHIP(
    agency_affiliate_purchase_id: string | number,
    uuid: string,
  ) {
    return `${this.selldone_api_url}/partners/deals/${agency_affiliate_purchase_id}/transfer-ownership/${uuid}`;
  }

  /**
   * Shop cancel active deal endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/cancel-deal
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopDealCancellationController::api_requestCancelShopDeal` expose observed keys including `uuid`, `error`, `error_msg`, `shop_id`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CANCEL_ACTIVE_DEAL(123);
   * ```
   */
  POST_SHOP_CANCEL_ACTIVE_DEAL(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cancel-deal`;
  }

  //―――――――――――――――――――――― Shop > Webhooks ――――――――――――――――――――
  /**
   * Shop webhooks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/webhooks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhooksController::list` expose observed keys including `webhooks`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_WEBHOOKS(123);
   * ```
   */
  GET_SHOP_WEBHOOKS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks`;
  }

  /**
   * Add shop webhook endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/webhooks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhooksController::add` expose observed keys including `url`, `type`, `success`, `webhook`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_WEBHOOK(123);
   * ```
   */
  POST_ADD_SHOP_WEBHOOK(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks`;
  }

  /**
   * Update shop webhook endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/webhooks/{webhook_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param webhook_id - webhook_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhooksController::update` expose observed keys including `url`, `type`, `error`, `error_msg`, `success`, `webhook`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_SHOP_WEBHOOK(123, 123);
   * ```
   */
  PUT_UPDATE_SHOP_WEBHOOK(
    shop_id: string | number,
    webhook_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks/${webhook_id}`;
  }

  /**
   * Shop webhook calls endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/webhooks/{webhook_id}/calls
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param webhook_id - webhook_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhookCallsController::listOfCalls` expose observed keys including `error`, `error_msg`, `calls`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_WEBHOOK_CALLS(123, 123);
   * ```
   */
  GET_SHOP_WEBHOOK_CALLS(
    shop_id: string | number,
    webhook_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks/${webhook_id}/calls`;
  }

  /**
   * Shop webhook endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/webhooks/{webhook_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param webhook_id - webhook_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhooksController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_WEBHOOK(123, 123);
   * ```
   */
  DELETE_SHOP_WEBHOOK(shop_id: string | number, webhook_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks/${webhook_id}`;
  }

  /**
   * Shop webhook reset endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/webhooks/{webhook_id}/reset
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param webhook_id - webhook_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhookCallsController::resetWebhook` expose observed keys including `error`, `error_msg`, `fails`, `success`, `webhook`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WEBHOOK_RESET(123, 123);
   * ```
   */
  POST_SHOP_WEBHOOK_RESET(
    shop_id: string | number,
    webhook_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks/${webhook_id}/reset`;
  }

  /**
   * Shop webhook test endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/webhooks/{webhook_id}/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param webhook_id - webhook_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopWebhookTestController::testWebhook` expose observed keys including `error`, `error_msg`, `success`, `response`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_WEBHOOK_TEST(123, 123);
   * ```
   */
  POST_SHOP_WEBHOOK_TEST(
    shop_id: string | number,
    webhook_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/webhooks/${webhook_id}/test`;
  }

  //―――――――――――――――――――――― Business Model ――――――――――――――――――――
  /**
   * Set business model endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/business-model
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBusinessModelController::api_setShopBusinessModel` expose observed keys including `model`, `error`, `error_msg`, `status`, `reselling`, `marketplace`, `success`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_BUSINESS_MODEL(123);
   * ```
   */
  POST_SET_BUSINESS_MODEL(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/business-model`;
  }

  //―――――――――――――――――――――― Marketplace ――――――――――――――――――――

  /**
   * Shop vendor set setting endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/business-model/marketplace
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMarketplaceSettingController::updateShopMarketplaceSetting` expose observed keys including `enable`, `mode`, `product`, `need_verify`, `category`, `multi`, `hidden_customer`, `shipping`, `listing`, `documents`, `type`, `title`, `guide`, `code`, `link`, `marketplace`, `success`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VENDOR_SET_SETTING(123);
   * ```
   */
  POST_SHOP_VENDOR_SET_SETTING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/business-model/marketplace`;
  }

  /**
   * Shop vendors endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::MARKETPLACE
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::list` expose observed keys including `offset`, `limit`, `compact`, `error`, `error_msg`, `vendors`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDORS(123);
   * ```
   */
  GET_SHOP_VENDORS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors`;
  }

  /**
   * Shop add vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::add` expose observed keys including `enable`, `access`, `name`, `description`, `address`, `web`, `tel`, `bank`, `bank_info`, `user_id`, `page_id`, `slug`, `map_id`, `pricing_id`, `country`, `state`, `business`, `business_name`, `tax_id`, `invite`, `augment`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_ADD_VENDOR(123);
   * ```
   */
  POST_SHOP_ADD_VENDOR(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors`;
  }

  /**
   * Shop edit vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/vendors/{vendor_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::update` expose observed keys including `enable`, `access`, `name`, `description`, `address`, `web`, `tel`, `bank`, `bank_info`, `user_id`, `page_id`, `slug`, `map_id`, `pricing_id`, `country`, `state`, `business`, `business_name`, `tax_id`, `augment`, `error`, `error_msg`, `status`, `invite`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_USER_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_EDIT_VENDOR(123, 401);
   * ```
   */
  PUT_SHOP_EDIT_VENDOR(shop_id: string | number, vendor_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}`;
  }

  /**
   * Shop edit vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/vendors/{vendor_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_EDIT_VENDOR(123, 401);
   * ```
   */
  DELETE_SHOP_EDIT_VENDOR(
    shop_id: string | number,
    vendor_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}`;
  }

  /**
   * Shop vendor send invite email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/vendors/{vendor_id}/send-invitation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::send_invitation` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VENDOR_SEND_INVITE_EMAIL(123, 401);
   * ```
   */
  POST_SHOP_VENDOR_SEND_INVITE_EMAIL(
    shop_id: string | number,
    vendor_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}/send-invitation`;
  }

  /**
   * Upload vendor icon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/vendors/{vendor_id}/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsController::uploadIcon` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_UPLOAD_VENDOR_ICON(123, 401);
   * ```
   */
  POST_UPLOAD_VENDOR_ICON(
    shop_id: string | number,
    vendor_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}/icon`;
  }

  /**
   * Shop vendor pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/pricings
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPricingController::list` expose observed keys including `offset`, `limit`, `pricings`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_PRICINGS(123);
   * ```
   */
  GET_SHOP_VENDOR_PRICINGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pricings`;
  }

  /**
   * Shop add vendor pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pricings
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPricingController::add` expose observed keys including `commission`, `title`, `description`, `error`, `error_msg`, `success`, `pricing`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_ADD_VENDOR_PRICINGS(123);
   * ```
   */
  POST_SHOP_ADD_VENDOR_PRICINGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/pricings`;
  }

  /**
   * Shop edit vendor pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/pricings/{pricing_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param pricing_id - Pricing row identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPricingController::update` expose observed keys including `error`, `error_msg`, `title`, `description`, `commission`, `price`, `success`, `pricing`, `updated_products_count`, `updated_extra_pricings_count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_EDIT_VENDOR_PRICINGS(123, 123);
   * ```
   */
  PUT_SHOP_EDIT_VENDOR_PRICINGS(
    shop_id: string | number,
    pricing_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pricings/${pricing_id}`;
  }

  /**
   * Shop edit vendor pricings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/pricings/{pricing_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param pricing_id - Pricing row identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPricingController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_EDIT_VENDOR_PRICINGS(123, 123);
   * ```
   */
  DELETE_SHOP_EDIT_VENDOR_PRICINGS(
    shop_id: string | number,
    pricing_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pricings/${pricing_id}`;
  }

  /**
   * All vendor products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendor-products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductVendorsController::api_getListOfAllProductVendors` expose observed keys including `offset`, `limit`, `status`, `vendor_products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ALL_VENDOR_PRODUCTS(123);
   * ```
   */
  GET_ALL_VENDOR_PRODUCTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-products`;
  }

  /**
   * Product vendors endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVendorsController::list` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_VENDORS(123, 456);
   * ```
   */
  GET_PRODUCT_VENDORS(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/vendors`;
  }

  /**
   * Add product vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductVendorsController::add` expose observed keys including `vendor_id`, `variant_id`, `raw_price`, `price`, `pricing_id`, `lead`, `quantity`, `error`, `error_msg`, `success`, `vendor`, `vendor_product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_VARIANT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PRODUCT_VENDOR(123, 456);
   * ```
   */
  POST_ADD_PRODUCT_VENDOR(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/vendors`;
  }

  /**
   * Edit product vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/vendors/{vendor_product_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param vendor_product_id - vendor_product_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductVendorsController::update` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_PRODUCT_VENDOR(123, 456, 123);
   * ```
   */
  PUT_EDIT_PRODUCT_VENDOR(
    shop_id: string | number,
    product_id: string | number,
    vendor_product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/vendors/${vendor_product_id}`;
  }

  /**
   * Product vendor endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/vendors/{vendor_product_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param vendor_product_id - vendor_product_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductVendorsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`, `product`, `variant`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_VENDOR(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_VENDOR(
    shop_id: string | number,
    product_id: string | number,
    vendor_product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/vendors/${vendor_product_id}`;
  }

  /**
   * Shop vendor accounts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendor-accounts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorAccountsController::list` expose observed keys including `offset`, `limit`, `accounts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_ACCOUNTS(123);
   * ```
   */
  GET_SHOP_VENDOR_ACCOUNTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-accounts`;
  }

  /**
   * Shop vendor account transactions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendor-accounts/{account_id}/transactions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param account_id - Wallet/account identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorAccountTransactionsController::list` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `transactions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_ACCOUNT_TRANSACTIONS(123, 666);
   * ```
   */
  GET_SHOP_VENDOR_ACCOUNT_TRANSACTIONS(
    shop_id: string | number,
    account_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-accounts/${account_id}/transactions`;
  }

  /**
   * Shop vendor payments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendor-payments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPaymentsController::list` expose observed keys including `offset`, `limit`, `payments`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_PAYMENTS(123);
   * ```
   */
  GET_SHOP_VENDOR_PAYMENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-payments`;
  }

  /**
   * Shop vendor payment add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/vendor-payments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPaymentsController::add` expose observed keys including `currency`, `vendor_id`, `amount`, `note`, `option`, `error`, `error_msg`, `success`, `account`, `payment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VENDOR_PAYMENT_ADD(123);
   * ```
   */
  POST_SHOP_VENDOR_PAYMENT_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-payments`;
  }

  /**
   * Shop vendor payment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/vendor-payments/{payments_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param payment_id - payment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPaymentsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_VENDOR_PAYMENT(123, 123);
   * ```
   */
  DELETE_SHOP_VENDOR_PAYMENT(
    shop_id: string | number,
    payment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-payments/${payment_id}`;
  }

  /**
   * Shop vendor requests endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendor-requests
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorRequestsController::list` expose observed keys including `offset`, `limit`, `vendor_requests`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_REQUESTS(123);
   * ```
   */
  GET_SHOP_VENDOR_REQUESTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-requests`;
  }

  /**
   * Shop vendor requests accept endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/vendor-requests/{request_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param request_id - request_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorRequestsController::accept` expose observed keys including `accept`, `error`, `error_msg`, `accepted_at`, `rejected_at`, `success`, `vendor_request`, `vendor`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VENDOR_REQUESTS_ACCEPT(123, 123);
   * ```
   */
  POST_SHOP_VENDOR_REQUESTS_ACCEPT(
    shop_id: string | number,
    request_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-requests/${request_id}`;
  }

  /**
   * Shop vendor request attachment download link endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/vendor-requests/{request_id}/attachments/{attachment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param request_id - request_id route parameter identifier.
   * @param attachment_id - attachment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorRequestsController::download` expose observed keys including `error`, `error_msg`, `ResponseContentDisposition`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_REQUEST_ATTACHMENT_DOWNLOAD_LINK(123, 123, 123);
   * ```
   */
  GET_SHOP_VENDOR_REQUEST_ATTACHMENT_DOWNLOAD_LINK(
    shop_id: string | number,
    request_id: string | number,
    attachment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-requests/${request_id}/attachments/${attachment_id}`;
  }

  /**
   * Shop vendor payment options endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendors/{vendor_id}/payment-options
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPaymentOptionsController::options` expose observed keys including `with_balance`, `error`, `error_msg`, `vendor`, `options`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_PAYMENT_OPTIONS(123, 401);
   * ```
   */
  GET_SHOP_VENDOR_PAYMENT_OPTIONS(
    shop_id: string | number,
    vendor_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}/payment-options`;
  }

  /**
   * Shop vendor payment reverse transfer endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/vendor-payments/{payments_id}/revers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_VENDOR_PAYMENT_WRITE (backoffice:vendor-payment:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param payment_id - payment_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorPaymentsController::reversal` expose observed keys including `currency`, `amount`, `reason`, `error`, `error_msg`, `success`, `account`, `payment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VENDOR_PAYMENT_REVERSE_TRANSFER(123, 123);
   * ```
   */
  POST_SHOP_VENDOR_PAYMENT_REVERSE_TRANSFER(
    shop_id: string | number,
    payment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-payments/${payment_id}/revers`;
  }

  /**
   * Shop vendor finance download endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/vendors/{vendor_id}/finance/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorFinanceReportController::api_downloadVendorFinance` expose observed keys including `start`, `end`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VENDOR_FINANCE_DOWNLOAD(123, 401);
   * ```
   */
  GET_SHOP_VENDOR_FINANCE_DOWNLOAD(shop_id: string | number,vendor_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}/finance/excel`;
  }


  //―――――――――――――――――――――― Import Vendors ――――――――――――――――――――

  /**
   * Import vendors endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/import/vendors
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopVendorsImportController::import` expose observed keys including `dataset`, `error`, `error_msg`, `success`, `success_count`, `fail_count`, `errors`, `updated_count`, `added_count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_IMPORT_VENDORS(123);
   * ```
   */
  POST_IMPORT_VENDORS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/import/vendors`;
  }

  /**
   * Download all shop vendors endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/vendors/export/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopVendorsExportController::export` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DOWNLOAD_ALL_SHOP_VENDORS(123);
   * ```
   */
  GET_DOWNLOAD_ALL_SHOP_VENDORS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/export/excel`;
  }

  //―――――――――――――――――――――― 🦠 Connect ――――――――――――――――――――

  /**
   * Shop connects endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/connects
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectController::list` expose observed keys including `offset`, `limit`, `shop_connects`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECTS(123);
   * ```
   */
  GET_SHOP_CONNECTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects`;
  }

  /**
   * Connect services endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/connect-services
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectController::connectServices` expose observed keys including `offset`, `limit`, `connects`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CONNECT_SERVICES(123);
   * ```
   */
  GET_CONNECT_SERVICES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-services`;
  }

  /**
   * Shop connect info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/connects/{shop_connect_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectController::getShopConnectInfo` expose observed keys including `error`, `error_msg`, `service_name`, `shop_id`, `password`, `shop_connect`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECT_INFO(123, 123);
   * ```
   */
  GET_SHOP_CONNECT_INFO(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}`;
  }

  /**
   * Shop connect setup direct endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connects/{connect_code}/setup
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param connect_code - Code value passed as a route segment.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectSetupDirectController::api_setupDirectShopConnect` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_SETUP_DIRECT(123, "value");
   * ```
   */
  POST_SHOP_CONNECT_SETUP_DIRECT(
    shop_id: string | number,
    connect_code: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${connect_code}/setup`;
  }

  /**
   * Update shop connect endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/connects/{shop_connect_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectEditController::updateShopConnect` expose observed keys including `enable`, `auto_confirm`, `endpoint`, `shipping`, `overwrite`, `error`, `error_msg`, `user_id`, `test`, `shop_connect`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_SHOP_CONNECT(123, 123);
   * ```
   */
  PUT_UPDATE_SHOP_CONNECT(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}`;
  }

  /**
   * Shop connect endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/connects/{shop_connect_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectEditController::deleteShopConnect` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CONNECT(123, 123);
   * ```
   */
  DELETE_SHOP_CONNECT(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}`;
  }

  /**
   * Shop connect products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/connects/{shop_connect_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectProductsController::list` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECT_PRODUCTS(123, 123);
   * ```
   */
  GET_SHOP_CONNECT_PRODUCTS(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}/products`;
  }

  /**
   * Shop connect request sync endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/connects/{shop_connect_id}/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectSyncController::requestSync` expose observed keys including `reset`, `error`, `error_msg`, `shop_connect`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_REQUEST_SYNC(123, 123);
   * ```
   */
  POST_SHOP_CONNECT_REQUEST_SYNC(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}/sync`;
  }

  /**
   * Shop connect test endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/connects/{shop_connect_id}/test
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectSyncController::testConnection` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECT_TEST(123, 123);
   * ```
   */
  GET_SHOP_CONNECT_TEST(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}/test`;
  }

  /**
   * Shop connect orders endpoint URL.
   *
   * Builds a legacy SDK URL for listing connect orders.
   *
   * @endpoint GET /shops/{shop_id}/connects/{shop_connect_id}/orders
   * @auth TODO(api-docs): verify whether this SDK URL is stale before use.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - Shop connect identifier from the route path.
   * @returns Absolute URL string for the SDK connect orders path.
   *
   * Response shape:
   * - TODO(api-docs): unresolved route; no controller response can be documented as fact.
   *
   * Possible errors:
   * - TODO(api-docs): route may return 404 because no active Laravel route was found.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECT_ORDERS(123, 456);
   * ```
   */
  GET_SHOP_CONNECT_ORDERS(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}/orders`;
  }

  /**
   * Shop connect basket sync endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connect-orders/{shop_connect_order_id}/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_order_id - shop_connect_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectOrderController::requestSyncOrder` expose observed keys including `error`, `error_msg`, `success`, `connect_order`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_BASKET_SYNC(123, 123);
   * ```
   */
  POST_SHOP_CONNECT_BASKET_SYNC(
    shop_id: string | number,
    shop_connect_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-orders/${shop_connect_order_id}/sync`;
  }

  /**
   * Shop connect basket confirm endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connect-orders/{shop_connect_order_id}/confirm
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_order_id - shop_connect_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectOrderController::confirmOrder` expose observed keys including `error`, `error_msg`, `success`, `connect_order`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_BASKET_CONFIRM(123, 123);
   * ```
   */
  POST_SHOP_CONNECT_BASKET_CONFIRM(
    shop_id: string | number,
    shop_connect_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-orders/${shop_connect_order_id}/confirm`;
  }

  /**
   * Shop connect basket refresh endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connect-orders/{shop_connect_order_id}/refresh
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_order_id - shop_connect_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectOrderController::refreshOrder` expose observed keys including `error`, `error_msg`, `success`, `connect_order`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_BASKET_REFRESH(123, 123);
   * ```
   */
  POST_SHOP_CONNECT_BASKET_REFRESH(
    shop_id: string | number,
    shop_connect_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-orders/${shop_connect_order_id}/refresh`;
  }

  /**
   * Shop connect basket cancel endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connect-orders/{shop_connect_order_id}/cancel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_order_id - shop_connect_order_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectOrderController::cancelOrder` expose observed keys including `error`, `error_msg`, `success`, `connect_order`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_BASKET_CANCEL(123, 123);
   * ```
   */
  POST_SHOP_CONNECT_BASKET_CANCEL(
    shop_id: string | number,
    shop_connect_order_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-orders/${shop_connect_order_id}/cancel`;
  }

  /**
   * Shop connect logs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/connects/{shop_connect_id}/logs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param shop_connect_id - shop_connect_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectLogsController::list` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `logs`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONNECT_LOGS(123, 123);
   * ```
   */
  GET_SHOP_CONNECT_LOGS(
    shop_id: string | number,
    shop_connect_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connects/${shop_connect_id}/logs`;
  }

  /**
   * Shop connect sync product endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/connect-products/{product_id}/sync
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopConnectProductController::api_requestSyncProduct` expose observed keys including `error`, `error_msg`, `success`, `debug`, `product`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONNECT_SYNC_PRODUCT(123, 456);
   * ```
   */
  POST_SHOP_CONNECT_SYNC_PRODUCT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/connect-products/${product_id}/sync`;
  }

  //―――――――――――――――――――――― Shop > Valuation ――――――――――――――――――――
  /**
   * Shop valuations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/valuations
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopValuationsController::api_getValuationsList` expose observed keys including `offset`, `limit`, `valuations`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VALUATIONS(123);
   * ```
   */
  GET_SHOP_VALUATIONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/valuations`;
  }

  /**
   * Add shop valuation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/valuations
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopValuationsController::api_addValuation` expose observed keys including `title`, `structure`, `conditions`, `success`, `valuation`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_SHOP_VALUATION(123);
   * ```
   */
  POST_ADD_SHOP_VALUATION(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/valuations`;
  }

  /**
   * Edit shop valuation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/valuations/{valuation_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param valuation_id - valuation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopValuationsController::api_editValuation` expose observed keys including `title`, `structure`, `conditions`, `error`, `error_msg`, `shop_id`, `user_id`, `success`, `valuation`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_VALUATION(123, 123);
   * ```
   */
  PUT_EDIT_SHOP_VALUATION(
    shop_id: string | number,
    valuation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/valuations/${valuation_id}`;
  }

  /**
   * Shop valuation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/valuations/{valuation_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::ACCOUNTING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param valuation_id - valuation_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopValuationsController::api_deleteValuation` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_VALUATION(123, 123);
   * ```
   */
  DELETE_SHOP_VALUATION(
    shop_id: string | number,
    valuation_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/valuations/${valuation_id}`;
  }

  /**
   * Set product valuation endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/valuation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductValuationController::api_setProductValuation` expose observed keys including `valuation_id`, `error`, `error_msg`, `price_input`, `success`, `valuation`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_PRODUCT_VALUATION(123, 456);
   * ```
   */
  POST_SET_PRODUCT_VALUATION(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/valuation`;
  }

  //―――――――――――――――――――――― Link preview ――――――――――――――――――――
  /**
   * Link preview endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /iframe/preview
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `EmbedController::getEmbedPreview` expose observed keys including `url`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_LINK_PREVIEW();
   * ```
   */
  GET_LINK_PREVIEW() {
    return `${this.selldone_api_url}/iframe/preview`;
  }

  //―――――――――――――――――――――― Article > Comments ――――――――――――――――――――
  /**
   * Shop comments endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/comments
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::listOfComments` expose observed keys including `offset`, `limit`, `comments`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_COMMENTS(123);
   * ```
   */
  GET_SHOP_COMMENTS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments`;
  }

  /**
   * Shop comment reply endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/comments/{comment_id}/reply
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::commentReply` expose observed keys including `reply`, `success`, `comment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_COMMENT_REPLY(123, 302);
   * ```
   */
  PUT_SHOP_COMMENT_REPLY(
    shop_id: string | number,
    comment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments/${comment_id}/reply`;
  }

  /**
   * Shop comment reviewed endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/comments/{comment_id}/review
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::commentReview` expose observed keys including `reviewed`, `success`, `comment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_COMMENT_REVIEWED(123, 302);
   * ```
   */
  PUT_SHOP_COMMENT_REVIEWED(
    shop_id: string | number,
    comment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments/${comment_id}/review`;
  }

  /**
   * Shop comment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint DELETE /shops/{shop_id}/comments/{comment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::commentDelete` expose observed keys including `reply`, `deleted_at`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_COMMENT(123, 302);
   * ```
   */
  DELETE_SHOP_COMMENT(shop_id: string | number, comment_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments/${comment_id}`;
  }

  /**
   * Shop comment detail endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/comments/{comment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::commentDetail` expose observed keys including `comment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_COMMENT_DETAIL(123, 302);
   * ```
   */
  GET_SHOP_COMMENT_DETAIL(
    shop_id: string | number,
    comment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments/${comment_id}`;
  }

  /**
   * Shop comment restore endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/comments/{comment_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCommentsController::commentRestore` expose observed keys including `error`, `error_msg`, `success`, `comment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_COMMENT_RESTORE(123, 302);
   * ```
   */
  POST_SHOP_COMMENT_RESTORE(
    shop_id: string | number,
    comment_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/comments/${comment_id}/restore`;
  }

  //█████████████████████████████████████████████████████████████
  //――――――――――――――――――――――――――― Partners ―――――――――――――――――――――――――
  //█████████████████████████████████████████████████████████████

  /**
   * Send request partnership endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /partnership
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnersPublicController::addRequest` expose observed keys including `type`, `business`, `registered`, `country`, `state`, `size`, `language`, `message`, `external_service`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_REQUEST_PARTNERSHIP();
   * ```
   */
  POST_SEND_REQUEST_PARTNERSHIP() {
    return `${this.selldone_api_url}/partnership`;
  }

  /**
   * Send request supplier endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /supplier
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SuppliersPublicController::addRequest` expose observed keys including `business`, `country`, `language`, `message`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SEND_REQUEST_SUPPLIER();
   * ```
   */
  POST_SEND_REQUEST_SUPPLIER() {
    return `${this.selldone_api_url}/supplier`;
  }

  //█████████████████████████████████████████████████████████████
  //――――――――――――――――――――――――――― Logging ―――――――――――――――――――――――――
  //█████████████████████████████████████████████████████████████
  /**
   * Shop loggings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/logging
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::AUTOMATION
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLoggingController::list` expose observed keys including `loggings`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LOGGINGS(123);
   * ```
   */
  GET_SHOP_LOGGINGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/logging`;
  }

  //█████████████████████████████████████████████████████████████
  //―――――――――――――――――――――――― Logistic Profiles ―――――――――――――――――――――――
  //█████████████████████████████████████████████████████████████
  /**
   * Shop logistic profiles endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/logistic-profiles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopLogisticProfilesController::list` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LOGISTIC_PROFILES(123);
   * ```
   */
  GET_SHOP_LOGISTIC_PROFILES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles`;
  }

  /**
   * Shop create logistic profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/logistic-profiles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesController::add` expose observed keys including `type`, `language`, `name`, `title`, `body`, `error`, `error_msg`, `success`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CREATE_LOGISTIC_PROFILE(123);
   * ```
   */
  POST_SHOP_CREATE_LOGISTIC_PROFILE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles`;
  }

  /**
   * Shop logistic profile edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/logistic-profiles/{profile_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesController::edit` expose observed keys including `name`, `error`, `error_msg`, `success`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_LOGISTIC_PROFILE_EDIT(123, 123);
   * ```
   */
  PUT_SHOP_LOGISTIC_PROFILE_EDIT(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}`;
  }

  /**
   * Shop logistic profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/logistic-profiles/{profile_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LOGISTIC_PROFILE(123, 123);
   * ```
   */
  DELETE_SHOP_LOGISTIC_PROFILE(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}`;
  }

  /**
   * Shop logistic profile endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/logistic-profiles/{profile_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesController::profileInfo` expose observed keys including `error`, `error_msg`, `profile`, `articles`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LOGISTIC_PROFILE(123, 123);
   * ```
   */
  GET_SHOP_LOGISTIC_PROFILE(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}`;
  }

  /**
   * Shop logistic profile set article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/logistic-profiles/{profile_id}/articles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesArticlesController::setArticle` expose observed keys including `article_id`, `language`, `title`, `body`, `error`, `error_msg`, `success`, `article`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LOGISTIC_PROFILE_SET_ARTICLE(123, 123);
   * ```
   */
  POST_SHOP_LOGISTIC_PROFILE_SET_ARTICLE(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}/articles`;
  }

  /**
   * Shop logistic profile article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/logistic-profiles/{profile_id}/articles/{article_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_REPORT_READ (backoffice:report:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesArticlesController::deleteArticle` expose observed keys including `error`, `error_msg`, `success`, `id`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LOGISTIC_PROFILE_ARTICLE(123, 123, 301);
   * ```
   */
  DELETE_SHOP_LOGISTIC_PROFILE_ARTICLE(
    shop_id: string | number,
    profile_id: string | number,
    article_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}/articles/${article_id}`;
  }

  /**
   * Shop logistic profile products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/logistic-profiles/{profile_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesProductsController::list` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LOGISTIC_PROFILE_PRODUCTS(123, 123);
   * ```
   */
  GET_SHOP_LOGISTIC_PROFILE_PRODUCTS(
    shop_id: string | number,
    profile_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}/products`;
  }

  /**
   * Shop logistic profile auto translate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/logistic-profiles/{profile_id}/articles/{article_id}/translate-article
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_BUSINESS_PROFILE_WRITE (backoffice:business-profile:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param profile_id - Profile identifier used by the route; when omitted in this SDK method, the backend uses the main profile variant.
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLogisticProfilesTranslateController::autoTranslateProfile` expose observed keys including `language`, `error`, `error_msg`, `supported`, `keyFilePath`, `source`, `target`, `format`, `success`, `article`, `profile`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LOGISTIC_PROFILE_AUTO_TRANSLATE(123, 123, 301);
   * ```
   */
  POST_SHOP_LOGISTIC_PROFILE_AUTO_TRANSLATE(
    shop_id: string | number,
    profile_id: string | number,
    article_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/logistic-profiles/${profile_id}/articles/${article_id}/translate-article`;
  }

  //█████████████████████████████████████████████████████████████
  //――――――――――――――――――――――――――― Article ―――――――――――――――――――――――――
  //█████████████████████████████████████████████████████████████

  //―――――――――――――――――――――― SEO ――――――――――――――――――――
  /**
   * Article SEO audit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /shops/{shop_id}/seo-audit/articles/{article_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopSEOAuditingController::auditArticle` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_ARTICLE_SEO_AUDIT(123, 301);
   * ```
   */
  GET_ARTICLE_SEO_AUDIT(shop_id: string | number, article_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/seo-audit/articles/${article_id}`;
  }

  //―――――――――――――――――――――― Import word file ――――――――――――――――――――
  /**
   * Shop converter word html endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/converter/word/html
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `BlogImportController::importWordForShop` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CONVERTER_WORD_HTML(123);
   * ```
   */
  POST_SHOP_CONVERTER_WORD_HTML(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/converter/word/html`;
  }

  //―――――――――――――――――――――― Translate ――――――――――――――――――――
  /**
   * Translate product article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/translate-article
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductTranslateController::api_productTranslateArticle` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TRANSLATE_PRODUCT_ARTICLE(123, 456);
   * ```
   */
  POST_TRANSLATE_PRODUCT_ARTICLE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/translate-article`;
  }

  //―――――――――――――――――――――― User Feedback ――――――――――――――――――――
  /**
   * Like article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /articles/{article_id}/like
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_PROFILE_READ (profile)
   * @permission None found in matched controller method by static inspection.
   *
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::likeArticle` expose observed keys including `liked`, `user_id`, `article_id`, `error`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_LIKE_ARTICLE(301);
   * ```
   */
  POST_LIKE_ARTICLE(article_id: string | number) {
    return `${this.selldone_api_url}/articles/${article_id}/like`;
  }

  /**
   * Star article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /articles/{article_id}/star
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_USER_PROFILE_READ (profile)
   * @permission None found in matched controller method by static inspection.
   *
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::starArticle` expose observed keys including `stared`, `user_id`, `article_id`, `error`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_STAR_ARTICLE(301);
   * ```
   */
  POST_STAR_ARTICLE(article_id: string | number) {
    return `${this.selldone_api_url}/articles/${article_id}/star`;
  }

  /**
   * Claps of article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /articles/{article_id}/claps
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PowerController::apiGetPowerReadArticle` expose observed keys including `error`, `add`, `power`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CLAPS_OF_ARTICLE(301);
   * ```
   */
  POST_CLAPS_OF_ARTICLE(article_id: string | number) {
    return `${this.selldone_api_url}/articles/${article_id}/claps`;
  }

  /**
   * Report article endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /articles/{article_id}/report/{category}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param article_id - Article identifier used by the route.
   * @param report - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::apiReportArticle` expose observed keys including `error`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_REPORT_ARTICLE(301, "value");
   * ```
   */
  POST_REPORT_ARTICLE(
    article_id: string | number,
    report: keyof typeof ArticleReport.Categories,
  ) {
    return `${this.selldone_api_url}/articles/${article_id}/report/${report}`;
  }

  //―――――――――――――――――――――― Timeline ――――――――――――――――――――
  /**
   * Shop articles timeline endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /shops/{shop_id}/timeline/articles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ArticleController::getShopArticleTimeLine` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_ARTICLES_TIMELINE(123);
   * ```
   */
  GET_SHOP_ARTICLES_TIMELINE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/timeline/articles`;
  }

  //―――――――――――――――――――――― Edit ――――――――――――――――――――
  /**
   * Add or edit article endpoint URL.
   *
   * Builds one of the article edit routes selected by `type`.
   *
   * @endpoint POST /article/product/edit
   * @endpoint POST /article/shop-blog/edit
   * @endpoint POST /api/article/blog/edit
   * @endpoint POST /api/article/help/edit
   * @auth Required; authenticated API token or staff/admin web session depending on `type` route.
   * @middleware `api-selldone`/`Authenticate:api` for shop article routes; `web`/staff middleware for blog/help routes.
   * @scope Type-specific; product/shop-blog routes enforce shop/article scopes in their controllers.
   * @permission Type-specific shop/staff permission.
   *
   * @param type - Article type route discriminator such as `product`, `shop-blog`, `blog`, or `help`.
   * @returns Absolute URL string for the selected article edit endpoint.
   *
   * Response shape:
   * - Backend returns the created/updated {@link Article} payload as implemented by the selected controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing required session/token.
   * - 403 token_has_no_access/forbidden: missing required scope or staff/shop permission.
   * - 404 not_found: article, product, shop, or help/blog context is not found.
   * - 422 validation_error: article payload fails validation.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_EDIT_ARTICLE("product");
   * ```
   */
  POST_ADD_EDIT_ARTICLE(type: Article.TypeValue) {
    return `${this.selldone_api_url}/article/${type}/edit`;
  }

  /**
   * Article body auto fix endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /articles/fix
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleFixController::api_fixBody` expose observed keys including `body`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ARTICLE_BODY_AUTO_FIX();
   * ```
   */
  POST_ARTICLE_BODY_AUTO_FIX() {
    return `${this.selldone_api_url}/articles/fix`;
  }

  /**
   * Shop all products article body auto fix endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/bulk-actions/fix-articles
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleFixController::api_fixAllProductsArticlesBody` expose observed keys including `ids`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_ALL_PRODUCTS_ARTICLE_BODY_AUTO_FIX(123);
   * ```
   */
  POST_SHOP_ALL_PRODUCTS_ARTICLE_BODY_AUTO_FIX(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/bulk-actions/fix-articles`;
  }


  //―――――――――――――――――――――― Upload ――――――――――――――――――――
  /**
   * Upload article image endpoint URL.
   *
   * Builds one of the article image upload routes selected by `type`. Product article uploads require the extra shop id segment.
   *
   * @endpoint POST /article/product/upload/{shop_id}
   * @endpoint POST /api/article/blog/upload
   * @endpoint POST /api/article/help/upload
   * @auth Required; authenticated API token or staff/admin web session depending on `type` route.
   * @middleware `api-selldone`/`Authenticate:api` for product route; `web`/staff middleware for blog/help routes.
   * @scope Type-specific; product route uses shop/article permissions, blog/help routes use staff/admin access.
   * @permission Type-specific shop/staff permission.
   *
   * @param type - Article type route discriminator.
   * @param extra - Optional extra route segment; for `product`, pass the `shop_id` required by the backend route.
   * @returns Absolute URL string for the selected article image upload endpoint.
   *
   * Response shape:
   * - Backend returns uploaded image/file metadata from the selected controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing required session/token.
   * - 403 forbidden: missing required scope or staff/shop permission.
   * - 422 validation_error: uploaded file fails validation.
   *
   * @example
   * ```ts
   * const url = API.UPLOAD_ARTICLE_IMAGE("product", 123);
   * ```
   */
  UPLOAD_ARTICLE_IMAGE(type: Article.TypeValue, extra = null) {
    if (!extra) return `${this.selldone_api_url}/article/${type}/upload`;
    return `${this.selldone_api_url}/article/${type}/upload/${extra}`;
  }

  /**
   * Article blog image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/blogs/upload
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBlogController::uploadArticleImageBlog` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.UPLOAD_ARTICLE_BLOG_IMAGE(123);
   * ```
   */
  UPLOAD_ARTICLE_BLOG_IMAGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/blogs/upload`;
  }

  //―――――――――――――――――――――― Delete ――――――――――――――――――――
  /**
   * Delete article endpoint URL.
   *
   * Builds one of the article delete routes selected by `type`.
   *
   * @endpoint DELETE /article/product/{article_id}
   * @endpoint DELETE /article/shop-blog/{article_id}
   * @endpoint DELETE /api/article/blog/{article_id}
   * @endpoint DELETE /api/article/help/{article_id}
   * @auth Required; authenticated API token or staff/admin web session depending on `type` route.
   * @middleware `api-selldone`/`Authenticate:api` for shop article routes; `web`/staff middleware for blog/help routes.
   * @scope Type-specific.
   * @permission Type-specific shop/staff permission.
   *
   * @param type - Article type route discriminator.
   * @param article_id - Article identifier passed to the selected route.
   * @returns Absolute URL string for the selected article delete endpoint.
   *
   * Response shape:
   * - Backend returns deletion result from the selected controller, usually including `success` and article/deleted id data.
   *
   * Possible errors:
   * - 401 unauthenticated: missing required session/token.
   * - 403 forbidden: missing required scope or permission.
   * - 404 not_found: article is not found.
   *
   * @example
   * ```ts
   * const url = API.DELETE_ARTICLE("product", 301);
   * ```
   */
  DELETE_ARTICLE(type: Article.TypeValue, article_id: string | number) {
    return `${this.selldone_api_url}/article/${type}/${article_id}`;
  }

  //―――――――――――――――――――――― Tags ――――――――――――――――――――
  /**
   * Shop article tags endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/articles/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopBlogController::api_getShopArticleTags` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_ARTICLE_TAGS(123);
   * ```
   */
  GET_SHOP_ARTICLE_TAGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/articles/tags`;
  }

  /**
   * Change tag endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/articles/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopBlogController::api_editShopArticleTag` expose observed keys including `new`, `old`, `tag`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_CHANGE_TAG(123);
   * ```
   */
  PUT_CHANGE_TAG(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/articles/tags`;
  }

  /**
   * Set shop article tags endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/articles/tags/{article_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_ARTICLES (articles)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ArticleController::api_setShopArticleTags` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_ARTICLE_TAGS(123, 301);
   * ```
   */
  POST_SET_SHOP_ARTICLE_TAGS(
    shop_id: string | number,
    article_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/articles/tags/${article_id}`;
  }

  //―――――――――――――――――――――― Comments ――――――――――――――――――――
  /**
   * Add comment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /article/{article_id}/comment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::addComment` expose observed keys including `body`, `error`, `error_msg`, `id`, `name`, `comments_count`, `new_comments_count`, `comment`, `count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_COMMENT(301);
   * ```
   */
  POST_ADD_COMMENT(article_id: string | number) {
    return `${this.selldone_api_url}/article/${article_id}/comment`;
  }

  /**
   * Update comment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /comment/{comment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::updateComment` expose observed keys including `body`, `error`, `error_msg`, `comment`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_UPDATE_COMMENT(302);
   * ```
   */
  PUT_UPDATE_COMMENT(comment_id: string | number) {
    return `${this.selldone_api_url}/comment/${comment_id}`;
  }

  /**
   * Comment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   *
   * @endpoint DELETE /comment/{comment_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param comment_id - Comment identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ArticleController::deleteComment` expose observed keys including `success`, `comment`, `count`, `product_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMMENT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_COMMENT(302);
   * ```
   */
  DELETE_COMMENT(comment_id: string | number) {
    return `${this.selldone_api_url}/comment/${comment_id}`;
  }

  //―――――――――――――――――――――― Follow ――――――――――――――――――――
  /**
   * Follow user endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /user/follow/{following_user_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_USER_PROFILE_READ (profile)
   * @permission None found in matched controller method by static inspection.
   *
   * @param user_id - User identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `FollowController::follow` expose observed keys including `follow`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_VERIFIED_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_FOLLOW_USER(102);
   * ```
   */
  POST_FOLLOW_USER(user_id: string | number) {
    return `${this.selldone_api_url}/user/follow/${user_id}`;
  }

  //―――――――――――――――――――――― Providers (Connect OS) ――――――――――――――――――――
  /**
   * My providers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /providers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersController::list` expose observed keys including `offset`, `limit`, `providers`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PROVIDERS();
   * ```
   */
  GET_MY_PROVIDERS() {
    return `${this.selldone_api_url}/providers`;
  }

  /**
   * Add provider endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /providers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersAddController::add` expose observed keys including `name`, `phone`, `web`, `mode`, `enable`, `description`, `read_categories`, `write_categories`, `read_products`, `write_products`, `read_orders`, `write_orders`, `read_customers`, `write_customers`, `confirm`, `error`, `error_msg`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PROVIDER();
   * ```
   */
  POST_ADD_PROVIDER() {
    return `${this.selldone_api_url}/providers`;
  }

  /**
   * My provider update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /providers/{provider_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersEditController::edit` expose observed keys including `name`, `phone`, `web`, `mode`, `enable`, `description`, `read_categories`, `write_categories`, `read_products`, `write_products`, `read_orders`, `write_orders`, `read_customers`, `write_customers`, `confirm`, `error`, `error_msg`, `info`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_PROVIDER_UPDATE(123);
   * ```
   */
  PUT_MY_PROVIDER_UPDATE(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}`;
  }

  /**
   * My provider upload icon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /providers/{provider_id}/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersEditController::uploadIcon` expose observed keys including `photo`, `error`, `error_msg`, `icon`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PROVIDER_UPLOAD_ICON(123);
   * ```
   */
  POST_MY_PROVIDER_UPLOAD_ICON(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/icon`;
  }

  /**
   * My provider info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /providers/{provider_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersController::info` expose observed keys including `error`, `error_msg`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PROVIDER_INFO(123);
   * ```
   */
  GET_MY_PROVIDER_INFO(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}`;
  }

  /**
   * My provider auth endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /providers/{provider_id}/auth
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersAuthController::setAuth` expose observed keys including `mode`, `login_url`, `token_url`, `client_id`, `client_secret`, `error`, `error_msg`, `auth`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_PROVIDER_AUTH(123);
   * ```
   */
  PUT_MY_PROVIDER_AUTH(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/auth`;
  }

  /**
   * My provider webhooks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /providers/{provider_id}/webhooks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersWebhookController::setWebhook` expose observed keys including `notify_shop`, `sync_category`, `sync_product`, `sync_customers`, `create_order`, `get_order`, `confirm_order`, `cancel_order`, `error`, `error_msg`, `secret`, `webhooks`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_PROVIDER_WEBHOOKS(123);
   * ```
   */
  PUT_MY_PROVIDER_WEBHOOKS(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/webhooks`;
  }

  /**
   * My provider webhooks regenerate sign key endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /providers/{provider_id}/webhooks/invalidate-sign-key
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersWebhookController::resetWebhookSecret` expose observed keys including `error`, `error_msg`, `webhooks`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PROVIDER_WEBHOOKS_REGENERATE_SIGN_KEY(123);
   * ```
   */
  POST_MY_PROVIDER_WEBHOOKS_REGENERATE_SIGN_KEY(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/webhooks/invalidate-sign-key`;
  }

  /**
   * My provider secret regenerate sign key endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /providers/{provider_id}/invalidate-secret-key
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersEditController::resetSecret` expose observed keys including `error`, `error_msg`, `secret`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PROVIDER_SECRET_REGENERATE_SIGN_KEY(123);
   * ```
   */
  POST_MY_PROVIDER_SECRET_REGENERATE_SIGN_KEY(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/invalidate-secret-key`;
  }

  /**
   * My provider shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /providers/{provider_id}/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ConnectProviderApiShopsController::api_list` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PROVIDER_SHOPS(123);
   * ```
   */
  GET_MY_PROVIDER_SHOPS(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/shops`;
  }

  /**
   * My provider pay25usd and create bridge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /providers/{provider_id}/pay
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersPayController::payAndCreateBridge` expose observed keys including `account_number`, `error`, `error_msg`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PROVIDER_PAY25USD_AND_CREATE_BRIDGE(123);
   * ```
   */
  POST_MY_PROVIDER_PAY25USD_AND_CREATE_BRIDGE(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/pay`;
  }

  /**
   * My provider request verify endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /providers/{provider_id}/request-verify
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProvidersEditController::requestReview` expose observed keys including `error`, `error_msg`, `reviewing`, `success`, `provider`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PROVIDER_REQUEST_VERIFY(123);
   * ```
   */
  POST_MY_PROVIDER_REQUEST_VERIFY(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/request-verify`;
  }

  /**
   * My provider logs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /providers/{provider_id}/logs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_CONNECT_PROVIDER_WRITE (connect:provider:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param provider_id - provider_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ConnectProviderLogsController::list` expose observed keys including `error`, `error_msg`, `offset`, `limit`, `logs`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PROVIDER_LOGS(123);
   * ```
   */
  GET_MY_PROVIDER_LOGS(provider_id: string | number) {
    return `${this.selldone_api_url}/providers/${provider_id}/logs`;
  }

  //―――――――――――――――――――――― Layouts (Custom storefront & backoffice) ――――――――――――――――――――

  /**
   * Public layouts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /layouts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_DEVELOPER_READ (selldone:developer:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PublicLayoutsController::api_getPublicLayouts` expose observed keys including `offset`, `limit`, `layouts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PUBLIC_LAYOUTS();
   * ```
   */
  GET_PUBLIC_LAYOUTS() {
    return `${this.selldone_api_url}/layouts`;
  }

  /**
   * Set shop layout endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/layout
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopLayoutController::api_setShopLayout` expose observed keys including `layout_id`, `success`, `layout`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_LAYOUT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SET_SHOP_LAYOUT(123);
   * ```
   */
  POST_SET_SHOP_LAYOUT(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/layout`;
  }

  /**
   * My layouts endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /developer/layouts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_DEVELOPER_READ (selldone:developer:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DeveloperLayoutsController::list` expose observed keys including `offset`, `limit`, `contain`, `layouts`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_LAYOUTS();
   * ```
   */
  GET_MY_LAYOUTS() {
    return `${this.selldone_api_url}/developer/layouts`;
  }

  /**
   * My layout info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /developer/layouts/{layout_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_DEVELOPER_READ (selldone:developer:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param layout_id - layout_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DeveloperLayoutsController::info` expose observed keys including `error`, `error_msg`, `layout`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_LAYOUT_INFO(123);
   * ```
   */
  GET_MY_LAYOUT_INFO(layout_id: string | number) {
    return `${this.selldone_api_url}/developer/layouts/${layout_id}`;
  }

  /**
   * My layout deploys endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /developer/layouts/{layout_id}/deploys
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_DEVELOPER_READ (selldone:developer:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param layout_id - layout_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `DeveloperLayoutDeploysController::list` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `deploys`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_LAYOUT_DEPLOYS(123);
   * ```
   */
  GET_MY_LAYOUT_DEPLOYS(layout_id: string | number) {
    return `${this.selldone_api_url}/developer/layouts/${layout_id}/deploys`;
  }

  //―――――――――――――――――――――― Container (Custom instances) ――――――――――――――――――――
  GET_CONTAINER_SETUP_PAGE(params: {
    return_url: string;
    domain?: string | null;
    name?: string | null;
    shop_id?: number | string | null;
  }) {
    const query = new URLSearchParams();

    query.set("return_url", params.return_url);

    if (params.domain) {
      query.set("domain", params.domain);
    }

    if (params.name) {
      query.set("name", params.name);
    }

    if (params.shop_id) {
      query.set("shop_id", String(params.shop_id));
    }

    return `${SetupService.MainServiceUrl()}/container/setup?${query.toString()}`;
  }

  /**
   * Shop containers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/containers
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContainerManageController::api_getContainers` expose observed keys including `containers`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CONTAINERS(123);
   * ```
   */
  GET_SHOP_CONTAINERS(shop_id: number | string) {
    return `${this.selldone_api_url}/shops/${shop_id}/containers`;
  }

  /**
   * Shop container endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/containers/{container_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param container_id - container_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopContainerManageController::api_deleteContainer` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CONTAINER(123, 123);
   * ```
   */
  DELETE_SHOP_CONTAINER(
    shop_id: number | string,
    container_id: number | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/containers/${container_id}`;
  }

  //―――――――――――――――――――――― Shop > Map Search (tags) ――――――――――――――――――――
  /**
   * Shop map tags endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/map/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMapController::list` expose observed keys including `offset`, `limit`, `map_tags`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MAP_TAGS(123);
   * ```
   */
  GET_SHOP_MAP_TAGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/map/tags`;
  }

  /**
   * Create shop map tag endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/map/tags
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMapController::create` expose observed keys including `tags`, `title`, `address`, `location`, `no`, `unit`, `name`, `phone`, `message`, `postal`, `country`, `state`, `city`, `range`, `success`, `map_tag`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CREATE_SHOP_MAP_TAG(123);
   * ```
   */
  POST_CREATE_SHOP_MAP_TAG(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/map/tags`;
  }

  /**
   * Edit shop map tag endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/map/tags/{map_tag_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param map_tag_id - map_tag_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMapController::edit` expose observed keys including `tags`, `title`, `address`, `location`, `no`, `unit`, `name`, `phone`, `message`, `postal`, `country`, `state`, `city`, `range`, `error`, `error_msg`, `user_id`, `lat`, `lng`, `success`, `map_tag`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_SHOP_MAP_TAG(123, 123);
   * ```
   */
  PUT_EDIT_SHOP_MAP_TAG(shop_id: string | number, map_tag_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/map/tags/${map_tag_id}`;
  }

  /**
   * Shop map tag endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/map/tags/{map_tag_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param map_tag_id - map_tag_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMapController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_MAP_TAG(123, 123);
   * ```
   */
  DELETE_SHOP_MAP_TAG(shop_id: string | number, map_tag_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/map/tags/${map_tag_id}`;
  }

  /**
   * Shop map tag products endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/map/tags/{map_tag_id}/products
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_GIFTCARD_WRITE (backoffice:giftcard:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param map_tag_id - map_tag_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopMapController::products` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `products`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_MAP_TAG_PRODUCTS(123, 123);
   * ```
   */
  GET_SHOP_MAP_TAG_PRODUCTS(
    shop_id: string | number,
    map_tag_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/map/tags/${map_tag_id}/products`;
  }

  //―――――――――――――――――――――― User > Participants ――――――――――――――――――――
  /**
   * My participants endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /participants
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ParticipantsController::list` expose observed keys including `offset`, `limit`, `search`, `participants`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_PARTICIPANTS();
   * ```
   */
  GET_MY_PARTICIPANTS() {
    return `${this.selldone_api_url}/participants`;
  }

  /**
   * My participants add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /participants
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ParticipantsController::add` expose observed keys including `link`, `error`, `error_msg`, `success`, `participant`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PARTICIPANTS_ADD();
   * ```
   */
  POST_MY_PARTICIPANTS_ADD() {
    return `${this.selldone_api_url}/participants`;
  }

  /**
   * My participants play game endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /participants/{participant_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param participant_id - participant_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ParticipantsController::play` expose observed keys including `card`, `account_number`, `error`, `error_msg`, `amount`, `currency`, `account_id`, `transaction_id`, `success`, `participant`, `win_amount`, `win_currency`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::TRANSACTION_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_PARTICIPANTS_PLAY_GAME(123);
   * ```
   */
  POST_MY_PARTICIPANTS_PLAY_GAME(participant_id: string | number) {
    return `${this.selldone_api_url}/participants/${participant_id}`;
  }

  //―――――――――――――――――――――― User > Agency affiliate deals ――――――――――――――――――――
  /**
   * My affiliate deals endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /affiliate-deals
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MyAffiliateDealsController::list` expose observed keys including `offset`, `limit`, `search`, `affiliates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AFFILIATE_DEALS();
   * ```
   */
  GET_MY_AFFILIATE_DEALS() {
    return `${this.selldone_api_url}/affiliate-deals`;
  }

  //―――――――――――――――――――――― Shop > Metaverse ――――――――――――――――――――
  /**
   * Metaverse lands add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/metaverse/lands
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::APPLICATIONS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MetaverseLandsController::add` expose observed keys including `name`, `enable`, `config`, `structure`, `error`, `error_msg`, `success`, `land`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_METAVERSE_LANDS_ADD(123);
   * ```
   */
  POST_METAVERSE_LANDS_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/metaverse/lands`;
  }

  /**
   * Metaverse lands edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/metaverse/lands/{land_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::APPLICATIONS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param land_id - land_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MetaverseLandsController::edit` expose observed keys including `name`, `enable`, `config`, `structure`, `error`, `error_msg`, `user_id`, `success`, `land`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_METAVERSE_LANDS_EDIT(123, 123);
   * ```
   */
  PUT_METAVERSE_LANDS_EDIT(shop_id: string | number, land_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/metaverse/lands/${land_id}`;
  }

  /**
   * Metaverse lands endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/metaverse/lands
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::APPLICATIONS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param land_id - land_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `MetaverseLandsController::list` expose observed keys including `offset`, `limit`, `lands`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_METAVERSE_LANDS(123, 123);
   * ```
   */
  GET_METAVERSE_LANDS(shop_id: string | number, land_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/metaverse/lands`;
  }

  //―――――――――――――――――――――― 🎗️ Product > Subscription Prices ――――――――――――――――――――

  /**
   * Product add subscription price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/subscription-prices
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonProductsController::addSubscriptionPrice` expose observed keys including `currency`, `title`, `description`, `enable`, `period`, `price`, `sku`, `gateway_code`, `error`, `error_msg`, `success`, `subscription_price`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_ADD_SUBSCRIPTION_PRICE(123, 456);
   * ```
   */
  POST_PRODUCT_ADD_SUBSCRIPTION_PRICE(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/subscription-prices`;
  }

  /**
   * Product edit subscription price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/subscription-prices/{price_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param price_id - price_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonProductsController::editSubscriptionPrice` expose observed keys including `currency`, `title`, `description`, `enable`, `period`, `price`, `sku`, `gateway_code`, `error`, `error_msg`, `user_id`, `success`, `subscription_price`, `extra`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_EDIT_SUBSCRIPTION_PRICE(123, 456, 123);
   * ```
   */
  PUT_PRODUCT_EDIT_SUBSCRIPTION_PRICE(
    shop_id: string | number,
    product_id: string | number,
    price_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/subscription-prices/${price_id}`;
  }

  /**
   * Product edit subscription price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/subscription-prices/{price_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param price_id - price_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonProductsController::deleteSubscriptionPrice` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_EDIT_SUBSCRIPTION_PRICE(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_EDIT_SUBSCRIPTION_PRICE(
    shop_id: string | number,
    product_id: string | number,
    price_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/subscription-prices/${price_id}`;
  }

  /**
   * Restore product edit subscription price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/subscription-prices/{price_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param price_id - price_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonProductsController::restoreSubscriptionPrice` expose observed keys including `error`, `error_msg`, `success`, `subscription_price`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RESTORE_PRODUCT_EDIT_SUBSCRIPTION_PRICE(123, 456, 123);
   * ```
   */
  POST_RESTORE_PRODUCT_EDIT_SUBSCRIPTION_PRICE(
    shop_id: string | number,
    product_id: string | number,
    price_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/subscription-prices/${price_id}/restore`;
  }

  /**
   * Shop subscription prices endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/subscription-prices
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionPriceController::api_getList` expose observed keys including `offset`, `limit`, `subscription_prices`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_SUBSCRIPTION_PRICES(123);
   * ```
   */
  GET_SHOP_SUBSCRIPTION_PRICES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/subscription-prices`;
  }

  /**
   * Export product subscribers endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/export/subscribers/{render}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param render - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductController::exportProductSubscribers` expose observed keys including `start`, `end`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_EXPORT_PRODUCT_SUBSCRIBERS(123, 456, "value");
   * ```
   */
  GET_EXPORT_PRODUCT_SUBSCRIBERS(
    shop_id: string | number,
    product_id: string | number,
    render: "excel" | "pdf",
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/export/subscribers/${render}`;
  }

  //―――――――――――――――――――――― Shop > 🎗️ Ribbon (Subscription) ――――――――――――――――――――
  /**
   * Product subscription price available billings endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/ribbon/billings
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonBillingController::listOfBillingOptions` expose observed keys including `currency`, `gateways`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_SUBSCRIPTION_PRICE_AVAILABLE_BILLINGS(123);
   * ```
   */
  GET_PRODUCT_SUBSCRIPTION_PRICE_AVAILABLE_BILLINGS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/ribbon/billings`;
  }

  /**
   * Set shop ribbon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/ribbon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonController::api_setShopRibbon` expose observed keys including `shipping`, `billing`, `cart`, `count`, `ribbon`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SET_SHOP_RIBBON(123);
   * ```
   */
  PUT_SET_SHOP_RIBBON(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/ribbon`;
  }

  /**
   * Basket subscription bills sync manually endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/process-center/baskets/{basket_id}/sync-bills
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BasketSyncBillsController::api_syncBasketBillsManually` expose observed keys including `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUNT_BASKET_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_BASKET_SUBSCRIPTION_BILLS_SYNC_MANUALLY(123, 333);
   * ```
   */
  POST_BASKET_SUBSCRIPTION_BILLS_SYNC_MANUALLY(
    shop_id: string | number,
    basket_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/sync-bills`;
  }

  /**
   * Shop ribbons endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/ribbons
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::CHANNELS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopRibbonController::api_getShopRibbons` expose observed keys including `offset`, `limit`, `ribbons`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_RIBBONS(123);
   * ```
   */
  GET_SHOP_RIBBONS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/ribbons`;
  }

  /**
   * Product ribbon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ribbon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductController::api_setProductRibbon` expose observed keys including `mode`, `items`, `count`, `shipping`, `billing`, `error`, `error_msg`, `product_id`, `variant_id`, `ribbon`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_RIBBON(123, 456);
   * ```
   */
  POST_PRODUCT_RIBBON(shop_id: string | number, product_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon`;
  }

  /**
   * Product subscription contents endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/ribbon/contents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductContentsController::list` expose observed keys including `offset`, `limit`, `error`, `error_msg`, `contents`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_SUBSCRIPTION_CONTENTS(123, 456);
   * ```
   */
  GET_PRODUCT_SUBSCRIPTION_CONTENTS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents`;
  }

  /**
   * Product subscription add content endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ribbon/contents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductContentsController::add` expose observed keys including `title`, `description`, `mail`, `subject`, `body`, `error`, `error_msg`, `success`, `content`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SUBSCRIPTION_ADD_CONTENT(123, 456);
   * ```
   */
  POST_PRODUCT_SUBSCRIPTION_ADD_CONTENT(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents`;
  }

  /**
   * Product subscription edit content endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/ribbon/contents/{content_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param content_id - Subscription content identifier scoped to the route product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductContentsController::edit` expose observed keys including `title`, `description`, `mail`, `subject`, `body`, `error`, `error_msg`, `user_id`, `success`, `content`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_SUBSCRIPTION_EDIT_CONTENT(123, 456, 123);
   * ```
   */
  PUT_PRODUCT_SUBSCRIPTION_EDIT_CONTENT(
    shop_id: string | number,
    product_id: string | number,
    content_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents/${content_id}`;
  }

  /**
   * Product subscription content confirm endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ribbon/contents/{content_id}/confirm
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param content_id - Subscription content identifier scoped to the route product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductContentsController::confirm` expose observed keys including `available`, `error`, `error_msg`, `user_id`, `in_que`, `success`, `content`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SUBSCRIPTION_CONTENT_CONFIRM(123, 456, 123);
   * ```
   */
  POST_PRODUCT_SUBSCRIPTION_CONTENT_CONFIRM(
    shop_id: string | number,
    product_id: string | number,
    content_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents/${content_id}/confirm`;
  }

  /**
   * Product subscription content endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/ribbon/contents/{content_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param content_id - Subscription content identifier scoped to the route product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopSubscriptionProductContentsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`, `errors`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_SUBSCRIPTION_CONTENT(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_SUBSCRIPTION_CONTENT(
    shop_id: string | number,
    product_id: string | number,
    content_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents/${content_id}`;
  }

  /**
   * Product subscription content email preview endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/ribbon/contents/{content_id}/email-preview
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_RIBBON_WRITE (backoffice:ribbon:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param content_id - Subscription content identifier scoped to the route product.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopProductContentEmailsController::contentEmailPreview` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_SUBSCRIPTION_CONTENT_EMAIL_PREVIEW(123, 456, 123);
   * ```
   */
  GET_PRODUCT_SUBSCRIPTION_CONTENT_EMAIL_PREVIEW(
    shop_id: string | number,
    product_id: string | number,
    content_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ribbon/contents/${content_id}/email-preview`;
  }

  /**
   * Product embed patterns endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/embed-patterns/product
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductEmbedController::api_getEmbedProductPatterns` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_EMBED_PATTERNS(123);
   * ```
   */
  GET_PRODUCT_EMBED_PATTERNS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/embed-patterns/product`;
  }

  /**
   * Product set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductTranslationController::api_setProductTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_SET_TRANSLATIONS(123, 456, "value");
   * ```
   */
  PUT_PRODUCT_SET_TRANSLATIONS(
    shop_id: string | number,
    product_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/translations/${key}`;
  }

  /**
   * Category set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/categories/{category_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CategoryTranslationController::api_setCategoryTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_CATEGORY_SET_TRANSLATIONS(123, 789, "value");
   * ```
   */
  PUT_CATEGORY_SET_TRANSLATIONS(
    shop_id: string | number,
    category_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/categories/${category_id}/translations/${key}`;
  }

  /**
   * Cross sell set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/cross-sells/{cross_sell_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CATEGORY_WRITE (backoffice:category:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param cross_sell_id - cross_sell_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CrossSellTranslationController::api_setCrossSellTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_CROSS_SELL_SET_TRANSLATIONS(123, 456, 123, "value");
   * ```
   */
  PUT_CROSS_SELL_SET_TRANSLATIONS(
    shop_id: string | number,
    product_id: number | string,
    cross_sell_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cross-sells/${cross_sell_id}/translations/${key}`;
  }

  /**
   * Shop set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopTranslationController::api_setShopTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_SET_TRANSLATIONS(123, "value");
   * ```
   */
  PUT_SHOP_SET_TRANSLATIONS(shop_id: string | number, key: string) {
    return `${this.selldone_api_url}/shops/${shop_id}/translations/${key}`;
  }

  /**
   * Discount code set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/discount-codes/{discount_code_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_DISCOUNT_CODE_WRITE (backoffice:discount-code:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param discount_code_id - discount_code_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `DiscountCodeTranslationController::api_setDiscountCodeTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_DISCOUNT_CODE_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_DISCOUNT_CODE_SET_TRANSLATIONS(
    shop_id: string | number,
    discount_code_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/discount-codes/${discount_code_id}/translations/${key}`;
  }

  /**
   * Coupon set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/coupons/{coupon_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COUPON_WRITE (backoffice:coupon:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param coupon_id - coupon_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `CouponTranslationController::api_setCouponTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_COUPON_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_COUPON_SET_TRANSLATIONS(
    shop_id: string | number,
    coupon_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/coupons/${coupon_id}/translations/${key}`;
  }

  /**
   * Set cashback translations endpoint URL.
   *
   * Builds the SDK URL for cashback translation update.
   *
   * @endpoint PUT /shops/{shop_id}/cashback/{cashback_id}/translations/{key}
   * @auth Required if corrected to the active backend route; OAuth2/Bearer API token via `Authenticate:api`.
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write) on the active backend route.
   * @permission ShopPermissionRegion::INCENTIVES on the active backend route.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - Cashback identifier scoped to the route shop.
   * @param key - Translation key route segment.
   * @returns Absolute URL string for the current SDK path.
   *
   * Response shape:
   * - TODO(api-docs): current SDK path does not match the active Laravel route; inspect the plural `cashbacks` route before relying on response shape.
   *
   * Possible errors:
   * - TODO(api-docs): current SDK path may return 404 because no active singular `cashback` route was found.
   *
   * @example
   * ```ts
   * const url = API.PUT_CASHBACK_SET_TRANSLATIONS(123, 456, "title");
   * ```
   */
  PUT_CASHBACK_SET_TRANSLATIONS(
    shop_id: string | number,
    cashback_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashback/${cashback_id}/translations/${key}`;
  }

  /**
   * Offer set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/offers/{offer_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param offer_id - offer_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `OfferTranslationController::api_setOfferTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_OFFER_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_OFFER_SET_TRANSLATIONS(
    shop_id: string | number,
    offer_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/offers/${offer_id}/translations/${key}`;
  }

  /**
   * Gift card set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/gift-cards/{gift_card_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param gift_card_id - gift_card_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `GiftCardTranslationController::api_setGiftcardTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_GIFT_CARD_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_GIFT_CARD_SET_TRANSLATIONS(
    shop_id: string | number,
    gift_card_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/gift-cards/${gift_card_id}/translations/${key}`;
  }

  /**
   * Lottery set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/lotteries/{lottery_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param lottery_id - lottery_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `LotteryTranslationController::api_setLotteryTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_LOTTERY_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_LOTTERY_SET_TRANSLATIONS(
    shop_id: string | number,
    lottery_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/lotteries/${lottery_id}/translations/${key}`;
  }

  /**
   * Include set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/includes/{include_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param include_id - include_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductIncludeTranslationController::api_setIncludeTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_INCLUDE_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_INCLUDE_SET_TRANSLATIONS(
    shop_id: string | number,
    include_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/includes/${include_id}/translations/${key}`;
  }

  /**
   * POS shop translate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/translate
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_ADD (backoffice:shop:add)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ShopAutoTranslateController::api_autoTranslate` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POS_SHOP_TRANSLATE(123);
   * ```
   */
  POS_SHOP_TRANSLATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/translate`;
  }

  /**
   * Vendor set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/vendors/{vendor_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_id - Marketplace vendor identifier scoped to the route shop.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `VendorTranslationController::api_setVendorTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_VENDOR_SET_TRANSLATIONS(123, 401, "value");
   * ```
   */
  PUT_VENDOR_SET_TRANSLATIONS(
    shop_id: string | number,
    vendor_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendors/${vendor_id}/translations/${key}`;
  }

  /**
   * Vendor pricing set translations endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/vendor-pricings/{vendor_pricing_id}/translations/{translation_key}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_OFFER_WRITE (backoffice:offer:write)
   * @permission ShopPermissionRegion::MARKETPLACE
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param vendor_pricing_id - vendor_pricing_id route parameter identifier.
   * @param key - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `VendorPricingTranslationController::api_setVendorPricingTranslations` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_VENDOR_PRICING_SET_TRANSLATIONS(123, 123, "value");
   * ```
   */
  PUT_VENDOR_PRICING_SET_TRANSLATIONS(
    shop_id: string | number,
    vendor_pricing_id: number | string,
    key: string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/vendor-pricings/${vendor_pricing_id}/translations/${key}`;
  }

  //―――――――――――――――――――――― Investors ――――――――――――――――――――
  /**
   * Investors files endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /investors/files/{category}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_SELLDONE_INVESTOR (selldone:investor)
   * @permission None found in matched controller method by static inspection.
   *
   * @param category - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SelldoneInvestorsPageController::list` expose observed keys including `offset`, `limit`, `search`, `files`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_INVESTORS_FILES("value");
   * ```
   */
  GET_INVESTORS_FILES(category: string) {
    return `${this.selldone_api_url}/investors/files/${category}`;
  }

  //―――――――――――――――――――――― 🪐 Partner ――――――――――――――――――――
  /**
   * My agencies endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerController::list` expose observed keys including `offset`, `limit`, `search`, `partners`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCIES();
   * ```
   */
  GET_MY_AGENCIES() {
    return `${this.selldone_api_url}/agencies`;
  }

  /**
   * My agency info endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /agencies/{agency_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerController::info` expose observed keys including `free`, `agency`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_INFO(123);
   * ```
   */
  GET_MY_AGENCY_INFO(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}`;
  }

  /**
   * My agency clients endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/clients
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerClientsController::list` expose observed keys including `offset`, `limit`, `search`, `error`, `error_msg`, `clients`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_CLIENTS(123);
   * ```
   */
  GET_MY_AGENCY_CLIENTS(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/clients`;
  }

  /**
   * My agency add client giveaway endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/clients/add-client
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerClientsController::addClientManually` expose observed keys including `plan`, `error`, `error_msg`, `success`, `client`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_USER_BY_EMAIL_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_ADD_CLIENT_GIVEAWAY(123);
   * ```
   */
  POST_MY_AGENCY_ADD_CLIENT_GIVEAWAY(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/clients/add-client`;
  }

  /**
   * My agency send client activation email endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /agencies/{agency_id}/clients/{client_id}/send-activation
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param client_id - client_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerClientsController::sendActivationEmail` expose observed keys including `error`, `error_msg`, `success`, `client`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_SEND_CLIENT_ACTIVATION_EMAIL(123, 123);
   * ```
   */
  POST_MY_AGENCY_SEND_CLIENT_ACTIVATION_EMAIL(
    agency_id: string | number,
    client_id: string | number,
  ) {
    return `${this.selldone_api_url}/agencies/${agency_id}/clients/${client_id}/send-activation`;
  }

  /**
   * My agency transfer deal endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/clients/transfer-client
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerTransferClientsController::transferClientManually` expose observed keys including `uuid`, `code`, `error`, `error_msg`, `plan`, `agency_id`, `client_id`, `plan_id`, `expire_at`, `success`, `client`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_TRANSFER_DEAL(123);
   * ```
   */
  POST_MY_AGENCY_TRANSFER_DEAL(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/clients/transfer-client`;
  }

  /**
   * My agency purchases endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/purchases
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerPurchasesController::list` expose observed keys including `offset`, `limit`, `search`, `purchases`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_PURCHASES(123);
   * ```
   */
  GET_MY_AGENCY_PURCHASES(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/purchases`;
  }

  /**
   * My agency affiliates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/affiliates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerAffiliatesController::list` expose observed keys including `offset`, `limit`, `search`, `affiliates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_AFFILIATES(123);
   * ```
   */
  GET_MY_AGENCY_AFFILIATES(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/affiliates`;
  }

  /**
   * My agency shops endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerShopsController::list` expose observed keys including `offset`, `limit`, `search`, `error`, `error_msg`, `shops`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_SHOPS(123);
   * ```
   */
  GET_MY_AGENCY_SHOPS(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/shops`;
  }

  /**
   * My agency create shop endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/shops
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerShopsController::add` expose observed keys including `title`, `currency`, `plan`, `language`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_CREATE_SHOP(123);
   * ```
   */
  POST_MY_AGENCY_CREATE_SHOP(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/shops`;
  }

  /**
   * My agency create shop temporary access endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint POST /agencies/{agency_id}/shops/{shop_id}/access
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerShopsController::api_agencyCreateNewShopTemporaryAccess` expose observed keys including `error`, `error_msg`, `success`, `access`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_CREATE_SHOP_TEMPORARY_ACCESS(123, 123);
   * ```
   */
  POST_MY_AGENCY_CREATE_SHOP_TEMPORARY_ACCESS(
    agency_id: string | number,
    shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/agencies/${agency_id}/shops/${shop_id}/access`;
  }

  /**
   * My agency add plan endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/plans
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerPlansController::api_addPlan` expose observed keys including `name`, `enable`, `license`, `currency`, `yearly`, `published`, `error`, `error_msg`, `soldout`, `price`, `price_net`, `gift`, `success`, `plan`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_ADD_PLAN(123);
   * ```
   */
  POST_MY_AGENCY_ADD_PLAN(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/plans`;
  }

  /**
   * My agency update plan endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /agencies/{agency_id}/plans/{plan_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param plan_id - plan_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerPlansController::api_editPlan` expose observed keys including `name`, `enable`, `published`, `error`, `error_msg`, `soldout`, `price`, `price_net`, `gift`, `officer_id`, `agency_price`, `success`, `plan`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_AGENCY_UPDATE_PLAN(123, 123);
   * ```
   */
  PUT_MY_AGENCY_UPDATE_PLAN(
    agency_id: string | number,
    plan_id: string | number,
  ) {
    return `${this.selldone_api_url}/agencies/${agency_id}/plans/${plan_id}`;
  }

  /**
   * My agency plan endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /agencies/{agency_id}/plans/{plan_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param plan_id - plan_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerPlansController::api_deletePlan` expose observed keys including `error`, `error_msg`, `associated_shops_count`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_AGENCY_PLAN(123, 123);
   * ```
   */
  DELETE_MY_AGENCY_PLAN(agency_id: string | number, plan_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/plans/${plan_id}`;
  }

  /**
   * Calculate my agency plan price endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/subscription/price
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerPlanPriceCalculatorController::api_calculatePlanAgencyPrice` expose observed keys including `yearly`, `code`, `currency`, `license_price`, `agency_price`, `commission`, `daily_refund_price`, `months`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CALCULATE_MY_AGENCY_PLAN_PRICE(123);
   * ```
   */
  POST_CALCULATE_MY_AGENCY_PLAN_PRICE(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/subscription/price`;
  }

  /**
   * Agency set connected wallet account endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/account
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWalletController::api_connectAccount` expose observed keys including `account_id`, `error`, `error_msg`, `success`, `agency`, `account`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_ACTIVE_ACCOUNT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AGENCY_SET_CONNECTED_WALLET_ACCOUNT(123);
   * ```
   */
  POST_AGENCY_SET_CONNECTED_WALLET_ACCOUNT(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/account`;
  }

  /**
   * Agency set connected create wallet endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/create-wallet
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWalletController::api_createWallet` expose observed keys including `currency`, `success`, `wallet`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AGENCY_SET_CONNECTED_CREATE_WALLET(123);
   * ```
   */
  POST_AGENCY_SET_CONNECTED_CREATE_WALLET(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/create-wallet`;
  }

  /**
   * Agency set connected charge wallet endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/charge-wallet
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWalletChargeController::api_chargeWallet` expose observed keys including `currency`, `amount`, `error`, `error_msg`, `success`, `wallet`, `transaction`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_VALID_AMOUNT_ERROR()`, `ErrorCodes::NOT_FOUND_ACCOUNT_ERROR()`, `ErrorCodes::NOT_MATCH_ACCOUNT_CURRENCY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AGENCY_SET_CONNECTED_CHARGE_WALLET(123);
   * ```
   */
  POST_AGENCY_SET_CONNECTED_CHARGE_WALLET(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/charge-wallet`;
  }

  /**
   * Agency claim gift credit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/claim-gift
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWalletChargeController::api_claimGiftCharge` expose observed keys including `code`, `error`, `error_msg`, `success`, `wallet`, `amount`, `currency`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AGENCY_CLAIM_GIFT_CREDIT(123);
   * ```
   */
  POST_AGENCY_CLAIM_GIFT_CREDIT(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/claim-gift`;
  }

  /**
   * Agency logs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/calls
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerLogsController::api_getCalls` expose observed keys including `offset`, `limit`, `search`, `calls`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_AGENCY_LOGS(123);
   * ```
   */
  GET_AGENCY_LOGS(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/calls`;
  }

  //―――――――――――――――――――――― Agency > Documents ――――――――――――――――――――

  /**
   * My agency uploaded documents list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/documents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerDocumentsController::list` expose observed keys including `offset`, `limit`, `search`, `documents`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_UPLOADED_DOCUMENTS_LIST(123);
   * ```
   */
  GET_MY_AGENCY_UPLOADED_DOCUMENTS_LIST(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/documents`;
  }

  /**
   * My agency documents download URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint GET /agencies/{agency_id}/documents/{document_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param document_id - document_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerDocumentsController::download` expose observed keys including `error`, `error_msg`, `ResponseContentDisposition`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_DOCUMENTS_DOWNLOAD_URL(123, 123);
   * ```
   */
  GET_MY_AGENCY_DOCUMENTS_DOWNLOAD_URL(
    agency_id: string | number,
    document_id: string | number,
  ) {
    return `${this.selldone_api_url}/agencies/${agency_id}/documents/${document_id}`;
  }

  /**
   * My agency document add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/documents
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerDocumentsController::add` expose observed keys including `type`, `note`, `success`, `document`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_DOCUMENT_ADD(123);
   * ```
   */
  POST_MY_AGENCY_DOCUMENT_ADD(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/documents`;
  }

  /**
   * My agency document endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /agencies/{agency_id}/documents/{document_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param document_id - document_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerDocumentsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_AGENCY_DOCUMENT(123, 123);
   * ```
   */
  DELETE_MY_AGENCY_DOCUMENT(
    agency_id: string | number,
    document_id: string | number,
  ) {
    return `${this.selldone_api_url}/agencies/${agency_id}/documents/${document_id}`;
  }

  //―――――――――――――――――――――― Agency > FAQ ――――――――――――――――――――

  /**
   * My agency add daq endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/faqs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerFaqsController::add` expose observed keys including `question`, `answer`, `order`, `success`, `faq`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_ADD_DAQ(123);
   * ```
   */
  POST_MY_AGENCY_ADD_DAQ(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/faqs`;
  }

  /**
   * My agency edit daq endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /agencies/{agency_id}/faqs/{faq_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param faq_id - faq_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerFaqsController::edit` expose observed keys including `question`, `answer`, `order`, `error`, `error_msg`, `user_id`, `success`, `faq`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_MY_AGENCY_EDIT_DAQ(123, 123);
   * ```
   */
  PUT_MY_AGENCY_EDIT_DAQ(agency_id: string | number, faq_id: number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/faqs/${faq_id}`;
  }

  /**
   * My agency daq endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   *
   * @endpoint DELETE /agencies/{agency_id}/faqs/{faq_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @param faq_id - faq_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerFaqsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_MY_AGENCY_DAQ(123, 123);
   * ```
   */
  DELETE_MY_AGENCY_DAQ(agency_id: string | number, faq_id: number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/faqs/${faq_id}`;
  }

  //―――――――――――――――――――――― Agency > Subscriptions ――――――――――――――――――――

  /**
   * My agency subscriptions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /agencies/{agency_id}/subscriptions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_READ (agency:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWaitingListController::list` expose observed keys including `offset`, `limit`, `search`, `subscriptions`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_AGENCY_SUBSCRIPTIONS(123);
   * ```
   */
  GET_MY_AGENCY_SUBSCRIPTIONS(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/subscriptions`;
  }

  /**
   * My agency subscriptions send endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /agencies/{agency_id}/subscriptions/send
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_AGENCY_WRITE (agency:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param agency_id - agency_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PartnerWaitingListController::sendNotificationEmails` expose observed keys including `confirm`, `error`, `error_msg`, `total`, `send`, `errors`, `sent_at`, `success`, `message`, `report`, `codes`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_MY_AGENCY_SUBSCRIPTIONS_SEND(123);
   * ```
   */
  POST_MY_AGENCY_SUBSCRIPTIONS_SEND(agency_id: string | number) {
    return `${this.selldone_api_url}/agencies/${agency_id}/subscriptions/send`;
  }

  //―――――――――――――――――――――― Shop > Stream Users ――――――――――――――――――――
  /**
   * Shop stream users endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/stream-users
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StreamUserController::api_getShopStreamUsersList` expose observed keys including `offset`, `limit`, `search`, `stream_users`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STREAM_USERS(123);
   * ```
   */
  GET_SHOP_STREAM_USERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/stream-users`;
  }

  /**
   * Shop streams endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/streams
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StreamUserController::api_getShopStreams` expose observed keys including `offset`, `limit`, `search`, `streams`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_STREAMS(123);
   * ```
   */
  GET_SHOP_STREAMS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/streams`;
  }

  /**
   * Shop stream user endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/stream-users/{stream_user_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param stream_user_id - stream_user_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `StreamUserController::api_deleteStreamUser` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_STREAM_USER(123, 123);
   * ```
   */
  DELETE_SHOP_STREAM_USER(
    shop_id: string | number,
    stream_user_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/stream-users/${stream_user_id}`;
  }

  /**
   * Download all shop stream users endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/stream-users/export/excel
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::MARKETING
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `StreamUserExportController::api_downloadAllShopStreamUsers` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_DOWNLOAD_ALL_SHOP_STREAM_USERS(123);
   * ```
   */
  GET_DOWNLOAD_ALL_SHOP_STREAM_USERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/stream-users/export/excel`;
  }

  //―――――――――――――――――――――― AI (CI) ――――――――――――――――――――

  /**
   * AI page builder auto generate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @scope SCOPE_BACKOFFICE_AI_WRITE (backoffice:ai:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIPageBuilderController::AI_generatePageContent` expose observed keys including `error`, `error_msg`, `prompt`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_PAGE_BUILDER_AUTO_GENERATE(123, 111);
   * ```
   */
  POST_AI_PAGE_BUILDER_AUTO_GENERATE(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/ai`;
  }

  /**
   * AI article auto generate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/articles/{article_id}/ai
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_AI_WRITE (backoffice:ai:write)
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::BLOG
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param article_id - Article identifier used by the route.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIArticleController::AI_generateArticleContent` expose observed keys including `title`, `body`, `force`, `error`, `error_msg`, `success`, `usage_tokens`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_ARTICLE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_ARTICLE_AUTO_GENERATE(123, 301);
   * ```
   */
  POST_AI_ARTICLE_AUTO_GENERATE(
    shop_id: string | number,
    article_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/articles/${article_id}/ai`;
  }

  /**
   * AI page builder section content generate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/ai/sections
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @scope SCOPE_BACKOFFICE_AI_WRITE (backoffice:ai:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `AIPageBuilderSectionController::AI_generatePageSectionContent` expose observed keys including `section`, `prompt`, `generated`, `tokens`, `replacements`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_PAGE_BUILDER_SECTION_CONTENT_GENERATE(123, 111);
   * ```
   */
  POST_AI_PAGE_BUILDER_SECTION_CONTENT_GENERATE(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/ai/sections`;
  }

  /**
   * AI product auto create spec endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/ai/spec
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductSpecAIController::api_specGenerate` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_PRODUCT_AUTO_CREATE_SPEC(123, 456);
   * ```
   */
  POST_AI_PRODUCT_AUTO_CREATE_SPEC(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/ai/spec`;
  }

  /**
   * AI page builder image to component endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   *
   * @endpoint POST /shops/{shop_id}/pages/{page_id}/ai/image-to-component
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `AIPageBuilderImagetoComponentController::AI_generateComponentFromImage` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_AI_PAGE_BUILDER_IMAGE_TO_COMPONENT(123, 111);
   * ```
   */
  POST_AI_PAGE_BUILDER_IMAGE_TO_COMPONENT(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/ai/image-to-component`;
  }


  //―――――――――――――――――――――― Shop > Quota ――――――――――――――――――――
  /**
   * Set shop quota endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/quota
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopQuotaController::api_getShopQuota` expose observed keys including `quota`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.SET_SHOP_QUOTA(123);
   * ```
   */
  SET_SHOP_QUOTA(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/quota`;
  }

  //―――――――――――――――――――――― Shop > Variant Assets ――――――――――――――――――――
  /**
   * Shop variant assets endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/variant-assets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VariantAssetController::list` expose observed keys including `offset`, `limit`, `search`, `compact`, `variant_assets`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_VARIANT_ASSETS(123);
   * ```
   */
  GET_SHOP_VARIANT_ASSETS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/variant-assets`;
  }

  /**
   * Shop variant asset upload endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/variant-assets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VariantAssetController::add` expose observed keys including `photo`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_VARIANT_ASSET_UPLOAD(123);
   * ```
   */
  POST_SHOP_VARIANT_ASSET_UPLOAD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/variant-assets`;
  }

  /**
   * Shop variant asset upload endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/variant-assets/{variant_asset_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param variant_asset_id - variant_asset_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `VariantAssetController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_VARIANT_ASSET_UPLOAD(123, 123);
   * ```
   */
  DELETE_SHOP_VARIANT_ASSET_UPLOAD(
    shop_id: string | number,
    variant_asset_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/variant-assets/${variant_asset_id}`;
  }

  //―――――――――――――――――――――― Shop > Product Includes ――――――――――――――――――――
  /**
   * Shop includes endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/includes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopIncludesController::list` expose observed keys including `offset`, `limit`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_INCLUDES(123);
   * ```
   */
  GET_SHOP_INCLUDES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/includes`;
  }

  /**
   * Shop include add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/includes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopIncludesAddController::add` expose observed keys including `title`, `description`, `code`, `photo`, `url`, `path`, `theme`, `page_id`, `augment`, `error`, `error_msg`, `success`, `include`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_INCLUDE_ADD(123);
   * ```
   */
  POST_SHOP_INCLUDE_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/includes`;
  }

  /**
   * Shop include edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/includes/{include_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param include_id - include_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopIncludesEditController::edit` expose observed keys including `title`, `description`, `code`, `photo`, `url`, `path`, `theme`, `page_id`, `augment`, `error`, `error_msg`, `user_id`, `success`, `include`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_INCLUDE_EDIT(123, 123);
   * ```
   */
  POST_SHOP_INCLUDE_EDIT(
    shop_id: string | number,
    include_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/includes/${include_id}`;
  }

  /**
   * Shop include edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/includes/{include_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param include_id - include_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopIncludesDeleteController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_INCLUDE_EDIT(123, 123);
   * ```
   */
  DELETE_SHOP_INCLUDE_EDIT(
    shop_id: string | number,
    include_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/includes/${include_id}`;
  }

  /**
   * Shop augment upload image endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/augment/image
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PAGES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopAugmentUploadController::upload` expose observed keys including `photo`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_AUGMENT_UPLOAD_IMAGE(123);
   * ```
   */
  POST_SHOP_AUGMENT_UPLOAD_IMAGE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/augment/image`;
  }

  /**
   * Product include set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{products_id}/includes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductIncludesSetController::set` expose observed keys including `ids`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_INCLUDE_SET(123, 456);
   * ```
   */
  POST_PRODUCT_INCLUDE_SET(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/includes`;
  }

  /**
   * Shop page augment structure endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/pages/{page_id}/augment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PAGE_WRITE (backoffice:page:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param page_id - Page identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `PageBuilderAugmentController::api_getAugmentStructure` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_PAGE_AUGMENT_STRUCTURE(123, 111);
   * ```
   */
  GET_SHOP_PAGE_AUGMENT_STRUCTURE(
    shop_id: string | number,
    page_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/pages/${page_id}/augment`;
  }

  //―――――――――――――――――――――― Shop > Product > Category ――――――――――――――――――――

  /**
   * Product set shortcut categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/shortcuts
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `ProductShortcutsController::api_setProductShortcuts` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_SET_SHORTCUT_CATEGORIES(123, 456);
   * ```
   */
  POST_PRODUCT_SET_SHORTCUT_CATEGORIES(
    shop_id: string | number,
    product_id: number | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/shortcuts`;
  }


  //―――――――――――――――――――――― Shop > Property Set ――――――――――――――――――――
  /**
   * Property sets endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/property-sets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PROPERTY_SETS_WRITE (backoffice:property-set:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PropertiesSetController::api_getPropertySetsList` expose observed keys including `offset`, `limit`, `search`, `source`, `contain`, `with_count`, `with_sample`, `products`, `property_sets`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PROPERTY_SETS(123);
   * ```
   */
  GET_PROPERTY_SETS(
      shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/property-sets`;
  }


  /**
   * Add property set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/property-sets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PROPERTY_SETS_WRITE (backoffice:property-set:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PropertiesSetController::api_addPropertySet` expose observed keys including `title`, `description`, `variants`, `code`, `name`, `icon`, `values`, `success`, `property_set`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_ADD_PROPERTY_SET(123);
   * ```
   */
  POST_ADD_PROPERTY_SET(
      shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/property-sets`;
  }

  /**
   * Edit property set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/property-sets/{property_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PROPERTY_SETS_WRITE (backoffice:property-set:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param property_set_id - property_set_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PropertiesSetController::api_editPropertySet` expose observed keys including `error`, `error_msg`, `variants`, `code`, `name`, `icon`, `values`, `title`, `description`, `success`, `property_set`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_EDIT_PROPERTY_SET(123, 123);
   * ```
   */
  PUT_EDIT_PROPERTY_SET(
      shop_id: string | number,
      property_set_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/property-sets/${property_set_id}`;
  }
  /**
   * Edit property set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/property-sets/{property_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PROPERTY_SETS_WRITE (backoffice:property-set:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param property_set_id - property_set_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PropertiesSetController::api_deletePropertySet` expose observed keys including `error`, `error_msg`, `property_set_id`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_EDIT_PROPERTY_SET(123, 123);
   * ```
   */
  DELETE_EDIT_PROPERTY_SET(
      shop_id: string | number,
      property_set_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/property-sets/${property_set_id}`;
  }


  //―――――――――――――――――――――― Shop > Product > Property Set ――――――――――――――――――――

  /**
   * Product set property set endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/property-set
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `PropertiesSetAssignProductController::api_assignProductPropertySet` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_SET_PROPERTY_SET(123, 456);
   * ```
   */
  PUT_PRODUCT_SET_PROPERTY_SET(
      shop_id: string | number,
      product_id: number | string,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/property-set`;
  }

  //―――――――――――――――――――――― Shop > Product > Cross-selling ――――――――――――――――――――
  /**
   * Product cross sell items endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/products/{product_id}/cross-sells
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductCrossSellingController::list` expose observed keys including `offset`, `limit`, `cross_sells`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_PRODUCT_CROSS_SELL_ITEMS(123, 456);
   * ```
   */
  GET_PRODUCT_CROSS_SELL_ITEMS(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cross-sells`;
  }

  /**
   * Product cross sell add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/products/{product_id}/cross-sells
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductCrossSellingAddController::add` expose observed keys including `message`, `discount`, `targets`, `action`, `error`, `error_msg`, `cross_sells`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_PRODUCT_CROSS_SELL_ADD(123, 456);
   * ```
   */
  POST_PRODUCT_CROSS_SELL_ADD(
    shop_id: string | number,
    product_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cross-sells`;
  }

  /**
   * Product cross sell edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/products/{product_id}/cross-sells/{cross_sell_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param cross_sell_id - cross_sell_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductCrossSellingEditController::edit` expose observed keys including `message`, `discount`, `action`, `error`, `error_msg`, `user_id`, `cross_sell`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_PRODUCT_CROSS_SELL_EDIT(123, 456, 123);
   * ```
   */
  PUT_PRODUCT_CROSS_SELL_EDIT(
    shop_id: string | number,
    product_id: string | number,
    cross_sell_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cross-sells/${cross_sell_id}`;
  }

  /**
   * Product cross sell edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/products/{product_id}/cross-sells/{cross_sell_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::PRODUCTS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param product_id - Product identifier scoped to the route shop.
   * @param cross_sell_id - cross_sell_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ProductCrossSellingDeleteController::delete` expose observed keys including `error`, `error_msg`, `id`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_PRODUCT_CROSS_SELL_EDIT(123, 456, 123);
   * ```
   */
  DELETE_PRODUCT_CROSS_SELL_EDIT(
    shop_id: string | number,
    product_id: string | number,
    cross_sell_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/products/${product_id}/cross-sells/${cross_sell_id}`;
  }

  //―――――――――――――――――――――― Shop > Notes ――――――――――――――――――――
  /**
   * Shop note add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/notes
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_NOTE_WRITE (backoffice:note:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNoteController::add` expose observed keys including `message`, `mentions`, `page_id`, `product_id`, `popup_id`, `element_id`, `error`, `error_msg`, `valid_ids`, `note`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CUSTOM_PAGE_ERROR()`, `ErrorCodes::NOT_FOUND_PRODUCT_ERROR()`, `ErrorCodes::POPUP_NOTFOUND_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_NOTE_ADD(123);
   * ```
   */
  POST_SHOP_NOTE_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/notes`;
  }

  /**
   * Shop note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/notes/{note_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_NOTE_WRITE (backoffice:note:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param note_id - Note identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNoteController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_NOTE(123, 123);
   * ```
   */
  DELETE_SHOP_NOTE(shop_id: string | number, note_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/notes/${note_id}`;
  }

  //―――――――――――――――――――――― USer > Notifications Repository ――――――――――――――――――――
  /**
   * My notifications repository endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /notifications
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_NOTIFICATION (backoffice:notifications)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserNotificationsController::api_getUserNotifications` expose observed keys including `offset`, `limit`, `shop_id`, `mode`, `notifications`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_NOTIFICATIONS_REPOSITORY();
   * ```
   */
  GET_MY_NOTIFICATIONS_REPOSITORY() {
    return `${this.selldone_api_url}/notifications`;
  }

  /**
   * Read my notifications repository endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /notifications/read
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`, `Illuminate\Auth\Middleware\Authenticate`
   * @scope SCOPE_BACKOFFICE_NOTIFICATION (backoffice:notifications)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `UserNotificationsController::api_readMyNotifications` expose observed keys including `ids`, `shop_id`, `read_at`, `success`, `count`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_READ_MY_NOTIFICATIONS_REPOSITORY();
   * ```
   */
  POST_READ_MY_NOTIFICATIONS_REPOSITORY() {
    return `${this.selldone_api_url}/notifications/read`;
  }

  /**
   * Shop notifications repository endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/notifications
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopNotificationsRepositoryController::list` expose observed keys including `offset`, `limit`, `notifications`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_NOTIFICATIONS_REPOSITORY(123);
   * ```
   */
  GET_SHOP_NOTIFICATIONS_REPOSITORY(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/notifications`;
  }

  //―――――――――――――――――――――― Shop > Clusters ――――――――――――――――――――

  /**
   * Shop clusters endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/clusters
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopClustersController::list` expose observed keys including `offset`, `limit`, `contain`, `cluster_id`, `clusters`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CLUSTERS(123);
   * ```
   */
  GET_SHOP_CLUSTERS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clusters`;
  }

  /**
   * Shop clusters add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/clusters
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopClustersController::add` expose observed keys including `name`, `description`, `color`, `parent_id`, `photo`, `error`, `error_msg`, `success`, `cluster`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CLUSTERS_ADD(123);
   * ```
   */
  POST_SHOP_CLUSTERS_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clusters`;
  }

  /**
   * Shop clusters edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/clusters/{cluster_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cluster_id - cluster_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopClustersController::edit` expose observed keys including `name`, `description`, `color`, `parent_id`, `error`, `error_msg`, `user_id`, `success`, `cluster`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CLUSTERS_EDIT(123, 123);
   * ```
   */
  PUT_SHOP_CLUSTERS_EDIT(
    shop_id: string | number,
    cluster_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/clusters/${cluster_id}`;
  }

  /**
   * Shop cluster endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/clusters/{cluster_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cluster_id - cluster_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopClustersController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CLUSTER(123, 123);
   * ```
   */
  DELETE_SHOP_CLUSTER(shop_id: string | number, cluster_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/clusters/${cluster_id}`;
  }

  /**
   * Shop cluster upload icon endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/clusters/{cluster_id}/icon
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cluster_id - cluster_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopClustersController::uploadIcon` expose observed keys including `photo`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CLUSTER_UPLOAD_ICON(123, 123);
   * ```
   */
  POST_SHOP_CLUSTER_UPLOAD_ICON(
    shop_id: string | number,
    cluster_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/clusters/${cluster_id}/icon`;
  }

  /**
   * Shop cluster resources endpoint URL.
   *
   * Builds the URL for listing cluster resources. `cluster_id` is optional in the backend route and is appended only when provided.
   *
   * @endpoint GET /shops/{shop_id}/resources/{cluster_id?}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Authenticate:api`, `EnsureEmailIsVerified`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::SETTINGS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cluster_id - Optional cluster identifier; when omitted the backend returns resources without a cluster filter.
   * @returns Absolute URL string for the cluster resources endpoint.
   *
   * Response shape:
   * - Backend response body is defined by `ShopClusterResourcesController::resources`.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid token/session.
   * - 403 token_has_no_access: missing shop read scope or settings-region permission.
   * - 404 not_found: shop or cluster is not found.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CLUSTERS_RESOURCES(123, 456);
   * ```
   */
  GET_SHOP_CLUSTERS_RESOURCES(
    shop_id: string | number,
    cluster_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/resources${
      cluster_id ? "/" + cluster_id : ""
    }`;
  }

  //―――――――――――――――――――――― Accounts ――――――――――――――――――――
  /**
   * My subscriptions endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   *
   * @endpoint GET /subscriptions
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PremiumSubscriptionPurchaseController::api_getMySubscriptions` expose observed keys including `verified`, `subscriptions`, `gateways`, `currency`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_MY_SUBSCRIPTIONS();
   * ```
   */
  GET_MY_SUBSCRIPTIONS() {
    return `${this.selldone_api_url}/subscriptions`;
  }

  /**
   * Subscribe to premium endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /subscriptions/premium/{gateway_code}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @param gateway_code - Payment gateway code in the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PremiumSubscriptionPurchaseController::api_subscribeToPremium` expose observed keys including `period`, `error`, `error_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUNT_GATEWAY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SUBSCRIBE_TO_PREMIUM("stripe");
   * ```
   */
  POST_SUBSCRIBE_TO_PREMIUM(gateway_code: string) {
    return `${this.selldone_api_url}/subscriptions/premium/${gateway_code}`;
  }

  //―――――――――――――――――――――― Selldone Collect VAT ――――――――――――――――――――
  /**
   * Calculate selldone VAT endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /selldone-vat
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_COMPANY_READ (backoffice:company:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CollectSelldoneVATController::api_calculateSelldoneVAT` expose observed keys including `company_id`, `percent`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::NOT_FOUND_COMPANY_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_CALCULATE_SELLDONE_VAT();
   * ```
   */
  POST_CALCULATE_SELLDONE_VAT() {
    return `${this.selldone_api_url}/selldone-vat`;
  }

  //―――――――――――――――――――――― Customer Wallets ――――――――――――――――――――

  /**
   * Shop all customers wallets endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/wallets
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CUSTOMER_WRITE (backoffice:customer:write)
   * @permission ShopPermissionRegion::CUSTOMERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopCustomerWalletsController::api_getWallets` expose observed keys including `offset`, `limit`, `search`, `sortBy`, `sortDesc`, `wallets`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_ALL_CUSTOMERS_WALLETS(123);
   * ```
   */
  GET_SHOP_ALL_CUSTOMERS_WALLETS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/wallets`;
  }

  /**
   * Shop cashback program endpoint URL.
   *
   * Builds a legacy SDK URL for cashback program setup.
   *
   * @endpoint POST /shops/{shop_id}/cashback-program
   * @auth TODO(api-docs): verify whether this SDK URL is stale before use.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string for the SDK cashback-program path.
   *
   * Response shape:
   * - TODO(api-docs): unresolved route; no backend response can be documented as fact.
   *
   * Possible errors:
   * - TODO(api-docs): route may return 404 because no active Laravel route was found.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CASHBACK_PROGRAM(123);
   * ```
   */
  POST_SHOP_CASHBACK_PROGRAM(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashback-program`;
  }

  //―――――――――――――――――――――― Shop Cashback ――――――――――――――――――――

  /**
   * Shop cashbacks endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/cashbacks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackController::api_getCashbacks` expose observed keys including `offset`, `limit`, `cluster_id`, `cashbacks`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_CASHBACKS(123);
   * ```
   */
  GET_SHOP_CASHBACKS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks`;
  }

  /**
   * Cashback data endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/cashbacks/{cashback_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackController::api_getCashbackInfo` expose observed keys including `success`, `cashback`, `data`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CASHBACK_DATA(123, 123);
   * ```
   */
  GET_CASHBACK_DATA(shop_id: string | number, cashback_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}`;
  }

  /**
   * Cashback orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/cashbacks/{cashback_id}/orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackController::api_getShopCashbackOrders` expose observed keys including `error`, `cashback_orders`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_CASHBACK_ORDERS(123, 123);
   * ```
   */
  GET_CASHBACK_ORDERS(shop_id: string | number, cashback_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}/orders`;
  }

  /**
   * Shop cashback endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/cashbacks/{cashback_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackDeleteController::api_deleteCashback` expose observed keys including `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CASHBACK(123, 123);
   * ```
   */
  DELETE_SHOP_CASHBACK(shop_id: string | number, cashback_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}`;
  }

  /**
   * Shop cashback restore endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/cashbacks/{cashback_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackDeleteController::api_restoreCashback` expose observed keys including `error`, `error_msg`, `success`, `cashback`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CASHBACK_RESTORE(123, 123);
   * ```
   */
  POST_SHOP_CASHBACK_RESTORE(
    shop_id: string | number,
    cashback_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}/restore`;
  }

  /**
   * Shop cashback add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/cashbacks
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackAddController::api_addCashback` expose observed keys including `enable`, `boosted`, `percent`, `limit`, `currency`, `title`, `description`, `qualify`, `only_first_buy`, `min_purchase`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `start`, `end`, `cluster_id`, `error`, `error_msg`, `success`, `cashback`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CASHBACK_ADD(123);
   * ```
   */
  POST_SHOP_CASHBACK_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks`;
  }

  /**
   * Shop cashback edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/cashbacks/{cashback_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackEditController::api_editCashback` expose observed keys including `enable`, `boosted`, `percent`, `limit`, `currency`, `title`, `description`, `qualify`, `only_first_buy`, `min_purchase`, `no_club`, `bronze_club`, `silver_club`, `gold_club`, `platinum_club`, `diamond_club`, `start`, `end`, `cluster_id`, `error`, `error_msg`, `success`, `cashback`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`, `ErrorCodes::LICENSE_EXCEEDS_LIMIT()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_CASHBACK_EDIT(123, 123);
   * ```
   */
  PUT_SHOP_CASHBACK_EDIT(
    shop_id: string | number,
    cashback_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}`;
  }

  /**
   * Shop cashback add note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/cashbacks/{cashback_id}/note
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackNoteController::api_addNote` expose observed keys including `body`, `user_id`, `user_name`, `date`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_CASHBACK_ADD_NOTE(123, 123);
   * ```
   */
  POST_SHOP_CASHBACK_ADD_NOTE(
    shop_id: string | number,
    cashback_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}/note`;
  }

  /**
   * Shop cashback note endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/cashbacks/{cashback_id}/note/{note_index}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_CASHBACK_WRITE (backoffice:cashback:write)
   * @permission ShopPermissionRegion::INCENTIVES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param cashback_id - cashback_id route parameter identifier.
   * @param note_index - Route parameter passed through to the backend endpoint.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `CashbackNoteController::api_deleteNote` expose observed keys including `error`, `error_msg`, `note`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`, `ErrorCodes::NOT_FOUND_CASHBACK_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_CASHBACK_NOTE(123, 123, "value");
   * ```
   */
  DELETE_SHOP_CASHBACK_NOTE(
    shop_id: string | number,
    cashback_id: string | number,
    note_index: number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/cashbacks/${cashback_id}/note/${note_index}`;
  }
  //―――――――――――――――――――――― Shop > Download Bulk Daily Orders ――――――――――――――――――――
  /**
   * Download daily orders endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/process-center/baskets/export/daily-orders
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_READ (backoffice:order:read)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `BulkOrdersDailyExportController::api_downloadShopBulkPaidOrdersDaily` expose observed keys including `date`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DOWNLOAD_DAILY_ORDERS(123);
   * ```
   */
  DOWNLOAD_DAILY_ORDERS(
      shop_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/export/daily-orders`;
  }



  //―――――――――――――――――――――― Shop > Print Templates ――――――――――――――――――――
  /**
   * Shop print templates endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `readShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/print-templates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRINT_READ (backoffice:print:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPrintTemplatesController::api_getPrintTemplatesList` expose observed keys including `offset`, `limit`, `sortBy`, `sortDesc`, `print_templates`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_PRINT_TEMPLATES(123);
   * ```
   */
  GET_SHOP_PRINT_TEMPLATES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/print-templates`;
  }

  /**
   * Shop print template add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/print-templates
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRINT_READ (backoffice:print:read)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPrintTemplatesController::api_postPrintTemplatesAdd` expose observed keys including `language`, `title`, `width`, `height`, `elements`, `fonts`, `type`, `success`, `print_template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_PRINT_TEMPLATE_ADD(123);
   * ```
   */
  POST_SHOP_PRINT_TEMPLATE_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/print-templates`;
  }

  /**
   * Shop print template update endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint PUT /shops/{shop_id}/print-templates/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRINT_WRITE (backoffice:print:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPrintTemplatesController::api_editPrintTemplate` expose observed keys including `language`, `title`, `width`, `height`, `elements`, `fonts`, `type`, `error`, `error_msg`, `user_id`, `success`, `print_template`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_PRINT_TEMPLATE_UPDATE(123, 123);
   * ```
   */
  PUT_SHOP_PRINT_TEMPLATE_UPDATE(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/print-templates/${template_id}`;
  }

  /**
   * Shop print template endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/print-templates/{template_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRINT_WRITE (backoffice:print:write)
   * @permission ShopPermissionRegion::LOGISTIC
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param template_id - template_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopPrintTemplatesController::api_deletePrintTemplate` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_PRINT_TEMPLATE(123, 123);
   * ```
   */
  DELETE_SHOP_PRINT_TEMPLATE(
    shop_id: string | number,
    template_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/print-templates/${template_id}`;
  }

  //―――――――――――――――――――――― Token SLDN ――――――――――――――――――――
  /**
   * Token play game endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /sldn/play-game
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SLDNPlayGameController::api_playGame` expose observed keys including `card`, `address`, `code`, `error`, `error_msg`, `success`, `amount`, `success_msg`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TOKEN_PLAY_GAME();
   * ```
   */
  POST_TOKEN_PLAY_GAME() {
    return `${this.selldone_api_url}/sldn/play-game`;
  }


  /**
   * Token purchase endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /sldn/purchase
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SLDNPurchaseController::api_purchaseSLDN` expose observed keys including `sldn_amount`, `sol_amount`, `recipient_address`, `error`, `error_msg`, `success`, `currency`, `address`, `qr_code`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TOKEN_PURCHASE();
   * ```
   */
  POST_TOKEN_PURCHASE() {
    return `${this.selldone_api_url}/sldn/purchase`;
  }
  /**
   * Token purchase check endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `CheckTokenScopesOrThrow`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /sldn/purchase/check
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_WALLET_WRITE (backoffice:wallet:write)
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `SLDNPurchaseController::api_checkPurchaseSLDN` expose observed keys including `address`, `error`, `error_msg`, `success`, `purchase`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_TOKEN_PURCHASE_CHECK();
   * ```
   */
  POST_TOKEN_PURCHASE_CHECK() {
    return `${this.selldone_api_url}/sldn/purchase/check`;
  }



  /**
   * Shop order item set count adjustment endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/process-center/baskets/{basket_id}/items/{item_id}/count-adjustment
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_ORDER_WRITE (backoffice:order:write)
   * @permission ShopPermissionRegion::ORDERS
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param basket_id - Basket/order identifier scoped to the route shop.
   * @param basket_item_id - basket_item_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - See `BasketCountAdjustmentController::api_setBasketItemCountAdjustment` for the exact backend response body; no array keys were statically detected in the matched method.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_ORDER_ITEM_SET_COUNT_ADJUSTMENT(123, 333, 123);
   * ```
   */
  PUT_SHOP_ORDER_ITEM_SET_COUNT_ADJUSTMENT(
      shop_id: string | number,
      basket_id: string | number,
      basket_item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/process-center/baskets/${basket_id}/items/${basket_item_id}/count-adjustment`;
  }



  //―――――――――――――――――――――― Red Alert ――――――――――――――――――――

  /**
   * Red alert report endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /red-alert
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PublicRedAlertController::api_submitReport` expose observed keys including `store_address`, `message`, `reporter_type`, `criminal_activity`, `future_assistance`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RED_ALERT_REPORT();
   * ```
   */
  POST_RED_ALERT_REPORT() {
    return `${this.selldone_api_url}/red-alert`;
  }
  /**
   * Red alert report check store endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /red-alert/check-store
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope None found in matched route/controller method by static inspection.
   * @permission None found in matched controller method by static inspection.
   *
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `PublicRedAlertController::api_checkStore` expose observed keys including `store_address`, `shop`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_RED_ALERT_REPORT_CHECK_STORE();
   * ```
   */
  POST_RED_ALERT_REPORT_CHECK_STORE() {
    return `${this.selldone_api_url}/red-alert/check-store`;
  }





  //―――――――――――――――――――――― Shop Listing――――――――――――――――――――

  // Listing config (GET is optional if you add it in routes)
  /**
   * Shop listing endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/listing
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingController::get` expose observed keys including `shop_id`, `success`, `listing`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING(123);
   * ```
   */
  GET_SHOP_LISTING(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing`;
  }

  /**
   * Shop listing save endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingController::save` expose observed keys including `title`, `description`, `is_active`, `moderation_mode`, `max_pending_per_user`, `require_first_approved_before_more`, `max_items_per_user`, `submission_enabled`, `default_currency`, `form_schema`, `settings`, `shop_id`, `success`, `listing`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_SAVE(123);
   * ```
   */
  POST_SHOP_LISTING_SAVE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing`;
  }

  // Categories (IMPORTANT: should be /listing/categories to avoid conflict with items)
  /**
   * Shop listing categories endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCategoriesListController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `parent_id`, `sortBy`, `sortDesc`, `with_trashed`, `shop_id`, `parent`, `categories`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_CATEGORIES(123);
   * ```
   */
  GET_SHOP_LISTING_CATEGORIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/categories`;
  }

  /**
   * Shop listing category add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/categories
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCategoriesUpsertController::add` expose observed keys including `title`, `slug`, `description`, `parent_id`, `design_id`, `price_format`, `sort_order`, `is_active`, `meta`, `form_schema`, `icon`, `shop_id`, `error`, `error_msg`, `created_by`, `parent`, `success`, `category`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_CATEGORY_ADD(123);
   * ```
   */
  POST_SHOP_LISTING_CATEGORY_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/categories`;
  }

  /**
   * Shop listing category edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCategoriesUpsertController::edit` expose observed keys including `title`, `slug`, `description`, `parent_id`, `design_id`, `price_format`, `sort_order`, `is_active`, `meta`, `form_schema`, `icon`, `error`, `error_msg`, `parent`, `success`, `category`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_CATEGORY_EDIT(123, 789);
   * ```
   */
  POST_SHOP_LISTING_CATEGORY_EDIT(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/categories/${category_id}`;
  }

  /**
   * Shop listing category endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/categories/{category_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param category_id - Category identifier scoped to the route shop.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCategoriesUpsertController::delete` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_CATEGORY(123, 789);
   * ```
   */
  DELETE_SHOP_LISTING_CATEGORY(
    shop_id: string | number,
    category_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/categories/${category_id}`;
  }

  // Items
  /**
   * Shop listing items endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/items
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemsController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `status`, `category_id`, `user_id`, `company_id`, `badge_id`, `badge_ids`, `sortBy`, `sortDesc`, `with_trashed`, `shop_id`, `category`, `media`, `items`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_ITEMS(123);
   * ```
   */
  GET_SHOP_LISTING_ITEMS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items`;
  }

  /**
   * Shop listing item add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemsController::add` expose observed keys including `category_id`, `title`, `slug`, `subtitle`, `description`, `price`, `price_to`, `currency`, `price_unit`, `country_code`, `city`, `address`, `lat`, `lng`, `tel`, `website`, `links`, `opening_hours`, `meta`, `company_id`, `badge_ids`, `attributes`, `status`, `shop_id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_ADD(123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items`;
  }

  /**
   * Shop listing item edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemEditController::edit` expose observed keys including `category_id`, `title`, `slug`, `subtitle`, `description`, `price`, `price_to`, `currency`, `price_unit`, `country_code`, `city`, `address`, `lat`, `lng`, `tel`, `website`, `links`, `opening_hours`, `meta`, `attributes`, `company_id`, `badge_ids`, `status`, `is_featured`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_EDIT(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_EDIT(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}`;
  }

  /**
   * Shop listing item endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/items/{item_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemDeleteController::delete` expose observed keys including `shop_id`, `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_ITEM(123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_ITEM(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}`;
  }

  // Moderation
  /**
   * Shop listing item approve endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/approve
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::approve` expose observed keys including `publish`, `penalty_points`, `penalty_note`, `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_APPROVE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_APPROVE(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/approve`;
  }

  /**
   * Shop listing item reject endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::reject` expose observed keys including `reason`, `penalty_points`, `penalty_note`, `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_REJECT(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_REJECT(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/reject`;
  }

  /**
   * Shop listing item publish endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/publish
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::publish` expose observed keys including `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_PUBLISH(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_PUBLISH(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/publish`;
  }

  /**
   * Shop listing item unpublish endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/unpublish
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::unpublish` expose observed keys including `archive`, `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_UNPUBLISH(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_UNPUBLISH(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/unpublish`;
  }

  /**
   * Shop listing item archive endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/archive
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::archive` expose observed keys including `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_ARCHIVE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_ARCHIVE(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/archive`;
  }

  /**
   * Shop listing item restore endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::restore` expose observed keys including `to`, `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_RESTORE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_RESTORE(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/restore`;
  }

  /**
   * Shop listing item penalty endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/moderation/penalty
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemModerationController::penalty` expose observed keys including `penalty_points`, `penalty_note`, `shop_id`, `error`, `error_msg`, `category`, `media`, `success`, `item`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_PENALTY(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_PENALTY(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/moderation/penalty`;
  }

  // Media
  /**
   * Shop listing item media list endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/items/{item_id}/media/list
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::list` expose observed keys including `type`, `with_trashed`, `shop_id`, `error`, `error_msg`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_ITEM_MEDIA_LIST(123, 123);
   * ```
   */
  GET_SHOP_LISTING_ITEM_MEDIA_LIST(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/list`;
  }

  /**
   * Shop listing item media images endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/media/images
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::uploadImages` expose observed keys including `photo`, `photos`, `shop_id`, `error`, `error_msg`, `success`, `upload`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_MEDIA_IMAGES(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_MEDIA_IMAGES(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/images`;
  }

  /**
   * Shop listing item media URL endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/media/url
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::addUrl` expose observed keys including `type`, `url`, `alt`, `meta`, `sort_order`, `is_main`, `shop_id`, `error`, `error_msg`, `item_id`, `provider`, `path`, `mime`, `size`, `width`, `height`, `duration`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_MEDIA_URL(123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_MEDIA_URL(
    shop_id: string | number,
    item_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/url`;
  }

  /**
   * Shop listing item media endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/items/{item_id}/media/{media_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @param media_id - media_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::delete` expose observed keys including `shop_id`, `error`, `error_msg`, `is_main`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_ITEM_MEDIA(123, 123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_ITEM_MEDIA(
    shop_id: string | number,
    item_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/${media_id}`;
  }

  /**
   * Shop listing item media edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/items/{item_id}/media/{media_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @param media_id - media_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::edit` expose observed keys including `alt`, `meta`, `url`, `sort_order`, `is_main`, `photo`, `shop_id`, `error`, `error_msg`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_ITEM_MEDIA_EDIT(123, 123, 123);
   * ```
   */
  POST_SHOP_LISTING_ITEM_MEDIA_EDIT(
    shop_id: string | number,
    item_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/${media_id}`;
  }

  /**
   * Shop listing item media main endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write endpoint; updates existing backend state.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint PUT /shops/{shop_id}/listing/items/{item_id}/media/{media_id}/main
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Item identifier scoped to the route resource.
   * @param media_id - media_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingItemMediaController::setMain` expose observed keys including `shop_id`, `error`, `error_msg`, `is_main`, `success`, `media`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_LISTING_ITEM_MEDIA_MAIN(123, 123, 123);
   * ```
   */
  PUT_SHOP_LISTING_ITEM_MEDIA_MAIN(
    shop_id: string | number,
    item_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/${media_id}/main`;
  }

  /**
   * Reorder listing item media endpoint URL.
   *
   * Builds the SDK URL for listing media reorder.
   *
   * @endpoint PUT /shops/{shop_id}/listing/items/{item_id}/media/{media_id}/reorder
   * @auth Required if corrected to the active backend route; OAuth2/Bearer API token via `Authenticate:api`.
   * @scope Backoffice listing/shop write scope as enforced by `ShopListingItemMediaController::reorder`.
   * @permission Listing/shop permission as enforced by the controller.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param item_id - Listing item identifier scoped to the shop.
   * @param media_id - Media identifier included by the current SDK URL; active backend reorder route does not include this segment.
   * @returns Absolute URL string for the current SDK path.
   *
   * Response shape:
   * - TODO(api-docs): current SDK path does not match active route; inspect `ShopListingItemMediaController::reorder` before relying on response shape.
   *
   * Possible errors:
   * - TODO(api-docs): current SDK path may return 404 because no active route includes `{media_id}/reorder`.
   *
   * @example
   * ```ts
   * const url = API.PUT_SHOP_LISTING_ITEM_MEDIA_REORDER(123, 456, 789);
   * ```
   */
  PUT_SHOP_LISTING_ITEM_MEDIA_REORDER(
    shop_id: string | number,
    item_id: string | number,
    media_id: string | number,
  ) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/items/${item_id}/media/${media_id}/reorder`;
  }

















  // Listing Designs
  /**
   * Shop listing designs endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/designs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_READ (backoffice:shop:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingDesignsController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `sortBy`, `sortDesc`, `shop_id`, `success`, `designs`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_DESIGNS(123);
   * ```
   */
  GET_SHOP_LISTING_DESIGNS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/designs`;
  }

  /**
   * Shop listing design add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/designs
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingDesignsController::add` expose observed keys including `card_template`, `details_template`, `shop_id`, `success`, `design`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_DESIGN_ADD(123);
   * ```
   */
  POST_SHOP_LISTING_DESIGN_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/designs`;
  }

  /**
   * Shop listing design edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/designs/{design_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param design_id - design_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingDesignsController::edit` expose observed keys including `card_template`, `details_template`, `error`, `error_msg`, `success`, `design`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_DESIGN_EDIT(123, 123);
   * ```
   */
  POST_SHOP_LISTING_DESIGN_EDIT(shop_id: string | number, design_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/designs/${design_id}`;
  }

  /**
   * Shop listing design endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/designs/{design_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_SHOP_WRITE (backoffice:shop:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param design_id - design_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingDesignsController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_DESIGN(123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_DESIGN(shop_id: string | number, design_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/designs/${design_id}`;
  }













  // Listing Badges
  /**
   * Shop listing badges endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/badges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingBadgesController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `with_trashed`, `sortBy`, `sortDesc`, `shop_id`, `success`, `badges`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_BADGES(123);
   * ```
   */
  GET_SHOP_LISTING_BADGES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/badges`;
  }

  /**
   * Shop listing badge add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/badges
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingBadgesController::add` expose observed keys including `title`, `description`, `sort_order`, `is_active`, `shop_id`, `success`, `badge`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_BADGE_ADD(123);
   * ```
   */
  POST_SHOP_LISTING_BADGE_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/badges`;
  }

  /**
   * Shop listing badge edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/badges/{badge_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param badge_id - badge_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingBadgesController::edit` expose observed keys including `title`, `description`, `sort_order`, `is_active`, `error`, `error_msg`, `success`, `badge`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_BADGE_EDIT(123, 123);
   * ```
   */
  POST_SHOP_LISTING_BADGE_EDIT(shop_id: string | number, badge_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/badges/${badge_id}`;
  }

  /**
   * Shop listing badge endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/badges/{badge_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param badge_id - badge_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingBadgesController::delete` expose observed keys including `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_BADGE(123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_BADGE(shop_id: string | number, badge_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/badges/${badge_id}`;
  }

// Listing Companies
  /**
   * Shop listing companies endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/companies
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCompaniesController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `with_trashed`, `sortBy`, `sortDesc`, `shop_id`, `success`, `companies`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_COMPANIES(123);
   * ```
   */
  GET_SHOP_LISTING_COMPANIES(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/companies`;
  }

  /**
   * Shop listing company add endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/companies
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCompaniesController::add` expose observed keys including `name`, `description`, `website`, `tel`, `sort_order`, `is_active`, `shop_id`, `success`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_COMPANY_ADD(123);
   * ```
   */
  POST_SHOP_LISTING_COMPANY_ADD(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/companies`;
  }

  /**
   * Shop listing company edit endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/companies/{company_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCompaniesController::edit` expose observed keys including `name`, `description`, `website`, `tel`, `sort_order`, `is_active`, `error`, `error_msg`, `success`, `company`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_COMPANY_EDIT(123, 999);
   * ```
   */
  POST_SHOP_LISTING_COMPANY_EDIT(shop_id: string | number, company_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/companies/${company_id}`;
  }

  /**
   * Shop listing company endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/companies/{company_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param company_id - Company identifier owned by the authenticated user.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingCompaniesController::delete` expose observed keys including `error`, `error_msg`, `company_id`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_COMPANY(123, 999);
   * ```
   */
  DELETE_SHOP_LISTING_COMPANY(shop_id: string | number, company_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/companies/${company_id}`;
  }









  // Listing all reviews (backoffice)

  /**
   * Shop listing reviews endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/reviews
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewsListController::list` expose observed keys including `offset`, `limit`, `search`, `item_id`, `user_id`, `status`, `has_answer`, `rating_min`, `rating_max`, `sortBy`, `sortDesc`, `with_trashed`, `success`, `reviews`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_REVIEWS(123);
   * ```
   */
  GET_SHOP_LISTING_REVIEWS(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews`;
  }

  /**
   * Shop listing review reply endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/reviews/{review}/reply
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewActionsController::reply` expose observed keys including `answer`, `error`, `error_msg`, `success`, `review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_REVIEW_REPLY(123, 123);
   * ```
   */
  POST_SHOP_LISTING_REVIEW_REPLY(shop_id: string | number, review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews/${review_id}/reply`;
  }

  /**
   * Shop listing review approve endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/reviews/{review}/approve
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewActionsController::approve` expose observed keys including `error`, `error_msg`, `success`, `review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_REVIEW_APPROVE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_REVIEW_APPROVE(shop_id: string | number, review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews/${review_id}/approve`;
  }

  /**
   * Shop listing review reject endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/reviews/{review}/reject
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewActionsController::reject` expose observed keys including `error`, `error_msg`, `success`, `review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_REVIEW_REJECT(123, 123);
   * ```
   */
  POST_SHOP_LISTING_REVIEW_REJECT(shop_id: string | number, review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews/${review_id}/reject`;
  }

  /**
   * Shop listing review endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/reviews/{review}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewActionsController::delete` expose observed keys including `error`, `error_msg`, `success`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_REVIEW(123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_REVIEW(shop_id: string | number, review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews/${review_id}`;
  }

  /**
   * Shop listing review restore endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/reviews/{review}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param review_id - review_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingReviewActionsController::restore` expose observed keys including `error`, `error_msg`, `success`, `review`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_REVIEW_RESTORE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_REVIEW_RESTORE(shop_id: string | number, review_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/reviews/${review_id}/restore`;
  }










  //━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  //  Shop ▶ Listing ▶ Messages (Dashboard)
  //━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * Shop listing messages endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint GET /shops/{shop_id}/listing/messages
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessagesListController::list` expose observed keys including `offset`, `limit`, `contain`, `search`, `status`, `item_id`, `user_id`, `has_reply`, `with_trashed`, `sortBy`, `sortDesc`, `shop_id`, `success`, `messages`, `total`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_MESSAGES(123);
   * ```
   */
  GET_SHOP_LISTING_MESSAGES(shop_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages`;
  }

  /**
   * Shop listing message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/listing/messages/{message_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessagesListController::get` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_MESSAGE(123, 123);
   * ```
   */
  GET_SHOP_LISTING_MESSAGE(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}`;
  }

  /**
   * Shop listing message read endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/messages/{message_id}/read
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::markRead` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_MESSAGE_READ(123, 123);
   * ```
   */
  POST_SHOP_LISTING_MESSAGE_READ(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}/read`;
  }

  /**
   * Shop listing message archive endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/messages/{message_id}/archive
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::archive` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_MESSAGE_ARCHIVE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_MESSAGE_ARCHIVE(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}/archive`;
  }

  /**
   * Shop listing message spam endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/messages/{message_id}/spam
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::spam` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_MESSAGE_SPAM(123, 123);
   * ```
   */
  POST_SHOP_LISTING_MESSAGE_SPAM(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}/spam`;
  }

  /**
   * Shop listing message reply endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/messages/{message_id}/reply
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::reply` expose observed keys including `reply`, `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_MESSAGE_REPLY(123, 123);
   * ```
   */
  POST_SHOP_LISTING_MESSAGE_REPLY(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}/reply`;
  }

  /**
   * Shop listing message endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Delete endpoint; removes, detaches, cancels, archives, or otherwise destroys backend state as implemented by the controller.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint DELETE /shops/{shop_id}/listing/messages/{message_id}
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::delete` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `id`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.DELETE_SHOP_LISTING_MESSAGE(123, 123);
   * ```
   */
  DELETE_SHOP_LISTING_MESSAGE(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}`;
  }

  /**
   * Shop listing message restore endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   *
   * @endpoint POST /shops/{shop_id}/listing/messages/{message_id}/restore
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @param message_id - message_id route parameter identifier.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingMessageActionsController::restore` expose observed keys including `shop_id`, `error`, `error_msg`, `success`, `message`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_MESSAGE_RESTORE(123, 123);
   * ```
   */
  POST_SHOP_LISTING_MESSAGE_RESTORE(shop_id: number, message_id: number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/messages/${message_id}/restore`;
  }



// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Shop ▶ Listing ▶ Active
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * Shop listing set active endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/active
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingController::setActive` expose observed keys including `is_active`, `error`, `error_msg`, `shop_id`, `moderation_mode`, `submission_enabled`, `default_currency`, `success`, `listing`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_SET_ACTIVE(123);
   * ```
   */
  POST_SHOP_LISTING_SET_ACTIVE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/active`;
  }


// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Shop ▶ Listing ▶ Search Settings
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * Shop listing search endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Read endpoint.
   * Backend method uses access helper(s): `hasShopAccess`.
   *
   * @endpoint GET /shops/{shop_id}/listing/search
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_READ (backoffice:product:read)
   * @permission None found in matched controller method by static inspection.
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingSearchSettingsGetController::get` expose observed keys including `success`, `listing_active`, `submission_enabled`, `settings`, `available_fields`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.GET_SHOP_LISTING_SEARCH(123);
   * ```
   */
  GET_SHOP_LISTING_SEARCH(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/search`;
  }

  /**
   * Shop listing search save endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/search
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingSearchSettingsSaveController::save` expose observed keys including `search_settings`, `auto_generate`, `error`, `error_msg`, `key`, `label`, `source`, `type`, `multiple`, `kv_mode`, `options`, `success`, `listing`, `id`, `shop_id`, `is_active`, `submission_enabled`, `default_currency`, `form_schema`, `updated_at`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_SEARCH_SAVE(123);
   * ```
   */
  POST_SHOP_LISTING_SEARCH_SAVE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/search`;
  }

  /**
   * Shop listing search generate endpoint URL.
   *
   * Builds the SDK URL for the matched Laravel route and does not perform the HTTP request itself.
   * Write/action endpoint; may create records, upload files, or trigger backend actions depending on request body.
   * Backend method uses access helper(s): `hasShopAccess`, `writeShopAccess`.
   * Backend request validation is implemented in the matched controller/form request; send only fields accepted by that backend validator.
   *
   * @endpoint POST /shops/{shop_id}/listing/search/generate
   * @domain api.localhost
   * @auth Required; OAuth2/Bearer API token via `Authenticate:api`.
   * @middleware `api-selldone`, `Illuminate\Auth\Middleware\Authenticate:api`, `App\Http\Middleware\Google2FAMiddleware`, `Illuminate\Auth\Middleware\EnsureEmailIsVerified`, `App\Http\Middleware\Testing`, `Illuminate\Http\Middleware\SetCacheHeaders:private;max_age=0`
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path.
   * @returns Absolute URL string using `selldone-api`/`selldone-dapi` setup metadata; call it with the SDK HTTP client using the documented HTTP method.
   *
   * Response shape:
   * - Backend response arrays in `ShopListingSearchSettingsGenerateController::generate` expose observed keys including `keys`, `scan_status`, `max_options`, `error`, `error_msg`, `success`, `settings`.
   * - Model/resource fields follow the Laravel resource/model serialization returned by the controller.
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for route middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token is missing a required scope or shop access check fails.
   * - 404 not_found: shop/resource not found or inaccessible through the shop access helper.
   * - 422 validation_error: Laravel validator rejects request body/query fields.
   * - Backend ErrorCodes observed: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()`, `ErrorCodes::NOT_FOUND_SHOP_ERROR()`.
   * - 5xx server_error: unexpected backend failure.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_SEARCH_GENERATE(123);
   * ```
   */
  POST_SHOP_LISTING_SEARCH_GENERATE(shop_id: string | number) {
    return `${this.selldone_api_url}/shops/${shop_id}/listing/search/generate`;
  }



// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Shop ▶ Listing ▶ Form Builder
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * Listing form-builder image upload endpoint URL.
   *
   * Builds the SDK URL for uploading images used by the Listing onboarding/form-builder schema, including slide illustrations and select option images/icons. This method only returns the URL; callers must send a `multipart/form-data` request with the documented backend field.
   * Write/upload endpoint; modifies shop storage and may increment shop capacity tracking after a successful upload.
   *
   * @endpoint POST /api/shops/{shop_id}/listing/form-builder/upload-image
   * @auth Required; OAuth2/Bearer API token via the backoffice API middleware.
   * @scope SCOPE_BACKOFFICE_PRODUCT_WRITE (backoffice:product:write)
   * @permission ShopPermissionRegion::CATEGORIES
   *
   * @param shop_id - Selldone shop identifier from the route path. The backend checks token access with `hasShopAccess` and write access with `writeShopAccess`.
   * @returns Relative API URL string for the upload endpoint; call it with `POST` and `multipart/form-data`.
   *
   * Request body:
   * - `file`: required image file validated by Laravel as `required|image|max:5120` (maximum 5 MB).
   *
   * Response shape:
   * - `success`: `true` on successful upload.
   * - `path`: flat Selldone CDN/storage path returned by `ShopCDN::UploadImageCenter`.
   * - `url`: public storage URL when `Storage::url` can resolve it; otherwise `null`.
   * - `error`: `true` for controller-level upload failures.
   * - `error_msg`: failure reason such as `No file uploaded.`, `Upload failed.`, or `Upload failed (no path returned).`
   *
   * Possible errors:
   * - 401 unauthenticated: missing or invalid authenticated session/token for API middleware.
   * - 403 token_has_no_access: `ErrorCodes::TOKEN_HAS_NO_ACCESS_ERROR()` when the token lacks `backoffice:product:write`.
   * - 404 not_found: `ErrorCodes::NOT_FOUND_SHOP_ERROR()` when the shop is not found or write access is denied for `ShopPermissionRegion::CATEGORIES`.
   * - 422 validation_error: `file` is missing, is not an image, or exceeds 5 MB.
   * - 5xx server_error: unexpected storage/CDN failure not handled by the controller.
   *
   * @example
   * ```ts
   * const url = API.POST_SHOP_LISTING_FORM_BUILDER_UPLOAD_IMAGE(123);
   * ```
   */
  POST_SHOP_LISTING_FORM_BUILDER_UPLOAD_IMAGE (shop_id: string | number) {
    return `/api/shops/${shop_id}/listing/form-builder/upload-image`;
  }



}
